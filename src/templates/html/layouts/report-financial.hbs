{{!-- 
================================================================================
SORIVA TEMPLATES - FINANCIAL REPORT
================================================================================
@file report-financial.hbs
@version 2.0.0
@author Soriva AI (Risenex Dynamics)
@copyright 2025 Risenex Dynamics. All rights reserved.

SYNCED WITH: report-fields.config.ts
INTERFACE: FinancialReportData extends BaseReportData

FEATURES:
- 100% Dynamic - All fields from config
- Bilingual Support (English + Hindi)
- A4 Print-Ready (210mm x 297mm)
- Income Statement (P&L)
- Balance Sheet
- Cash Flow Statement
- Budget Analysis
- Financial Ratios
- Tax Summary
- Audit Information
- Multi-Currency Support
================================================================================
--}}
<!DOCTYPE html>
<html lang="{{#if language}}{{language}}{{else}}en{{/if}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{reportTitle}} | {{organization.name}}</title>
    <style>
        /* ============================================
           CSS VARIABLES & ROOT SETTINGS
           ============================================ */
        :root {
            /* Primary Colors */
            --primary-color: {{#if brandColor}}{{brandColor}}{{else}}#0F766E{{/if}};
            --primary-light: {{#if brandColorLight}}{{brandColorLight}}{{else}}#14B8A6{{/if}};
            --primary-dark: {{#if brandColorDark}}{{brandColorDark}}{{else}}#0D9488{{/if}};
            
            /* Financial Colors */
            --profit-color: #10B981;
            --loss-color: #EF4444;
            --neutral-color: #6B7280;
            --asset-color: #3B82F6;
            --liability-color: #F59E0B;
            --equity-color: #8B5CF6;
            
            /* Status Colors */
            --success-color: #10B981;
            --warning-color: #F59E0B;
            --danger-color: #EF4444;
            --info-color: #3B82F6;
            
            /* Neutral Colors */
            --text-primary: #111827;
            --text-secondary: #4B5563;
            --text-muted: #6B7280;
            --text-light: #9CA3AF;
            --border-color: #E5E7EB;
            --bg-light: #F9FAFB;
            --bg-white: #FFFFFF;
            
            /* Typography */
            --font-primary: 'Segoe UI', 'Noto Sans', 'Arial', sans-serif;
            --font-hindi: 'Noto Sans Devanagari', 'Mangal', sans-serif;
            --font-mono: 'Consolas', 'Monaco', monospace;
            
            /* Spacing */
            --spacing-xs: 4px;
            --spacing-sm: 8px;
            --spacing-md: 16px;
            --spacing-lg: 24px;
            --spacing-xl: 32px;
        }

        /* ============================================
           RESET & BASE STYLES
           ============================================ */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-primary);
            font-size: 9pt;
            line-height: 1.4;
            color: var(--text-primary);
            background: var(--bg-white);
        }

        /* ============================================
           A4 PAGE SETUP
           ============================================ */
        @page {
            size: A4;
            margin: 12mm 10mm 15mm 10mm;
        }

        .report-container {
            width: 210mm;
            min-height: 297mm;
            margin: 0 auto;
            background: var(--bg-white);
        }

        .page {
            padding: 10mm;
            page-break-after: always;
            position: relative;
        }

        .page:last-child {
            page-break-after: auto;
        }

        /* ============================================
           HEADER SECTION
           ============================================ */
        .report-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding-bottom: var(--spacing-sm);
            border-bottom: 3px solid var(--primary-color);
            margin-bottom: var(--spacing-md);
        }

        .header-left {
            flex: 1;
        }

        .organization-logo {
            max-height: 45px;
            max-width: 140px;
            object-fit: contain;
            margin-bottom: var(--spacing-xs);
        }

        .organization-name {
            font-size: 11pt;
            font-weight: 700;
            color: var(--primary-color);
        }

        .organization-name-hindi {
            font-family: var(--font-hindi);
            font-size: 9pt;
            color: var(--text-secondary);
        }

        .organization-details {
            font-size: 8pt;
            color: var(--text-muted);
        }

        .header-right {
            text-align: right;
        }

        .report-number {
            font-size: 8pt;
            color: var(--text-secondary);
        }

        .report-date {
            font-size: 8pt;
            color: var(--text-secondary);
        }

        /* ============================================
           TITLE BANNER
           ============================================ */
        .report-banner {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
            border-radius: 6px;
            padding: var(--spacing-md);
            color: white;
            margin-bottom: var(--spacing-md);
            text-align: center;
        }

        .report-type-badge {
            display: inline-block;
            padding: 3px 12px;
            background: rgba(255,255,255,0.2);
            border-radius: 15px;
            font-size: 8pt;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: var(--spacing-xs);
        }

        .report-title {
            font-size: 16pt;
            font-weight: 700;
            margin-bottom: var(--spacing-xs);
        }

        .report-period {
            font-size: 10pt;
            opacity: 0.9;
        }

        .currency-badge {
            display: inline-block;
            margin-top: var(--spacing-sm);
            padding: 2px 10px;
            background: rgba(255,255,255,0.15);
            border-radius: 10px;
            font-size: 8pt;
        }

        /* ============================================
           SECTION STYLES
           ============================================ */
        .section {
            margin-bottom: var(--spacing-md);
        }

        .section-title {
            font-size: 11pt;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: var(--spacing-sm);
            padding-bottom: var(--spacing-xs);
            border-bottom: 2px solid var(--primary-light);
            display: flex;
            align-items: center;
            gap: var(--spacing-xs);
        }

        .section-title-hindi {
            font-family: var(--font-hindi);
            font-size: 9pt;
            color: var(--text-muted);
            font-weight: 400;
            margin-left: var(--spacing-xs);
        }

        .section-icon {
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--primary-color);
            color: white;
            border-radius: 3px;
            font-size: 10pt;
        }

        /* ============================================
           FINANCIAL SUMMARY CARDS
           ============================================ */
        .summary-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-md);
        }

        .summary-card {
            background: var(--bg-light);
            border-radius: 6px;
            padding: var(--spacing-sm);
            text-align: center;
            border-left: 3px solid var(--primary-color);
        }

        .summary-card.revenue { border-left-color: var(--success-color); }
        .summary-card.expense { border-left-color: var(--danger-color); }
        .summary-card.profit { border-left-color: var(--info-color); }
        .summary-card.margin { border-left-color: var(--equity-color); }

        .summary-label {
            font-size: 7pt;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .summary-label-hindi {
            font-family: var(--font-hindi);
            font-size: 7pt;
            display: block;
        }

        .summary-value {
            font-size: 14pt;
            font-weight: 700;
            font-family: var(--font-mono);
            margin: var(--spacing-xs) 0;
        }

        .summary-value.positive { color: var(--profit-color); }
        .summary-value.negative { color: var(--loss-color); }

        .summary-change {
            font-size: 8pt;
            padding: 2px 6px;
            border-radius: 10px;
        }

        .change-up { background: #D1FAE5; color: #059669; }
        .change-down { background: #FEE2E2; color: #DC2626; }

        /* ============================================
           FINANCIAL TABLES
           ============================================ */
        .financial-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: var(--spacing-md);
            font-size: 8pt;
        }

        .financial-table thead {
            background: var(--primary-color);
            color: white;
        }

        .financial-table th {
            padding: 6px 8px;
            text-align: left;
            font-weight: 600;
            font-size: 8pt;
            text-transform: uppercase;
        }

        .financial-table th.amount {
            text-align: right;
        }

        .financial-table th .hindi {
            font-family: var(--font-hindi);
            font-size: 7pt;
            font-weight: 400;
            opacity: 0.8;
            display: block;
        }

        .financial-table td {
            padding: 5px 8px;
            border-bottom: 1px solid var(--border-color);
        }

        .financial-table td.amount {
            text-align: right;
            font-family: var(--font-mono);
            font-weight: 500;
        }

        .financial-table td.amount.positive { color: var(--profit-color); }
        .financial-table td.amount.negative { color: var(--loss-color); }

        .financial-table tr.subtotal {
            background: var(--bg-light);
            font-weight: 600;
        }

        .financial-table tr.total {
            background: var(--primary-color);
            color: white;
            font-weight: 700;
        }

        .financial-table tr.total td {
            border-bottom: none;
        }

        .financial-table tr.section-header {
            background: #F3F4F6;
        }

        .financial-table tr.section-header td {
            font-weight: 600;
            color: var(--primary-color);
            padding-top: 8px;
        }

        .indent-1 { padding-left: 20px !important; }
        .indent-2 { padding-left: 35px !important; }

        .variance-badge {
            display: inline-block;
            padding: 1px 5px;
            border-radius: 3px;
            font-size: 7pt;
            font-weight: 600;
        }

        .variance-positive { background: #D1FAE5; color: #059669; }
        .variance-negative { background: #FEE2E2; color: #DC2626; }
        .variance-neutral { background: #F3F4F6; color: #6B7280; }

        /* ============================================
           BALANCE SHEET LAYOUT
           ============================================ */
        .balance-sheet-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: var(--spacing-md);
        }

        .bs-section {
            background: var(--bg-light);
            border-radius: 6px;
            padding: var(--spacing-sm);
        }

        .bs-section.assets { border-top: 3px solid var(--asset-color); }
        .bs-section.liabilities { border-top: 3px solid var(--liability-color); }
        .bs-section.equity { border-top: 3px solid var(--equity-color); }

        .bs-section-title {
            font-size: 10pt;
            font-weight: 700;
            margin-bottom: var(--spacing-sm);
            display: flex;
            align-items: center;
            gap: var(--spacing-xs);
        }

        .bs-section-title.assets { color: var(--asset-color); }
        .bs-section-title.liabilities { color: var(--liability-color); }
        .bs-section-title.equity { color: var(--equity-color); }

        /* ============================================
           CASH FLOW SECTION
           ============================================ */
        .cashflow-summary {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-md);
        }

        .cashflow-card {
            background: var(--bg-white);
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: var(--spacing-sm);
            text-align: center;
        }

        .cashflow-card.operating { border-top: 3px solid var(--info-color); }
        .cashflow-card.investing { border-top: 3px solid var(--warning-color); }
        .cashflow-card.financing { border-top: 3px solid var(--equity-color); }

        .cashflow-label {
            font-size: 8pt;
            color: var(--text-muted);
            margin-bottom: var(--spacing-xs);
        }

        .cashflow-value {
            font-size: 14pt;
            font-weight: 700;
            font-family: var(--font-mono);
        }

        .cashflow-value.inflow { color: var(--profit-color); }
        .cashflow-value.outflow { color: var(--loss-color); }

        /* ============================================
           FINANCIAL RATIOS
           ============================================ */
        .ratios-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: var(--spacing-md);
        }

        .ratio-category {
            background: var(--bg-light);
            border-radius: 6px;
            padding: var(--spacing-sm);
        }

        .ratio-category-title {
            font-size: 9pt;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: var(--spacing-sm);
            padding-bottom: var(--spacing-xs);
            border-bottom: 1px solid var(--border-color);
        }

        .ratio-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 4px 0;
            border-bottom: 1px dashed var(--border-color);
        }

        .ratio-item:last-child {
            border-bottom: none;
        }

        .ratio-name {
            font-size: 8pt;
            color: var(--text-secondary);
        }

        .ratio-value {
            font-size: 10pt;
            font-weight: 700;
            font-family: var(--font-mono);
        }

        .ratio-status {
            display: inline-block;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            margin-left: 4px;
        }

        .status-excellent { background: #10B981; }
        .status-good { background: #3B82F6; }
        .status-average { background: #F59E0B; }
        .status-poor { background: #EF4444; }
        .status-critical { background: #DC2626; }

        .ratio-trend {
            font-size: 10pt;
            margin-left: 4px;
        }

        .trend-improving { color: var(--success-color); }
        .trend-declining { color: var(--danger-color); }
        .trend-stable { color: var(--neutral-color); }

        /* ============================================
           BUDGET ANALYSIS
           ============================================ */
        .budget-bar {
            height: 20px;
            background: var(--border-color);
            border-radius: 10px;
            overflow: hidden;
            margin: var(--spacing-sm) 0;
        }

        .budget-bar-fill {
            height: 100%;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 8pt;
            font-weight: 600;
            color: white;
        }

        .budget-under { background: var(--success-color); }
        .budget-on_track { background: var(--info-color); }
        .budget-over { background: var(--danger-color); }

        /* ============================================
           TAX SUMMARY
           ============================================ */
        .tax-summary-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-md);
        }

        .tax-card {
            background: var(--bg-light);
            border-radius: 6px;
            padding: var(--spacing-sm);
            text-align: center;
        }

        .tax-label {
            font-size: 8pt;
            color: var(--text-muted);
        }

        .tax-value {
            font-size: 14pt;
            font-weight: 700;
            font-family: var(--font-mono);
        }

        .tax-status {
            padding: 2px 6px;
            border-radius: 4px;
            font-size: 7pt;
            font-weight: 600;
            text-transform: uppercase;
        }

        .tax-pending { background: #FEF3C7; color: #D97706; }
        .tax-paid { background: #D1FAE5; color: #059669; }
        .tax-overdue { background: #FEE2E2; color: #DC2626; }

        /* ============================================
           AUDIT INFO
           ============================================ */
        .audit-box {
            background: var(--bg-light);
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: var(--spacing-md);
            margin-bottom: var(--spacing-md);
        }

        .audit-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: var(--spacing-sm);
        }

        .audit-opinion {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 8pt;
            font-weight: 600;
            text-transform: uppercase;
        }

        .opinion-unqualified { background: #D1FAE5; color: #059669; }
        .opinion-qualified { background: #FEF3C7; color: #D97706; }
        .opinion-adverse { background: #FEE2E2; color: #DC2626; }
        .opinion-disclaimer { background: #F3F4F6; color: #6B7280; }

        .audit-details {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: var(--spacing-sm);
            font-size: 9pt;
        }

        .audit-detail-item {
            display: flex;
            flex-direction: column;
        }

        .audit-detail-label {
            font-size: 8pt;
            color: var(--text-muted);
        }

        .audit-detail-value {
            font-weight: 600;
            color: var(--text-primary);
        }

        /* ============================================
           CERTIFICATION
           ============================================ */
        .certification-box {
            background: linear-gradient(135deg, #F0FDF4 0%, #DCFCE7 100%);
            border: 1px solid #BBF7D0;
            border-radius: 6px;
            padding: var(--spacing-md);
            margin-top: var(--spacing-md);
        }

        .certification-title {
            font-size: 10pt;
            font-weight: 700;
            color: #059669;
            margin-bottom: var(--spacing-sm);
        }

        .certification-statement {
            font-size: 9pt;
            color: var(--text-secondary);
            font-style: italic;
            margin-bottom: var(--spacing-sm);
        }

        .certification-signature {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-top: var(--spacing-md);
        }

        /* ============================================
           SIGNATURES
           ============================================ */
        .signatures-section {
            margin-top: var(--spacing-lg);
            padding-top: var(--spacing-md);
            border-top: 2px solid var(--border-color);
        }

        .signatures-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: var(--spacing-lg);
        }

        .signature-block {
            text-align: center;
        }

        .signature-line {
            border-bottom: 1px solid var(--text-primary);
            height: 40px;
            margin-bottom: var(--spacing-xs);
        }

        .signature-name {
            font-size: 9pt;
            font-weight: 600;
            color: var(--text-primary);
        }

        .signature-designation {
            font-size: 8pt;
            color: var(--text-muted);
        }

        /* ============================================
           COMPARISON SECTION
           ============================================ */
        .comparison-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-md);
        }

        .comparison-card {
            background: var(--bg-white);
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: var(--spacing-sm);
        }

        .comparison-title {
            font-size: 8pt;
            color: var(--text-muted);
            text-transform: uppercase;
            margin-bottom: var(--spacing-xs);
        }

        .comparison-item {
            display: flex;
            justify-content: space-between;
            padding: 3px 0;
            font-size: 8pt;
        }

        .comparison-item-label {
            color: var(--text-secondary);
        }

        .comparison-item-value {
            font-weight: 600;
            font-family: var(--font-mono);
        }

        /* ============================================
           NOTES SECTION
           ============================================ */
        .notes-list {
            list-style: decimal;
            padding-left: 20px;
            font-size: 8pt;
            color: var(--text-secondary);
        }

        .notes-list li {
            margin-bottom: var(--spacing-xs);
        }

        /* ============================================
           PRINT STYLES
           ============================================ */
        @media print {
            body {
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }

            .report-container {
                width: 100%;
            }

            .page {
                page-break-after: always;
                padding: 0;
            }
        }

        /* ============================================
           UTILITY CLASSES
           ============================================ */
        .text-right { text-align: right; }
        .text-center { text-align: center; }
        .font-mono { font-family: var(--font-mono); }
        .font-hindi { font-family: var(--font-hindi); }
        .mt-sm { margin-top: var(--spacing-sm); }
        .mt-md { margin-top: var(--spacing-md); }
        .mb-sm { margin-bottom: var(--spacing-sm); }
        .mb-md { margin-bottom: var(--spacing-md); }
    </style>
</head>
<body>
    <div class="report-container">
        <!-- ============================================
             PAGE 1: OVERVIEW & INCOME STATEMENT
             ============================================ -->
        <div class="page">
            <!-- Header -->
            <header class="report-header">
                <div class="header-left">
                    {{#if organization.logo}}
                    <img src="{{organization.logo}}" alt="{{organization.name}}" class="organization-logo">
                    {{/if}}
                    <div class="organization-name">{{organization.name}}</div>
                    {{#if organization.nameHindi}}
                    <div class="organization-name-hindi">{{organization.nameHindi}}</div>
                    {{/if}}
                    <div class="organization-details">
                        {{#if organization.gstin}}GSTIN: {{organization.gstin}}{{/if}}
                        {{#if organization.cin}} | CIN: {{organization.cin}}{{/if}}
                    </div>
                </div>
                <div class="header-right">
                    <div class="report-number">Report No: <strong>{{reportNumber}}</strong></div>
                    <div class="report-date">Date: {{reportDate}}</div>
                </div>
            </header>

            <!-- Title Banner -->
            <div class="report-banner">
                <span class="report-type-badge">
                    {{#ifEquals financialReportType 'income_statement'}}Income Statement{{/ifEquals}}
                    {{#ifEquals financialReportType 'balance_sheet'}}Balance Sheet{{/ifEquals}}
                    {{#ifEquals financialReportType 'cash_flow'}}Cash Flow Statement{{/ifEquals}}
                    {{#ifEquals financialReportType 'combined'}}Financial Statements{{/ifEquals}}
                    {{#ifEquals financialReportType 'budget'}}Budget Report{{/ifEquals}}
                    {{#ifEquals financialReportType 'variance'}}Variance Analysis{{/ifEquals}}
                    {{#ifEquals financialReportType 'ratio'}}Financial Ratios{{/ifEquals}}
                    {{#ifEquals financialReportType 'tax'}}Tax Report{{/ifEquals}}
                </span>
                <h1 class="report-title">{{reportTitle}}</h1>
                <div class="report-period">
                    {{#ifEquals financialPeriod.type 'monthly'}}Monthly Report{{/ifEquals}}
                    {{#ifEquals financialPeriod.type 'quarterly'}}Q{{financialPeriod.quarter}} {{financialPeriod.year}}{{/ifEquals}}
                    {{#ifEquals financialPeriod.type 'semi_annual'}}H{{#if (lte financialPeriod.quarter 2)}}1{{else}}2{{/if}} {{financialPeriod.year}}{{/ifEquals}}
                    {{#ifEquals financialPeriod.type 'annual'}}FY {{financialPeriod.year}}{{/ifEquals}}
                    {{#ifEquals financialPeriod.type 'ytd'}}Year to Date{{/ifEquals}}
                    | {{financialPeriod.startDate}} to {{financialPeriod.endDate}}
                </div>
                <span class="currency-badge">Currency: {{currency}}</span>
            </div>

            <!-- Financial Summary -->
            {{#if incomeStatement}}
            <div class="summary-grid">
                <div class="summary-card revenue">
                    <div class="summary-label">Total Revenue <span class="summary-label-hindi">कुल राजस्व</span></div>
                    <div class="summary-value positive">{{#formatCurrency incomeStatement.totalRevenue currency}}{{/formatCurrency}}</div>
                    {{#if comparison.previousPeriod}}
                    <span class="summary-change {{#if (gt incomeStatement.totalRevenue comparison.previousPeriod.revenue)}}change-up{{else}}change-down{{/if}}">
                        vs Previous
                    </span>
                    {{/if}}
                </div>
                <div class="summary-card expense">
                    <div class="summary-label">Total Expenses <span class="summary-label-hindi">कुल व्यय</span></div>
                    <div class="summary-value negative">{{#formatCurrency incomeStatement.totalOperatingExpenses currency}}{{/formatCurrency}}</div>
                </div>
                <div class="summary-card profit">
                    <div class="summary-label">Net Income <span class="summary-label-hindi">शुद्ध आय</span></div>
                    <div class="summary-value {{#if (gt incomeStatement.netIncome 0)}}positive{{else}}negative{{/if}}">
                        {{#formatCurrency incomeStatement.netIncome currency}}{{/formatCurrency}}
                    </div>
                </div>
                <div class="summary-card margin">
                    <div class="summary-label">Net Margin <span class="summary-label-hindi">शुद्ध मार्जिन</span></div>
                    <div class="summary-value">{{incomeStatement.netMargin}}%</div>
                </div>
            </div>

            <!-- Income Statement -->
            <div class="section">
                <div class="section-title">
                    <span class="section-icon">📊</span>
                    Income Statement / Profit & Loss
                    <span class="section-title-hindi">आय विवरण</span>
                </div>

                <table class="financial-table">
                    <thead>
                        <tr>
                            <th>Particulars <span class="hindi">विवरण</span></th>
                            <th class="amount">Amount <span class="hindi">राशि</span></th>
                            {{#if comparison.previousPeriod}}
                            <th class="amount">Previous <span class="hindi">पिछला</span></th>
                            {{/if}}
                            <th class="amount">% of Revenue</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Revenue Section -->
                        <tr class="section-header">
                            <td colspan="4"><strong>Revenue / राजस्व</strong></td>
                        </tr>
                        {{#each incomeStatement.revenue}}
                        <tr>
                            <td class="indent-1">{{category}}{{#if subcategory}} - {{subcategory}}{{/if}}</td>
                            <td class="amount">{{#formatCurrency amount ../currency}}{{/formatCurrency}}</td>
                            {{#if ../comparison.previousPeriod}}
                            <td class="amount">{{#if previousAmount}}{{#formatCurrency previousAmount ../currency}}{{/formatCurrency}}{{else}}—{{/if}}</td>
                            {{/if}}
                            <td class="amount">{{#if percentOfTotal}}{{percentOfTotal}}%{{else}}—{{/if}}</td>
                        </tr>
                        {{/each}}
                        <tr class="subtotal">
                            <td><strong>Total Revenue / कुल राजस्व</strong></td>
                            <td class="amount"><strong>{{#formatCurrency incomeStatement.totalRevenue currency}}{{/formatCurrency}}</strong></td>
                            {{#if comparison.previousPeriod}}
                            <td class="amount"><strong>{{#formatCurrency comparison.previousPeriod.revenue currency}}{{/formatCurrency}}</strong></td>
                            {{/if}}
                            <td class="amount">100%</td>
                        </tr>

                        <!-- COGS -->
                        {{#if incomeStatement.costOfGoodsSold}}
                        <tr>
                            <td>Less: Cost of Goods Sold / बेची गई वस्तुओं की लागत</td>
                            <td class="amount negative">({{#formatCurrency incomeStatement.costOfGoodsSold currency}}{{/formatCurrency}})</td>
                            <td class="amount">—</td>
                            <td class="amount">—</td>
                        </tr>
                        {{/if}}

                        <!-- Gross Profit -->
                        <tr class="subtotal">
                            <td><strong>Gross Profit / सकल लाभ</strong></td>
                            <td class="amount"><strong>{{#formatCurrency incomeStatement.grossProfit currency}}{{/formatCurrency}}</strong></td>
                            {{#if comparison.previousPeriod}}
                            <td class="amount">—</td>
                            {{/if}}
                            <td class="amount">{{incomeStatement.grossMargin}}%</td>
                        </tr>

                        <!-- Operating Expenses -->
                        <tr class="section-header">
                            <td colspan="4"><strong>Operating Expenses / परिचालन व्यय</strong></td>
                        </tr>
                        {{#each incomeStatement.operatingExpenses}}
                        <tr>
                            <td class="indent-1">{{category}}{{#if subcategory}} - {{subcategory}}{{/if}}</td>
                            <td class="amount negative">({{#formatCurrency amount ../currency}}{{/formatCurrency}})</td>
                            {{#if ../comparison.previousPeriod}}
                            <td class="amount">{{#if previousAmount}}({{#formatCurrency previousAmount ../currency}}{{/formatCurrency}}){{else}}—{{/if}}</td>
                            {{/if}}
                            <td class="amount">{{#if percentOfRevenue}}{{percentOfRevenue}}%{{else}}—{{/if}}</td>
                        </tr>
                        {{/each}}
                        <tr class="subtotal">
                            <td><strong>Total Operating Expenses / कुल परिचालन व्यय</strong></td>
                            <td class="amount negative"><strong>({{#formatCurrency incomeStatement.totalOperatingExpenses currency}}{{/formatCurrency}})</strong></td>
                            {{#if comparison.previousPeriod}}
                            <td class="amount"><strong>({{#formatCurrency comparison.previousPeriod.expenses currency}}{{/formatCurrency}})</strong></td>
                            {{/if}}
                            <td class="amount">—</td>
                        </tr>

                        <!-- Operating Income -->
                        <tr class="subtotal">
                            <td><strong>Operating Income / परिचालन आय</strong></td>
                            <td class="amount {{#if (gt incomeStatement.operatingIncome 0)}}positive{{else}}negative{{/if}}">
                                <strong>{{#formatCurrency incomeStatement.operatingIncome currency}}{{/formatCurrency}}</strong>
                            </td>
                            {{#if comparison.previousPeriod}}
                            <td class="amount">—</td>
                            {{/if}}
                            <td class="amount">{{incomeStatement.operatingMargin}}%</td>
                        </tr>

                        <!-- Other Income/Expenses -->
                        {{#if incomeStatement.otherIncome}}
                        <tr>
                            <td>Add: Other Income / अन्य आय</td>
                            <td class="amount positive">{{#formatCurrency incomeStatement.otherIncome currency}}{{/formatCurrency}}</td>
                            <td class="amount">—</td>
                            <td class="amount">—</td>
                        </tr>
                        {{/if}}
                        {{#if incomeStatement.otherExpenses}}
                        <tr>
                            <td>Less: Other Expenses / अन्य व्यय</td>
                            <td class="amount negative">({{#formatCurrency incomeStatement.otherExpenses currency}}{{/formatCurrency}})</td>
                            <td class="amount">—</td>
                            <td class="amount">—</td>
                        </tr>
                        {{/if}}
                        {{#if incomeStatement.interestExpense}}
                        <tr>
                            <td>Less: Interest Expense / ब्याज व्यय</td>
                            <td class="amount negative">({{#formatCurrency incomeStatement.interestExpense currency}}{{/formatCurrency}})</td>
                            <td class="amount">—</td>
                            <td class="amount">—</td>
                        </tr>
                        {{/if}}

                        <!-- Income Before Tax -->
                        <tr class="subtotal">
                            <td><strong>Income Before Tax / कर पूर्व आय</strong></td>
                            <td class="amount {{#if (gt incomeStatement.incomeBeforeTax 0)}}positive{{else}}negative{{/if}}">
                                <strong>{{#formatCurrency incomeStatement.incomeBeforeTax currency}}{{/formatCurrency}}</strong>
                            </td>
                            <td class="amount">—</td>
                            <td class="amount">—</td>
                        </tr>

                        <!-- Tax -->
                        <tr>
                            <td>Less: Tax Expense / कर व्यय {{#if incomeStatement.effectiveTaxRate}}({{incomeStatement.effectiveTaxRate}}%){{/if}}</td>
                            <td class="amount negative">({{#formatCurrency incomeStatement.taxExpense currency}}{{/formatCurrency}})</td>
                            <td class="amount">—</td>
                            <td class="amount">—</td>
                        </tr>

                        <!-- Net Income -->
                        <tr class="total">
                            <td><strong>Net Income / शुद्ध आय</strong></td>
                            <td class="amount">
                                <strong>{{#formatCurrency incomeStatement.netIncome currency}}{{/formatCurrency}}</strong>
                            </td>
                            {{#if comparison.previousPeriod}}
                            <td class="amount"><strong>{{#formatCurrency comparison.previousPeriod.profit currency}}{{/formatCurrency}}</strong></td>
                            {{/if}}
                            <td class="amount">{{incomeStatement.netMargin}}%</td>
                        </tr>

                        {{#if incomeStatement.eps}}
                        <tr>
                            <td>Earnings Per Share (EPS) / प्रति शेयर आय</td>
                            <td class="amount">{{#formatCurrency incomeStatement.eps currency}}{{/formatCurrency}}</td>
                            <td class="amount">—</td>
                            <td class="amount">—</td>
                        </tr>
                        {{/if}}
                    </tbody>
                </table>
            </div>
            {{/if}}
        </div>

        <!-- ============================================
             PAGE 2: BALANCE SHEET
             ============================================ -->
        {{#if balanceSheet}}
        <div class="page">
            <div class="section">
                <div class="section-title">
                    <span class="section-icon">⚖️</span>
                    Balance Sheet
                    <span class="section-title-hindi">तुलन पत्र</span>
                </div>

                <div class="balance-sheet-grid">
                    <!-- Assets Column -->
                    <div class="bs-section assets">
                        <div class="bs-section-title assets">📈 Assets / संपत्ति</div>
                        
                        <table class="financial-table">
                            <thead>
                                <tr>
                                    <th>Particulars</th>
                                    <th class="amount">Current</th>
                                    <th class="amount">Previous</th>
                                </tr>
                            </thead>
                            <tbody>
                                <!-- Current Assets -->
                                <tr class="section-header">
                                    <td colspan="3"><strong>Current Assets / चालू संपत्ति</strong></td>
                                </tr>
                                {{#each balanceSheet.assets.current}}
                                <tr>
                                    <td class="indent-1">{{name}}</td>
                                    <td class="amount">{{#formatCurrency currentPeriod ../../currency}}{{/formatCurrency}}</td>
                                    <td class="amount">{{#if previousPeriod}}{{#formatCurrency previousPeriod ../../currency}}{{/formatCurrency}}{{else}}—{{/if}}</td>
                                </tr>
                                {{/each}}
                                <tr class="subtotal">
                                    <td><strong>Total Current Assets</strong></td>
                                    <td class="amount"><strong>{{#formatCurrency balanceSheet.assets.totalCurrent currency}}{{/formatCurrency}}</strong></td>
                                    <td class="amount">—</td>
                                </tr>

                                <!-- Non-Current Assets -->
                                <tr class="section-header">
                                    <td colspan="3"><strong>Non-Current Assets / अचल संपत्ति</strong></td>
                                </tr>
                                {{#each balanceSheet.assets.nonCurrent}}
                                <tr>
                                    <td class="indent-1">{{name}}</td>
                                    <td class="amount">{{#formatCurrency currentPeriod ../../currency}}{{/formatCurrency}}</td>
                                    <td class="amount">{{#if previousPeriod}}{{#formatCurrency previousPeriod ../../currency}}{{/formatCurrency}}{{else}}—{{/if}}</td>
                                </tr>
                                {{/each}}
                                <tr class="subtotal">
                                    <td><strong>Total Non-Current Assets</strong></td>
                                    <td class="amount"><strong>{{#formatCurrency balanceSheet.assets.totalNonCurrent currency}}{{/formatCurrency}}</strong></td>
                                    <td class="amount">—</td>
                                </tr>

                                <tr class="total">
                                    <td><strong>TOTAL ASSETS</strong></td>
                                    <td class="amount"><strong>{{#formatCurrency balanceSheet.assets.totalAssets currency}}{{/formatCurrency}}</strong></td>
                                    <td class="amount">—</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>

                    <!-- Liabilities & Equity Column -->
                    <div>
                        <div class="bs-section liabilities mb-sm">
                            <div class="bs-section-title liabilities">📉 Liabilities / देनदारियां</div>
                            
                            <table class="financial-table">
                                <thead>
                                    <tr>
                                        <th>Particulars</th>
                                        <th class="amount">Current</th>
                                        <th class="amount">Previous</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!-- Current Liabilities -->
                                    <tr class="section-header">
                                        <td colspan="3"><strong>Current Liabilities / चालू देनदारियां</strong></td>
                                    </tr>
                                    {{#each balanceSheet.liabilities.current}}
                                    <tr>
                                        <td class="indent-1">{{name}}</td>
                                        <td class="amount">{{#formatCurrency currentPeriod ../../currency}}{{/formatCurrency}}</td>
                                        <td class="amount">{{#if previousPeriod}}{{#formatCurrency previousPeriod ../../currency}}{{/formatCurrency}}{{else}}—{{/if}}</td>
                                    </tr>
                                    {{/each}}
                                    <tr class="subtotal">
                                        <td><strong>Total Current Liabilities</strong></td>
                                        <td class="amount"><strong>{{#formatCurrency balanceSheet.liabilities.totalCurrent currency}}{{/formatCurrency}}</strong></td>
                                        <td class="amount">—</td>
                                    </tr>

                                    <!-- Non-Current Liabilities -->
                                    <tr class="section-header">
                                        <td colspan="3"><strong>Non-Current Liabilities / दीर्घकालिक देनदारियां</strong></td>
                                    </tr>
                                    {{#each balanceSheet.liabilities.nonCurrent}}
                                    <tr>
                                        <td class="indent-1">{{name}}</td>
                                        <td class="amount">{{#formatCurrency currentPeriod ../../currency}}{{/formatCurrency}}</td>
                                        <td class="amount">{{#if previousPeriod}}{{#formatCurrency previousPeriod ../../currency}}{{/formatCurrency}}{{else}}—{{/if}}</td>
                                    </tr>
                                    {{/each}}
                                    <tr class="subtotal">
                                        <td><strong>Total Non-Current Liabilities</strong></td>
                                        <td class="amount"><strong>{{#formatCurrency balanceSheet.liabilities.totalNonCurrent currency}}{{/formatCurrency}}</strong></td>
                                        <td class="amount">—</td>
                                    </tr>

                                    <tr class="subtotal">
                                        <td><strong>TOTAL LIABILITIES</strong></td>
                                        <td class="amount"><strong>{{#formatCurrency balanceSheet.liabilities.totalLiabilities currency}}{{/formatCurrency}}</strong></td>
                                        <td class="amount">—</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>

                        <div class="bs-section equity">
                            <div class="bs-section-title equity">💰 Equity / इक्विटी</div>
                            
                            <table class="financial-table">
                                <tbody>
                                    {{#each balanceSheet.equity.items}}
                                    <tr>
                                        <td class="indent-1">{{name}}</td>
                                        <td class="amount">{{#formatCurrency currentPeriod ../../currency}}{{/formatCurrency}}</td>
                                        <td class="amount">{{#if previousPeriod}}{{#formatCurrency previousPeriod ../../currency}}{{/formatCurrency}}{{else}}—{{/if}}</td>
                                    </tr>
                                    {{/each}}
                                    <tr class="subtotal">
                                        <td><strong>TOTAL EQUITY</strong></td>
                                        <td class="amount"><strong>{{#formatCurrency balanceSheet.equity.totalEquity currency}}{{/formatCurrency}}</strong></td>
                                        <td class="amount">—</td>
                                    </tr>
                                    <tr class="total">
                                        <td><strong>TOTAL LIABILITIES & EQUITY</strong></td>
                                        <td class="amount"><strong>{{#formatCurrency balanceSheet.assets.totalAssets currency}}{{/formatCurrency}}</strong></td>
                                        <td class="amount">—</td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/if}}

        <!-- ============================================
             PAGE 3: CASH FLOW STATEMENT
             ============================================ -->
        {{#if cashFlow}}
        <div class="page">
            <div class="section">
                <div class="section-title">
                    <span class="section-icon">💵</span>
                    Cash Flow Statement
                    <span class="section-title-hindi">नकदी प्रवाह विवरण</span>
                </div>

                <!-- Cash Flow Summary -->
                <div class="cashflow-summary">
                    <div class="cashflow-card operating">
                        <div class="cashflow-label">Operating Activities / परिचालन गतिविधियां</div>
                        <div class="cashflow-value {{#if (gt cashFlow.netOperating 0)}}inflow{{else}}outflow{{/if}}">
                            {{#formatCurrency cashFlow.netOperating currency}}{{/formatCurrency}}
                        </div>
                    </div>
                    <div class="cashflow-card investing">
                        <div class="cashflow-label">Investing Activities / निवेश गतिविधियां</div>
                        <div class="cashflow-value {{#if (gt cashFlow.netInvesting 0)}}inflow{{else}}outflow{{/if}}">
                            {{#formatCurrency cashFlow.netInvesting currency}}{{/formatCurrency}}
                        </div>
                    </div>
                    <div class="cashflow-card financing">
                        <div class="cashflow-label">Financing Activities / वित्तपोषण गतिविधियां</div>
                        <div class="cashflow-value {{#if (gt cashFlow.netFinancing 0)}}inflow{{else}}outflow{{/if}}">
                            {{#formatCurrency cashFlow.netFinancing currency}}{{/formatCurrency}}
                        </div>
                    </div>
                </div>

                <table class="financial-table">
                    <thead>
                        <tr>
                            <th>Particulars <span class="hindi">विवरण</span></th>
                            <th class="amount">Amount <span class="hindi">राशि</span></th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Operating Activities -->
                        <tr class="section-header">
                            <td colspan="2"><strong>A. Cash Flow from Operating Activities / परिचालन गतिविधियों से नकदी प्रवाह</strong></td>
                        </tr>
                        {{#each cashFlow.operating}}
                        <tr>
                            <td class="indent-1">{{description}}</td>
                            <td class="amount {{#if isInflow}}positive{{else}}negative{{/if}}">
                                {{#if isInflow}}{{#formatCurrency amount ../currency}}{{/formatCurrency}}{{else}}({{#formatCurrency amount ../currency}}{{/formatCurrency}}){{/if}}
                            </td>
                        </tr>
                        {{/each}}
                        <tr class="subtotal">
                            <td><strong>Net Cash from Operating Activities</strong></td>
                            <td class="amount {{#if (gt cashFlow.netOperating 0)}}positive{{else}}negative{{/if}}">
                                <strong>{{#formatCurrency cashFlow.netOperating currency}}{{/formatCurrency}}</strong>
                            </td>
                        </tr>

                        <!-- Investing Activities -->
                        <tr class="section-header">
                            <td colspan="2"><strong>B. Cash Flow from Investing Activities / निवेश गतिविधियों से नकदी प्रवाह</strong></td>
                        </tr>
                        {{#each cashFlow.investing}}
                        <tr>
                            <td class="indent-1">{{description}}</td>
                            <td class="amount {{#if isInflow}}positive{{else}}negative{{/if}}">
                                {{#if isInflow}}{{#formatCurrency amount ../currency}}{{/formatCurrency}}{{else}}({{#formatCurrency amount ../currency}}{{/formatCurrency}}){{/if}}
                            </td>
                        </tr>
                        {{/each}}
                        <tr class="subtotal">
                            <td><strong>Net Cash from Investing Activities</strong></td>
                            <td class="amount {{#if (gt cashFlow.netInvesting 0)}}positive{{else}}negative{{/if}}">
                                <strong>{{#formatCurrency cashFlow.netInvesting currency}}{{/formatCurrency}}</strong>
                            </td>
                        </tr>

                        <!-- Financing Activities -->
                        <tr class="section-header">
                            <td colspan="2"><strong>C. Cash Flow from Financing Activities / वित्तपोषण गतिविधियों से नकदी प्रवाह</strong></td>
                        </tr>
                        {{#each cashFlow.financing}}
                        <tr>
                            <td class="indent-1">{{description}}</td>
                            <td class="amount {{#if isInflow}}positive{{else}}negative{{/if}}">
                                {{#if isInflow}}{{#formatCurrency amount ../currency}}{{/formatCurrency}}{{else}}({{#formatCurrency amount ../currency}}{{/formatCurrency}}){{/if}}
                            </td>
                        </tr>
                        {{/each}}
                        <tr class="subtotal">
                            <td><strong>Net Cash from Financing Activities</strong></td>
                            <td class="amount {{#if (gt cashFlow.netFinancing 0)}}positive{{else}}negative{{/if}}">
                                <strong>{{#formatCurrency cashFlow.netFinancing currency}}{{/formatCurrency}}</strong>
                            </td>
                        </tr>

                        <!-- Net Change -->
                        <tr class="total">
                            <td><strong>Net Increase/(Decrease) in Cash (A+B+C)</strong></td>
                            <td class="amount">
                                <strong>{{#formatCurrency cashFlow.netCashFlow currency}}{{/formatCurrency}}</strong>
                            </td>
                        </tr>

                        <!-- Opening & Closing Balance -->
                        <tr>
                            <td>Opening Cash Balance / प्रारंभिक नकद शेष</td>
                            <td class="amount">{{#formatCurrency cashFlow.openingBalance currency}}{{/formatCurrency}}</td>
                        </tr>
                        <tr class="total">
                            <td><strong>Closing Cash Balance / अंतिम नकद शेष</strong></td>
                            <td class="amount"><strong>{{#formatCurrency cashFlow.closingBalance currency}}{{/formatCurrency}}</strong></td>
                        </tr>

                        {{#if cashFlow.freeCashFlow}}
                        <tr>
                            <td>Free Cash Flow / मुक्त नकदी प्रवाह</td>
                            <td class="amount {{#if (gt cashFlow.freeCashFlow 0)}}positive{{else}}negative{{/if}}">
                                {{#formatCurrency cashFlow.freeCashFlow currency}}{{/formatCurrency}}
                            </td>
                        </tr>
                        {{/if}}
                    </tbody>
                </table>
            </div>
        </div>
        {{/if}}

        <!-- ============================================
             PAGE 4: RATIOS & BUDGET ANALYSIS
             ============================================ -->
        <div class="page">
            <!-- Financial Ratios -->
            {{#if financialRatios}}
            <div class="section">
                <div class="section-title">
                    <span class="section-icon">📐</span>
                    Financial Ratios
                    <span class="section-title-hindi">वित्तीय अनुपात</span>
                </div>

                <div class="ratios-grid">
                    <!-- Profitability Ratios -->
                    {{#if financialRatios.profitability}}
                    <div class="ratio-category">
                        <div class="ratio-category-title">💰 Profitability / लाभप्रदता</div>
                        {{#each financialRatios.profitability}}
                        <div class="ratio-item">
                            <span class="ratio-name">{{name}}</span>
                            <span>
                                <span class="ratio-value">{{value}}{{unit}}</span>
                                <span class="ratio-status status-{{status}}"></span>
                                {{#if trend}}
                                <span class="ratio-trend trend-{{trend}}">
                                    {{#ifEquals trend 'improving'}}↑{{/ifEquals}}
                                    {{#ifEquals trend 'declining'}}↓{{/ifEquals}}
                                    {{#ifEquals trend 'stable'}}→{{/ifEquals}}
                                </span>
                                {{/if}}
                            </span>
                        </div>
                        {{/each}}
                    </div>
                    {{/if}}

                    <!-- Liquidity Ratios -->
                    {{#if financialRatios.liquidity}}
                    <div class="ratio-category">
                        <div class="ratio-category-title">💧 Liquidity / तरलता</div>
                        {{#each financialRatios.liquidity}}
                        <div class="ratio-item">
                            <span class="ratio-name">{{name}}</span>
                            <span>
                                <span class="ratio-value">{{value}}{{unit}}</span>
                                <span class="ratio-status status-{{status}}"></span>
                            </span>
                        </div>
                        {{/each}}
                    </div>
                    {{/if}}

                    <!-- Solvency Ratios -->
                    {{#if financialRatios.solvency}}
                    <div class="ratio-category">
                        <div class="ratio-category-title">🏦 Solvency / शोधन क्षमता</div>
                        {{#each financialRatios.solvency}}
                        <div class="ratio-item">
                            <span class="ratio-name">{{name}}</span>
                            <span>
                                <span class="ratio-value">{{value}}{{unit}}</span>
                                <span class="ratio-status status-{{status}}"></span>
                            </span>
                        </div>
                        {{/each}}
                    </div>
                    {{/if}}

                    <!-- Efficiency Ratios -->
                    {{#if financialRatios.efficiency}}
                    <div class="ratio-category">
                        <div class="ratio-category-title">⚡ Efficiency / दक्षता</div>
                        {{#each financialRatios.efficiency}}
                        <div class="ratio-item">
                            <span class="ratio-name">{{name}}</span>
                            <span>
                                <span class="ratio-value">{{value}}{{unit}}</span>
                                <span class="ratio-status status-{{status}}"></span>
                            </span>
                        </div>
                        {{/each}}
                    </div>
                    {{/if}}
                </div>
            </div>
            {{/if}}

            <!-- Budget Analysis -->
            {{#if budgetAnalysis}}
            <div class="section">
                <div class="section-title">
                    <span class="section-icon">📊</span>
                    Budget Analysis
                    <span class="section-title-hindi">बजट विश्लेषण</span>
                </div>

                <!-- Budget Summary -->
                <div class="summary-grid" style="grid-template-columns: repeat(4, 1fr);">
                    <div class="summary-card">
                        <div class="summary-label">Total Budget</div>
                        <div class="summary-value">{{#formatCurrency budgetAnalysis.totalBudget currency}}{{/formatCurrency}}</div>
                    </div>
                    <div class="summary-card">
                        <div class="summary-label">Total Actual</div>
                        <div class="summary-value">{{#formatCurrency budgetAnalysis.totalActual currency}}{{/formatCurrency}}</div>
                    </div>
                    <div class="summary-card">
                        <div class="summary-label">Variance</div>
                        <div class="summary-value {{#if (gt budgetAnalysis.totalVariance 0)}}positive{{else}}negative{{/if}}">
                            {{#formatCurrency budgetAnalysis.totalVariance currency}}{{/formatCurrency}}
                        </div>
                    </div>
                    <div class="summary-card">
                        <div class="summary-label">Variance %</div>
                        <div class="summary-value {{#if (gt budgetAnalysis.totalVariancePercent 0)}}positive{{else}}negative{{/if}}">
                            {{budgetAnalysis.totalVariancePercent}}%
                        </div>
                    </div>
                </div>

                <!-- Budget Items Table -->
                <table class="financial-table">
                    <thead>
                        <tr>
                            <th>Category <span class="hindi">श्रेणी</span></th>
                            <th class="amount">Budgeted <span class="hindi">बजट</span></th>
                            <th class="amount">Actual <span class="hindi">वास्तविक</span></th>
                            <th class="amount">Variance <span class="hindi">विचलन</span></th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each budgetAnalysis.items}}
                        <tr>
                            <td>{{category}}{{#if subcategory}} - {{subcategory}}{{/if}}</td>
                            <td class="amount">{{#formatCurrency budgeted ../currency}}{{/formatCurrency}}</td>
                            <td class="amount">{{#formatCurrency actual ../currency}}{{/formatCurrency}}</td>
                            <td class="amount">
                                <span class="variance-badge variance-{{#if (gt variance 0)}}positive{{else if (lt variance 0)}}negative{{else}}neutral{{/if}}">
                                    {{#if (gt variance 0)}}+{{/if}}{{variancePercent}}%
                                </span>
                            </td>
                            <td>
                                <div class="budget-bar" style="width: 80px; height: 12px;">
                                    <div class="budget-bar-fill budget-{{status}}" style="width: {{#calculatePercent actual budgeted}}{{/calculatePercent}}%;"></div>
                                </div>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            {{/if}}
        </div>

        <!-- ============================================
             PAGE 5: TAX & AUDIT
             ============================================ -->
        <div class="page">
            <!-- Tax Summary -->
            {{#if taxSummary}}
            <div class="section">
                <div class="section-title">
                    <span class="section-icon">🏛️</span>
                    Tax Summary
                    <span class="section-title-hindi">कर सारांश</span>
                </div>

                <div class="tax-summary-grid">
                    <div class="tax-card">
                        <div class="tax-label">Total Tax Liability / कुल कर देयता</div>
                        <div class="tax-value">{{#formatCurrency taxSummary.totalTaxLiability currency}}{{/formatCurrency}}</div>
                    </div>
                    <div class="tax-card">
                        <div class="tax-label">Tax Paid / भुगतान किया गया कर</div>
                        <div class="tax-value" style="color: var(--success-color);">{{#formatCurrency taxSummary.totalTaxPaid currency}}{{/formatCurrency}}</div>
                    </div>
                    <div class="tax-card">
                        <div class="tax-label">Tax Due / देय कर</div>
                        <div class="tax-value" style="color: var(--danger-color);">{{#formatCurrency taxSummary.totalTaxDue currency}}{{/formatCurrency}}</div>
                    </div>
                </div>

                <table class="financial-table">
                    <thead>
                        <tr>
                            <th>Tax Type <span class="hindi">कर प्रकार</span></th>
                            <th class="amount">Taxable Amount</th>
                            <th class="amount">Rate</th>
                            <th class="amount">Tax Amount</th>
                            <th>Due Date</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each taxSummary.items}}
                        <tr>
                            <td><strong>{{type}}</strong><br><span style="font-size: 7pt; color: var(--text-muted);">{{description}}</span></td>
                            <td class="amount">{{#formatCurrency taxableAmount ../currency}}{{/formatCurrency}}</td>
                            <td class="amount">{{rate}}%</td>
                            <td class="amount">{{#formatCurrency taxAmount ../currency}}{{/formatCurrency}}</td>
                            <td>{{#if dueDate}}{{dueDate}}{{else}}—{{/if}}</td>
                            <td>
                                <span class="tax-status tax-{{status}}">{{status}}</span>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            {{/if}}

            <!-- Audit Information -->
            {{#if auditInfo}}
            <div class="section">
                <div class="section-title">
                    <span class="section-icon">✅</span>
                    Audit Information
                    <span class="section-title-hindi">लेखा परीक्षा जानकारी</span>
                </div>

                <div class="audit-box">
                    <div class="audit-header">
                        <div>
                            <strong style="font-size: 11pt;">{{auditInfo.auditorName}}</strong>
                            {{#if auditInfo.auditorFirm}}
                            <br><span style="color: var(--text-muted);">{{auditInfo.auditorFirm}}</span>
                            {{/if}}
                        </div>
                        {{#if auditInfo.auditOpinion}}
                        <span class="audit-opinion opinion-{{auditInfo.auditOpinion}}">
                            {{#ifEquals auditInfo.auditOpinion 'unqualified'}}Unqualified Opinion{{/ifEquals}}
                            {{#ifEquals auditInfo.auditOpinion 'qualified'}}Qualified Opinion{{/ifEquals}}
                            {{#ifEquals auditInfo.auditOpinion 'adverse'}}Adverse Opinion{{/ifEquals}}
                            {{#ifEquals auditInfo.auditOpinion 'disclaimer'}}Disclaimer of Opinion{{/ifEquals}}
                        </span>
                        {{/if}}
                    </div>
                    <div class="audit-details">
                        <div class="audit-detail-item">
                            <span class="audit-detail-label">Audit Type</span>
                            <span class="audit-detail-value">
                                {{#ifEquals auditInfo.auditType 'internal'}}Internal Audit{{/ifEquals}}
                                {{#ifEquals auditInfo.auditType 'external'}}External Audit{{/ifEquals}}
                                {{#ifEquals auditInfo.auditType 'statutory'}}Statutory Audit{{/ifEquals}}
                            </span>
                        </div>
                        {{#if auditInfo.registrationNumber}}
                        <div class="audit-detail-item">
                            <span class="audit-detail-label">Registration No.</span>
                            <span class="audit-detail-value">{{auditInfo.registrationNumber}}</span>
                        </div>
                        {{/if}}
                        {{#if auditInfo.auditDate}}
                        <div class="audit-detail-item">
                            <span class="audit-detail-label">Audit Date</span>
                            <span class="audit-detail-value">{{auditInfo.auditDate}}</span>
                        </div>
                        {{/if}}
                    </div>
                    {{#if auditInfo.notes}}
                    <div class="mt-sm">
                        <strong style="font-size: 9pt;">Audit Notes:</strong>
                        <ol class="notes-list">
                            {{#each auditInfo.notes}}
                            <li>{{this}}</li>
                            {{/each}}
                        </ol>
                    </div>
                    {{/if}}
                </div>
            </div>
            {{/if}}

            <!-- Certification -->
            {{#if certification}}
            <div class="certification-box">
                <div class="certification-title">📜 Certification / प्रमाणन</div>
                <div class="certification-statement">
                    "{{certification.statement}}"
                </div>
                <div class="certification-signature">
                    <div>
                        <div style="font-weight: 600;">{{certification.certifiedBy.name}}</div>
                        <div style="font-size: 8pt; color: var(--text-muted);">{{certification.certifiedBy.designation}}</div>
                    </div>
                    <div style="font-size: 9pt;">
                        Date: {{certification.date}}
                    </div>
                </div>
            </div>
            {{/if}}

            <!-- Signatures -->
            <div class="signatures-section">
                <div class="section-title">
                    <span class="section-icon">✍️</span>
                    Authorizations
                    <span class="section-title-hindi">प्राधिकरण</span>
                </div>
                <div class="signatures-grid">
                    <div class="signature-block">
                        <div class="signature-line"></div>
                        <div class="signature-name">{{preparedBy.name}}</div>
                        <div class="signature-designation">{{preparedBy.designation}}</div>
                        <div style="font-size: 7pt; color: var(--text-muted); margin-top: 2px;">Prepared By | Date: {{reportDate}}</div>
                    </div>

                    {{#if reviewedBy}}
                    <div class="signature-block">
                        <div class="signature-line"></div>
                        <div class="signature-name">{{reviewedBy.name}}</div>
                        <div class="signature-designation">{{reviewedBy.designation}}</div>
                        <div style="font-size: 7pt; color: var(--text-muted); margin-top: 2px;">Reviewed By | Date: __________</div>
                    </div>
                    {{/if}}

                    {{#if approvedBy}}
                    <div class="signature-block">
                        <div class="signature-line"></div>
                        <div class="signature-name">{{approvedBy.name}}</div>
                        <div class="signature-designation">{{approvedBy.designation}}</div>
                        <div style="font-size: 7pt; color: var(--text-muted); margin-top: 2px;">Approved By | Date: __________</div>
                    </div>
                    {{/if}}
                </div>
            </div>

            <!-- Footer -->
            <div style="margin-top: var(--spacing-lg); padding-top: var(--spacing-sm); border-top: 1px solid var(--border-color); text-align: center; font-size: 7pt; color: var(--text-muted);">
                <p><strong>{{organization.name}}</strong> | GSTIN: {{organization.gstin}} | CIN: {{organization.cin}}</p>
                <p>Report: {{reportNumber}} | Version: {{version}} | Currency: {{currency}} | Generated: {{reportDate}}</p>
                {{#if disclaimer}}
                <p style="margin-top: 4px; font-style: italic;">{{disclaimer}}</p>
                {{/if}}
            </div>
        </div>
    </div>
</body>
</html>
