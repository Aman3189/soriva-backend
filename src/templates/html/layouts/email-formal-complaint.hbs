<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{emailSubject}}</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - EMAIL FORMAL: COMPLAINT
       Formal Complaints to Companies, Services, Organizations
       Includes: complaintType, referenceNumber, incidentDate,
                 complaintDetails, previousAttempts, desiredResolution,
                 deadline, attachmentsList
    ═══════════════════════════════════════════════════════════════ */
    
    /* CSS Custom Properties */
    :root {
      --accent-primary: {{#if accentColor}}{{accentColor}}{{else}}#dc2626{{/if}};
      --accent-dark: {{#if accentColor}}{{accentColor}}{{else}}#b91c1c{{/if}};
      --accent-light: #fef2f2;
      --warning-gold: #ca8a04;
      --warning-bg: #fefce8;
      --success-green: #16a34a;
      --text-primary: #1a1a1a;
      --text-secondary: #475569;
      --text-muted: #64748b;
      --text-light: #94a3b8;
      --bg-white: #ffffff;
      --bg-light: #f8f9fa;
      --bg-page: #f1f5f9;
      --border-light: #e2e8f0;
      --border-red: #fecaca;
    }

    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      -webkit-print-color-adjust: exact !important;
      print-color-adjust: exact !important;
    }

    body {
      font-family: 'Georgia', 'Times New Roman', serif;
      font-size: 11pt;
      line-height: 1.7;
      color: var(--text-primary);
      background: var(--bg-page);
      padding: 20px;
    }

    /* Email Container */
    .email-container {
      max-width: 750px;
      margin: 0 auto;
      background: var(--bg-white);
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1);
      border: 1px solid var(--border-light);
    }

    /* ─────────────────────────────────────────────────────────────
       Header Section - Complaint Alert Style
    ───────────────────────────────────────────────────────────── */
    .complaint-header {
      background: linear-gradient(135deg, var(--accent-primary) 0%, var(--accent-dark) 100%);
      padding: 24px 36px;
      border-bottom: 4px solid var(--warning-gold);
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .complaint-badge {
      display: flex;
      align-items: center;
      gap: 10px;
      background: rgba(255, 255, 255, 0.15);
      padding: 10px 18px;
      border-radius: 6px;
      border: 1px solid rgba(255, 255, 255, 0.25);
    }

    .complaint-icon {
      width: 22px;
      height: 22px;
      fill: #ffffff;
    }

    .complaint-title {
      font-family: 'Arial', sans-serif;
      font-size: 12px;
      font-weight: 700;
      color: #ffffff;
      letter-spacing: 1.5px;
      text-transform: uppercase;
    }

    .complaint-date {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      color: rgba(255, 255, 255, 0.85);
    }

    /* ─────────────────────────────────────────────────────────────
       Complaint Type & Reference Banner
    ───────────────────────────────────────────────────────────── */
    .complaint-banner {
      background: var(--accent-light);
      padding: 16px 36px;
      border-bottom: 1px solid var(--border-red);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 12px;
    }

    .complaint-type-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 600;
      padding: 8px 14px;
      border-radius: 20px;
      background: var(--bg-white);
      color: var(--accent-primary);
      border: 1px solid var(--border-red);
    }

    .type-icon {
      width: 14px;
      height: 14px;
    }

    .reference-info {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      color: var(--text-secondary);
      text-align: right;
    }

    .reference-number {
      font-weight: 700;
      color: var(--accent-primary);
      font-size: 11px;
    }

    /* ─────────────────────────────────────────────────────────────
       Parties Information Section
    ───────────────────────────────────────────────────────────── */
    .parties-section {
      display: grid;
      grid-template-columns: 1fr 1fr;
      border-bottom: 1px solid var(--border-light);
    }

    .party-box {
      padding: 20px 28px;
    }

    .party-box.sender {
      background: var(--bg-light);
      border-right: 1px solid var(--border-light);
    }

    .party-box.recipient {
      background: var(--bg-white);
    }

    .party-label {
      font-family: 'Arial', sans-serif;
      font-size: 9px;
      font-weight: 700;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .party-label-icon {
      width: 12px;
      height: 12px;
      stroke: var(--text-muted);
    }

    .party-name {
      font-family: 'Arial', sans-serif;
      font-size: 12pt;
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 4px;
    }

    .party-detail {
      font-family: 'Arial', sans-serif;
      font-size: 9.5pt;
      color: var(--text-secondary);
      margin-bottom: 3px;
    }

    .party-address {
      font-size: 9pt;
      color: var(--text-muted);
      margin-top: 8px;
      padding-top: 8px;
      border-top: 1px dashed var(--border-light);
      white-space: pre-line;
      line-height: 1.5;
    }

    /* ─────────────────────────────────────────────────────────────
       Subject Line
    ───────────────────────────────────────────────────────────── */
    .subject-section {
      background: var(--bg-light);
      padding: 16px 36px;
      border-bottom: 1px solid var(--border-light);
    }

    .subject-label {
      font-family: 'Arial', sans-serif;
      font-size: 9px;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 6px;
    }

    .subject-text {
      font-family: 'Arial', sans-serif;
      font-size: 12pt;
      font-weight: 700;
      color: var(--accent-primary);
    }

    /* ─────────────────────────────────────────────────────────────
       Email Body Section
    ───────────────────────────────────────────────────────────── */
    .email-body {
      padding: 36px;
    }

    .salutation {
      font-size: 11pt;
      color: var(--text-primary);
      margin-bottom: 24px;
    }

    .email-content {
      text-align: justify;
      margin-bottom: 28px;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Complaint Details Box
    ───────────────────────────────────────────────────────────── */
    .complaint-details-box {
      background: var(--accent-light);
      border: 1px solid var(--border-red);
      border-left: 4px solid var(--accent-primary);
      border-radius: 0 8px 8px 0;
      padding: 24px;
      margin: 24px 0;
    }

    .details-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px;
    }

    .details-icon {
      width: 20px;
      height: 20px;
      fill: var(--accent-primary);
    }

    .details-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: var(--accent-dark);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .details-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    .incident-date {
      display: inline-block;
      background: var(--bg-white);
      padding: 6px 12px;
      border-radius: 4px;
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      color: var(--text-secondary);
      margin-bottom: 12px;
    }

    .incident-date strong {
      color: var(--accent-primary);
    }

    /* ─────────────────────────────────────────────────────────────
       Previous Attempts Box
    ───────────────────────────────────────────────────────────── */
    .previous-attempts-box {
      background: #f0f9ff;
      border: 1px solid #bae6fd;
      border-left: 4px solid #0284c7;
      border-radius: 0 8px 8px 0;
      padding: 24px;
      margin: 24px 0;
    }

    .attempts-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px;
    }

    .attempts-icon {
      width: 20px;
      height: 20px;
      fill: #0284c7;
    }

    .attempts-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: #075985;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .attempts-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Desired Resolution Box
    ───────────────────────────────────────────────────────────── */
    .resolution-box {
      background: #f0fdf4;
      border: 1px solid #bbf7d0;
      border-left: 4px solid var(--success-green);
      border-radius: 0 8px 8px 0;
      padding: 24px;
      margin: 24px 0;
    }

    .resolution-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px;
    }

    .resolution-icon {
      width: 20px;
      height: 20px;
      fill: var(--success-green);
    }

    .resolution-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: #166534;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .resolution-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Deadline Warning Box
    ───────────────────────────────────────────────────────────── */
    .deadline-box {
      background: var(--warning-bg);
      border: 2px solid var(--warning-gold);
      border-radius: 8px;
      padding: 16px 20px;
      margin: 24px 0;
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .deadline-icon {
      width: 28px;
      height: 28px;
      fill: var(--warning-gold);
      flex-shrink: 0;
    }

    .deadline-content {
      flex: 1;
    }

    .deadline-label {
      font-family: 'Arial', sans-serif;
      font-size: 9px;
      font-weight: 700;
      color: #a16207;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 4px;
    }

    .deadline-value {
      font-family: 'Arial', sans-serif;
      font-size: 11pt;
      font-weight: 700;
      color: #854d0e;
    }

    /* ─────────────────────────────────────────────────────────────
       Attachments Section
    ───────────────────────────────────────────────────────────── */
    .attachments-section {
      background: var(--bg-light);
      border: 1px solid var(--border-light);
      border-radius: 8px;
      padding: 20px 24px;
      margin: 24px 0;
    }

    .attachments-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .attachments-icon {
      width: 18px;
      height: 18px;
      stroke: var(--text-muted);
    }

    .attachments-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .attachments-list {
      font-family: 'Arial', sans-serif;
      font-size: 9.5pt;
      color: var(--text-secondary);
      white-space: pre-line;
      line-height: 1.8;
    }

    /* ─────────────────────────────────────────────────────────────
       Closing & Signature
    ───────────────────────────────────────────────────────────── */
    .email-closing {
      margin-top: 36px;
      padding-top: 24px;
      border-top: 1px solid var(--border-light);
    }

    .closing-phrase {
      color: var(--text-primary);
      margin-bottom: 28px;
      font-style: italic;
    }

    .signature-block {
      border-left: 4px solid var(--accent-primary);
      padding-left: 18px;
    }

    .sender-name {
      font-size: 13pt;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 4px;
      font-family: 'Arial', sans-serif;
    }

    .sender-designation {
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 3px;
    }

    .sender-company {
      font-size: 10pt;
      color: var(--text-muted);
      font-style: italic;
    }

    /* Extended Signature */
    .extended-signature {
      margin-top: 16px;
      padding-top: 14px;
      border-top: 1px dashed var(--border-light);
    }

    .contact-info {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
    }

    .contact-item {
      display: flex;
      align-items: center;
      margin-bottom: 5px;
      color: var(--text-muted);
    }

    .contact-item:last-child {
      margin-bottom: 0;
    }

    .contact-icon {
      width: 14px;
      height: 14px;
      margin-right: 10px;
      stroke: var(--text-light);
    }

    .contact-link {
      color: var(--accent-primary);
      text-decoration: none;
    }

    .contact-link:hover {
      text-decoration: underline;
    }

    /* ─────────────────────────────────────────────────────────────
       Footer
    ───────────────────────────────────────────────────────────── */
    .email-footer {
      background: var(--bg-light);
      padding: 16px 36px;
      border-top: 1px solid var(--border-light);
      text-align: center;
    }

    .footer-note {
      font-family: 'Arial', sans-serif;
      font-size: 8pt;
      color: var(--text-light);
      font-style: italic;
    }

    /* ─────────────────────────────────────────────────────────────
       Print Styles
    ───────────────────────────────────────────────────────────── */
    @media print {
      body {
        background: #ffffff;
        padding: 0;
      }

      .email-container {
        box-shadow: none;
        border-radius: 0;
        border: none;
        max-width: 100%;
      }

      .complaint-header,
      .complaint-details-box,
      .previous-attempts-box,
      .resolution-box,
      .deadline-box {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }

      @page {
        margin: 0.5in;
        size: A4;
      }
    }

    /* ─────────────────────────────────────────────────────────────
       Responsive Styles
    ───────────────────────────────────────────────────────────── */
    @media screen and (max-width: 640px) {
      body {
        padding: 10px;
      }

      .complaint-header,
      .complaint-banner,
      .subject-section,
      .email-body,
      .email-footer {
        padding-left: 20px;
        padding-right: 20px;
      }

      .header-content {
        flex-direction: column;
        gap: 12px;
        align-items: flex-start;
      }

      .complaint-banner {
        flex-direction: column;
        align-items: flex-start;
      }

      .reference-info {
        text-align: left;
      }

      .parties-section {
        grid-template-columns: 1fr;
      }

      .party-box.sender {
        border-right: none;
        border-bottom: 1px solid var(--border-light);
      }

      .deadline-box {
        flex-direction: column;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- ═══════════════════════════════════════════════════════════
         COMPLAINT HEADER
    ════════════════════════════════════════════════════════════ -->
    <div class="complaint-header">
      <div class="header-content">
        <div class="complaint-badge">
          <svg class="complaint-icon" viewBox="0 0 24 24">
            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/>
          </svg>
          <span class="complaint-title">Formal Complaint</span>
        </div>
        <div class="complaint-date">Date: {{emailDate}}</div>
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         COMPLAINT TYPE & REFERENCE BANNER
    ════════════════════════════════════════════════════════════ -->
    <div class="complaint-banner">
      {{#if complaint.complaintType}}
      <div class="complaint-type-badge">
        <svg class="type-icon" viewBox="0 0 24 24" fill="currentColor">
          {{#ifEquals complaint.complaintType "product"}}
          <path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/>
          {{/ifEquals}}
          {{#ifEquals complaint.complaintType "service"}}
          <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
          {{/ifEquals}}
          {{#ifEquals complaint.complaintType "billing"}}
          <path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm1 15h-2v-2h2zm0-4h-2V7h2z"/>
          {{/ifEquals}}
          {{#ifEquals complaint.complaintType "delivery"}}
          <path d="M1 3h15v13H1zM16 8h4l3 3v5h-2a3 3 0 0 1-6 0H9a3 3 0 0 1-6 0H1"/>
          {{/ifEquals}}
          {{#ifEquals complaint.complaintType "staff"}}
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2M12 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8z"/>
          {{/ifEquals}}
          {{#ifEquals complaint.complaintType "quality"}}
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14M22 4L12 14.01l-3-3"/>
          {{/ifEquals}}
        </svg>
        {{#ifEquals complaint.complaintType "product"}}Product Issue{{/ifEquals}}
        {{#ifEquals complaint.complaintType "service"}}Service Issue{{/ifEquals}}
        {{#ifEquals complaint.complaintType "billing"}}Billing Issue{{/ifEquals}}
        {{#ifEquals complaint.complaintType "delivery"}}Delivery Issue{{/ifEquals}}
        {{#ifEquals complaint.complaintType "staff"}}Staff Behavior{{/ifEquals}}
        {{#ifEquals complaint.complaintType "quality"}}Quality Issue{{/ifEquals}}
        {{#ifEquals complaint.complaintType "other"}}Other Issue{{/ifEquals}}
      </div>
      {{/if}}

      <div class="reference-info">
        {{#if complaint.referenceNumber}}
        <div class="reference-number">Ref: {{complaint.referenceNumber}}</div>
        {{/if}}
        {{#if complaint.incidentDate}}
        <div>Incident Date: {{complaint.incidentDate}}</div>
        {{/if}}
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         PARTIES INFORMATION (Sender & Recipient)
    ════════════════════════════════════════════════════════════ -->
    <div class="parties-section">
      <!-- Sender (Complainant) -->
      <div class="party-box sender">
        <div class="party-label">
          <svg class="party-label-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
            <circle cx="12" cy="7" r="4"/>
          </svg>
          From (Complainant)
        </div>
        <div class="party-name">{{senderName}}</div>
        {{#if senderDesignation}}
        <div class="party-detail">{{senderDesignation}}</div>
        {{/if}}
        {{#if senderCompany}}
        <div class="party-detail">{{senderCompany}}</div>
        {{/if}}
        {{#if senderEmail}}
        <div class="party-detail">{{senderEmail}}</div>
        {{/if}}
        {{#if senderPhone}}
        <div class="party-detail">{{senderPhone}}</div>
        {{/if}}
        {{#if senderAddress}}
        <div class="party-address">{{senderAddress}}</div>
        {{/if}}
      </div>

      <!-- Recipient (Company) -->
      <div class="party-box recipient">
        <div class="party-label">
          <svg class="party-label-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
            <polyline points="9 22 9 12 15 12 15 22"/>
          </svg>
          To (Company/Organization)
        </div>
        <div class="party-name">{{recipientName}}</div>
        {{#if recipientDesignation}}
        <div class="party-detail">{{recipientDesignation}}</div>
        {{/if}}
        {{#if recipientDepartment}}
        <div class="party-detail">{{recipientDepartment}}</div>
        {{/if}}
        {{#if recipientCompany}}
        <div class="party-detail">{{recipientCompany}}</div>
        {{/if}}
        {{#if recipientEmail}}
        <div class="party-detail">{{recipientEmail}}</div>
        {{/if}}
        {{#if recipientAddress}}
        <div class="party-address">{{recipientAddress}}</div>
        {{/if}}
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         SUBJECT LINE
    ════════════════════════════════════════════════════════════ -->
    <div class="subject-section">
      <div class="subject-label">Subject</div>
      <div class="subject-text">{{emailSubject}}</div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         EMAIL BODY
    ════════════════════════════════════════════════════════════ -->
    <div class="email-body">
      <!-- Salutation -->
      <div class="salutation">
        {{salutation}} {{recipientName}},
      </div>

      <!-- Email Content -->
      <div class="email-content">
        {{{emailBody}}}
      </div>

      <!-- Complaint Details Box -->
      {{#if complaint.complaintDescription}}
      <div class="complaint-details-box">
        <div class="details-header">
          <svg class="details-icon" viewBox="0 0 24 24">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
            <line x1="16" y1="13" x2="8" y2="13"/>
            <line x1="16" y1="17" x2="8" y2="17"/>
            <polyline points="10 9 9 9 8 9"/>
          </svg>
          <span class="details-title">Complaint Details</span>
        </div>
        {{#if complaint.incidentDate}}
        <div class="incident-date">
          <strong>Incident Date:</strong> {{complaint.incidentDate}}
        </div>
        {{/if}}
        <div class="details-content">
          {{{complaint.complaintDescription}}}
        </div>
      </div>
      {{/if}}

      <!-- Previous Attempts Box -->
      {{#if complaint.previousAttempts}}
      <div class="previous-attempts-box">
        <div class="attempts-header">
          <svg class="attempts-icon" viewBox="0 0 24 24">
            <path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/>
            <path d="M3 3v5h5"/>
          </svg>
          <span class="attempts-title">Previous Resolution Attempts</span>
        </div>
        <div class="attempts-content">
          {{{complaint.previousAttempts}}}
        </div>
      </div>
      {{/if}}

      <!-- Desired Resolution Box -->
      {{#if complaint.desiredResolution}}
      <div class="resolution-box">
        <div class="resolution-header">
          <svg class="resolution-icon" viewBox="0 0 24 24">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span class="resolution-title">Desired Resolution</span>
        </div>
        <div class="resolution-content">
          {{{complaint.desiredResolution}}}
        </div>
      </div>
      {{/if}}

      <!-- Deadline Warning Box -->
      {{#if complaint.deadline}}
      <div class="deadline-box">
        <svg class="deadline-icon" viewBox="0 0 24 24">
          <circle cx="12" cy="12" r="10"/>
          <polyline points="12 6 12 12 16 14"/>
        </svg>
        <div class="deadline-content">
          <div class="deadline-label">Response Expected By</div>
          <div class="deadline-value">{{complaint.deadline}}</div>
        </div>
      </div>
      {{/if}}

      <!-- Attachments Section -->
      {{#if attachmentsList}}
      <div class="attachments-section">
        <div class="attachments-header">
          <svg class="attachments-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48"/>
          </svg>
          <span class="attachments-title">Attachments / Enclosures</span>
        </div>
        <div class="attachments-list">{{attachmentsList}}</div>
      </div>
      {{/if}}

      <!-- ─────────────────────────────────────────────────────────
           Closing & Signature
      ────────────────────────────────────────────────────────── -->
      <div class="email-closing">
        <div class="closing-phrase">{{closing}},</div>
        
        <div class="signature-block">
          <div class="sender-name">{{senderName}}</div>
          
          {{#if senderDesignation}}
          <div class="sender-designation">{{senderDesignation}}</div>
          {{/if}}
          
          {{#if senderCompany}}
          <div class="sender-company">{{senderCompany}}</div>
          {{/if}}

          {{#if includeSignature}}
          <div class="extended-signature">
            <div class="contact-info">
              {{#if senderEmail}}
              <div class="contact-item">
                <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                  <polyline points="22,6 12,13 2,6"/>
                </svg>
                <a href="mailto:{{senderEmail}}" class="contact-link">{{senderEmail}}</a>
              </div>
              {{/if}}
              
              {{#if senderPhone}}
              <div class="contact-item">
                <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                </svg>
                <span>{{senderPhone}}</span>
              </div>
              {{/if}}

              {{#if senderAddress}}
              <div class="contact-item">
                <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                  <circle cx="12" cy="10" r="3"/>
                </svg>
                <span>{{senderAddress}}</span>
              </div>
              {{/if}}
            </div>
          </div>
          {{/if}}
        </div>
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         FOOTER
    ════════════════════════════════════════════════════════════ -->
    <div class="email-footer">
      <p class="footer-note">This formal complaint has been submitted for official records and expects a timely response.</p>
    </div>
  </div>
</body>
</html>
