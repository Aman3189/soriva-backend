<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cover Letter - {{cover.jobTitle}}</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - LETTER FORMAL: COVER LETTER
       Job Application & Career Cover Letters
       Includes: coverType, jobTitle, jobReference, keySkills,
                 achievements, whyCompany, whyYou, availability,
                 salaryExpectation, portfolioUrl, linkedinUrl
    ═══════════════════════════════════════════════════════════════ */
    
    /* CSS Custom Properties */
    :root {
      --accent-primary: {{#if accentColor}}{{accentColor}}{{else}}#059669{{/if}};
      --accent-dark: {{#if accentColor}}{{accentColor}}{{else}}#047857{{/if}};
      --accent-light: #ecfdf5;
      --accent-border: #a7f3d0;
      --purple-primary: #7c3aed;
      --purple-light: #f5f3ff;
      --purple-border: #ddd6fe;
      --blue-primary: #0284c7;
      --blue-light: #f0f9ff;
      --blue-border: #bae6fd;
      --amber-primary: #d97706;
      --amber-light: #fffbeb;
      --amber-border: #fde68a;
      --text-primary: #1a1a1a;
      --text-secondary: #475569;
      --text-muted: #64748b;
      --text-light: #94a3b8;
      --bg-white: #ffffff;
      --bg-light: #f8f9fa;
      --bg-page: #f1f5f9;
      --border-light: #e2e8f0;
      --border-medium: #cbd5e1;
    }

    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      -webkit-print-color-adjust: exact !important;
      print-color-adjust: exact !important;
    }

    body {
      font-family: 'Georgia', 'Times New Roman', serif;
      font-size: 11pt;
      line-height: 1.7;
      color: var(--text-primary);
      background: var(--bg-page);
      padding: 20px;
    }

    /* Letter Container - A4 Style */
    .letter-container {
      max-width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: var(--bg-white);
      border-radius: 4px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1);
      border: 1px solid var(--border-light);
    }

    /* ─────────────────────────────────────────────────────────────
       Header Section - Personal Branding
    ───────────────────────────────────────────────────────────── */
    .cover-header {
      background: linear-gradient(135deg, var(--accent-primary) 0%, var(--accent-dark) 100%);
      padding: 32px 40px;
      text-align: center;
      border-bottom: 4px solid var(--accent-border);
    }

    .applicant-name {
      font-family: 'Arial', sans-serif;
      font-size: 24pt;
      font-weight: 700;
      color: #ffffff;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }

    .applicant-tagline {
      font-family: 'Arial', sans-serif;
      font-size: 11pt;
      color: rgba(255, 255, 255, 0.9);
      margin-bottom: 16px;
    }

    .contact-bar {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: rgba(255, 255, 255, 0.85);
    }

    .contact-bar-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .contact-bar-icon {
      width: 14px;
      height: 14px;
      stroke: rgba(255, 255, 255, 0.85);
      fill: none;
    }

    .contact-bar a {
      color: rgba(255, 255, 255, 0.85);
      text-decoration: none;
    }

    .contact-bar a:hover {
      text-decoration: underline;
    }

    /* ─────────────────────────────────────────────────────────────
       Job Target Banner
    ───────────────────────────────────────────────────────────── */
    .job-banner {
      background: var(--accent-light);
      padding: 18px 40px;
      border-bottom: 1px solid var(--accent-border);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 12px;
    }

    .job-info {
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .job-icon {
      width: 36px;
      height: 36px;
      background: var(--bg-white);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 1px solid var(--accent-border);
    }

    .job-icon svg {
      width: 20px;
      height: 20px;
      fill: var(--accent-primary);
    }

    .job-details {
      display: flex;
      flex-direction: column;
    }

    .job-title {
      font-family: 'Arial', sans-serif;
      font-size: 12pt;
      font-weight: 700;
      color: var(--accent-dark);
    }

    .job-company {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      color: var(--text-secondary);
    }

    .job-meta {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 4px;
    }

    .job-reference {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: var(--text-muted);
      background: var(--bg-white);
      padding: 4px 10px;
      border-radius: 4px;
      border: 1px solid var(--border-light);
    }

    .cover-type-badge {
      font-family: 'Arial', sans-serif;
      font-size: 8px;
      font-weight: 600;
      padding: 4px 10px;
      border-radius: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      background: var(--accent-primary);
      color: #ffffff;
    }

    /* ─────────────────────────────────────────────────────────────
       Letter Body Section
    ───────────────────────────────────────────────────────────── */
    .letter-body {
      padding: 36px 40px;
    }

    /* Date Section */
    .letter-date {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 24px;
      text-align: right;
    }

    /* Recipient Block */
    .recipient-block {
      margin-bottom: 24px;
      line-height: 1.6;
    }

    .recipient-name {
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 2px;
    }

    .recipient-designation {
      color: var(--text-secondary);
      font-size: 10.5pt;
    }

    .recipient-company {
      color: var(--text-secondary);
      font-size: 10.5pt;
      font-weight: 500;
    }

    .recipient-address {
      color: var(--text-muted);
      font-size: 10pt;
      margin-top: 6px;
    }

    /* Subject Line */
    .subject-line {
      margin-bottom: 24px;
      padding: 14px 18px;
      background: var(--accent-light);
      border-left: 4px solid var(--accent-primary);
      border-radius: 0 6px 6px 0;
    }

    .subject-label {
      font-family: 'Arial', sans-serif;
      font-size: 8pt;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 4px;
    }

    .subject-text {
      font-weight: 600;
      color: var(--accent-dark);
      font-size: 11pt;
    }

    /* Where Found / Referral */
    .source-info {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: var(--text-muted);
      margin-bottom: 20px;
      font-style: italic;
    }

    .referrer-highlight {
      color: var(--accent-primary);
      font-weight: 600;
      font-style: normal;
    }

    /* Salutation */
    .salutation {
      font-size: 11pt;
      color: var(--text-primary);
      margin-bottom: 20px;
    }

    /* Letter Content */
    .letter-content {
      text-align: justify;
      margin-bottom: 24px;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Skills & Expertise Box
    ───────────────────────────────────────────────────────────── */
    .skills-box {
      background: var(--blue-light);
      border: 1px solid var(--blue-border);
      border-left: 4px solid var(--blue-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .skills-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .skills-icon {
      width: 20px;
      height: 20px;
      fill: var(--blue-primary);
    }

    .skills-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #075985;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .skills-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Achievements Box
    ───────────────────────────────────────────────────────────── */
    .achievements-box {
      background: var(--amber-light);
      border: 1px solid var(--amber-border);
      border-left: 4px solid var(--amber-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .achievements-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .achievements-icon {
      width: 20px;
      height: 20px;
      fill: var(--amber-primary);
    }

    .achievements-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #92400e;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .achievements-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Why Company Box
    ───────────────────────────────────────────────────────────── */
    .why-company-box {
      background: var(--purple-light);
      border: 1px solid var(--purple-border);
      border-left: 4px solid var(--purple-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .why-company-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .why-company-icon {
      width: 20px;
      height: 20px;
      fill: var(--purple-primary);
    }

    .why-company-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #5b21b6;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .why-company-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Why Hire Me Box
    ───────────────────────────────────────────────────────────── */
    .why-me-box {
      background: var(--accent-light);
      border: 1px solid var(--accent-border);
      border-left: 4px solid var(--accent-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .why-me-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .why-me-icon {
      width: 20px;
      height: 20px;
      fill: var(--accent-primary);
    }

    .why-me-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: var(--accent-dark);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .why-me-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Availability & Expectations
    ───────────────────────────────────────────────────────────── */
    .availability-section {
      background: var(--bg-light);
      border: 1px solid var(--border-light);
      border-radius: 8px;
      padding: 18px 24px;
      margin: 24px 0;
      display: flex;
      flex-wrap: wrap;
      gap: 24px;
    }

    .availability-item {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .availability-icon {
      width: 18px;
      height: 18px;
      stroke: var(--accent-primary);
      fill: none;
    }

    .availability-label {
      font-family: 'Arial', sans-serif;
      font-size: 8pt;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .availability-value {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      font-weight: 600;
      color: var(--text-primary);
    }

    /* ─────────────────────────────────────────────────────────────
       Closing & Signature Section
    ───────────────────────────────────────────────────────────── */
    .closing-section {
      margin-top: 32px;
    }

    .closing-phrase {
      font-style: italic;
      margin-bottom: 36px;
      color: var(--text-primary);
    }

    .signature-block {
      border-left: 4px solid var(--accent-primary);
      padding-left: 18px;
    }

    .signature-image {
      max-height: 50px;
      margin-bottom: 8px;
    }

    .sender-name {
      font-size: 13pt;
      font-weight: 700;
      color: var(--accent-primary);
      margin-bottom: 4px;
      font-family: 'Arial', sans-serif;
    }

    .sender-current-role {
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 2px;
    }

    .sender-experience {
      font-size: 9.5pt;
      color: var(--text-muted);
    }

    /* Extended Contact in Signature */
    .signature-contact {
      margin-top: 14px;
      padding-top: 12px;
      border-top: 1px dashed var(--border-light);
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
    }

    .contact-item {
      display: flex;
      align-items: center;
      margin-bottom: 4px;
      color: var(--text-muted);
    }

    .contact-item:last-child {
      margin-bottom: 0;
    }

    .contact-icon {
      width: 12px;
      height: 12px;
      margin-right: 8px;
      stroke: var(--text-light);
    }

    .contact-link {
      color: var(--accent-primary);
      text-decoration: none;
    }

    .contact-link:hover {
      text-decoration: underline;
    }

    /* Portfolio & LinkedIn Links */
    .profile-links {
      margin-top: 12px;
      display: flex;
      gap: 16px;
    }

    .profile-link {
      display: flex;
      align-items: center;
      gap: 6px;
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: var(--accent-primary);
      text-decoration: none;
      padding: 6px 12px;
      background: var(--accent-light);
      border-radius: 4px;
      border: 1px solid var(--accent-border);
      transition: all 0.2s ease;
    }

    .profile-link:hover {
      background: var(--accent-primary);
      color: #ffffff;
    }

    .profile-link svg {
      width: 14px;
      height: 14px;
      fill: currentColor;
    }

    /* ─────────────────────────────────────────────────────────────
       Print Styles
    ───────────────────────────────────────────────────────────── */
    @media print {
      body {
        background: #ffffff;
        padding: 0;
      }

      .letter-container {
        box-shadow: none;
        border-radius: 0;
        border: none;
        max-width: 100%;
        min-height: auto;
      }

      .cover-header,
      .job-banner,
      .skills-box,
      .achievements-box,
      .why-company-box,
      .why-me-box,
      .availability-section {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }

      @page {
        margin: 12mm;
        size: A4;
      }
    }

    /* ─────────────────────────────────────────────────────────────
       Responsive Styles
    ───────────────────────────────────────────────────────────── */
    @media screen and (max-width: 640px) {
      body {
        padding: 10px;
      }

      .cover-header {
        padding: 24px 20px;
      }

      .applicant-name {
        font-size: 18pt;
      }

      .contact-bar {
        flex-direction: column;
        gap: 8px;
      }

      .job-banner {
        padding: 16px 20px;
        flex-direction: column;
        align-items: flex-start;
      }

      .job-meta {
        align-items: flex-start;
      }

      .letter-body {
        padding: 24px 20px;
      }

      .availability-section {
        flex-direction: column;
        gap: 16px;
      }

      .profile-links {
        flex-direction: column;
        gap: 8px;
      }
    }
  </style>
</head>
<body>
  <div class="letter-container">
    <!-- ═══════════════════════════════════════════════════════════
         HEADER SECTION - Personal Branding
    ════════════════════════════════════════════════════════════ -->
    <div class="cover-header">
      <div class="applicant-name">{{senderName}}</div>
      {{#if cover.currentRole}}
      <div class="applicant-tagline">{{cover.currentRole}}</div>
      {{/if}}
      <div class="contact-bar">
        {{#if senderEmail}}
        <div class="contact-bar-item">
          <svg class="contact-bar-icon" viewBox="0 0 24 24" stroke-width="2">
            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
            <polyline points="22,6 12,13 2,6"/>
          </svg>
          <a href="mailto:{{senderEmail}}">{{senderEmail}}</a>
        </div>
        {{/if}}
        {{#if senderPhone}}
        <div class="contact-bar-item">
          <svg class="contact-bar-icon" viewBox="0 0 24 24" stroke-width="2">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72"/>
          </svg>
          <span>{{senderPhone}}</span>
        </div>
        {{/if}}
        {{#if senderCity}}
        <div class="contact-bar-item">
          <svg class="contact-bar-icon" viewBox="0 0 24 24" stroke-width="2">
            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
            <circle cx="12" cy="10" r="3"/>
          </svg>
          <span>{{senderCity}}{{#if senderState}}, {{senderState}}{{/if}}</span>
        </div>
        {{/if}}
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         JOB TARGET BANNER
    ════════════════════════════════════════════════════════════ -->
    <div class="job-banner">
      <div class="job-info">
        <div class="job-icon">
          <svg viewBox="0 0 24 24">
            <rect x="2" y="7" width="20" height="14" rx="2" ry="2"/>
            <path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/>
          </svg>
        </div>
        <div class="job-details">
          <div class="job-title">{{cover.jobTitle}}</div>
          <div class="job-company">{{cover.companyName}}</div>
        </div>
      </div>
      <div class="job-meta">
        {{#if cover.jobReference}}
        <div class="job-reference">Ref: {{cover.jobReference}}</div>
        {{/if}}
        {{#if cover.coverType}}
        <div class="cover-type-badge">
          {{#ifEquals cover.coverType "job-application"}}Job Application{{/ifEquals}}
          {{#ifEquals cover.coverType "internship"}}Internship{{/ifEquals}}
          {{#ifEquals cover.coverType "career-change"}}Career Change{{/ifEquals}}
          {{#ifEquals cover.coverType "referral"}}Referral{{/ifEquals}}
          {{#ifEquals cover.coverType "networking"}}Networking{{/ifEquals}}
          {{#ifEquals cover.coverType "cold-contact"}}Cold Contact{{/ifEquals}}
          {{#ifEquals cover.coverType "internal-transfer"}}Internal Transfer{{/ifEquals}}
          {{#ifEquals cover.coverType "promotion"}}Promotion{{/ifEquals}}
          {{#ifEquals cover.coverType "freelance"}}Freelance{{/ifEquals}}
          {{#ifEquals cover.coverType "academic"}}Academic{{/ifEquals}}
        </div>
        {{/if}}
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         LETTER BODY SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="letter-body">
      <!-- Date -->
      <div class="letter-date">{{letterDate}}</div>

      <!-- Recipient Block -->
      <div class="recipient-block">
        <div class="recipient-name">{{recipientName}}</div>
        {{#if recipientDesignation}}
        <div class="recipient-designation">{{recipientDesignation}}</div>
        {{/if}}
        {{#if recipientCompany}}
        <div class="recipient-company">{{recipientCompany}}</div>
        {{/if}}
        {{#if recipientDepartment}}
        <div class="recipient-designation">{{recipientDepartment}}</div>
        {{/if}}
        <div class="recipient-address">
          {{#if recipientAddress}}{{recipientAddress}}<br>{{/if}}
          {{#if recipientCity}}{{recipientCity}}{{#if recipientState}}, {{recipientState}}{{/if}} {{recipientPincode}}{{/if}}
        </div>
      </div>

      <!-- Subject Line -->
      {{#if subject}}
      <div class="subject-line">
        <div class="subject-label">Subject</div>
        <div class="subject-text">{{subject}}</div>
      </div>
      {{/if}}

      <!-- Where Found / Referral Info -->
      {{#if cover.whereFound}}
      <div class="source-info">
        Found this opportunity on {{cover.whereFound}}{{#if cover.referrerName}}, referred by <span class="referrer-highlight">{{cover.referrerName}}</span>{{/if}}.
      </div>
      {{else}}
      {{#if cover.referrerName}}
      <div class="source-info">
        Referred by <span class="referrer-highlight">{{cover.referrerName}}</span>.
      </div>
      {{/if}}
      {{/if}}

      <!-- Salutation -->
      <div class="salutation">
        {{salutation}},
      </div>

      <!-- Letter Content -->
      <div class="letter-content">
        {{{letterBody}}}
      </div>

      <!-- Skills & Expertise Box -->
      {{#if cover.keySkills}}
      <div class="skills-box">
        <div class="skills-header">
          <svg class="skills-icon" viewBox="0 0 24 24">
            <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
          </svg>
          <span class="skills-title">Key Skills & Expertise</span>
        </div>
        <div class="skills-content">
          {{{cover.keySkills}}}
        </div>
      </div>
      {{/if}}

      <!-- Achievements Box -->
      {{#if cover.achievements}}
      <div class="achievements-box">
        <div class="achievements-header">
          <svg class="achievements-icon" viewBox="0 0 24 24">
            <circle cx="12" cy="8" r="7"/>
            <polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88"/>
          </svg>
          <span class="achievements-title">Key Achievements</span>
        </div>
        <div class="achievements-content">
          {{{cover.achievements}}}
        </div>
      </div>
      {{/if}}

      <!-- Why This Company Box -->
      {{#if cover.whyCompany}}
      <div class="why-company-box">
        <div class="why-company-header">
          <svg class="why-company-icon" viewBox="0 0 24 24">
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
          </svg>
          <span class="why-company-title">Why {{cover.companyName}}?</span>
        </div>
        <div class="why-company-content">
          {{{cover.whyCompany}}}
        </div>
      </div>
      {{/if}}

      <!-- Why Hire Me Box -->
      {{#if cover.whyYou}}
      <div class="why-me-box">
        <div class="why-me-header">
          <svg class="why-me-icon" viewBox="0 0 24 24">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span class="why-me-title">Why I'm the Right Fit</span>
        </div>
        <div class="why-me-content">
          {{{cover.whyYou}}}
        </div>
      </div>
      {{/if}}

      <!-- Availability & Expectations -->
      {{#if cover.availability}}
      <div class="availability-section">
        {{#if cover.availability}}
        <div class="availability-item">
          <svg class="availability-icon" viewBox="0 0 24 24" stroke-width="2">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
            <line x1="16" y1="2" x2="16" y2="6"/>
            <line x1="8" y1="2" x2="8" y2="6"/>
            <line x1="3" y1="10" x2="21" y2="10"/>
          </svg>
          <div>
            <div class="availability-label">Availability</div>
            <div class="availability-value">{{cover.availability}}</div>
          </div>
        </div>
        {{/if}}

        {{#if cover.salaryExpectation}}
        <div class="availability-item">
          <svg class="availability-icon" viewBox="0 0 24 24" stroke-width="2">
            <line x1="12" y1="1" x2="12" y2="23"/>
            <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
          </svg>
          <div>
            <div class="availability-label">Expected Compensation</div>
            <div class="availability-value">{{cover.salaryExpectation}}</div>
          </div>
        </div>
        {{/if}}

        {{#if cover.yearsExperience}}
        <div class="availability-item">
          <svg class="availability-icon" viewBox="0 0 24 24" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
          <div>
            <div class="availability-label">Experience</div>
            <div class="availability-value">{{cover.yearsExperience}} Years</div>
          </div>
        </div>
        {{/if}}
      </div>
      {{/if}}

      <!-- ─────────────────────────────────────────────────────────
           Closing & Signature Section
      ────────────────────────────────────────────────────────── -->
      <div class="closing-section">
        <div class="closing-phrase">{{closing}},</div>
        
        <div class="signature-block">
          {{#if signatureImage}}
          <img src="{{signatureImage}}" alt="Signature" class="signature-image">
          {{/if}}
          
          <div class="sender-name">{{senderName}}</div>
          
          {{#if cover.currentRole}}
          <div class="sender-current-role">{{cover.currentRole}}</div>
          {{/if}}
          
          {{#if cover.yearsExperience}}
          <div class="sender-experience">{{cover.yearsExperience}}+ years of experience</div>
          {{/if}}

          {{#if includeSignature}}
          <div class="signature-contact">
            {{#if senderEmail}}
            <div class="contact-item">
              <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <polyline points="22,6 12,13 2,6"/>
              </svg>
              <a href="mailto:{{senderEmail}}" class="contact-link">{{senderEmail}}</a>
            </div>
            {{/if}}
            
            {{#if senderPhone}}
            <div class="contact-item">
              <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72"/>
              </svg>
              <span>{{senderPhone}}</span>
            </div>
            {{/if}}
          </div>

          <!-- Portfolio & LinkedIn Links -->
          {{#if cover.portfolioUrl}}
          <div class="profile-links">
            {{#if cover.portfolioUrl}}
            <a href="{{cover.portfolioUrl}}" class="profile-link" target="_blank">
              <svg viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="10"/>
                <line x1="2" y1="12" x2="22" y2="12"/>
                <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
              </svg>
              Portfolio
            </a>
            {{/if}}
            {{#if cover.linkedinUrl}}
            <a href="{{cover.linkedinUrl}}" class="profile-link" target="_blank">
              <svg viewBox="0 0 24 24">
                <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"/>
                <rect x="2" y="9" width="4" height="12"/>
                <circle cx="4" cy="4" r="2"/>
              </svg>
              LinkedIn
            </a>
            {{/if}}
          </div>
          {{/if}}
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</body>
</html>
