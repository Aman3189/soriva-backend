{{!-- 
  ═══════════════════════════════════════════════════════════════════════════════
  SORIVA - ASSIGNMENT TEMPLATE: WORKSHEET
  Style: Fill-in practice worksheet
  Best For: School worksheets, coaching practice, competitive exam prep
  Features: Clean Q&A layout, answer blanks, multiple question types, scoring
  ═══════════════════════════════════════════════════════════════════════════════
--}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - Worksheet</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       CSS RESET & BASE STYLES
    ═══════════════════════════════════════════════════════════════ */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --primary-color: {{#if accentColor}}{{accentColor}}{{else}}#dc2626{{/if}};
      --secondary-color: #b91c1c;
      --accent-color: #2563eb;
      --success-color: #059669;
      --text-color: #1f2937;
      --text-light: #6b7280;
      --text-muted: #9ca3af;
      --border-color: #d1d5db;
      --border-dark: #9ca3af;
      --bg-light: #f9fafb;
      --font-sans: 'Segoe UI', 'Arial', sans-serif;
      --font-mono: 'Consolas', 'Monaco', monospace;
    }

    /* ═══════════════════════════════════════════════════════════════
       PAGE SETUP
    ═══════════════════════════════════════════════════════════════ */
    @page {
      size: A4;
      margin: 0.5in;
    }

    @media print {
      body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
      .page-break { page-break-before: always; }
    }

    html {
      font-size: 11pt;
    }

    body {
      font-family: var(--font-sans);
      line-height: 1.5;
      color: var(--text-color);
      background: #fff;
      padding: 0;
      max-width: 8.5in;
      margin: 0 auto;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER SECTION
    ═══════════════════════════════════════════════════════════════ */
    .worksheet-header {
      border: 2px solid var(--primary-color);
      margin-bottom: 1rem;
    }

    .header-top {
      background: var(--primary-color);
      color: #fff;
      padding: 0.6rem 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .header-top .institution {
      font-size: 1rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .header-top .worksheet-badge {
      background: rgba(255,255,255,0.2);
      padding: 0.25rem 0.75rem;
      border-radius: 15px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .header-body {
      padding: 0.75rem 1rem;
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 1rem;
    }

    .worksheet-title {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--primary-color);
      margin-bottom: 0.25rem;
    }

    .worksheet-subtitle {
      font-size: 0.9rem;
      color: var(--text-light);
    }

    .header-meta {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
      font-size: 0.85rem;
    }

    .meta-row {
      display: flex;
      gap: 0.5rem;
    }

    .meta-row .label {
      color: var(--text-light);
      min-width: 60px;
    }

    .meta-row .value {
      font-weight: 500;
    }

    /* ═══════════════════════════════════════════════════════════════
       STUDENT INFO STRIP
    ═══════════════════════════════════════════════════════════════ */
    .student-strip {
      display: flex;
      gap: 1.5rem;
      padding: 0.75rem 1rem;
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      margin-bottom: 1rem;
    }

    .student-field {
      display: flex;
      align-items: center;
      gap: 0.4rem;
    }

    .student-field .field-label {
      font-size: 0.8rem;
      color: var(--text-light);
    }

    .student-field .field-input {
      border-bottom: 1.5px solid var(--border-dark);
      min-width: 120px;
      font-weight: 500;
    }

    .student-field .field-input.short {
      min-width: 60px;
    }

    /* ═══════════════════════════════════════════════════════════════
       INFO BAR (Marks, Time, etc.)
    ═══════════════════════════════════════════════════════════════ */
    .info-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.6rem 1rem;
      background: linear-gradient(90deg, var(--bg-light) 0%, #fff 100%);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      margin-bottom: 1rem;
    }

    .info-item {
      display: flex;
      align-items: center;
      gap: 0.4rem;
      font-size: 0.85rem;
    }

    .info-item .info-icon {
      font-size: 1rem;
    }

    .info-item .info-label {
      color: var(--text-light);
    }

    .info-item .info-value {
      font-weight: 700;
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       INSTRUCTIONS BOX
    ═══════════════════════════════════════════════════════════════ */
    .instructions-box {
      background: #fffbeb;
      border: 1px solid #fde68a;
      border-left: 4px solid #f59e0b;
      padding: 0.75rem 1rem;
      margin-bottom: 1rem;
      border-radius: 0 8px 8px 0;
    }

    .instructions-box .inst-title {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #d97706;
      font-weight: 700;
      margin-bottom: 0.4rem;
    }

    .instructions-box ul {
      list-style: none;
      margin: 0;
      font-size: 0.85rem;
    }

    .instructions-box li {
      padding: 0.15rem 0;
      padding-left: 1.25rem;
      position: relative;
    }

    .instructions-box li::before {
      content: "→";
      position: absolute;
      left: 0;
      color: #f59e0b;
    }

    /* ═══════════════════════════════════════════════════════════════
       SECTION HEADER
    ═══════════════════════════════════════════════════════════════ */
    .section-header {
      background: var(--primary-color);
      color: #fff;
      padding: 0.5rem 1rem;
      font-size: 0.9rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin: 1rem 0 0.75rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .section-header .section-marks {
      background: rgba(255,255,255,0.2);
      padding: 0.2rem 0.6rem;
      border-radius: 10px;
      font-size: 0.75rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       QUESTION STYLES
    ═══════════════════════════════════════════════════════════════ */
    .question-item {
      padding: 0.75rem 0;
      border-bottom: 1px dashed var(--border-color);
      display: flex;
      gap: 0.75rem;
    }

    .question-item:last-child {
      border-bottom: none;
    }

    .q-number {
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      width: 28px;
      height: 28px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.8rem;
      font-weight: 700;
      color: var(--primary-color);
      flex-shrink: 0;
    }

    .q-content {
      flex: 1;
    }

    .q-text {
      font-size: 0.95rem;
      line-height: 1.6;
      margin-bottom: 0.5rem;
    }

    .q-marks {
      font-size: 0.7rem;
      color: var(--text-muted);
      float: right;
      background: var(--bg-light);
      padding: 0.15rem 0.5rem;
      border-radius: 10px;
    }

    /* ═══════════════════════════════════════════════════════════════
       ANSWER BLANKS
    ═══════════════════════════════════════════════════════════════ */
    .answer-blank {
      display: block;
      width: 100%;
      border-bottom: 1.5px solid var(--border-dark);
      min-height: 28px;
      margin-top: 0.5rem;
    }

    .answer-blank.multi-line {
      min-height: 60px;
      border: 1px solid var(--border-color);
      border-radius: 4px;
      background: repeating-linear-gradient(
        transparent,
        transparent 26px,
        var(--border-color) 26px,
        var(--border-color) 27px
      );
    }

    .answer-blank.large {
      min-height: 100px;
    }

    /* Inline blank for fill-in-the-blank */
    .inline-blank {
      display: inline-block;
      border-bottom: 1.5px solid var(--primary-color);
      min-width: 80px;
      margin: 0 0.25rem;
      text-align: center;
    }

    /* ═══════════════════════════════════════════════════════════════
       MCQ OPTIONS
    ═══════════════════════════════════════════════════════════════ */
    .mcq-options {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 0.4rem 1.5rem;
      margin-top: 0.5rem;
    }

    .mcq-options.single-column {
      grid-template-columns: 1fr;
    }

    .mcq-option {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.9rem;
    }

    .mcq-option .option-box {
      width: 18px;
      height: 18px;
      border: 1.5px solid var(--border-dark);
      border-radius: 3px;
      flex-shrink: 0;
    }

    .mcq-option .option-circle {
      width: 18px;
      height: 18px;
      border: 1.5px solid var(--border-dark);
      border-radius: 50%;
      flex-shrink: 0;
    }

    .mcq-option .option-letter {
      font-weight: 600;
      color: var(--primary-color);
      min-width: 20px;
    }

    /* ═══════════════════════════════════════════════════════════════
       TRUE / FALSE
    ═══════════════════════════════════════════════════════════════ */
    .tf-options {
      display: flex;
      gap: 1.5rem;
      margin-top: 0.5rem;
    }

    .tf-option {
      display: flex;
      align-items: center;
      gap: 0.4rem;
      font-size: 0.9rem;
    }

    .tf-option .tf-box {
      width: 20px;
      height: 20px;
      border: 1.5px solid var(--border-dark);
      border-radius: 3px;
    }

    /* ═══════════════════════════════════════════════════════════════
       MATCHING COLUMNS
    ═══════════════════════════════════════════════════════════════ */
    .matching-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 0.5rem;
      font-size: 0.9rem;
    }

    .matching-table th {
      background: var(--bg-light);
      padding: 0.5rem;
      text-align: left;
      font-weight: 600;
      border: 1px solid var(--border-color);
    }

    .matching-table td {
      padding: 0.5rem;
      border: 1px solid var(--border-color);
      vertical-align: top;
    }

    .matching-table .answer-col {
      width: 80px;
      text-align: center;
    }

    .matching-table .match-blank {
      display: inline-block;
      border-bottom: 1.5px solid var(--border-dark);
      width: 40px;
      text-align: center;
    }

    /* ═══════════════════════════════════════════════════════════════
       DIAGRAM / FIGURE SPACE
    ═══════════════════════════════════════════════════════════════ */
    .diagram-space {
      border: 2px dashed var(--border-color);
      border-radius: 8px;
      padding: 1.5rem;
      margin: 0.75rem 0;
      text-align: center;
      min-height: 120px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      color: var(--text-muted);
    }

    .diagram-space img {
      max-width: 100%;
      max-height: 150px;
    }

    .diagram-space .diagram-label {
      font-size: 0.8rem;
      margin-top: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       CALCULATION SPACE
    ═══════════════════════════════════════════════════════════════ */
    .calculation-space {
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 1rem;
      margin-top: 0.5rem;
      min-height: 100px;
      background: 
        linear-gradient(90deg, transparent 19px, var(--border-color) 19px, var(--border-color) 20px, transparent 20px),
        linear-gradient(var(--border-color) 1px, transparent 1px);
      background-size: 20px 25px;
    }

    .calculation-space .space-label {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-muted);
      margin-bottom: 0.5rem;
      background: #fff;
      display: inline-block;
      padding-right: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       SUB-QUESTIONS
    ═══════════════════════════════════════════════════════════════ */
    .sub-questions {
      margin-top: 0.5rem;
      padding-left: 1rem;
    }

    .sub-question {
      display: flex;
      gap: 0.5rem;
      padding: 0.4rem 0;
    }

    .sub-question .sub-label {
      font-weight: 600;
      color: var(--text-light);
      min-width: 25px;
    }

    .sub-question .sub-text {
      flex: 1;
    }

    .sub-question .sub-marks {
      font-size: 0.7rem;
      color: var(--text-muted);
    }

    .sub-question .sub-blank {
      display: block;
      border-bottom: 1.5px solid var(--border-dark);
      min-height: 25px;
      margin-top: 0.3rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       WORD BANK
    ═══════════════════════════════════════════════════════════════ */
    .word-bank {
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 0.75rem 1rem;
      margin-bottom: 1rem;
    }

    .word-bank .bank-title {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-light);
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .word-bank .words {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    .word-bank .word {
      background: #fff;
      border: 1px solid var(--border-color);
      padding: 0.25rem 0.75rem;
      border-radius: 15px;
      font-size: 0.85rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       SCORING TABLE
    ═══════════════════════════════════════════════════════════════ */
    .scoring-table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 1.5rem;
      font-size: 0.85rem;
    }

    .scoring-table th, .scoring-table td {
      border: 1px solid var(--border-color);
      padding: 0.5rem;
      text-align: center;
    }

    .scoring-table th {
      background: var(--bg-light);
      font-weight: 600;
    }

    .scoring-table .total-row {
      background: var(--primary-color);
      color: #fff;
      font-weight: 700;
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
    ═══════════════════════════════════════════════════════════════ */
    .worksheet-footer {
      margin-top: 1.5rem;
      padding-top: 0.75rem;
      border-top: 2px solid var(--primary-color);
      display: flex;
      justify-content: space-between;
      font-size: 0.8rem;
      color: var(--text-muted);
    }

    .worksheet-footer .footer-note {
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       ANSWER KEY (Optional - for teacher's copy)
    ═══════════════════════════════════════════════════════════════ */
    .answer-key {
      margin-top: 2rem;
      padding: 1rem;
      background: #ecfdf5;
      border: 2px solid #10b981;
      border-radius: 10px;
    }

    .answer-key .key-title {
      font-size: 0.9rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #059669;
      margin-bottom: 0.75rem;
      text-align: center;
    }

    .answer-key .key-grid {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 0.5rem;
      font-size: 0.85rem;
    }

    .answer-key .key-item {
      display: flex;
      gap: 0.4rem;
    }

    .answer-key .key-num {
      font-weight: 600;
      color: #059669;
    }
  </style>
</head>
<body>

  <!-- ═══════════════════════════════════════════════════════════════
       HEADER
  ═══════════════════════════════════════════════════════════════ -->
  <header class="worksheet-header">
    <div class="header-top">
      <div class="institution">{{institution}}</div>
      <div class="worksheet-badge">Worksheet</div>
    </div>
    
    <div class="header-body">
      <div>
        <h1 class="worksheet-title">{{title}}</h1>
        {{#if subtitle}}
        <div class="worksheet-subtitle">{{subtitle}}</div>
        {{/if}}
      </div>
      
      <div class="header-meta">
        {{#if subject}}
        <div class="meta-row">
          <span class="label">Subject:</span>
          <span class="value">{{subject}}</span>
        </div>
        {{/if}}
        {{#if chapter}}
        <div class="meta-row">
          <span class="label">Chapter:</span>
          <span class="value">{{chapter}}</span>
        </div>
        {{/if}}
        {{#if class}}
        <div class="meta-row">
          <span class="label">Class:</span>
          <span class="value">{{class}}</span>
        </div>
        {{/if}}
      </div>
    </div>
  </header>

  <!-- ═══════════════════════════════════════════════════════════════
       STUDENT INFO STRIP
  ═══════════════════════════════════════════════════════════════ -->
  <div class="student-strip">
    <div class="student-field">
      <span class="field-label">Name:</span>
      <span class="field-input">{{#if studentName}}{{studentName}}{{/if}}</span>
    </div>
    <div class="student-field">
      <span class="field-label">Roll No:</span>
      <span class="field-input short">{{#if rollNumber}}{{rollNumber}}{{/if}}</span>
    </div>
    <div class="student-field">
      <span class="field-label">Section:</span>
      <span class="field-input short">{{#if section}}{{section}}{{/if}}</span>
    </div>
    <div class="student-field">
      <span class="field-label">Date:</span>
      <span class="field-input short">{{#if submissionDate}}{{formatDate submissionDate}}{{/if}}</span>
    </div>
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       INFO BAR
  ═══════════════════════════════════════════════════════════════ -->
  <div class="info-bar">
    {{#if totalMarks}}
    <div class="info-item">
      <span class="info-icon">📝</span>
      <span class="info-label">Total Marks:</span>
      <span class="info-value">{{totalMarks}}</span>
    </div>
    {{/if}}
    
    {{#if duration}}
    <div class="info-item">
      <span class="info-icon">⏱️</span>
      <span class="info-label">Time:</span>
      <span class="info-value">{{duration}}</span>
    </div>
    {{/if}}
    
    {{#if questions}}
    <div class="info-item">
      <span class="info-icon">❓</span>
      <span class="info-label">Questions:</span>
      <span class="info-value">{{questions.length}}</span>
    </div>
    {{/if}}
    
    {{#if instructor}}
    <div class="info-item">
      <span class="info-icon">👤</span>
      <span class="info-label">Teacher:</span>
      <span class="info-value">{{instructor}}</span>
    </div>
    {{/if}}
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       INSTRUCTIONS
  ═══════════════════════════════════════════════════════════════ -->
  {{#if instructions}}
  <div class="instructions-box">
    <div class="inst-title">📋 Instructions</div>
    <ul>
      {{#each instructions}}
      <li>{{this}}</li>
      {{/each}}
    </ul>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       WORD BANK (if provided for fill-in-the-blanks)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if wordBank}}
  <div class="word-bank">
    <div class="bank-title">📦 Word Bank (Use these words to fill in the blanks)</div>
    <div class="words">
      {{#each wordBank}}
      <span class="word">{{this}}</span>
      {{/each}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       QUESTIONS
  ═══════════════════════════════════════════════════════════════ -->
  {{#if questions}}
  <section class="questions-section">
    {{#each questions}}
    <div class="question-item">
      <div class="q-number">{{this.questionNumber}}</div>
      <div class="q-content">
        <div class="q-text">
          {{#if ../showMarks}}
          {{#if this.marks}}
          <span class="q-marks">[{{this.marks}} marks]</span>
          {{/if}}
          {{/if}}
          {{this.questionText}}
        </div>
        
        {{!-- Sub-questions --}}
        {{#if this.subQuestions}}
        <div class="sub-questions">
          {{#each this.subQuestions}}
          <div class="sub-question">
            <span class="sub-label">({{this.label}})</span>
            <span class="sub-text">
              {{this.text}}
              {{#if this.marks}}<span class="sub-marks">[{{this.marks}}]</span>{{/if}}
            </span>
          </div>
          <div class="sub-blank" style="margin-left: 25px;"></div>
          {{/each}}
        </div>
        {{/if}}
        
        {{!-- MCQ Options --}}
        {{#if this.options}}
        <div class="mcq-options {{#ifLte this.options.length 4}}{{else}}single-column{{/ifLte}}">
          {{#each this.options}}
          <div class="mcq-option">
            <div class="option-circle"></div>
            <span class="option-letter">({{this.letter}})</span>
            <span>{{this.text}}</span>
          </div>
          {{/each}}
        </div>
        {{/if}}
        
        {{!-- True/False --}}
        {{#ifEquals this.questionType "true-false"}}
        <div class="tf-options">
          <div class="tf-option">
            <div class="tf-box"></div>
            <span>True</span>
          </div>
          <div class="tf-option">
            <div class="tf-box"></div>
            <span>False</span>
          </div>
        </div>
        {{/ifEquals}}
        
        {{!-- Diagram Space --}}
        {{#if this.diagram}}
        <div class="diagram-space">
          {{#if this.diagram}}
          <img src="{{this.diagram}}" alt="Diagram">
          {{else}}
          <span>📊 Diagram Space</span>
          {{/if}}
          <div class="diagram-label">Figure {{this.questionNumber}}</div>
        </div>
        {{/if}}
        
        {{!-- Answer Space based on question type --}}
        {{#ifEquals this.questionType "short-answer"}}
        <div class="answer-blank"></div>
        {{/ifEquals}}
        
        {{#ifEquals this.questionType "long-answer"}}
        <div class="answer-blank multi-line large"></div>
        {{/ifEquals}}
        
        {{#ifEquals this.questionType "numerical"}}
        <div class="calculation-space">
          <div class="space-label">Show your working:</div>
        </div>
        <div style="margin-top: 0.5rem;">
          <strong>Answer:</strong> <span class="inline-blank" style="min-width: 100px;"></span>
        </div>
        {{/ifEquals}}
        
        {{#ifEquals this.questionType "diagram"}}
        <div class="diagram-space" style="min-height: 150px;">
          <span>✏️ Draw your diagram here</span>
        </div>
        {{/ifEquals}}
        
        {{!-- Default answer blank if no specific type --}}
        {{#unless this.questionType}}
        {{#unless this.options}}
        {{#unless this.subQuestions}}
        <div class="answer-blank"></div>
        {{/unless}}
        {{/unless}}
        {{/unless}}
        
      </div>
    </div>
    {{/each}}
  </section>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       SCORING TABLE (for grading)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if showRubric}}
  {{#if rubric}}
  <table class="scoring-table">
    <thead>
      <tr>
        <th>Section</th>
        <th>Max Marks</th>
        <th>Obtained</th>
      </tr>
    </thead>
    <tbody>
      {{#each rubric}}
      <tr>
        <td style="text-align: left;">{{this.criterion}}</td>
        <td>{{this.maxMarks}}</td>
        <td></td>
      </tr>
      {{/each}}
      <tr class="total-row">
        <td>TOTAL</td>
        <td>{{totalMarks}}</td>
        <td></td>
      </tr>
    </tbody>
  </table>
  {{else}}
  <table class="scoring-table">
    <thead>
      <tr>
        <th colspan="2">For Office Use Only</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td style="width: 70%;">Total Marks Obtained</td>
        <td style="min-width: 80px; height: 40px;"></td>
      </tr>
      <tr>
        <td>Out of</td>
        <td>{{totalMarks}}</td>
      </tr>
      <tr>
        <td>Teacher's Signature</td>
        <td style="height: 50px;"></td>
      </tr>
    </tbody>
  </table>
  {{/if}}
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       ANSWER KEY (if showAnswers is true - Teacher's Copy)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if showAnswers}}
  {{#if questions}}
  <div class="answer-key">
    <div class="key-title">🔑 Answer Key (Teacher's Copy)</div>
    <div class="key-grid">
      {{#each questions}}
      {{#if this.answer}}
      <div class="key-item">
        <span class="key-num">{{this.questionNumber}}.</span>
        <span>{{this.answer}}</span>
      </div>
      {{/if}}
      {{/each}}
    </div>
  </div>
  {{/if}}
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       FOOTER
  ═══════════════════════════════════════════════════════════════ -->
  <footer class="worksheet-footer">
    <div class="footer-note">*** All the best! ***</div>
    <div>{{institution}}</div>
  </footer>

</body>
</html>
