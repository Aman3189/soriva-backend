<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - Study Notes</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- MathJax for Formula Rendering -->
  <script src="https://polyfill.io/v3/polyfill.min.js?features=es6"></script>
  <script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - STUDY NOTES TEMPLATE (COMPREHENSIVE)
       Style: Modern
       Supports: Nursery → Primary → Secondary → Higher Secondary → Competitive
       Subjects: Science, Maths, Humanities, Languages, Commerce
       "Balwadi se IIT tak" - Complete Education Coverage
       ═══════════════════════════════════════════════════════════════ */
    
    :root {
      --primary: #1a365d;
      --primary-light: #2d4a6f;
      --primary-lighter: #3d5a80;
      --accent: #3182ce;
      --accent-light: #63b3ed;
      --success: #10b981;
      --warning: #f59e0b;
      --danger: #ef4444;
      --info: #6366f1;
      
      --bg-primary: #ffffff;
      --bg-secondary: #f8fafc;
      --bg-tertiary: #f1f5f9;
      
      --text-primary: #1e293b;
      --text-secondary: #475569;
      --text-muted: #94a3b8;
      
      --border-light: #e2e8f0;
      --border-medium: #cbd5e1;
      
      --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
      --shadow-md: 0 4px 6px rgba(0,0,0,0.07);
      --shadow-lg: 0 10px 25px rgba(0,0,0,0.1);
      --shadow-xl: 0 20px 50px rgba(0,0,0,0.15);
      
      --radius-sm: 4px;
      --radius-md: 8px;
      --radius-lg: 12px;
      --radius-xl: 16px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: linear-gradient(135deg, #e2e8f0 0%, #f1f5f9 100%);
      color: var(--text-primary);
      line-height: 1.7;
      padding: 30px;
      min-height: 100vh;
    }

    /* ═══════════════════════════════════════════════════════════════
       MAIN CONTAINER - A4 PAGE
       ═══════════════════════════════════════════════════════════════ */
    
    .notes-container {
      width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: var(--bg-primary);
      box-shadow: var(--shadow-xl);
      border-radius: var(--radius-md);
      position: relative;
      overflow: hidden;
    }

    /* Multi-page support */
    .page-break {
      page-break-before: always;
      padding-top: 30px;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER - Title & Meta Information
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showHeader}}
    .header {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 50%, var(--primary-lighter) 100%);
      color: white;
      padding: 30px 40px;
      position: relative;
      overflow: hidden;
    }

    .header::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -20%;
      width: 60%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
      pointer-events: none;
    }

    .header::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--accent) 0%, var(--accent-light) 50%, var(--accent) 100%);
    }

    .header-content {
      position: relative;
      z-index: 1;
    }

    .header-top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 15px;
    }

    .subject-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 6px 14px;
      background: rgba(255,255,255,0.15);
      border-radius: 20px;
      font-size: 12px;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .subject-badge i {
      font-size: 11px;
    }

    .difficulty-badge {
      padding: 5px 12px;
      border-radius: 15px;
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .difficulty-easy { background: rgba(16, 185, 129, 0.2); color: #a7f3d0; }
    .difficulty-medium { background: rgba(245, 158, 11, 0.2); color: #fcd34d; }
    .difficulty-hard { background: rgba(239, 68, 68, 0.2); color: #fca5a5; }

    .notes-title {
      font-size: 28px;
      font-weight: 700;
      letter-spacing: -0.5px;
      margin-bottom: 8px;
      line-height: 1.3;
    }

    .notes-subtitle {
      font-size: 14px;
      font-weight: 300;
      color: rgba(255,255,255,0.8);
      margin-bottom: 15px;
    }

    .header-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      font-size: 12px;
    }

    .meta-item {
      display: flex;
      align-items: center;
      gap: 6px;
      color: rgba(255,255,255,0.85);
    }

    .meta-item i {
      font-size: 11px;
      color: rgba(255,255,255,0.6);
      width: 14px;
      text-align: center;
    }

    .meta-item strong {
      font-weight: 600;
      color: white;
    }

    /* Exam/Board Badge */
    .exam-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 4px 10px;
      background: rgba(245, 158, 11, 0.25);
      border: 1px solid rgba(245, 158, 11, 0.4);
      border-radius: 4px;
      font-size: 11px;
      font-weight: 600;
      color: #fcd34d;
    }

    .exam-badge i {
      color: #fbbf24;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       MAIN CONTENT AREA
       ═══════════════════════════════════════════════════════════════ */
    
    .notes-body {
      padding: 35px 40px;
    }

    /* ═══════════════════════════════════════════════════════════════
       TABLE OF CONTENTS
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showTableOfContents}}
    .toc {
      background: var(--bg-secondary);
      border: 1px solid var(--border-light);
      border-radius: var(--radius-lg);
      padding: 20px 25px;
      margin-bottom: 30px;
    }

    .toc-title {
      font-size: 14px;
      font-weight: 700;
      color: var(--primary);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .toc-title i {
      font-size: 14px;
    }

    .toc-list {
      list-style: none;
      columns: 2;
      column-gap: 30px;
    }

    .toc-item {
      padding: 6px 0;
      border-bottom: 1px dashed var(--border-light);
      font-size: 13px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .toc-item:last-child {
      border-bottom: none;
    }

    .toc-number {
      width: 22px;
      height: 22px;
      background: var(--primary);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 10px;
      font-weight: 600;
      flex-shrink: 0;
    }

    .toc-link {
      color: var(--text-secondary);
      text-decoration: none;
      transition: color 0.2s;
    }

    .toc-link:hover {
      color: var(--primary);
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       SECTION STYLES
       ═══════════════════════════════════════════════════════════════ */
    
    .section {
      margin-bottom: 30px;
    }

    .section-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 18px;
      padding-bottom: 10px;
      border-bottom: 2px solid var(--primary);
    }

    .section-icon {
      width: 36px;
      height: 36px;
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
      border-radius: var(--radius-md);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 16px;
    }

    .section-title {
      font-size: 18px;
      font-weight: 700;
      color: var(--primary);
      letter-spacing: -0.3px;
    }

    .section-title-hi {
      font-size: 12px;
      color: var(--text-muted);
      font-weight: 400;
      margin-left: 8px;
    }

    /* ═══════════════════════════════════════════════════════════════
       INTRODUCTION & SUMMARY
       ═══════════════════════════════════════════════════════════════ */
    
    .intro-box {
      background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
      border-left: 4px solid var(--accent);
      border-radius: 0 var(--radius-md) var(--radius-md) 0;
      padding: 20px 25px;
      margin-bottom: 25px;
    }

    .intro-box p {
      font-size: 14px;
      color: var(--text-secondary);
      line-height: 1.8;
    }

    .summary-box {
      background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
      border: 1px solid rgba(16, 185, 129, 0.2);
      border-radius: var(--radius-lg);
      padding: 20px 25px;
      margin-bottom: 25px;
    }

    .summary-box .section-mini-title {
      color: #065f46;
    }

    .section-mini-title {
      font-size: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .section-mini-title i {
      font-size: 12px;
    }

    /* ═══════════════════════════════════════════════════════════════
       KEY POINTS
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showKeyPoints}}
    .key-points {
      background: var(--bg-secondary);
      border-radius: var(--radius-lg);
      padding: 25px;
      margin-bottom: 25px;
    }

    .key-points-list {
      list-style: none;
    }

    .key-point-item {
      display: flex;
      align-items: flex-start;
      gap: 12px;
      padding: 12px 0;
      border-bottom: 1px solid var(--border-light);
    }

    .key-point-item:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }

    .key-point-icon {
      width: 24px;
      height: 24px;
      background: var(--primary);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 10px;
      flex-shrink: 0;
      margin-top: 2px;
    }

    .key-point-icon.critical { background: var(--danger); }
    .key-point-icon.important { background: var(--warning); }
    .key-point-icon.moderate { background: var(--info); }

    .key-point-content {
      flex: 1;
    }

    .key-point-text {
      font-size: 14px;
      font-weight: 500;
      color: var(--text-primary);
      margin-bottom: 4px;
    }

    .key-point-explanation {
      font-size: 12px;
      color: var(--text-secondary);
    }

    .key-point-exam-tag {
      display: inline-block;
      padding: 2px 8px;
      background: rgba(245, 158, 11, 0.15);
      color: #b45309;
      border-radius: 10px;
      font-size: 10px;
      font-weight: 600;
      margin-top: 6px;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       DEFINITIONS
       ═══════════════════════════════════════════════════════════════ */
    
    .definitions-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 15px;
      margin-bottom: 25px;
    }

    .definition-card {
      background: white;
      border: 1px solid var(--border-light);
      border-radius: var(--radius-md);
      padding: 18px;
      transition: all 0.2s;
    }

    .definition-card:hover {
      border-color: var(--accent);
      box-shadow: var(--shadow-md);
    }

    .definition-term {
      font-size: 15px;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 6px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .definition-term-hi {
      font-size: 12px;
      color: var(--text-muted);
      font-weight: 400;
    }

    .definition-meaning {
      font-size: 13px;
      color: var(--text-secondary);
      line-height: 1.6;
      margin-bottom: 8px;
    }

    .definition-example {
      font-size: 12px;
      color: var(--text-muted);
      font-style: italic;
      padding-left: 12px;
      border-left: 2px solid var(--border-medium);
    }

    .definition-example strong {
      color: var(--text-secondary);
      font-style: normal;
    }

    /* ═══════════════════════════════════════════════════════════════
       FORMULAS SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showFormulas}}
    .formulas-section {
      margin-bottom: 30px;
    }

    .formula-card {
      background: linear-gradient(135deg, #fefce8 0%, #fef9c3 100%);
      border: 1px solid rgba(234, 179, 8, 0.3);
      border-radius: var(--radius-lg);
      padding: 20px;
      margin-bottom: 15px;
    }

    .formula-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 12px;
    }

    .formula-name {
      font-size: 14px;
      font-weight: 700;
      color: #854d0e;
    }

    .formula-importance {
      padding: 3px 8px;
      background: rgba(234, 179, 8, 0.2);
      color: #a16207;
      border-radius: 10px;
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
    }

    .formula-display {
      background: white;
      border-radius: var(--radius-md);
      padding: 15px 20px;
      text-align: center;
      font-family: 'JetBrains Mono', monospace;
      font-size: 18px;
      color: var(--primary);
      font-weight: 600;
      margin-bottom: 12px;
      border: 1px solid rgba(234, 179, 8, 0.2);
    }

    .formula-description {
      font-size: 13px;
      color: #713f12;
      margin-bottom: 10px;
    }

    .formula-variables {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 10px;
    }

    .variable-tag {
      display: inline-flex;
      align-items: center;
      gap: 5px;
      padding: 4px 10px;
      background: white;
      border-radius: 15px;
      font-size: 11px;
      border: 1px solid rgba(234, 179, 8, 0.3);
    }

    .variable-symbol {
      font-family: 'JetBrains Mono', monospace;
      font-weight: 600;
      color: var(--primary);
    }

    .variable-name {
      color: var(--text-secondary);
    }

    .variable-unit {
      color: var(--text-muted);
      font-size: 10px;
    }

    .formula-conditions {
      font-size: 12px;
      color: #92400e;
      font-style: italic;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .formula-conditions i {
      font-size: 11px;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       THEOREMS & LAWS
       ═══════════════════════════════════════════════════════════════ */
    
    .theorem-card {
      background: linear-gradient(135deg, #faf5ff 0%, #f3e8ff 100%);
      border: 1px solid rgba(147, 51, 234, 0.2);
      border-left: 4px solid #9333ea;
      border-radius: 0 var(--radius-lg) var(--radius-lg) 0;
      padding: 20px;
      margin-bottom: 15px;
    }

    .theorem-name {
      font-size: 15px;
      font-weight: 700;
      color: #6b21a8;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .theorem-name i {
      font-size: 14px;
    }

    .theorem-statement {
      font-size: 14px;
      color: #581c87;
      line-height: 1.7;
      padding: 12px 15px;
      background: rgba(255,255,255,0.6);
      border-radius: var(--radius-md);
      margin-bottom: 12px;
    }

    .theorem-proof {
      font-size: 13px;
      color: var(--text-secondary);
    }

    .theorem-proof-title {
      font-weight: 600;
      color: #7c3aed;
      margin-bottom: 6px;
    }

    /* ═══════════════════════════════════════════════════════════════
       CHEMICAL REACTIONS (Chemistry)
       ═══════════════════════════════════════════════════════════════ */
    
    .reaction-card {
      background: linear-gradient(135deg, #ecfeff 0%, #cffafe 100%);
      border: 1px solid rgba(6, 182, 212, 0.2);
      border-radius: var(--radius-lg);
      padding: 18px;
      margin-bottom: 15px;
    }

    .reaction-name {
      font-size: 13px;
      font-weight: 600;
      color: #0e7490;
      margin-bottom: 10px;
    }

    .reaction-equation {
      background: white;
      padding: 12px 18px;
      border-radius: var(--radius-md);
      font-family: 'JetBrains Mono', monospace;
      font-size: 15px;
      text-align: center;
      color: var(--text-primary);
      margin-bottom: 10px;
      border: 1px solid rgba(6, 182, 212, 0.2);
    }

    .reaction-type {
      display: inline-block;
      padding: 3px 10px;
      background: rgba(6, 182, 212, 0.15);
      color: #0891b2;
      border-radius: 10px;
      font-size: 11px;
      font-weight: 600;
    }

    .reaction-details {
      margin-top: 10px;
      font-size: 12px;
      color: #155e75;
    }

    /* ═══════════════════════════════════════════════════════════════
       BIOLOGICAL PROCESSES (Biology)
       ═══════════════════════════════════════════════════════════════ */
    
    .bio-process-card {
      background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
      border: 1px solid rgba(34, 197, 94, 0.2);
      border-radius: var(--radius-lg);
      padding: 18px;
      margin-bottom: 15px;
    }

    .bio-process-name {
      font-size: 15px;
      font-weight: 700;
      color: #166534;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .bio-process-name i {
      color: #22c55e;
    }

    .bio-process-steps {
      list-style: none;
      counter-reset: bio-step;
    }

    .bio-step {
      counter-increment: bio-step;
      display: flex;
      align-items: flex-start;
      gap: 10px;
      padding: 8px 0;
      font-size: 13px;
      color: #15803d;
    }

    .bio-step::before {
      content: counter(bio-step);
      width: 22px;
      height: 22px;
      background: #22c55e;
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 11px;
      font-weight: 600;
      flex-shrink: 0;
    }

    .bio-io {
      display: flex;
      gap: 20px;
      margin-top: 12px;
      padding-top: 12px;
      border-top: 1px dashed rgba(34, 197, 94, 0.3);
    }

    .bio-io-box {
      flex: 1;
      background: rgba(255,255,255,0.7);
      padding: 10px;
      border-radius: var(--radius-md);
    }

    .bio-io-label {
      font-size: 10px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: #16a34a;
      margin-bottom: 5px;
    }

    .bio-io-items {
      font-size: 12px;
      color: #166534;
    }

    /* ═══════════════════════════════════════════════════════════════
       TIMELINE (History)
       ═══════════════════════════════════════════════════════════════ */
    
    .timeline {
      position: relative;
      padding-left: 30px;
      margin-bottom: 25px;
    }

    .timeline::before {
      content: '';
      position: absolute;
      left: 8px;
      top: 0;
      bottom: 0;
      width: 3px;
      background: linear-gradient(180deg, var(--primary) 0%, var(--accent) 100%);
      border-radius: 2px;
    }

    .timeline-item {
      position: relative;
      padding: 15px 20px;
      background: var(--bg-secondary);
      border-radius: var(--radius-md);
      margin-bottom: 15px;
      margin-left: 20px;
    }

    .timeline-item::before {
      content: '';
      position: absolute;
      left: -28px;
      top: 20px;
      width: 14px;
      height: 14px;
      background: var(--accent);
      border: 3px solid white;
      border-radius: 50%;
      box-shadow: var(--shadow-sm);
    }

    .timeline-year {
      display: inline-block;
      padding: 3px 10px;
      background: var(--primary);
      color: white;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .timeline-event {
      font-size: 14px;
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 5px;
    }

    .timeline-description {
      font-size: 13px;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    .timeline-significance {
      margin-top: 8px;
      padding: 8px 12px;
      background: rgba(99, 102, 241, 0.1);
      border-radius: var(--radius-sm);
      font-size: 12px;
      color: #4338ca;
    }

    .timeline-significance strong {
      font-weight: 600;
    }

    /* ═══════════════════════════════════════════════════════════════
       HISTORICAL FIGURES / PERSONALITIES
       ═══════════════════════════════════════════════════════════════ */
    
    .personality-card {
      display: flex;
      gap: 15px;
      background: var(--bg-secondary);
      border-radius: var(--radius-lg);
      padding: 18px;
      margin-bottom: 15px;
      border: 1px solid var(--border-light);
    }

    .personality-avatar {
      width: 60px;
      height: 60px;
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 24px;
      font-weight: 700;
      flex-shrink: 0;
    }

    .personality-info {
      flex: 1;
    }

    .personality-name {
      font-size: 16px;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 3px;
    }

    .personality-title {
      font-size: 12px;
      color: var(--accent);
      font-weight: 500;
      margin-bottom: 8px;
    }

    .personality-lifespan {
      font-size: 11px;
      color: var(--text-muted);
    }

    .personality-contributions {
      margin-top: 10px;
    }

    .contribution-tag {
      display: inline-block;
      padding: 3px 10px;
      background: rgba(49, 130, 206, 0.1);
      color: var(--accent);
      border-radius: 12px;
      font-size: 11px;
      margin: 2px 4px 2px 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       GRAMMAR RULES (Languages)
       ═══════════════════════════════════════════════════════════════ */
    
    .grammar-card {
      background: linear-gradient(135deg, #fdf4ff 0%, #fae8ff 100%);
      border: 1px solid rgba(192, 38, 211, 0.2);
      border-radius: var(--radius-lg);
      padding: 18px;
      margin-bottom: 15px;
    }

    .grammar-rule {
      font-size: 14px;
      font-weight: 600;
      color: #86198f;
      margin-bottom: 10px;
    }

    .grammar-explanation {
      font-size: 13px;
      color: #a21caf;
      margin-bottom: 12px;
      line-height: 1.6;
    }

    .grammar-examples {
      background: rgba(255,255,255,0.7);
      border-radius: var(--radius-md);
      padding: 12px;
    }

    .grammar-example-item {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 13px;
      padding: 5px 0;
      color: #701a75;
    }

    .grammar-example-item i {
      color: #c026d3;
      font-size: 10px;
    }

    .grammar-example-correct {
      color: #16a34a;
      font-weight: 500;
    }

    .grammar-example-incorrect {
      color: #dc2626;
      text-decoration: line-through;
    }

    /* ═══════════════════════════════════════════════════════════════
       VOCABULARY
       ═══════════════════════════════════════════════════════════════ */
    
    .vocabulary-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 12px;
      margin-bottom: 25px;
    }

    .vocab-card {
      background: white;
      border: 1px solid var(--border-light);
      border-radius: var(--radius-md);
      padding: 14px;
      transition: all 0.2s;
    }

    .vocab-card:hover {
      border-color: var(--accent);
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    .vocab-word {
      font-size: 15px;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 4px;
    }

    .vocab-pronunciation {
      font-size: 11px;
      color: var(--text-muted);
      font-style: italic;
      margin-bottom: 6px;
    }

    .vocab-meaning {
      font-size: 13px;
      color: var(--text-secondary);
      margin-bottom: 8px;
    }

    .vocab-hindi {
      font-size: 12px;
      color: var(--accent);
      margin-bottom: 6px;
    }

    .vocab-usage {
      font-size: 12px;
      color: var(--text-muted);
      font-style: italic;
      padding-left: 10px;
      border-left: 2px solid var(--border-light);
    }

    /* ═══════════════════════════════════════════════════════════════
       MNEMONICS & MEMORY AIDS
       ═══════════════════════════════════════════════════════════════ */
    
    .mnemonic-card {
      background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
      border: 1px solid rgba(217, 119, 6, 0.3);
      border-radius: var(--radius-lg);
      padding: 18px;
      margin-bottom: 15px;
    }

    .mnemonic-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 12px;
    }

    .mnemonic-icon {
      width: 32px;
      height: 32px;
      background: #f59e0b;
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
    }

    .mnemonic-topic {
      font-size: 13px;
      font-weight: 600;
      color: #92400e;
    }

    .mnemonic-text {
      font-size: 18px;
      font-weight: 700;
      color: #78350f;
      text-align: center;
      padding: 15px;
      background: rgba(255,255,255,0.6);
      border-radius: var(--radius-md);
      margin-bottom: 12px;
      letter-spacing: 2px;
    }

    .mnemonic-explanation {
      font-size: 13px;
      color: #92400e;
      text-align: center;
    }

    .mnemonic-items {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 8px;
      margin-top: 10px;
    }

    .mnemonic-item {
      padding: 5px 12px;
      background: white;
      border-radius: 15px;
      font-size: 12px;
      font-weight: 500;
      color: #b45309;
    }

    /* ═══════════════════════════════════════════════════════════════
       DIAGRAMS & VISUAL AIDS
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showDiagrams}}
    .diagram-container {
      background: var(--bg-secondary);
      border: 1px solid var(--border-light);
      border-radius: var(--radius-lg);
      padding: 20px;
      margin-bottom: 20px;
      text-align: center;
    }

    .diagram-title {
      font-size: 14px;
      font-weight: 600;
      color: var(--primary);
      margin-bottom: 15px;
    }

    .diagram-placeholder {
      background: linear-gradient(135deg, #f1f5f9 0%, #e2e8f0 100%);
      border: 2px dashed var(--border-medium);
      border-radius: var(--radius-md);
      padding: 40px;
      color: var(--text-muted);
    }

    .diagram-placeholder i {
      font-size: 40px;
      margin-bottom: 10px;
      display: block;
    }

    .diagram-labels {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 10px;
      margin-top: 15px;
    }

    .diagram-label {
      padding: 4px 12px;
      background: var(--primary);
      color: white;
      border-radius: 15px;
      font-size: 11px;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       EXAMPLES SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showExamples}}
    .example-card {
      background: white;
      border: 1px solid var(--border-light);
      border-left: 4px solid var(--accent);
      border-radius: 0 var(--radius-md) var(--radius-md) 0;
      padding: 18px;
      margin-bottom: 15px;
    }

    .example-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }

    .example-title {
      font-size: 14px;
      font-weight: 600;
      color: var(--primary);
    }

    .example-difficulty {
      padding: 3px 10px;
      border-radius: 12px;
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
    }

    .example-difficulty.easy { background: #dcfce7; color: #166534; }
    .example-difficulty.medium { background: #fef3c7; color: #92400e; }
    .example-difficulty.hard { background: #fee2e2; color: #991b1b; }

    .example-question {
      font-size: 14px;
      color: var(--text-primary);
      margin-bottom: 15px;
      padding: 12px;
      background: var(--bg-secondary);
      border-radius: var(--radius-md);
    }

    .example-solution {
      padding-left: 15px;
      border-left: 2px solid var(--accent);
    }

    .example-step {
      font-size: 13px;
      color: var(--text-secondary);
      margin-bottom: 8px;
      display: flex;
      align-items: flex-start;
      gap: 10px;
    }

    .step-number {
      width: 20px;
      height: 20px;
      background: var(--accent);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 10px;
      font-weight: 600;
      flex-shrink: 0;
    }

    .example-answer {
      margin-top: 12px;
      padding: 10px 15px;
      background: linear-gradient(135deg, #dcfce7 0%, #bbf7d0 100%);
      border-radius: var(--radius-md);
      font-size: 14px;
      font-weight: 600;
      color: #166534;
    }

    .example-answer strong {
      color: #15803d;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       PRACTICE QUESTIONS
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showPracticeQuestions}}
    .practice-section {
      background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
      border-radius: var(--radius-lg);
      padding: 25px;
      margin-bottom: 25px;
    }

    .practice-question {
      background: white;
      border-radius: var(--radius-md);
      padding: 18px;
      margin-bottom: 15px;
      border: 1px solid rgba(59, 130, 246, 0.2);
    }

    .practice-question:last-child {
      margin-bottom: 0;
    }

    .question-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 10px;
    }

    .question-number {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 28px;
      height: 28px;
      background: var(--accent);
      color: white;
      border-radius: 50%;
      font-size: 12px;
      font-weight: 700;
    }

    .question-meta {
      display: flex;
      gap: 10px;
      align-items: center;
    }

    .question-type {
      padding: 3px 10px;
      background: rgba(99, 102, 241, 0.1);
      color: #4f46e5;
      border-radius: 10px;
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
    }

    .question-marks {
      font-size: 12px;
      font-weight: 600;
      color: var(--accent);
    }

    .question-text {
      font-size: 14px;
      color: var(--text-primary);
      line-height: 1.7;
      margin-bottom: 12px;
    }

    /* MCQ Options */
    .mcq-options {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
    }

    .mcq-option {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 10px 15px;
      background: var(--bg-secondary);
      border-radius: var(--radius-md);
      font-size: 13px;
      color: var(--text-secondary);
      transition: all 0.2s;
      cursor: pointer;
    }

    .mcq-option:hover {
      background: rgba(59, 130, 246, 0.1);
      color: var(--accent);
    }

    .mcq-option-letter {
      width: 24px;
      height: 24px;
      background: var(--border-light);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      font-weight: 600;
      color: var(--text-muted);
    }

    .mcq-option:hover .mcq-option-letter {
      background: var(--accent);
      color: white;
    }

    /* Previous Year Question Tag */
    .pyq-tag {
      display: inline-flex;
      align-items: center;
      gap: 5px;
      padding: 3px 10px;
      background: rgba(234, 179, 8, 0.15);
      color: #b45309;
      border-radius: 10px;
      font-size: 10px;
      font-weight: 600;
    }

    .pyq-tag i {
      font-size: 10px;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       QUICK REVISION SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showRevisionSection}}
    .revision-section {
      background: linear-gradient(135deg, #fef2f2 0%, #fecaca 100%);
      border: 2px solid rgba(239, 68, 68, 0.3);
      border-radius: var(--radius-lg);
      padding: 25px;
      margin-bottom: 25px;
    }

    .revision-title {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 16px;
      font-weight: 700;
      color: #991b1b;
      margin-bottom: 18px;
    }

    .revision-title i {
      font-size: 18px;
      color: #ef4444;
    }

    .revision-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 12px;
    }

    .revision-item {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      padding: 12px;
      background: rgba(255,255,255,0.8);
      border-radius: var(--radius-md);
    }

    .revision-bullet {
      width: 8px;
      height: 8px;
      background: #ef4444;
      border-radius: 50%;
      flex-shrink: 0;
      margin-top: 6px;
    }

    .revision-text {
      font-size: 13px;
      color: #7f1d1d;
      line-height: 1.5;
    }

    .revision-category {
      display: inline-block;
      padding: 2px 8px;
      background: rgba(239, 68, 68, 0.15);
      color: #b91c1c;
      border-radius: 8px;
      font-size: 10px;
      font-weight: 600;
      margin-top: 5px;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       EXAM TIPS
       ═══════════════════════════════════════════════════════════════ */
    
    .exam-tips {
      background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
      border: 1px solid rgba(34, 197, 94, 0.3);
      border-radius: var(--radius-lg);
      padding: 20px;
      margin-bottom: 25px;
    }

    .exam-tips-title {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 14px;
      font-weight: 700;
      color: #166534;
      margin-bottom: 15px;
    }

    .exam-tips-title i {
      color: #22c55e;
    }

    .exam-tip-item {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      padding: 8px 0;
      font-size: 13px;
      color: #15803d;
    }

    .exam-tip-item i {
      color: #22c55e;
      margin-top: 3px;
    }

    /* Common Mistakes */
    .common-mistakes {
      background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
      border: 1px solid rgba(239, 68, 68, 0.2);
      border-radius: var(--radius-lg);
      padding: 20px;
      margin-bottom: 25px;
    }

    .common-mistakes-title {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 14px;
      font-weight: 700;
      color: #991b1b;
      margin-bottom: 15px;
    }

    .common-mistakes-title i {
      color: #ef4444;
    }

    .mistake-item {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      padding: 8px 0;
      font-size: 13px;
      color: #b91c1c;
    }

    .mistake-item i {
      color: #ef4444;
      margin-top: 3px;
    }

    /* ═══════════════════════════════════════════════════════════════
       TABLES
       ═══════════════════════════════════════════════════════════════ */
    
    .data-table-container {
      overflow-x: auto;
      margin-bottom: 20px;
    }

    .data-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 13px;
    }

    .data-table caption {
      font-size: 14px;
      font-weight: 600;
      color: var(--primary);
      margin-bottom: 10px;
      text-align: left;
    }

    .data-table th {
      background: var(--primary);
      color: white;
      padding: 12px 15px;
      text-align: left;
      font-weight: 600;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .data-table th:first-child {
      border-radius: var(--radius-md) 0 0 0;
    }

    .data-table th:last-child {
      border-radius: 0 var(--radius-md) 0 0;
    }

    .data-table td {
      padding: 12px 15px;
      border-bottom: 1px solid var(--border-light);
      color: var(--text-secondary);
    }

    .data-table tr:nth-child(even) {
      background: var(--bg-secondary);
    }

    .data-table tr:hover {
      background: rgba(49, 130, 206, 0.05);
    }

    .data-table tr:last-child td:first-child {
      border-radius: 0 0 0 var(--radius-md);
    }

    .data-table tr:last-child td:last-child {
      border-radius: 0 0 var(--radius-md) 0;
    }

    /* Comparison Table */
    .comparison-table th {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
    }

    .comparison-table .aspect-cell {
      font-weight: 600;
      color: var(--primary);
      background: var(--bg-secondary);
    }

    /* ═══════════════════════════════════════════════════════════════
       REFERENCES SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .references-section {
      background: var(--bg-secondary);
      border-radius: var(--radius-lg);
      padding: 20px;
      margin-bottom: 25px;
    }

    .references-title {
      font-size: 14px;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .reference-item {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 8px 0;
      font-size: 13px;
      color: var(--text-secondary);
      border-bottom: 1px dashed var(--border-light);
    }

    .reference-item:last-child {
      border-bottom: none;
    }

    .reference-item i {
      color: var(--accent);
      width: 16px;
      text-align: center;
    }

    .reference-item a {
      color: var(--accent);
      text-decoration: none;
    }

    .reference-item a:hover {
      text-decoration: underline;
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showFooter}}
    .footer {
      background: var(--bg-secondary);
      border-top: 1px solid var(--border-light);
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .footer-left {
      display: flex;
      align-items: center;
      gap: 20px;
      font-size: 11px;
      color: var(--text-muted);
    }

    .footer-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .footer-item i {
      color: var(--primary);
      font-size: 10px;
    }

    .footer-right {
      font-size: 11px;
      color: var(--text-muted);
    }

    {{#if showPageNumbers}}
    .page-number {
      font-weight: 600;
      color: var(--primary);
    }
    {{/if}}
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       PRINT STYLES
       ═══════════════════════════════════════════════════════════════ */
    
    @media print {
      body {
        background: white;
        padding: 0;
      }
      
      .notes-container {
        box-shadow: none;
        width: 100%;
        min-height: auto;
        border-radius: 0;
      }

      .page-break {
        page-break-before: always;
      }

      .header::after {
        display: none;
      }
    }

    @page {
      size: A4;
      margin: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       RESPONSIVE (for screen viewing)
       ═══════════════════════════════════════════════════════════════ */
    
    @media screen and (max-width: 900px) {
      body {
        padding: 15px;
      }

      .notes-container {
        width: 100%;
        min-height: auto;
      }

      .header {
        padding: 25px;
      }

      .notes-title {
        font-size: 22px;
      }

      .notes-body {
        padding: 25px;
      }

      .toc-list {
        columns: 1;
      }

      .definitions-grid,
      .vocabulary-grid {
        grid-template-columns: 1fr;
      }

      .mcq-options {
        grid-template-columns: 1fr;
      }

      .revision-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="notes-container">
    
    <!-- ═══════════════════════════════════════════════════════════════
         HEADER
         ═══════════════════════════════════════════════════════════════ -->
    {{#if showHeader}}
    <header class="header">
      <div class="header-content">
        <div class="header-top">
          <div class="subject-badge">
            {{#if (eq subjectCategory "science")}}<i class="fas fa-flask"></i>{{/if}}
            {{#if (eq subjectCategory "mathematics")}}<i class="fas fa-square-root-alt"></i>{{/if}}
            {{#if (eq subjectCategory "social-science")}}<i class="fas fa-globe-asia"></i>{{/if}}
            {{#if (eq subjectCategory "languages")}}<i class="fas fa-language"></i>{{/if}}
            {{#if (eq subjectCategory "commerce")}}<i class="fas fa-chart-line"></i>{{/if}}
            {{#unless subjectCategory}}<i class="fas fa-book"></i>{{/unless}}
            {{subject}}
          </div>
          
          {{#if difficulty}}
          <span class="difficulty-badge difficulty-{{lowercase difficulty}}">
            {{difficulty}}
          </span>
          {{/if}}
        </div>

        <h1 class="notes-title">{{title}}</h1>
        
        {{#if subtitle}}
        <p class="notes-subtitle">{{subtitle}}</p>
        {{/if}}

        <div class="header-meta">
          {{#if topic}}
          <span class="meta-item">
            <i class="fas fa-bookmark"></i>
            <strong>Topic:</strong> {{topic}}
          </span>
          {{/if}}
          
          {{#if chapter}}
          <span class="meta-item">
            <i class="fas fa-book-open"></i>
            {{chapter}}
          </span>
          {{/if}}
          
          {{#if class}}
          <span class="meta-item">
            <i class="fas fa-graduation-cap"></i>
            {{class}}
          </span>
          {{/if}}
          
          {{#if board}}
          <span class="meta-item">
            <i class="fas fa-university"></i>
            {{board}}
          </span>
          {{/if}}

          {{#if targetExam}}
          <span class="exam-badge">
            <i class="fas fa-bullseye"></i>
            {{targetExam}}
          </span>
          {{/if}}

          {{#if weightage}}
          <span class="meta-item">
            <i class="fas fa-percent"></i>
            Weightage: <strong>{{weightage}}</strong>
          </span>
          {{/if}}

          {{#if estimatedTime}}
          <span class="meta-item">
            <i class="fas fa-clock"></i>
            {{estimatedTime}}
          </span>
          {{/if}}
        </div>
      </div>
    </header>
    {{/if}}

    <!-- ═══════════════════════════════════════════════════════════════
         MAIN CONTENT
         ═══════════════════════════════════════════════════════════════ -->
    <main class="notes-body">
      
      <!-- Table of Contents -->
      {{#if showTableOfContents}}
      <nav class="toc">
        <h2 class="toc-title"><i class="fas fa-list"></i> Contents</h2>
        <ol class="toc-list">
          {{#if introduction}}<li class="toc-item"><span class="toc-number">1</span><a href="#introduction" class="toc-link">Introduction</a></li>{{/if}}
          {{#if keyPoints}}<li class="toc-item"><span class="toc-number">2</span><a href="#keypoints" class="toc-link">Key Points</a></li>{{/if}}
          {{#if definitions}}<li class="toc-item"><span class="toc-number">3</span><a href="#definitions" class="toc-link">Definitions</a></li>{{/if}}
          {{#if formulas}}<li class="toc-item"><span class="toc-number">4</span><a href="#formulas" class="toc-link">Formulas</a></li>{{/if}}
          {{#if theorems}}<li class="toc-item"><span class="toc-number">5</span><a href="#theorems" class="toc-link">Theorems & Laws</a></li>{{/if}}
          {{#if examples}}<li class="toc-item"><span class="toc-number">6</span><a href="#examples" class="toc-link">Solved Examples</a></li>{{/if}}
          {{#if practiceQuestions}}<li class="toc-item"><span class="toc-number">7</span><a href="#practice" class="toc-link">Practice Questions</a></li>{{/if}}
          {{#if quickRevision}}<li class="toc-item"><span class="toc-number">8</span><a href="#revision" class="toc-link">Quick Revision</a></li>{{/if}}
        </ol>
      </nav>
      {{/if}}

      <!-- Introduction -->
      {{#if introduction}}
      <div class="intro-box" id="introduction">
        <p class="section-mini-title"><i class="fas fa-info-circle"></i> Introduction</p>
        <p>{{introduction}}</p>
      </div>
      {{/if}}

      <!-- Summary -->
      {{#if summary}}
      <div class="summary-box">
        <p class="section-mini-title"><i class="fas fa-compress-alt"></i> Summary</p>
        <p style="font-size: 14px; color: #166534; line-height: 1.8;">{{summary}}</p>
      </div>
      {{/if}}

      <!-- Key Points -->
      {{#if showKeyPoints}}
      {{#if keyPoints}}
      <section class="section" id="keypoints">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-star"></i></div>
          <h2 class="section-title">Key Points <span class="section-title-hi">मुख्य बिंदु</span></h2>
        </div>
        <div class="key-points">
          <ul class="key-points-list">
            {{#each keyPoints}}
            <li class="key-point-item">
              <span class="key-point-icon {{#if this.importance}}{{lowercase this.importance}}{{/if}}">
                <i class="fas fa-check"></i>
              </span>
              <div class="key-point-content">
                <p class="key-point-text">{{this.point}}</p>
                {{#if this.explanation}}
                <p class="key-point-explanation">{{this.explanation}}</p>
                {{/if}}
                {{#if this.examRelevance}}
                <span class="key-point-exam-tag"><i class="fas fa-bullseye"></i> {{this.examRelevance}}</span>
                {{/if}}
              </div>
            </li>
            {{/each}}
          </ul>
        </div>
      </section>
      {{/if}}
      {{/if}}

      <!-- Definitions -->
      {{#if definitions}}
      <section class="section" id="definitions">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-book"></i></div>
          <h2 class="section-title">Definitions <span class="section-title-hi">परिभाषाएं</span></h2>
        </div>
        <div class="definitions-grid">
          {{#each definitions}}
          <div class="definition-card">
            <h3 class="definition-term">
              {{this.term}}
              {{#if this.hindiMeaning}}
              <span class="definition-term-hi">({{this.hindiMeaning}})</span>
              {{/if}}
            </h3>
            <p class="definition-meaning">{{this.meaning}}</p>
            {{#if this.example}}
            <p class="definition-example"><strong>Example:</strong> {{this.example}}</p>
            {{/if}}
          </div>
          {{/each}}
        </div>
      </section>
      {{/if}}

      <!-- Formulas -->
      {{#if showFormulas}}
      {{#if formulas}}
      <section class="section formulas-section" id="formulas">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-superscript"></i></div>
          <h2 class="section-title">Formulas <span class="section-title-hi">सूत्र</span></h2>
        </div>
        {{#each formulas}}
        <div class="formula-card">
          <div class="formula-header">
            <h3 class="formula-name">{{this.name}}</h3>
            {{#if this.importance}}
            <span class="formula-importance">{{this.importance}}</span>
            {{/if}}
          </div>
          <div class="formula-display">{{this.formula}}</div>
          {{#if this.description}}
          <p class="formula-description">{{this.description}}</p>
          {{/if}}
          {{#if this.variables}}
          <div class="formula-variables">
            {{#each this.variables}}
            <span class="variable-tag">
              <span class="variable-symbol">{{this.symbol}}</span>
              <span class="variable-name">= {{this.name}}</span>
              {{#if this.unit}}<span class="variable-unit">({{this.unit}})</span>{{/if}}
            </span>
            {{/each}}
          </div>
          {{/if}}
          {{#if this.conditions}}
          <p class="formula-conditions"><i class="fas fa-info-circle"></i> {{this.conditions}}</p>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}
      {{/if}}

      <!-- Theorems & Laws -->
      {{#if theorems}}
      <section class="section" id="theorems">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-scroll"></i></div>
          <h2 class="section-title">Theorems & Laws <span class="section-title-hi">प्रमेय और नियम</span></h2>
        </div>
        {{#each theorems}}
        <div class="theorem-card">
          <h3 class="theorem-name"><i class="fas fa-gavel"></i> {{this.name}}</h3>
          <div class="theorem-statement">{{this.statement}}</div>
          {{#if this.proof}}
          <div class="theorem-proof">
            <p class="theorem-proof-title">Proof:</p>
            <p>{{this.proof}}</p>
          </div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Laws -->
      {{#if laws}}
      <section class="section">
        {{#each laws}}
        <div class="theorem-card">
          <h3 class="theorem-name"><i class="fas fa-balance-scale"></i> {{this.name}}</h3>
          <div class="theorem-statement">{{this.statement}}</div>
          {{#if this.applications}}
          <div style="margin-top: 12px;">
            <p style="font-size: 12px; font-weight: 600; color: #7c3aed; margin-bottom: 8px;">Applications:</p>
            <ul style="list-style: none; font-size: 13px; color: #581c87;">
              {{#each this.applications}}
              <li style="padding: 4px 0;"><i class="fas fa-chevron-right" style="font-size: 10px; margin-right: 8px; color: #a855f7;"></i>{{this}}</li>
              {{/each}}
            </ul>
          </div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Chemical Reactions -->
      {{#if chemicalReactions}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon" style="background: linear-gradient(135deg, #0891b2 0%, #06b6d4 100%);"><i class="fas fa-atom"></i></div>
          <h2 class="section-title">Chemical Reactions <span class="section-title-hi">रासायनिक अभिक्रियाएं</span></h2>
        </div>
        {{#each chemicalReactions}}
        <div class="reaction-card">
          {{#if this.name}}
          <p class="reaction-name">{{this.name}}</p>
          {{/if}}
          <div class="reaction-equation">{{this.equation}}</div>
          {{#if this.reactionType}}
          <span class="reaction-type">{{this.reactionType}}</span>
          {{/if}}
          {{#if this.conditions}}
          <p class="reaction-details"><strong>Conditions:</strong> {{this.conditions}}</p>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Biological Processes -->
      {{#if biologicalProcesses}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon" style="background: linear-gradient(135deg, #16a34a 0%, #22c55e 100%);"><i class="fas fa-dna"></i></div>
          <h2 class="section-title">Biological Processes <span class="section-title-hi">जैविक प्रक्रियाएं</span></h2>
        </div>
        {{#each biologicalProcesses}}
        <div class="bio-process-card">
          <h3 class="bio-process-name"><i class="fas fa-seedling"></i> {{this.name}}</h3>
          {{#if this.steps}}
          <ol class="bio-process-steps">
            {{#each this.steps}}
            <li class="bio-step">{{this}}</li>
            {{/each}}
          </ol>
          {{/if}}
          {{#if this.inputs}}
          <div class="bio-io">
            <div class="bio-io-box">
              <p class="bio-io-label">Inputs</p>
              <p class="bio-io-items">{{join this.inputs ", "}}</p>
            </div>
            {{#if this.outputs}}
            <div class="bio-io-box">
              <p class="bio-io-label">Outputs</p>
              <p class="bio-io-items">{{join this.outputs ", "}}</p>
            </div>
            {{/if}}
          </div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Timeline (History) -->
      {{#if timeline}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon" style="background: linear-gradient(135deg, #7c3aed 0%, #8b5cf6 100%);"><i class="fas fa-history"></i></div>
          <h2 class="section-title">Timeline <span class="section-title-hi">समयरेखा</span></h2>
        </div>
        <div class="timeline">
          {{#each timeline}}
          <div class="timeline-item">
            <span class="timeline-year">{{this.year}}</span>
            <h4 class="timeline-event">{{this.event}}</h4>
            {{#if this.description}}
            <p class="timeline-description">{{this.description}}</p>
            {{/if}}
            {{#if this.significance}}
            <div class="timeline-significance"><strong>Significance:</strong> {{this.significance}}</div>
            {{/if}}
          </div>
          {{/each}}
        </div>
      </section>
      {{/if}}

      <!-- Historical Figures -->
      {{#if historicalFigures}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-user-tie"></i></div>
          <h2 class="section-title">Important Personalities <span class="section-title-hi">महत्वपूर्ण व्यक्तित्व</span></h2>
        </div>
        {{#each historicalFigures}}
        <div class="personality-card">
          <div class="personality-avatar">{{initials this.name}}</div>
          <div class="personality-info">
            <h3 class="personality-name">{{this.name}}</h3>
            {{#if this.title}}<p class="personality-title">{{this.title}}</p>{{/if}}
            {{#if this.lifespan}}<p class="personality-lifespan">{{this.lifespan}}</p>{{/if}}
            {{#if this.contributions}}
            <div class="personality-contributions">
              {{#each this.contributions}}
              <span class="contribution-tag">{{this}}</span>
              {{/each}}
            </div>
            {{/if}}
          </div>
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Grammar Rules (Languages) -->
      {{#if grammarRules}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon" style="background: linear-gradient(135deg, #c026d3 0%, #d946ef 100%);"><i class="fas fa-spell-check"></i></div>
          <h2 class="section-title">Grammar Rules <span class="section-title-hi">व्याकरण नियम</span></h2>
        </div>
        {{#each grammarRules}}
        <div class="grammar-card">
          <h3 class="grammar-rule">{{this.rule}}</h3>
          <p class="grammar-explanation">{{this.explanation}}</p>
          {{#if this.examples}}
          <div class="grammar-examples">
            {{#each this.examples}}
            <p class="grammar-example-item"><i class="fas fa-check-circle"></i> {{this}}</p>
            {{/each}}
          </div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Vocabulary -->
      {{#if vocabulary}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-font"></i></div>
          <h2 class="section-title">Vocabulary <span class="section-title-hi">शब्दावली</span></h2>
        </div>
        <div class="vocabulary-grid">
          {{#each vocabulary}}
          <div class="vocab-card">
            <h4 class="vocab-word">{{this.word}}</h4>
            {{#if this.pronunciation}}<p class="vocab-pronunciation">/{{this.pronunciation}}/</p>{{/if}}
            <p class="vocab-meaning">{{this.meaning}}</p>
            {{#if this.hindiMeaning}}<p class="vocab-hindi">{{this.hindiMeaning}}</p>{{/if}}
            {{#if this.usage}}<p class="vocab-usage">"{{this.usage}}"</p>{{/if}}
          </div>
          {{/each}}
        </div>
      </section>
      {{/if}}

      <!-- Mnemonics -->
      {{#if mnemonics}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon" style="background: linear-gradient(135deg, #f59e0b 0%, #fbbf24 100%);"><i class="fas fa-brain"></i></div>
          <h2 class="section-title">Memory Tricks <span class="section-title-hi">याद करने के तरीके</span></h2>
        </div>
        {{#each mnemonics}}
        <div class="mnemonic-card">
          <div class="mnemonic-header">
            <div class="mnemonic-icon"><i class="fas fa-lightbulb"></i></div>
            <span class="mnemonic-topic">{{this.topic}}</span>
          </div>
          <div class="mnemonic-text">{{this.mnemonic}}</div>
          <p class="mnemonic-explanation">{{this.explanation}}</p>
          {{#if this.items}}
          <div class="mnemonic-items">
            {{#each this.items}}
            <span class="mnemonic-item">{{this}}</span>
            {{/each}}
          </div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Solved Examples -->
      {{#if showExamples}}
      {{#if examples}}
      <section class="section" id="examples">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-calculator"></i></div>
          <h2 class="section-title">Solved Examples <span class="section-title-hi">हल किए गए उदाहरण</span></h2>
        </div>
        {{#each examples}}
        <div class="example-card">
          <div class="example-header">
            <h4 class="example-title">{{#if this.title}}{{this.title}}{{else}}Example {{@index}}{{/if}}</h4>
            {{#if this.difficulty}}
            <span class="example-difficulty {{lowercase this.difficulty}}">{{this.difficulty}}</span>
            {{/if}}
          </div>
          <div class="example-question">
            <strong>Q:</strong> {{this.question}}
          </div>
          <div class="example-solution">
            {{#if this.steps}}
            {{#each this.steps}}
            <div class="example-step">
              <span class="step-number">{{math @index "+" 1}}</span>
              <span>{{this}}</span>
            </div>
            {{/each}}
            {{else}}
            <p style="font-size: 13px; color: var(--text-secondary);">{{this.solution}}</p>
            {{/if}}
          </div>
          {{#if this.answer}}
          <div class="example-answer"><strong>Answer:</strong> {{this.answer}}</div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}
      {{/if}}

      <!-- Tables -->
      {{#if tables}}
      <section class="section">
        <div class="section-header">
          <div class="section-icon"><i class="fas fa-table"></i></div>
          <h2 class="section-title">Data Tables</h2>
        </div>
        {{#each tables}}
        <div class="data-table-container">
          <table class="data-table">
            {{#if this.title}}<caption>{{this.title}}</caption>{{/if}}
            <thead>
              <tr>
                {{#each this.headers}}
                <th>{{this}}</th>
                {{/each}}
              </tr>
            </thead>
            <tbody>
              {{#each this.rows}}
              <tr>
                {{#each this}}
                <td>{{this}}</td>
                {{/each}}
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Practice Questions -->
      {{#if showPracticeQuestions}}
      {{#if practiceQuestions}}
      <section class="section" id="practice">
        <div class="section-header">
          <div class="section-icon" style="background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);"><i class="fas fa-pen"></i></div>
          <h2 class="section-title">Practice Questions <span class="section-title-hi">अभ्यास प्रश्न</span></h2>
        </div>
        <div class="practice-section">
          {{#each practiceQuestions}}
          <div class="practice-question">
            <div class="question-header">
              <span class="question-number">{{math @index "+" 1}}</span>
              <div class="question-meta">
                {{#if this.type}}<span class="question-type">{{this.type}}</span>{{/if}}
                {{#if this.marks}}<span class="question-marks">[{{this.marks}} marks]</span>{{/if}}
                {{#if this.year}}<span class="pyq-tag"><i class="fas fa-history"></i> {{this.examName}} {{this.year}}</span>{{/if}}
              </div>
            </div>
            <p class="question-text">{{this.question}}</p>
            {{#if this.options}}
            <div class="mcq-options">
              {{#each this.options}}
              <div class="mcq-option">
                <span class="mcq-option-letter">{{optionLetter @index}}</span>
                <span>{{this}}</span>
              </div>
              {{/each}}
            </div>
            {{/if}}
          </div>
          {{/each}}
        </div>
      </section>
      {{/if}}
      {{/if}}

      <!-- Quick Revision -->
      {{#if showRevisionSection}}
      {{#if quickRevision}}
      <section class="section" id="revision">
        <div class="revision-section">
          <h2 class="revision-title"><i class="fas fa-bolt"></i> Quick Revision</h2>
          <div class="revision-grid">
            {{#each quickRevision}}
            <div class="revision-item">
              <span class="revision-bullet"></span>
              <div>
                <p class="revision-text">{{this.point}}</p>
                {{#if this.category}}<span class="revision-category">{{this.category}}</span>{{/if}}
              </div>
            </div>
            {{/each}}
          </div>
        </div>
      </section>
      {{/if}}
      {{/if}}

      <!-- Exam Tips -->
      {{#if examTips}}
      <div class="exam-tips">
        <h3 class="exam-tips-title"><i class="fas fa-lightbulb"></i> Exam Tips</h3>
        {{#each examTips}}
        <p class="exam-tip-item"><i class="fas fa-check-circle"></i> {{this}}</p>
        {{/each}}
      </div>
      {{/if}}

      <!-- Common Mistakes -->
      {{#if commonMistakes}}
      <div class="common-mistakes">
        <h3 class="common-mistakes-title"><i class="fas fa-exclamation-triangle"></i> Common Mistakes to Avoid</h3>
        {{#each commonMistakes}}
        <p class="mistake-item"><i class="fas fa-times-circle"></i> {{this}}</p>
        {{/each}}
      </div>
      {{/if}}

      <!-- References -->
      {{#if references}}
      <section class="references-section">
        <h3 class="references-title"><i class="fas fa-bookmark"></i> References</h3>
        {{#each references}}
        <div class="reference-item">
          {{#if (eq this.type "textbook")}}<i class="fas fa-book"></i>{{/if}}
          {{#if (eq this.type "ncert")}}<i class="fas fa-book-reader"></i>{{/if}}
          {{#if (eq this.type "website")}}<i class="fas fa-globe"></i>{{/if}}
          {{#if (eq this.type "video")}}<i class="fab fa-youtube"></i>{{/if}}
          {{#unless this.type}}<i class="fas fa-link"></i>{{/unless}}
          {{#if this.url}}
          <a href="{{this.url}}" target="_blank">{{this.title}}</a>
          {{else}}
          <span>{{this.title}}{{#if this.author}} - {{this.author}}{{/if}}</span>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      {{#if ncertReference}}
      <div class="reference-item" style="background: var(--bg-secondary); padding: 12px; border-radius: var(--radius-md);">
        <i class="fas fa-book-reader" style="color: var(--accent);"></i>
        <span><strong>NCERT Reference:</strong> {{ncertReference}}</span>
      </div>
      {{/if}}

    </main>

    <!-- ═══════════════════════════════════════════════════════════════
         FOOTER
         ═══════════════════════════════════════════════════════════════ -->
    {{#if showFooter}}
    <footer class="footer">
      <div class="footer-left">
        {{#if author}}
        <span class="footer-item"><i class="fas fa-user"></i> {{author}}</span>
        {{/if}}
        {{#if institution}}
        <span class="footer-item"><i class="fas fa-university"></i> {{institution}}</span>
        {{/if}}
        {{#if lastUpdated}}
        <span class="footer-item"><i class="fas fa-calendar"></i> Updated: {{formatDate lastUpdated}}</span>
        {{/if}}
      </div>
      <div class="footer-right">
        {{#if showPageNumbers}}
        <span>Page <span class="page-number">1</span></span>
        {{/if}}
      </div>
    </footer>
    {{/if}}

  </div>
</body>
</html>
