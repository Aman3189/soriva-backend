<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{fullName}} - Cover Letter</title>
  <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - COVER LETTER TEMPLATE (COMPREHENSIVE)
       Style: Professional / Corporate
       Color: Navy Blue (#0f172a)
       Supports: Fresher → Executive → International
       ATS-Friendly: Yes
       ═══════════════════════════════════════════════════════════════ */
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Source Sans Pro', sans-serif;
      background: #e2e8f0;
      color: #1e293b;
      line-height: 1.7;
      padding: 30px;
    }

    .letter-container {
      width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: white;
      box-shadow: 0 10px 40px rgba(0,0,0,0.12);
      position: relative;
      overflow: hidden;
    }

    /* Multi-page support */
    .page-break {
      page-break-before: always;
      padding-top: 40px;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER / LETTERHEAD - Professional Style
       ═══════════════════════════════════════════════════════════════ */
    
    .header {
      background: #0f172a;
      color: white;
      padding: 30px 50px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .sender-info {
      flex: 1;
    }

    .sender-name {
      font-size: 26px;
      font-weight: 700;
      letter-spacing: 0.5px;
      margin-bottom: 5px;
    }

    .sender-title {
      font-size: 13px;
      font-weight: 400;
      color: #94a3b8;
      margin-bottom: 15px;
    }

    .sender-contacts {
      display: flex;
      flex-wrap: wrap;
      gap: 18px;
      font-size: 12px;
      color: #cbd5e1;
    }

    .contact-item {
      display: flex;
      align-items: center;
      gap: 7px;
    }

    .contact-item i {
      font-size: 11px;
      color: #64748b;
    }

    .contact-item a {
      color: #cbd5e1;
      text-decoration: none;
    }

    .contact-item a:hover {
      color: white;
    }

    .company-logo-placeholder {
      width: 80px;
      height: 80px;
      background: #1e293b;
      border: 2px solid #334155;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .initials-badge {
      font-size: 24px;
      font-weight: 700;
      color: #64748b;
      letter-spacing: 1px;
    }

    /* ═══════════════════════════════════════════════════════════════
       LETTER BODY
       ═══════════════════════════════════════════════════════════════ */
    
    .letter-body {
      padding: 40px 50px 80px;
    }

    /* Date and Recipient */
    .letter-meta {
      margin-bottom: 35px;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
    }

    .recipient-section {
      flex: 1;
    }

    .letter-date {
      font-size: 13px;
      color: #64748b;
      text-align: right;
    }

    .recipient-block {
      margin-bottom: 5px;
    }

    .recipient-name {
      font-size: 15px;
      font-weight: 700;
      color: #0f172a;
      margin-bottom: 2px;
    }

    .recipient-title {
      font-size: 13px;
      color: #475569;
      margin-bottom: 2px;
    }

    .company-name {
      font-size: 14px;
      font-weight: 600;
      color: #1e293b;
      margin-bottom: 2px;
    }

    .company-address {
      font-size: 13px;
      color: #64748b;
      line-height: 1.6;
    }

    /* Subject Line - Professional Style */
    .subject-line {
      margin: 30px 0;
      padding: 18px 22px;
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-left: 4px solid #0f172a;
    }

    .subject-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #64748b;
      font-weight: 600;
      margin-bottom: 5px;
    }

    .subject-text {
      font-size: 15px;
      font-weight: 700;
      color: #0f172a;
    }

    .job-reference {
      font-size: 12px;
      color: #64748b;
      margin-top: 5px;
    }

    .job-meta {
      display: flex;
      gap: 20px;
      margin-top: 10px;
      font-size: 11px;
      color: #64748b;
    }

    .job-meta-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .job-meta-item i {
      font-size: 10px;
      color: #0f172a;
    }

    /* Salutation */
    .salutation {
      font-size: 14px;
      font-weight: 600;
      color: #1e293b;
      margin-bottom: 22px;
    }

    /* Letter Content */
    .letter-content {
      font-size: 13px;
      color: #334155;
    }

    .letter-content p {
      margin-bottom: 18px;
      line-height: 1.85;
      text-align: justify;
    }

    .letter-content p:last-child {
      margin-bottom: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       HIGHLIGHT BOXES - Professional Style
       ═══════════════════════════════════════════════════════════════ */
    
    .highlight-box {
      margin: 28px 0;
      padding: 22px;
      background: #f8fafc;
      border: 1px solid #e2e8f0;
    }

    .highlight-box-title {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #0f172a;
      font-weight: 700;
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 2px solid #0f172a;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .highlight-box-title i {
      font-size: 13px;
    }

    /* Skills Tags - Professional */
    .skills-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .skill-tag {
      padding: 6px 14px;
      background: #0f172a;
      color: white;
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .skill-tag.secondary {
      background: white;
      color: #0f172a;
      border: 1px solid #cbd5e1;
    }

    /* ═══════════════════════════════════════════════════════════════
       ACHIEVEMENTS SECTION - Professional Style
       ═══════════════════════════════════════════════════════════════ */
    
    .achievements-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }

    .achievement-item {
      background: white;
      padding: 15px 18px;
      border: 1px solid #e2e8f0;
      border-left: 3px solid #0f172a;
    }

    .achievement-metric {
      font-size: 20px;
      font-weight: 700;
      color: #0f172a;
      margin-bottom: 3px;
    }

    .achievement-title {
      font-size: 12px;
      font-weight: 600;
      color: #334155;
    }

    .achievement-context {
      font-size: 11px;
      color: #64748b;
      margin-top: 3px;
    }

    /* ═══════════════════════════════════════════════════════════════
       LEADERSHIP SECTION - Professional Style
       ═══════════════════════════════════════════════════════════════ */
    
    .leadership-stats {
      display: flex;
      justify-content: space-between;
      gap: 15px;
      flex-wrap: wrap;
    }

    .leadership-stat {
      flex: 1;
      min-width: 90px;
      background: white;
      padding: 15px;
      border: 1px solid #e2e8f0;
      text-align: center;
    }

    .leadership-stat-value {
      font-size: 24px;
      font-weight: 700;
      color: #0f172a;
    }

    .leadership-stat-label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #64748b;
      margin-top: 4px;
      font-weight: 600;
    }

    /* ═══════════════════════════════════════════════════════════════
       FRESHER SECTION - Education & Projects
       ═══════════════════════════════════════════════════════════════ */
    
    .education-highlight {
      background: white;
      padding: 18px;
      border: 1px solid #e2e8f0;
      display: flex;
      align-items: center;
      gap: 18px;
    }

    .education-icon {
      width: 50px;
      height: 50px;
      background: #0f172a;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 20px;
      flex-shrink: 0;
    }

    .education-details {
      flex: 1;
    }

    .education-degree {
      font-size: 15px;
      font-weight: 700;
      color: #0f172a;
    }

    .education-college {
      font-size: 13px;
      color: #334155;
      margin-top: 2px;
    }

    .education-meta {
      font-size: 12px;
      color: #64748b;
      margin-top: 4px;
    }

    /* Projects List */
    .projects-list {
      margin-top: 15px;
    }

    .project-item {
      background: white;
      padding: 12px 15px;
      border: 1px solid #e2e8f0;
      border-left: 3px solid #0f172a;
      margin-bottom: 10px;
    }

    .project-item:last-child {
      margin-bottom: 0;
    }

    .project-name {
      font-size: 13px;
      font-weight: 600;
      color: #0f172a;
    }

    .project-tech {
      font-size: 11px;
      color: #64748b;
      margin-top: 3px;
    }

    /* ═══════════════════════════════════════════════════════════════
       INTERNATIONAL SECTION - Visa & Relocation
       ═══════════════════════════════════════════════════════════════ */
    
    .international-box {
      background: #fffbeb;
      border-color: #fcd34d;
    }

    .international-box .highlight-box-title {
      color: #92400e;
      border-bottom-color: #f59e0b;
    }

    .visa-info {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
    }

    .visa-item {
      flex: 1;
      min-width: 140px;
      background: white;
      padding: 12px 15px;
      border: 1px solid #fcd34d;
      border-left: 3px solid #f59e0b;
    }

    .visa-label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #92400e;
      margin-bottom: 4px;
      font-weight: 600;
    }

    .visa-value {
      font-size: 13px;
      font-weight: 600;
      color: #1e293b;
    }

    /* Language Proficiency */
    .languages-list {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-top: 15px;
    }

    .language-item {
      background: white;
      padding: 6px 12px;
      border: 1px solid #fcd34d;
      font-size: 12px;
    }

    .language-name {
      font-weight: 600;
      color: #92400e;
    }

    .language-level {
      color: #64748b;
      margin-left: 4px;
    }

    /* ═══════════════════════════════════════════════════════════════
       CERTIFICATIONS SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .certifications-list {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .cert-item {
      background: white;
      padding: 8px 14px;
      border: 1px solid #e2e8f0;
      font-size: 12px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .cert-item i {
      color: #059669;
      font-size: 12px;
    }

    .cert-name {
      font-weight: 600;
      color: #1e293b;
    }

    .cert-issuer {
      color: #64748b;
      font-size: 11px;
    }

    /* ═══════════════════════════════════════════════════════════════
       AVAILABILITY SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .availability-box {
      background: #f0fdf4;
      border-color: #86efac;
    }

    .availability-box .highlight-box-title {
      color: #166534;
      border-bottom-color: #22c55e;
    }

    .availability-info {
      display: flex;
      flex-wrap: wrap;
      gap: 25px;
    }

    .availability-item {
      display: flex;
      align-items: flex-start;
      gap: 10px;
    }

    .availability-item i {
      color: #16a34a;
      font-size: 16px;
      margin-top: 2px;
    }

    .availability-label {
      font-size: 9px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #166534;
      font-weight: 600;
    }

    .availability-value {
      font-size: 13px;
      font-weight: 600;
      color: #1e293b;
    }

    /* ═══════════════════════════════════════════════════════════════
       CLOSING SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .closing-section {
      margin-top: 40px;
    }

    .sign-off {
      font-size: 13px;
      color: #334155;
      margin-bottom: 35px;
    }

    .signature-block {
      border-top: 2px solid #0f172a;
      padding-top: 15px;
      display: inline-block;
    }

    .signature-name {
      font-size: 18px;
      font-weight: 700;
      color: #0f172a;
      margin-bottom: 3px;
    }

    .signature-title {
      font-size: 12px;
      color: #64748b;
    }

    .signature-contact {
      font-size: 11px;
      color: #94a3b8;
      margin-top: 8px;
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
       ═══════════════════════════════════════════════════════════════ */
    
    .footer {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      padding: 15px 50px;
      background: #0f172a;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 30px;
      font-size: 11px;
      color: #94a3b8;
    }

    .footer-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .footer-item i {
      font-size: 10px;
      color: #64748b;
    }

    .footer-item a {
      color: #94a3b8;
      text-decoration: none;
    }

    .footer-item a:hover {
      color: white;
    }

    /* ═══════════════════════════════════════════════════════════════
       PRINT STYLES
       ═══════════════════════════════════════════════════════════════ */
    
    @media print {
      body {
        background: white;
        padding: 0;
      }
      
      .letter-container {
        box-shadow: none;
        width: 100%;
        min-height: auto;
      }

      .page-break {
        page-break-before: always;
      }
    }

    @page {
      size: A4;
      margin: 0;
    }
  </style>
</head>
<body>
  <div class="letter-container">
    
    <!-- ═══════════════════════════════════════════════════════════════
         HEADER / LETTERHEAD - Professional Style
         ═══════════════════════════════════════════════════════════════ -->
    {{#if showLetterhead}}
    <header class="header">
      <div class="sender-info">
        <h1 class="sender-name">{{fullName}}</h1>
        {{#if currentRole}}
        <p class="sender-title">{{currentRole}}{{#if currentCompany}} | {{currentCompany}}{{/if}}</p>
        {{else}}
          {{#if degree}}
          <p class="sender-title">{{degree}}{{#if collegeName}} | {{collegeName}}{{/if}}</p>
          {{/if}}
        {{/if}}
        
        <div class="sender-contacts">
          {{#if email}}
          <span class="contact-item">
            <i class="fas fa-envelope"></i>
            <a href="mailto:{{email}}">{{email}}</a>
          </span>
          {{/if}}
          
          {{#if phone}}
          <span class="contact-item">
            <i class="fas fa-phone"></i>
            {{phone}}
          </span>
          {{/if}}
          
          {{#if city}}
          <span class="contact-item">
            <i class="fas fa-map-marker-alt"></i>
            {{city}}{{#if state}}, {{state}}{{/if}}{{#if country}} | {{country}}{{/if}}
          </span>
          {{/if}}
          
          {{#if linkedin}}
          <span class="contact-item">
            <i class="fab fa-linkedin"></i>
            <a href="https://{{linkedin}}" target="_blank">LinkedIn</a>
          </span>
          {{/if}}

          {{#if github}}
          <span class="contact-item">
            <i class="fab fa-github"></i>
            <a href="https://{{github}}" target="_blank">GitHub</a>
          </span>
          {{/if}}

          {{#if portfolio}}
          <span class="contact-item">
            <i class="fas fa-globe"></i>
            <a href="https://{{portfolio}}" target="_blank">Portfolio</a>
          </span>
          {{/if}}
        </div>
      </div>
      
      <div class="company-logo-placeholder">
        <span class="initials-badge">{{initials fullName}}</span>
      </div>
    </header>
    {{/if}}

    <!-- ═══════════════════════════════════════════════════════════════
         LETTER BODY
         ═══════════════════════════════════════════════════════════════ -->
    <main class="letter-body">
      
      <!-- Date and Recipient -->
      <div class="letter-meta">
        <div class="recipient-section">
          {{#if showRecipientAddress}}
          <div class="recipient-block">
            {{#if recipientName}}
            <p class="recipient-name">{{recipientName}}</p>
            {{/if}}
            {{#if recipientTitle}}
            <p class="recipient-title">{{recipientTitle}}</p>
            {{/if}}
            <p class="company-name">{{companyName}}</p>
            {{#if companyAddress}}
            <p class="company-address">
              {{companyAddress}}
              {{#if companyCity}}<br>{{companyCity}}{{#if companyState}}, {{companyState}}{{/if}}{{/if}}
              {{#if companyCountry}}<br>{{companyCountry}}{{/if}}
            </p>
            {{/if}}
          </div>
          {{/if}}
        </div>
        
        {{#if showDate}}
        <p class="letter-date">{{formatDate date}}</p>
        {{/if}}
      </div>

      <!-- Subject Line -->
      <div class="subject-line">
        <p class="subject-label">Re</p>
        <p class="subject-text">Application for {{jobTitle}} Position</p>
        {{#if jobReference}}
        <p class="job-reference">Reference No: {{jobReference}}</p>
        {{/if}}
        
        {{#if jobType}}
        <div class="job-meta">
          {{#if jobType}}
          <span class="job-meta-item">
            <i class="fas fa-briefcase"></i> {{jobType}}
          </span>
          {{/if}}
          {{#if workMode}}
          <span class="job-meta-item">
            <i class="fas fa-building"></i> {{workMode}}
          </span>
          {{/if}}
          {{#if referredBy}}
          <span class="job-meta-item">
            <i class="fas fa-user-friends"></i> Referred by {{referredBy}}
          </span>
          {{/if}}
        </div>
        {{/if}}
      </div>

      <!-- Salutation -->
      <p class="salutation">{{salutation}},</p>

      <!-- Letter Content -->
      <div class="letter-content">
        <!-- Opening Paragraph -->
        <p>{{openingParagraph}}</p>
        
        <!-- Body Paragraphs -->
        {{#each bodyParagraphs}}
        <p>{{this}}</p>
        {{/each}}

        <!-- ═══════════════════════════════════════════════════════════════
             FRESHER SECTION - Education & Projects
             ═══════════════════════════════════════════════════════════════ -->
        {{#if isFresher}}
        {{#if collegeName}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-graduation-cap"></i> Educational Background</p>
          <div class="education-highlight">
            <div class="education-icon">
              <i class="fas fa-university"></i>
            </div>
            <div class="education-details">
              <p class="education-degree">{{degree}}</p>
              <p class="education-college">{{collegeName}}</p>
              <p class="education-meta">
                {{#if graduationYear}}Graduation: {{graduationYear}}{{/if}}
                {{#if cgpa}} | CGPA: {{cgpa}}{{/if}}
              </p>
            </div>
          </div>

          {{#if academicProjects}}
          <div class="projects-list">
            <p style="font-size: 10px; text-transform: uppercase; letter-spacing: 2px; color: #0f172a; margin: 18px 0 12px; font-weight: 700;">Key Projects</p>
            {{#each academicProjects}}
            <div class="project-item">
              <p class="project-name">{{this.name}}{{#if this.role}} — {{this.role}}{{/if}}</p>
              {{#if this.technologies}}
              <p class="project-tech">Technologies: {{join this.technologies ", "}}</p>
              {{/if}}
            </div>
            {{/each}}
          </div>
          {{/if}}
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             KEY SKILLS SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showSkillsSection}}
        {{#if keySkills}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-star"></i> Core Competencies</p>
          <div class="skills-tags">
            {{#each keySkills}}
            <span class="skill-tag">{{this}}</span>
            {{/each}}
          </div>
          
          {{#if technicalSkills}}
          <div style="margin-top: 15px;">
            <p style="font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: #64748b; margin-bottom: 10px; font-weight: 600;">Technical Skills</p>
            <div class="skills-tags">
              {{#each technicalSkills}}
              <span class="skill-tag secondary">{{this}}</span>
              {{/each}}
            </div>
          </div>
          {{/if}}
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             ACHIEVEMENTS SECTION (Mid/Senior/Executive)
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showAchievements}}
        {{#if achievements}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-trophy"></i> Key Achievements</p>
          <div class="achievements-grid">
            {{#each achievements}}
            <div class="achievement-item">
              <p class="achievement-metric">{{this.metric}}</p>
              <p class="achievement-title">{{this.title}}</p>
              {{#if this.context}}
              <p class="achievement-context">{{this.context}}</p>
              {{/if}}
            </div>
            {{/each}}
          </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             LEADERSHIP SECTION (Senior/Executive)
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showMetrics}}
        {{#if teamSize}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-users-cog"></i> Leadership Profile</p>
          <div class="leadership-stats">
            {{#if teamSize}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{teamSize}}+</p>
              <p class="leadership-stat-label">Team Size</p>
            </div>
            {{/if}}
            {{#if directReports}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{directReports}}</p>
              <p class="leadership-stat-label">Direct Reports</p>
            </div>
            {{/if}}
            {{#if budgetManaged}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{budgetManaged}}</p>
              <p class="leadership-stat-label">Budget Managed</p>
            </div>
            {{/if}}
            {{#if revenueGenerated}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{revenueGenerated}}</p>
              <p class="leadership-stat-label">Revenue Impact</p>
            </div>
            {{/if}}
            {{#if yearsOfExperience}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{yearsOfExperience}}+</p>
              <p class="leadership-stat-label">Years Exp.</p>
            </div>
            {{/if}}
          </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             CERTIFICATIONS SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if certifications}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-certificate"></i> Professional Certifications</p>
          <div class="certifications-list">
            {{#each certifications}}
            <div class="cert-item">
              <i class="fas fa-check-circle"></i>
              <span>
                <span class="cert-name">{{this.name}}</span>
                {{#if this.issuer}}<span class="cert-issuer"> | {{this.issuer}}</span>{{/if}}
              </span>
            </div>
            {{/each}}
          </div>
        </div>
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             INTERNATIONAL/VISA SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if requiresSponsorship}}
        <div class="highlight-box international-box">
          <p class="highlight-box-title"><i class="fas fa-globe-americas"></i> Work Authorization & Relocation</p>
          <div class="visa-info">
            {{#if workAuthorizationStatus}}
            <div class="visa-item">
              <p class="visa-label">Authorization Status</p>
              <p class="visa-value">{{workAuthorizationStatus}}</p>
            </div>
            {{/if}}
            {{#if currentVisaStatus}}
            <div class="visa-item">
              <p class="visa-label">Current Visa</p>
              <p class="visa-value">{{currentVisaStatus}}</p>
            </div>
            {{/if}}
            {{#if relocationTimeline}}
            <div class="visa-item">
              <p class="visa-label">Relocation Timeline</p>
              <p class="visa-value">{{relocationTimeline}}</p>
            </div>
            {{/if}}
            {{#if targetLocation}}
            <div class="visa-item">
              <p class="visa-label">Target Location</p>
              <p class="visa-value">{{targetLocation}}</p>
            </div>
            {{/if}}
          </div>

          {{#if languages}}
          <div class="languages-list">
            {{#each languages}}
            <span class="language-item">
              <span class="language-name">{{this.language}}</span>
              <span class="language-level">({{this.level}})</span>
            </span>
            {{/each}}
          </div>
          {{/if}}

          {{#if ieltsScore}}
          <p style="font-size: 12px; margin-top: 12px; color: #64748b;">
            <strong>IELTS Score:</strong> {{ieltsScore}}
          </p>
          {{/if}}
        </div>
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             AVAILABILITY SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if availableFrom}}
        <div class="highlight-box availability-box">
          <p class="highlight-box-title"><i class="fas fa-calendar-check"></i> Availability</p>
          <div class="availability-info">
            {{#if availableFrom}}
            <div class="availability-item">
              <i class="fas fa-play-circle"></i>
              <div>
                <p class="availability-label">Available From</p>
                <p class="availability-value">{{availableFrom}}</p>
              </div>
            </div>
            {{/if}}
            {{#if noticePeriod}}
            <div class="availability-item">
              <i class="fas fa-clock"></i>
              <div>
                <p class="availability-label">Notice Period</p>
                <p class="availability-value">{{noticePeriod}}</p>
              </div>
            </div>
            {{/if}}
            {{#if interviewAvailability}}
            <div class="availability-item">
              <i class="fas fa-video"></i>
              <div>
                <p class="availability-label">Interview Availability</p>
                <p class="availability-value">{{interviewAvailability}}</p>
              </div>
            </div>
            {{/if}}
          </div>
        </div>
        {{/if}}
        
        <!-- Closing Paragraph -->
        <p>{{closingParagraph}}</p>
      </div>

      <!-- ═══════════════════════════════════════════════════════════════
           CLOSING & SIGNATURE
           ═══════════════════════════════════════════════════════════════ -->
      {{#if showSignature}}
      <div class="closing-section">
        <p class="sign-off">{{signOff}},</p>
        <div class="signature-block">
          <p class="signature-name">{{fullName}}</p>
          {{#if currentRole}}
          <p class="signature-title">{{currentRole}}{{#if currentCompany}} | {{currentCompany}}{{/if}}</p>
          {{else}}
            {{#if degree}}
            <p class="signature-title">{{degree}}</p>
            {{/if}}
          {{/if}}
          {{#if phone}}
          <p class="signature-contact">{{phone}} {{#if email}}| {{email}}{{/if}}</p>
          {{/if}}
        </div>
      </div>
      {{/if}}

    </main>

    <!-- ═══════════════════════════════════════════════════════════════
         FOOTER
         ═══════════════════════════════════════════════════════════════ -->
    <footer class="footer">
      {{#if email}}
      <span class="footer-item">
        <i class="fas fa-envelope"></i>
        <a href="mailto:{{email}}">{{email}}</a>
      </span>
      {{/if}}
      
      {{#if phone}}
      <span class="footer-item">
        <i class="fas fa-phone"></i>
        {{phone}}
      </span>
      {{/if}}
      
      {{#if linkedin}}
      <span class="footer-item">
        <i class="fab fa-linkedin"></i>
        <a href="https://{{linkedin}}" target="_blank">{{linkedin}}</a>
      </span>
      {{/if}}

      {{#if portfolio}}
      <span class="footer-item">
        <i class="fas fa-globe"></i>
        <a href="https://{{portfolio}}" target="_blank">{{portfolio}}</a>
      </span>
      {{/if}}
    </footer>

  </div>
</body>
</html>
