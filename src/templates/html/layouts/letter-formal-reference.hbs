<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reference Letter - {{reference.personName}}</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - LETTER FORMAL: REFERENCE LETTER
       Character & Professional Reference Letters
       Includes: referenceType, personName, personRole, relationship,
                 knowingDuration, referenceContext, characterTraits,
                 professionalSkills, specificExamples, reliability,
                 referenceRecommendation, purposeOfReference, contactPermission
    ═══════════════════════════════════════════════════════════════ */
    
    /* CSS Custom Properties */
    :root {
      --accent-primary: {{#if accentColor}}{{accentColor}}{{else}}#dc2626{{/if}};
      --accent-dark: {{#if accentColor}}{{accentColor}}{{else}}#b91c1c{{/if}};
      --accent-light: #fef2f2;
      --accent-border: #fecaca;
      --blue-primary: #0284c7;
      --blue-light: #f0f9ff;
      --blue-border: #bae6fd;
      --green-primary: #059669;
      --green-light: #ecfdf5;
      --green-border: #a7f3d0;
      --purple-primary: #7c3aed;
      --purple-light: #f5f3ff;
      --purple-border: #ddd6fe;
      --amber-primary: #d97706;
      --amber-light: #fffbeb;
      --amber-border: #fde68a;
      --teal-primary: #0d9488;
      --teal-light: #f0fdfa;
      --teal-border: #99f6e4;
      --slate-primary: #475569;
      --slate-light: #f8fafc;
      --slate-border: #e2e8f0;
      --text-primary: #1a1a1a;
      --text-secondary: #475569;
      --text-muted: #64748b;
      --text-light: #94a3b8;
      --bg-white: #ffffff;
      --bg-light: #f8f9fa;
      --bg-page: #f1f5f9;
      --border-light: #e2e8f0;
      --border-medium: #cbd5e1;
    }

    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      -webkit-print-color-adjust: exact !important;
      print-color-adjust: exact !important;
    }

    body {
      font-family: 'Georgia', 'Times New Roman', serif;
      font-size: 11pt;
      line-height: 1.7;
      color: var(--text-primary);
      background: var(--bg-page);
      padding: 20px;
    }

    /* Letter Container - A4 Style */
    .letter-container {
      max-width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: var(--bg-white);
      border-radius: 4px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1);
      border: 1px solid var(--border-light);
    }

    /* ─────────────────────────────────────────────────────────────
       Letterhead Section
    ───────────────────────────────────────────────────────────── */
    {{#if letterhead}}
    .letterhead {
      background: linear-gradient(135deg, var(--accent-primary) 0%, var(--accent-dark) 100%);
      padding: 28px 40px;
      border-bottom: 4px solid var(--green-primary);
    }

    .letterhead-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .company-info {
      display: flex;
      align-items: center;
      gap: 18px;
    }

    .company-logo {
      width: 60px;
      height: 60px;
      background: var(--bg-white);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 8px;
    }

    .company-logo img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
    }

    .company-logo-placeholder {
      width: 44px;
      height: 44px;
      fill: var(--accent-primary);
    }

    .company-details {
      color: #ffffff;
    }

    .company-name {
      font-family: 'Arial', sans-serif;
      font-size: 18pt;
      font-weight: 700;
      letter-spacing: 0.5px;
      margin-bottom: 4px;
    }

    .company-tagline {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      opacity: 0.85;
      letter-spacing: 0.5px;
    }

    .letterhead-contact {
      text-align: right;
      font-family: 'Arial', sans-serif;
      font-size: 8.5pt;
      color: rgba(255, 255, 255, 0.9);
      line-height: 1.6;
    }

    .letterhead-contact a {
      color: rgba(255, 255, 255, 0.9);
      text-decoration: none;
    }
    {{/if}}

    /* ─────────────────────────────────────────────────────────────
       Reference Header Banner
    ───────────────────────────────────────────────────────────── */
    .reference-banner {
      background: var(--green-light);
      padding: 20px 40px;
      border-bottom: 1px solid var(--green-border);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 16px;
    }

    .person-info {
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .person-avatar {
      width: 50px;
      height: 50px;
      background: var(--bg-white);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 2px solid var(--green-primary);
    }

    .person-avatar svg {
      width: 28px;
      height: 28px;
      fill: var(--green-primary);
    }

    .person-details {
      display: flex;
      flex-direction: column;
    }

    .person-name {
      font-family: 'Arial', sans-serif;
      font-size: 14pt;
      font-weight: 700;
      color: var(--text-primary);
    }

    .person-role {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      color: var(--text-secondary);
    }

    .reference-meta {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 8px;
    }

    .reference-type-badge {
      font-family: 'Arial', sans-serif;
      font-size: 9px;
      font-weight: 600;
      padding: 6px 14px;
      border-radius: 20px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      display: flex;
      align-items: center;
      gap: 6px;
      background: var(--green-primary);
      color: #ffffff;
    }

    .type-icon {
      width: 14px;
      height: 14px;
      fill: currentColor;
    }

    .purpose-badge {
      font-family: 'Arial', sans-serif;
      font-size: 8px;
      font-weight: 600;
      padding: 5px 12px;
      border-radius: 4px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      background: var(--bg-white);
      color: var(--text-secondary);
      border: 1px solid var(--border-light);
    }

    /* ─────────────────────────────────────────────────────────────
       Letter Body Section
    ───────────────────────────────────────────────────────────── */
    .letter-body {
      padding: 36px 40px;
    }

    /* Date Section */
    .letter-date {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 24px;
    }

    /* Recipient Block */
    .recipient-block {
      margin-bottom: 24px;
      line-height: 1.6;
    }

    .recipient-name {
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 2px;
    }

    .recipient-designation {
      color: var(--text-secondary);
      font-size: 10.5pt;
    }

    .recipient-company {
      color: var(--text-secondary);
      font-size: 10.5pt;
      font-weight: 500;
    }

    .recipient-address {
      color: var(--text-muted);
      font-size: 10pt;
      margin-top: 6px;
    }

    /* Subject Line */
    .subject-line {
      margin-bottom: 24px;
      padding: 14px 18px;
      background: var(--green-light);
      border-left: 4px solid var(--green-primary);
      border-radius: 0 6px 6px 0;
    }

    .subject-label {
      font-family: 'Arial', sans-serif;
      font-size: 8pt;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 4px;
    }

    .subject-text {
      font-weight: 600;
      color: #166534;
      font-size: 11pt;
    }

    /* Salutation */
    .salutation {
      font-size: 11pt;
      color: var(--text-primary);
      margin-bottom: 20px;
    }

    /* Letter Content */
    .letter-content {
      text-align: justify;
      margin-bottom: 24px;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Relationship Context Box
    ───────────────────────────────────────────────────────────── */
    .context-box {
      background: var(--blue-light);
      border: 1px solid var(--blue-border);
      border-left: 4px solid var(--blue-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .context-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .context-icon {
      width: 20px;
      height: 20px;
      fill: var(--blue-primary);
    }

    .context-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #075985;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .context-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
      margin-bottom: 14px;
      padding-bottom: 14px;
      border-bottom: 1px dashed var(--blue-border);
    }

    .meta-item {
      display: flex;
      align-items: center;
      gap: 8px;
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
    }

    .meta-icon {
      width: 14px;
      height: 14px;
      stroke: var(--blue-primary);
      fill: none;
    }

    .meta-label {
      color: var(--text-muted);
      font-size: 8pt;
      text-transform: uppercase;
    }

    .meta-value {
      color: var(--text-primary);
      font-weight: 600;
    }

    .context-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Character Traits Box
    ───────────────────────────────────────────────────────────── */
    .character-box {
      background: var(--purple-light);
      border: 1px solid var(--purple-border);
      border-left: 4px solid var(--purple-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .character-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .character-icon {
      width: 20px;
      height: 20px;
      fill: var(--purple-primary);
    }

    .character-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #5b21b6;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .character-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Professional Skills Box
    ───────────────────────────────────────────────────────────── */
    .skills-box {
      background: var(--teal-light);
      border: 1px solid var(--teal-border);
      border-left: 4px solid var(--teal-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .skills-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .skills-icon {
      width: 20px;
      height: 20px;
      fill: var(--teal-primary);
    }

    .skills-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #115e59;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .skills-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Specific Examples Box
    ───────────────────────────────────────────────────────────── */
    .examples-box {
      background: var(--amber-light);
      border: 1px solid var(--amber-border);
      border-left: 4px solid var(--amber-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .examples-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .examples-icon {
      width: 20px;
      height: 20px;
      fill: var(--amber-primary);
    }

    .examples-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #92400e;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .examples-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Reliability Box
    ───────────────────────────────────────────────────────────── */
    .reliability-box {
      background: var(--slate-light);
      border: 1px solid var(--slate-border);
      border-left: 4px solid var(--slate-primary);
      border-radius: 0 8px 8px 0;
      padding: 22px 26px;
      margin: 24px 0;
    }

    .reliability-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .reliability-icon {
      width: 20px;
      height: 20px;
      fill: var(--slate-primary);
    }

    .reliability-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: var(--slate-primary);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .reliability-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Final Recommendation Box
    ───────────────────────────────────────────────────────────── */
    .recommendation-box {
      background: linear-gradient(135deg, var(--green-light) 0%, var(--blue-light) 100%);
      border: 2px solid var(--green-primary);
      border-radius: 8px;
      padding: 24px 28px;
      margin: 28px 0;
    }

    .recommendation-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .recommendation-icon {
      width: 24px;
      height: 24px;
      fill: var(--green-primary);
    }

    .recommendation-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: #166534;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .recommendation-content {
      font-size: 11pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Closing & Signature Section
    ───────────────────────────────────────────────────────────── */
    .closing-section {
      margin-top: 36px;
    }

    .closing-phrase {
      font-style: italic;
      margin-bottom: 40px;
      color: var(--text-primary);
    }

    .signature-block {
      border-left: 4px solid var(--green-primary);
      padding-left: 18px;
    }

    .signature-image {
      max-height: 60px;
      margin-bottom: 8px;
    }

    .sender-name {
      font-size: 13pt;
      font-weight: 700;
      color: var(--green-primary);
      margin-bottom: 4px;
      font-family: 'Arial', sans-serif;
    }

    .sender-designation {
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 2px;
    }

    .sender-department {
      font-size: 9.5pt;
      color: var(--text-muted);
      margin-bottom: 2px;
    }

    .sender-company {
      font-size: 10pt;
      color: var(--text-secondary);
      font-weight: 500;
    }

    /* Contact Information */
    .signature-contact {
      margin-top: 14px;
      padding-top: 12px;
      border-top: 1px dashed var(--border-light);
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
    }

    .contact-item {
      display: flex;
      align-items: center;
      margin-bottom: 4px;
      color: var(--text-muted);
    }

    .contact-item:last-child {
      margin-bottom: 0;
    }

    .contact-icon {
      width: 12px;
      height: 12px;
      margin-right: 8px;
      stroke: var(--text-light);
    }

    .contact-link {
      color: var(--green-primary);
      text-decoration: none;
    }

    .contact-link:hover {
      text-decoration: underline;
    }

    /* Contact Permission */
    .contact-permission {
      margin-top: 16px;
      padding: 12px 16px;
      background: var(--green-light);
      border: 1px solid var(--green-border);
      border-radius: 6px;
      display: flex;
      align-items: center;
      gap: 10px;
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: #166534;
    }

    .permission-icon {
      width: 18px;
      height: 18px;
      fill: var(--green-primary);
    }

    /* ─────────────────────────────────────────────────────────────
       Print Styles
    ───────────────────────────────────────────────────────────── */
    @media print {
      body {
        background: #ffffff;
        padding: 0;
      }

      .letter-container {
        box-shadow: none;
        border-radius: 0;
        border: none;
        max-width: 100%;
        min-height: auto;
      }

      .letterhead,
      .reference-banner,
      .context-box,
      .character-box,
      .skills-box,
      .examples-box,
      .reliability-box,
      .recommendation-box,
      .contact-permission {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }

      @page {
        margin: 12mm;
        size: A4;
      }
    }

    /* ─────────────────────────────────────────────────────────────
       Responsive Styles
    ───────────────────────────────────────────────────────────── */
    @media screen and (max-width: 640px) {
      body {
        padding: 10px;
      }

      {{#if letterhead}}
      .letterhead {
        padding: 20px;
      }

      .letterhead-content {
        flex-direction: column;
        gap: 16px;
        text-align: center;
      }

      .company-info {
        flex-direction: column;
      }

      .letterhead-contact {
        text-align: center;
      }
      {{/if}}

      .reference-banner {
        padding: 16px 20px;
        flex-direction: column;
        align-items: flex-start;
      }

      .reference-meta {
        align-items: flex-start;
      }

      .letter-body {
        padding: 24px 20px;
      }

      .context-meta {
        flex-direction: column;
        gap: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="letter-container">
    <!-- ═══════════════════════════════════════════════════════════
         LETTERHEAD SECTION (Conditional)
    ════════════════════════════════════════════════════════════ -->
    {{#if letterhead}}
    <div class="letterhead">
      <div class="letterhead-content">
        <div class="company-info">
          <div class="company-logo">
            {{#if companyLogo}}
            <img src="{{companyLogo}}" alt="Company Logo">
            {{else}}
            <svg class="company-logo-placeholder" viewBox="0 0 24 24">
              <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
            </svg>
            {{/if}}
          </div>
          <div class="company-details">
            <div class="company-name">{{senderCompany}}</div>
            {{#if senderDepartment}}
            <div class="company-tagline">{{senderDepartment}}</div>
            {{/if}}
          </div>
        </div>
        <div class="letterhead-contact">
          {{#if senderAddress}}{{senderAddress}}<br>{{/if}}
          {{#if senderCity}}{{senderCity}}{{#if senderState}}, {{senderState}}{{/if}} {{senderPincode}}<br>{{/if}}
          {{#if senderPhone}}{{senderPhone}}<br>{{/if}}
          {{#if senderEmail}}<a href="mailto:{{senderEmail}}">{{senderEmail}}</a>{{/if}}
        </div>
      </div>
    </div>
    {{/if}}

    <!-- ═══════════════════════════════════════════════════════════
         REFERENCE BANNER
    ════════════════════════════════════════════════════════════ -->
    <div class="reference-banner">
      <div class="person-info">
        <div class="person-avatar">
          <svg viewBox="0 0 24 24">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
            <circle cx="12" cy="7" r="4"/>
          </svg>
        </div>
        <div class="person-details">
          <div class="person-name">{{reference.personName}}</div>
          {{#if reference.personRole}}
          <div class="person-role">{{reference.personRole}}</div>
          {{/if}}
        </div>
      </div>
      <div class="reference-meta">
        {{#if reference.referenceType}}
        <div class="reference-type-badge">
          <svg class="type-icon" viewBox="0 0 24 24">
            {{#ifEquals reference.referenceType "employment"}}
            <rect x="2" y="7" width="20" height="14" rx="2" ry="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "character"}}
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "academic"}}
            <path d="M22 10v6M2 10l10-5 10 5-10 5z"/><path d="M6 12v5c3 3 9 3 12 0v-5"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "tenant"}}
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "immigration"}}
            <circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "court"}}
            <circle cx="12" cy="12" r="10"/><line x1="14.31" y1="8" x2="20.05" y2="17.94"/><line x1="9.69" y1="8" x2="21.17" y2="8"/><line x1="7.38" y1="12" x2="13.12" y2="2.06"/><line x1="9.69" y1="16" x2="3.95" y2="6.06"/><line x1="14.31" y1="16" x2="2.83" y2="16"/><line x1="16.62" y1="12" x2="10.88" y2="21.94"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "adoption"}}
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "professional-membership"}}
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>
            {{/ifEquals}}
            {{#ifEquals reference.referenceType "volunteer"}}
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
            {{/ifEquals}}
          </svg>
          {{#ifEquals reference.referenceType "employment"}}Employment Reference{{/ifEquals}}
          {{#ifEquals reference.referenceType "character"}}Character Reference{{/ifEquals}}
          {{#ifEquals reference.referenceType "academic"}}Academic Reference{{/ifEquals}}
          {{#ifEquals reference.referenceType "tenant"}}Tenant Reference{{/ifEquals}}
          {{#ifEquals reference.referenceType "immigration"}}Immigration Reference{{/ifEquals}}
          {{#ifEquals reference.referenceType "court"}}Court Reference{{/ifEquals}}
          {{#ifEquals reference.referenceType "adoption"}}Adoption Reference{{/ifEquals}}
          {{#ifEquals reference.referenceType "professional-membership"}}Professional Membership{{/ifEquals}}
          {{#ifEquals reference.referenceType "volunteer"}}Volunteer Reference{{/ifEquals}}
        </div>
        {{/if}}
        {{#if reference.purposeOfReference}}
        <div class="purpose-badge">
          For: {{reference.purposeOfReference}}
        </div>
        {{/if}}
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         LETTER BODY SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="letter-body">
      <!-- Date -->
      <div class="letter-date">{{letterDate}}</div>

      <!-- Recipient Block -->
      <div class="recipient-block">
        <div class="recipient-name">{{recipientName}}</div>
        {{#if recipientDesignation}}
        <div class="recipient-designation">{{recipientDesignation}}</div>
        {{/if}}
        {{#if recipientCompany}}
        <div class="recipient-company">{{recipientCompany}}</div>
        {{/if}}
        <div class="recipient-address">
          {{#if recipientAddress}}{{recipientAddress}}<br>{{/if}}
          {{#if recipientCity}}{{recipientCity}}{{#if recipientState}}, {{recipientState}}{{/if}} {{recipientPincode}}{{/if}}
        </div>
      </div>

      <!-- Subject Line -->
      {{#if subject}}
      <div class="subject-line">
        <div class="subject-label">Subject</div>
        <div class="subject-text">{{subject}}</div>
      </div>
      {{/if}}

      <!-- Salutation -->
      <div class="salutation">
        {{salutation}},
      </div>

      <!-- Letter Content -->
      <div class="letter-content">
        {{{letterBody}}}
      </div>

      <!-- Relationship Context Box -->
      {{#if reference.referenceContext}}
      <div class="context-box">
        <div class="context-header">
          <svg class="context-icon" viewBox="0 0 24 24">
            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
            <circle cx="9" cy="7" r="4"/>
            <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
            <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
          </svg>
          <span class="context-title">How I Know {{reference.personName}}</span>
        </div>
        <div class="context-meta">
          {{#if reference.relationship}}
          <div class="meta-item">
            <svg class="meta-icon" viewBox="0 0 24 24" stroke-width="2">
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
              <circle cx="12" cy="7" r="4"/>
            </svg>
            <div>
              <div class="meta-label">Relationship</div>
              <div class="meta-value">
                {{#ifEquals reference.relationship "supervisor"}}Direct Supervisor{{/ifEquals}}
                {{#ifEquals reference.relationship "manager"}}Manager{{/ifEquals}}
                {{#ifEquals reference.relationship "colleague"}}Colleague{{/ifEquals}}
                {{#ifEquals reference.relationship "professor"}}Professor{{/ifEquals}}
                {{#ifEquals reference.relationship "teacher"}}Teacher{{/ifEquals}}
                {{#ifEquals reference.relationship "mentor"}}Mentor{{/ifEquals}}
                {{#ifEquals reference.relationship "client"}}Client{{/ifEquals}}
                {{#ifEquals reference.relationship "business-partner"}}Business Partner{{/ifEquals}}
                {{#ifEquals reference.relationship "friend"}}Friend{{/ifEquals}}
                {{#ifEquals reference.relationship "neighbor"}}Neighbor{{/ifEquals}}
                {{#ifEquals reference.relationship "family-friend"}}Family Friend{{/ifEquals}}
                {{#ifEquals reference.relationship "religious-leader"}}Religious Leader{{/ifEquals}}
                {{#ifEquals reference.relationship "community-leader"}}Community Leader{{/ifEquals}}
              </div>
            </div>
          </div>
          {{/if}}
          {{#if reference.knowingDuration}}
          <div class="meta-item">
            <svg class="meta-icon" viewBox="0 0 24 24" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12 6 12 12 16 14"/>
            </svg>
            <div>
              <div class="meta-label">Known For</div>
              <div class="meta-value">{{reference.knowingDuration}}</div>
            </div>
          </div>
          {{/if}}
        </div>
        <div class="context-content">
          {{{reference.referenceContext}}}
        </div>
      </div>
      {{/if}}

      <!-- Character Traits Box -->
      {{#if reference.characterTraits}}
      <div class="character-box">
        <div class="character-header">
          <svg class="character-icon" viewBox="0 0 24 24">
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
          </svg>
          <span class="character-title">Character & Personal Qualities</span>
        </div>
        <div class="character-content">
          {{{reference.characterTraits}}}
        </div>
      </div>
      {{/if}}

      <!-- Professional Skills Box -->
      {{#if reference.professionalSkills}}
      <div class="skills-box">
        <div class="skills-header">
          <svg class="skills-icon" viewBox="0 0 24 24">
            <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
          </svg>
          <span class="skills-title">Professional Skills</span>
        </div>
        <div class="skills-content">
          {{{reference.professionalSkills}}}
        </div>
      </div>
      {{/if}}

      <!-- Specific Examples Box -->
      {{#if reference.specificExamples}}
      <div class="examples-box">
        <div class="examples-header">
          <svg class="examples-icon" viewBox="0 0 24 24">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
            <line x1="16" y1="13" x2="8" y2="13"/>
            <line x1="16" y1="17" x2="8" y2="17"/>
          </svg>
          <span class="examples-title">Specific Examples</span>
        </div>
        <div class="examples-content">
          {{{reference.specificExamples}}}
        </div>
      </div>
      {{/if}}

      <!-- Reliability Box -->
      {{#if reference.reliability}}
      <div class="reliability-box">
        <div class="reliability-header">
          <svg class="reliability-icon" viewBox="0 0 24 24">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
          </svg>
          <span class="reliability-title">Reliability & Trustworthiness</span>
        </div>
        <div class="reliability-content">
          {{{reference.reliability}}}
        </div>
      </div>
      {{/if}}

      <!-- Final Recommendation Box -->
      {{#if reference.referenceRecommendation}}
      <div class="recommendation-box">
        <div class="recommendation-header">
          <svg class="recommendation-icon" viewBox="0 0 24 24">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span class="recommendation-title">My Recommendation</span>
        </div>
        <div class="recommendation-content">
          {{{reference.referenceRecommendation}}}
        </div>
      </div>
      {{/if}}

      <!-- ─────────────────────────────────────────────────────────
           Closing & Signature Section
      ────────────────────────────────────────────────────────── -->
      <div class="closing-section">
        <div class="closing-phrase">{{closing}},</div>
        
        <div class="signature-block">
          {{#if signatureImage}}
          <img src="{{signatureImage}}" alt="Signature" class="signature-image">
          {{/if}}
          
          <div class="sender-name">{{senderName}}</div>
          
          {{#if senderDesignation}}
          <div class="sender-designation">{{senderDesignation}}</div>
          {{/if}}
          
          {{#if senderDepartment}}
          <div class="sender-department">{{senderDepartment}}</div>
          {{/if}}
          
          {{#if senderCompany}}
          <div class="sender-company">{{senderCompany}}</div>
          {{/if}}

          {{#if includeSignature}}
          <div class="signature-contact">
            {{#if senderEmail}}
            <div class="contact-item">
              <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <polyline points="22,6 12,13 2,6"/>
              </svg>
              <a href="mailto:{{senderEmail}}" class="contact-link">{{senderEmail}}</a>
            </div>
            {{/if}}
            
            {{#if senderPhone}}
            <div class="contact-item">
              <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72"/>
              </svg>
              <span>{{senderPhone}}</span>
            </div>
            {{/if}}
          </div>
          {{/if}}

          <!-- Contact Permission -->
          {{#if reference.contactPermission}}
          <div class="contact-permission">
            <svg class="permission-icon" viewBox="0 0 24 24">
              <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
              <polyline points="22 4 12 14.01 9 11.01"/>
            </svg>
            <span>Please feel free to contact me for any further information or clarification regarding this reference.</span>
          </div>
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</body>
</html>
