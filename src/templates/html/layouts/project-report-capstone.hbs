{{!-- 
  ═══════════════════════════════════════════════════════════════════════════════
  SORIVA - PROJECT REPORT TEMPLATE: CAPSTONE
  Style: Final Year / Major / Graduation Project Report
  Best For: B.Tech final year, M.Tech thesis, Major projects, Capstone projects
  Features: Comprehensive chapters, Full documentation, Professional formatting
  ═══════════════════════════════════════════════════════════════════════════════
--}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{projectTitle}} - Capstone Project Report</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       CSS RESET & BASE STYLES
    ═══════════════════════════════════════════════════════════════ */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --primary-color: {{#if accentColor}}{{accentColor}}{{else}}#1e3a5f{{/if}};
      --secondary-color: #2d4a6f;
      --accent-color: #c9a227;
      --accent-light: #f0d77c;
      --success-color: #059669;
      --warning-color: #d97706;
      --danger-color: #dc2626;
      --text-color: #1f2937;
      --text-light: #6b7280;
      --text-muted: #9ca3af;
      --border-color: #e5e7eb;
      --bg-light: #f9fafb;
      --bg-cream: #fefce8;
      --bg-blue: #f0f9ff;
      --bg-green: #ecfdf5;
      --font-serif: 'Times New Roman', 'Georgia', serif;
      --font-sans: 'Segoe UI', 'Arial', sans-serif;
    }

    /* ═══════════════════════════════════════════════════════════════
       PAGE SETUP
    ═══════════════════════════════════════════════════════════════ */
    @page {
      size: A4;
      margin: 1in;
    }

    @media print {
      body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
      .page-break { page-break-before: always; }
      .no-break { page-break-inside: avoid; }
    }

    html {
      font-size: 12pt;
    }

    body {
      font-family: var(--font-serif);
      line-height: 1.8;
      color: var(--text-color);
      background: #fff;
      max-width: 8.27in;
      margin: 0 auto;
      padding: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       TITLE PAGE - FORMAL UNIVERSITY STYLE
    ═══════════════════════════════════════════════════════════════ */
    .title-page {
      min-height: 100vh;
      border: 4px double var(--primary-color);
      padding: 0.5rem;
      position: relative;
    }

    .title-page-inner {
      border: 1px solid var(--primary-color);
      min-height: calc(100vh - 1rem - 8px);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      text-align: center;
      padding: 1.5rem 2rem;
    }

    .title-header {
      width: 100%;
    }

    .university-block {
      margin-bottom: 0.75rem;
    }

    .university-name {
      font-family: var(--font-sans);
      font-size: 1.15rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: var(--primary-color);
    }

    .institution-name {
      font-family: var(--font-sans);
      font-size: 1.3rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 3px;
      margin-top: 0.25rem;
    }

    .institution-address {
      font-size: 0.9rem;
      color: var(--text-light);
      margin-top: 0.25rem;
    }

    .institution-logo {
      width: 100px;
      height: 100px;
      margin: 1rem auto;
    }

    .institution-logo img {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }

    .department-name {
      font-family: var(--font-sans);
      font-size: 1.05rem;
      font-weight: 600;
      color: var(--secondary-color);
    }

    .title-middle {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 1rem 0;
    }

    .project-type-badge {
      display: inline-block;
      background: var(--accent-color);
      color: #fff;
      font-family: var(--font-sans);
      font-size: 0.75rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      padding: 0.5rem 1.5rem;
      border-radius: 3px;
      margin-bottom: 1rem;
    }

    .report-label {
      font-size: 1rem;
      text-transform: uppercase;
      letter-spacing: 3px;
      color: var(--text-light);
      margin-bottom: 0.5rem;
    }

    .project-title {
      font-size: 1.6rem;
      font-weight: 700;
      text-transform: uppercase;
      line-height: 1.4;
      color: var(--primary-color);
      margin-bottom: 0.5rem;
      padding: 0 1rem;
    }

    .project-subtitle {
      font-size: 1.05rem;
      font-style: italic;
      color: var(--text-light);
      margin-bottom: 1.5rem;
    }

    .degree-info {
      font-size: 1rem;
      margin-bottom: 1.5rem;
    }

    .degree-info .degree-label {
      color: var(--text-light);
    }

    .degree-info .degree-name {
      font-weight: 600;
      color: var(--primary-color);
    }

    /* Team Members on Title Page */
    .team-section {
      margin: 1rem 0;
      width: 100%;
    }

    .team-label {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--text-muted);
      margin-bottom: 0.5rem;
    }

    .team-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
    }

    .team-member {
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 0.6rem 1rem;
      min-width: 180px;
    }

    .team-member .member-name {
      font-family: var(--font-sans);
      font-size: 0.95rem;
      font-weight: 600;
    }

    .team-member .member-roll {
      font-size: 0.85rem;
      color: var(--text-light);
    }

    .team-member.leader {
      border-color: var(--accent-color);
      border-width: 2px;
    }

    .team-member.leader::before {
      content: "★ Team Lead";
      display: block;
      font-size: 0.65rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--accent-color);
      margin-bottom: 0.2rem;
    }

    .title-footer {
      width: 100%;
    }

    .guide-section {
      display: flex;
      justify-content: center;
      gap: 3rem;
      margin-bottom: 1rem;
    }

    .guide-block {
      text-align: center;
    }

    .guide-block .guide-label {
      font-family: var(--font-sans);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-muted);
      margin-bottom: 0.25rem;
    }

    .guide-block .guide-name {
      font-size: 1rem;
      font-weight: 600;
    }

    .guide-block .guide-designation {
      font-size: 0.85rem;
      color: var(--text-light);
    }

    .academic-year {
      font-family: var(--font-sans);
      font-size: 1.15rem;
      font-weight: 700;
      color: var(--primary-color);
      margin-top: 1rem;
      padding-top: 0.75rem;
      border-top: 1px solid var(--border-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       CERTIFICATE PAGE
    ═══════════════════════════════════════════════════════════════ */
    .certificate-page {
      min-height: 100vh;
      padding: 2rem 0;
    }

    .certificate-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 4px;
      margin-bottom: 2rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .certificate-content {
      text-align: justify;
      line-height: 2.2;
      margin-bottom: 3rem;
    }

    .certificate-signatures {
      display: flex;
      justify-content: space-between;
      margin-top: 4rem;
    }

    .signature-block {
      text-align: center;
      min-width: 160px;
    }

    .signature-line {
      border-top: 1px solid var(--text-color);
      padding-top: 0.5rem;
      margin-top: 4rem;
    }

    .signature-name {
      font-weight: 600;
    }

    .signature-designation {
      font-size: 0.9rem;
      color: var(--text-light);
    }

    /* ═══════════════════════════════════════════════════════════════
       DECLARATION PAGE
    ═══════════════════════════════════════════════════════════════ */
    .declaration-page {
      min-height: 100vh;
      padding: 2rem 0;
    }

    .declaration-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 4px;
      margin-bottom: 2rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .declaration-content {
      text-align: justify;
      line-height: 2.2;
    }

    .declaration-footer {
      margin-top: 4rem;
      display: flex;
      justify-content: space-between;
    }

    .declaration-place-date {
      text-align: left;
    }

    .declaration-signatures {
      text-align: right;
    }

    .declaration-signatures .sig-item {
      margin-bottom: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       ACKNOWLEDGEMENT PAGE
    ═══════════════════════════════════════════════════════════════ */
    .acknowledgement-page {
      min-height: 100vh;
      padding: 2rem 0;
    }

    .acknowledgement-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 4px;
      margin-bottom: 2rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .acknowledgement-content {
      text-align: justify;
      line-height: 2.2;
    }

    .acknowledgement-signatures {
      margin-top: 3rem;
      text-align: right;
    }

    /* ═══════════════════════════════════════════════════════════════
       ABSTRACT PAGE
    ═══════════════════════════════════════════════════════════════ */
    .abstract-page {
      min-height: 100vh;
      padding: 2rem 0;
    }

    .abstract-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 4px;
      margin-bottom: 2rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .abstract-content {
      text-align: justify;
      text-indent: 2em;
      line-height: 2;
    }

    .keywords-box {
      margin-top: 2rem;
      padding: 1rem;
      background: var(--bg-light);
      border-left: 4px solid var(--accent-color);
      border-radius: 0 8px 8px 0;
    }

    .keywords-label {
      font-family: var(--font-sans);
      font-weight: 700;
      color: var(--primary-color);
    }

    .keywords-list {
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       TABLE OF CONTENTS
    ═══════════════════════════════════════════════════════════════ */
    .toc-page {
      min-height: 100vh;
      padding: 2rem 0;
    }

    .toc-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 4px;
      margin-bottom: 2rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .toc-list {
      list-style: none;
    }

    .toc-item {
      display: flex;
      justify-content: space-between;
      padding: 0.5rem 0;
      border-bottom: 1px dotted var(--border-color);
    }

    .toc-item.chapter {
      font-weight: 600;
      color: var(--primary-color);
      margin-top: 0.5rem;
    }

    .toc-item.sub {
      padding-left: 1.5rem;
      font-size: 0.95rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       CHAPTER STYLING
    ═══════════════════════════════════════════════════════════════ */
    .chapter {
      margin-bottom: 2rem;
    }

    .chapter-header {
      text-align: center;
      margin-bottom: 1.5rem;
      padding-bottom: 1rem;
      border-bottom: 3px double var(--primary-color);
    }

    .chapter-number {
      font-family: var(--font-sans);
      font-size: 1rem;
      text-transform: uppercase;
      letter-spacing: 3px;
      color: var(--text-light);
      margin-bottom: 0.25rem;
    }

    .chapter-title {
      font-family: var(--font-sans);
      font-size: 1.5rem;
      font-weight: 700;
      text-transform: uppercase;
      color: var(--primary-color);
    }

    .chapter-content {
      text-align: justify;
    }

    .chapter-content p {
      text-indent: 2em;
      margin-bottom: 1rem;
    }

    .chapter-content p.no-indent {
      text-indent: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       SECTION HEADINGS
    ═══════════════════════════════════════════════════════════════ */
    .section-heading {
      font-family: var(--font-sans);
      font-size: 1.15rem;
      font-weight: 700;
      margin: 1.5rem 0 1rem;
      color: var(--secondary-color);
    }

    .subsection-heading {
      font-family: var(--font-sans);
      font-size: 1.05rem;
      font-weight: 600;
      margin: 1.25rem 0 0.75rem;
      color: var(--text-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       LISTS
    ═══════════════════════════════════════════════════════════════ */
    .content-list {
      margin: 1rem 0;
      padding-left: 2rem;
    }

    .content-list li {
      margin-bottom: 0.5rem;
      line-height: 1.7;
    }

    .objectives-list {
      list-style: none;
      margin: 1rem 0;
    }

    .objectives-list li {
      padding: 0.5rem 0;
      padding-left: 2rem;
      position: relative;
      border-bottom: 1px dashed var(--border-color);
    }

    .objectives-list li:last-child {
      border-bottom: none;
    }

    .objectives-list li::before {
      content: "▪";
      position: absolute;
      left: 0;
      color: var(--accent-color);
      font-size: 1.25rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       TABLES
    ═══════════════════════════════════════════════════════════════ */
    .content-table {
      width: 100%;
      border-collapse: collapse;
      margin: 1.5rem 0;
      font-size: 0.95rem;
    }

    .content-table caption {
      font-family: var(--font-sans);
      font-weight: 600;
      margin-bottom: 0.5rem;
      text-align: left;
      color: var(--primary-color);
    }

    .content-table th {
      background: var(--primary-color);
      color: #fff;
      padding: 0.75rem;
      text-align: left;
      font-family: var(--font-sans);
      font-weight: 600;
    }

    .content-table td {
      padding: 0.75rem;
      border: 1px solid var(--border-color);
    }

    .content-table tr:nth-child(even) {
      background: var(--bg-light);
    }

    .table-caption {
      font-size: 0.9rem;
      text-align: center;
      margin-top: 0.5rem;
      color: var(--text-light);
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       FIGURES
    ═══════════════════════════════════════════════════════════════ */
    .figure {
      margin: 1.5rem 0;
      text-align: center;
    }

    .figure img {
      max-width: 100%;
      max-height: 400px;
      border: 1px solid var(--border-color);
    }

    .figure-caption {
      font-size: 0.9rem;
      margin-top: 0.5rem;
      color: var(--text-light);
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       TECH STACK BOX
    ═══════════════════════════════════════════════════════════════ */
    .tech-stack-box {
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      border-radius: 10px;
      padding: 1.25rem;
      margin: 1.5rem 0;
    }

    .tech-stack-box .ts-title {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary-color);
      margin-bottom: 1rem;
      text-align: center;
    }

    .tech-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
    }

    .tech-item {
      background: #fff;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 0.75rem;
      text-align: center;
    }

    .tech-item .tech-category {
      font-family: var(--font-sans);
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-muted);
      margin-bottom: 0.35rem;
    }

    .tech-item .tech-list {
      font-size: 0.9rem;
      font-weight: 500;
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       MILESTONE TIMELINE
    ═══════════════════════════════════════════════════════════════ */
    .milestone-timeline {
      margin: 1.5rem 0;
      padding: 1rem;
      background: var(--bg-cream);
      border-radius: 10px;
      border: 1px solid #fde68a;
    }

    .milestone-item {
      display: flex;
      gap: 1rem;
      padding: 0.75rem 0;
      border-bottom: 1px dashed var(--border-color);
    }

    .milestone-item:last-child {
      border-bottom: none;
    }

    .milestone-marker {
      width: 30px;
      height: 30px;
      background: var(--accent-color);
      color: #fff;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: var(--font-sans);
      font-size: 0.8rem;
      font-weight: 600;
      flex-shrink: 0;
    }

    .milestone-content {
      flex: 1;
    }

    .milestone-name {
      font-family: var(--font-sans);
      font-weight: 600;
      color: var(--text-color);
    }

    .milestone-date {
      font-size: 0.85rem;
      color: var(--text-light);
    }

    .milestone-status {
      font-size: 0.75rem;
      padding: 0.15rem 0.5rem;
      border-radius: 10px;
      display: inline-block;
      margin-top: 0.25rem;
    }

    .milestone-status.completed {
      background: #d1fae5;
      color: #059669;
    }

    .milestone-status.in-progress {
      background: #fef3c7;
      color: #d97706;
    }

    /* ═══════════════════════════════════════════════════════════════
       TEST CASES TABLE
    ═══════════════════════════════════════════════════════════════ */
    .test-table {
      width: 100%;
      border-collapse: collapse;
      margin: 1rem 0;
      font-size: 0.9rem;
    }

    .test-table th {
      background: var(--primary-color);
      color: #fff;
      padding: 0.6rem;
      text-align: left;
      font-family: var(--font-sans);
    }

    .test-table td {
      padding: 0.6rem;
      border: 1px solid var(--border-color);
      vertical-align: top;
    }

    .test-status {
      display: inline-block;
      padding: 0.15rem 0.5rem;
      border-radius: 10px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .test-status.pass {
      background: #d1fae5;
      color: #059669;
    }

    .test-status.fail {
      background: #fee2e2;
      color: #dc2626;
    }

    /* ═══════════════════════════════════════════════════════════════
       CONCLUSION BOX
    ═══════════════════════════════════════════════════════════════ */
    .conclusion-box {
      background: linear-gradient(135deg, var(--bg-blue) 0%, #e0f2fe 100%);
      border: 1px solid #bae6fd;
      border-radius: 10px;
      padding: 1.5rem;
      margin: 1.5rem 0;
    }

    .conclusion-box p {
      text-indent: 2em;
      line-height: 2;
    }

    /* ═══════════════════════════════════════════════════════════════
       FUTURE SCOPE BOX
    ═══════════════════════════════════════════════════════════════ */
    .future-scope-box {
      background: var(--bg-green);
      border: 1px solid #a7f3d0;
      border-radius: 10px;
      padding: 1.25rem;
      margin: 1.5rem 0;
    }

    .future-scope-box .fs-title {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 700;
      text-transform: uppercase;
      color: var(--success-color);
      margin-bottom: 0.75rem;
    }

    .future-scope-box ul {
      list-style: none;
      margin: 0;
    }

    .future-scope-box li {
      padding: 0.4rem 0;
      padding-left: 1.75rem;
      position: relative;
    }

    .future-scope-box li::before {
      content: "→";
      position: absolute;
      left: 0;
      color: var(--success-color);
      font-weight: bold;
    }

    /* ═══════════════════════════════════════════════════════════════
       REFERENCES
    ═══════════════════════════════════════════════════════════════ */
    .references-section {
      margin-top: 2rem;
    }

    .references-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 4px;
      margin-bottom: 1.5rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .reference-item {
      margin-bottom: 0.75rem;
      padding-left: 2rem;
      text-indent: -2rem;
      line-height: 1.6;
    }

    .reference-item .ref-num {
      font-weight: 600;
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       APPENDIX
    ═══════════════════════════════════════════════════════════════ */
    .appendix-section {
      margin-top: 2rem;
    }

    .appendix-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 4px;
      margin-bottom: 1.5rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .appendix-item {
      margin-bottom: 1.5rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid var(--border-color);
    }

    .appendix-item .appendix-label {
      font-family: var(--font-sans);
      font-weight: 700;
      color: var(--primary-color);
      margin-bottom: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       PAGE FOOTER
    ═══════════════════════════════════════════════════════════════ */
    .page-footer {
      margin-top: 2rem;
      padding-top: 1rem;
      border-top: 1px solid var(--border-color);
      display: flex;
      justify-content: space-between;
      font-size: 0.85rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>

  <!-- ═══════════════════════════════════════════════════════════════
       TITLE PAGE
  ═══════════════════════════════════════════════════════════════ -->
  <div class="title-page">
    <div class="title-page-inner">
      <div class="title-header">
        <div class="university-block">
          {{#if university}}
          <div class="university-name">{{university}}</div>
          {{/if}}
          <div class="institution-name">{{institution}}</div>
          {{#if institutionAddress}}
          <div class="institution-address">{{institutionAddress}}</div>
          {{/if}}
        </div>
        
        {{#if institutionLogo}}
        <div class="institution-logo">
          <img src="{{institutionLogo}}" alt="Logo">
        </div>
        {{/if}}
        
        <div class="department-name">Department of {{department}}</div>
      </div>
      
      <div class="title-middle">
        <div class="project-type-badge">Capstone Project</div>
        
        <div class="report-label">A Project Report On</div>
        <h1 class="project-title">{{projectTitle}}</h1>
        {{#if projectSubtitle}}
        <div class="project-subtitle">{{projectSubtitle}}</div>
        {{/if}}
        
        <div class="degree-info">
          <span class="degree-label">Submitted in partial fulfillment for the degree of </span>
          <span class="degree-name">{{course}} in {{department}}</span>
        </div>
        
        <div class="team-section">
          <div class="team-label">Submitted By</div>
          <div class="team-grid">
            {{#if teamMembers}}
              {{#each teamMembers}}
              <div class="team-member {{#ifEquals this.name ../teamLeader}}leader{{/ifEquals}}">
                <div class="member-name">{{this.name}}</div>
                <div class="member-roll">{{this.rollNumber}}</div>
              </div>
              {{/each}}
            {{else}}
              <div class="team-member">
                <div class="member-name">{{studentName}}</div>
                <div class="member-roll">{{rollNumber}}</div>
              </div>
            {{/if}}
          </div>
        </div>
      </div>
      
      <div class="title-footer">
        <div class="guide-section">
          {{#if guide}}
          <div class="guide-block">
            <div class="guide-label">Project Guide</div>
            <div class="guide-name">{{guide.name}}</div>
            <div class="guide-designation">{{guide.designation}}</div>
          </div>
          {{/if}}
          
          {{#if coGuide}}
          <div class="guide-block">
            <div class="guide-label">Co-Guide</div>
            <div class="guide-name">{{coGuide.name}}</div>
            <div class="guide-designation">{{coGuide.designation}}</div>
          </div>
          {{/if}}
        </div>
        
        <div class="academic-year">{{academicYear}}</div>
      </div>
    </div>
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       CERTIFICATE
  ═══════════════════════════════════════════════════════════════ -->
  {{#if certificate}}
  <div class="certificate-page page-break">
    <h2 class="certificate-title">Certificate</h2>
    <div class="certificate-content">
      {{certificate}}
    </div>
    
    <div class="certificate-signatures">
      {{#if guide}}
      <div class="signature-block">
        <div class="signature-line">
          <div class="signature-name">{{guide.name}}</div>
          <div class="signature-designation">{{guide.designation}}</div>
          <div class="signature-designation">(Project Guide)</div>
        </div>
      </div>
      {{/if}}
      
      {{#if hodName}}
      <div class="signature-block">
        <div class="signature-line">
          <div class="signature-name">{{hodName}}</div>
          <div class="signature-designation">{{hodDesignation}}</div>
          <div class="signature-designation">(Head of Department)</div>
        </div>
      </div>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       DECLARATION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if declaration}}
  <div class="declaration-page page-break">
    <h2 class="declaration-title">Declaration</h2>
    <div class="declaration-content">
      {{declaration}}
    </div>
    
    <div class="declaration-footer">
      <div class="declaration-place-date">
        <div>Place: _______________</div>
        <div>Date: {{formatDate submissionDate}}</div>
      </div>
      
      <div class="declaration-signatures">
        {{#if teamMembers}}
          {{#each teamMembers}}
          <div class="sig-item">{{this.name}}</div>
          {{/each}}
        {{else}}
          <div class="sig-item">{{studentName}}</div>
        {{/if}}
      </div>
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       ACKNOWLEDGEMENT
  ═══════════════════════════════════════════════════════════════ -->
  {{#if acknowledgement}}
  <div class="acknowledgement-page page-break">
    <h2 class="acknowledgement-title">Acknowledgement</h2>
    <div class="acknowledgement-content">
      {{acknowledgement}}
    </div>
    
    <div class="acknowledgement-signatures">
      {{#if teamMembers}}
        {{#each teamMembers}}
        <div>{{this.name}}</div>
        {{/each}}
      {{else}}
        <div>{{studentName}}</div>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       ABSTRACT
  ═══════════════════════════════════════════════════════════════ -->
  <div class="abstract-page page-break">
    <h2 class="abstract-title">Abstract</h2>
    <div class="abstract-content">
      {{abstract}}
    </div>
    
    {{#if keywords}}
    <div class="keywords-box">
      <span class="keywords-label">Keywords: </span>
      <span class="keywords-list">
        {{#each keywords}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
      </span>
    </div>
    {{/if}}
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       TABLE OF CONTENTS
  ═══════════════════════════════════════════════════════════════ -->
  {{#if showTableOfContents}}
  <div class="toc-page page-break">
    <h2 class="toc-title">Table of Contents</h2>
    <ul class="toc-list">
      <li class="toc-item">Certificate</li>
      <li class="toc-item">Declaration</li>
      <li class="toc-item">Acknowledgement</li>
      <li class="toc-item">Abstract</li>
      <li class="toc-item chapter">Chapter 1: Introduction</li>
      <li class="toc-item sub">1.1 Problem Statement</li>
      <li class="toc-item sub">1.2 Objectives</li>
      <li class="toc-item sub">1.3 Scope</li>
      <li class="toc-item chapter">Chapter 2: Literature Review</li>
      <li class="toc-item chapter">Chapter 3: System Analysis & Design</li>
      <li class="toc-item sub">3.1 System Requirements</li>
      <li class="toc-item sub">3.2 System Architecture</li>
      <li class="toc-item chapter">Chapter 4: Implementation</li>
      <li class="toc-item sub">4.1 Technology Stack</li>
      <li class="toc-item sub">4.2 Modules</li>
      <li class="toc-item chapter">Chapter 5: Testing</li>
      <li class="toc-item chapter">Chapter 6: Results & Discussion</li>
      <li class="toc-item chapter">Chapter 7: Conclusion & Future Scope</li>
      <li class="toc-item">References</li>
      <li class="toc-item">Appendix</li>
    </ul>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 1: INTRODUCTION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if introduction}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-number">Chapter 1</div>
      <h2 class="chapter-title">Introduction</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{introduction}}</p>
      
      {{#if problemStatement}}
      <h3 class="section-heading">1.1 Problem Statement</h3>
      <p>{{problemStatement}}</p>
      {{/if}}
      
      {{#if objectives}}
      <h3 class="section-heading">1.2 Objectives</h3>
      <p class="no-indent">The primary objectives of this project are:</p>
      <ul class="objectives-list">
        {{#each objectives}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      
      {{#if scope}}
      <h3 class="section-heading">1.3 Scope</h3>
      <p>{{scope}}</p>
      {{/if}}
      
      {{#if limitations}}
      <h3 class="section-heading">1.4 Limitations</h3>
      <ul class="content-list">
        {{#each limitations}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 2: LITERATURE REVIEW
  ═══════════════════════════════════════════════════════════════ -->
  {{#if literatureReview}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-number">Chapter 2</div>
      <h2 class="chapter-title">Literature Review</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{literatureReview}}</p>
      
      {{#if existingSystems}}
      <h3 class="section-heading">2.1 Existing Systems</h3>
      {{#each existingSystems}}
      <h4 class="subsection-heading">{{this.name}}</h4>
      <p>{{this.description}}</p>
      {{#if this.limitations}}
      <p class="no-indent"><strong>Limitations:</strong></p>
      <ul class="content-list">
        {{#each this.limitations}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      {{/each}}
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 3: SYSTEM ANALYSIS & DESIGN
  ═══════════════════════════════════════════════════════════════ -->
  {{#if methodology}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-number">Chapter 3</div>
      <h2 class="chapter-title">System Analysis & Design</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{methodology}}</p>
      
      {{#if systemRequirements}}
      <h3 class="section-heading">3.1 System Requirements</h3>
      
      {{#if systemRequirements.hardware}}
      <h4 class="subsection-heading">Hardware Requirements</h4>
      <ul class="content-list">
        {{#each systemRequirements.hardware}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      
      {{#if systemRequirements.software}}
      <h4 class="subsection-heading">Software Requirements</h4>
      <ul class="content-list">
        {{#each systemRequirements.software}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      
      {{#if systemRequirements.functional}}
      <h4 class="subsection-heading">Functional Requirements</h4>
      <ul class="content-list">
        {{#each systemRequirements.functional}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      {{/if}}
      
      {{#if systemArchitecture}}
      <h3 class="section-heading">3.2 System Architecture</h3>
      <p>{{systemArchitecture}}</p>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 4: IMPLEMENTATION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if implementation}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-number">Chapter 4</div>
      <h2 class="chapter-title">Implementation</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{implementation}}</p>
      
      {{#if techStack}}
      <h3 class="section-heading">4.1 Technology Stack</h3>
      <div class="tech-stack-box">
        <div class="ts-title">Technologies Used</div>
        <div class="tech-grid">
          {{#each techStack}}
          <div class="tech-item">
            <div class="tech-category">{{this.category}}</div>
            <div class="tech-list">
              {{#each this.technologies}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
            </div>
          </div>
          {{/each}}
        </div>
      </div>
      {{/if}}
      
      {{#if modules}}
      <h3 class="section-heading">4.2 Modules</h3>
      {{#each modules}}
      <h4 class="subsection-heading">{{math @index "+" 1}}. {{this.name}}</h4>
      <p>{{this.description}}</p>
      {{#if this.features}}
      <ul class="content-list">
        {{#each this.features}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      {{/each}}
      {{/if}}
      
      {{#if milestones}}
      <h3 class="section-heading">4.3 Project Timeline</h3>
      <div class="milestone-timeline">
        {{#each milestones}}
        <div class="milestone-item">
          <div class="milestone-marker">{{math @index "+" 1}}</div>
          <div class="milestone-content">
            <div class="milestone-name">{{this.name}}</div>
            {{#if this.endDate}}
            <div class="milestone-date">{{this.startDate}} - {{this.endDate}}</div>
            {{/if}}
            <span class="milestone-status {{this.status}}">
              {{#ifEquals this.status "completed"}}✓ Completed{{else}}⏳ In Progress{{/ifEquals}}
            </span>
          </div>
        </div>
        {{/each}}
      </div>
      {{/if}}
      
      {{#if screenshots}}
      <h3 class="section-heading">4.4 Screenshots</h3>
      {{#each screenshots}}
      <div class="figure">
        <img src="{{this.imageUrl}}" alt="{{this.caption}}">
        <div class="figure-caption">Figure {{this.number}}: {{this.caption}}</div>
      </div>
      {{/each}}
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 5: TESTING
  ═══════════════════════════════════════════════════════════════ -->
  {{#if testCases}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-number">Chapter 5</div>
      <h2 class="chapter-title">Testing</h2>
    </div>
    
    <div class="chapter-content">
      {{#if testingApproach}}
      <p>{{testingApproach}}</p>
      {{/if}}
      
      <h3 class="section-heading">5.1 Test Cases</h3>
      <table class="test-table">
        <thead>
          <tr>
            <th>Test ID</th>
            <th>Description</th>
            <th>Expected Result</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          {{#each testCases}}
          <tr>
            <td>{{this.id}}</td>
            <td>{{this.description}}</td>
            <td>{{this.expectedOutput}}</td>
            <td>
              <span class="test-status {{this.status}}">
                {{#ifEquals this.status "pass"}}✓ Pass{{else}}✗ Fail{{/ifEquals}}
              </span>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
      
      {{#if testResults}}
      <h3 class="section-heading">5.2 Test Results Summary</h3>
      <p>{{testResults}}</p>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 6: RESULTS & DISCUSSION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if results}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-number">Chapter 6</div>
      <h2 class="chapter-title">Results & Discussion</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{results}}</p>
      
      {{#if findings}}
      <h3 class="section-heading">6.1 Key Findings</h3>
      <ul class="objectives-list">
        {{#each findings}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      
      {{#if discussion}}
      <h3 class="section-heading">6.2 Discussion</h3>
      <p>{{discussion}}</p>
      {{/if}}
      
      {{#if challengesFaced}}
      <h3 class="section-heading">6.3 Challenges Faced</h3>
      <ul class="content-list">
        {{#each challengesFaced}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 7: CONCLUSION & FUTURE SCOPE
  ═══════════════════════════════════════════════════════════════ -->
  {{#if conclusion}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-number">Chapter 7</div>
      <h2 class="chapter-title">Conclusion & Future Scope</h2>
    </div>
    
    <div class="chapter-content">
      <h3 class="section-heading">7.1 Conclusion</h3>
      <div class="conclusion-box">
        <p>{{conclusion}}</p>
      </div>
      
      {{#if futureScope}}
      <h3 class="section-heading">7.2 Future Scope</h3>
      <div class="future-scope-box">
        <div class="fs-title">Future Enhancements</div>
        <ul>
          {{#each futureScope}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
      
      {{#if recommendations}}
      <h3 class="section-heading">7.3 Recommendations</h3>
      <ul class="content-list">
        {{#each recommendations}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       REFERENCES
  ═══════════════════════════════════════════════════════════════ -->
  {{#if references}}
  <div class="references-section page-break">
    <h2 class="references-title">References</h2>
    
    {{#each references}}
    <div class="reference-item">
      <span class="ref-num">[{{math @index "+" 1}}]</span>
      {{#if this.authors}}
        {{#each this.authors}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
      {{/if}}
      {{#if this.year}} ({{this.year}}).{{/if}}
      {{#if this.title}} "{{this.title}}".{{/if}}
      {{#if this.publisher}} {{this.publisher}}.{{/if}}
      {{#if this.url}} Available at: {{this.url}}{{/if}}
    </div>
    {{/each}}
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       APPENDIX
  ═══════════════════════════════════════════════════════════════ -->
  {{#if appendix}}
  <div class="appendix-section page-break">
    <h2 class="appendix-title">Appendix</h2>
    
    {{#each appendix}}
    <div class="appendix-item">
      <div class="appendix-label">{{this.label}}: {{this.title}}</div>
      {{#if this.content}}
      <p>{{this.content}}</p>
      {{/if}}
      {{#if this.imageUrl}}
      <div class="figure">
        <img src="{{this.imageUrl}}" alt="{{this.title}}">
      </div>
      {{/if}}
    </div>
    {{/each}}
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       PAGE FOOTER
  ═══════════════════════════════════════════════════════════════ -->
  <footer class="page-footer">
    <div>{{projectTitle}}</div>
    <div>{{institution}} | {{academicYear}}</div>
  </footer>

</body>
</html>
