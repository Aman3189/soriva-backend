{{!-- 
  ═══════════════════════════════════════════════════════════════════════════════
  SORIVA - PROJECT REPORT TEMPLATE: RESEARCH
  Style: Research-based project documentation
  Best For: Research projects, Data analysis, Survey-based studies, Experiments
  Features: Hypothesis, Literature review, Methodology, Data analysis, Findings
  ═══════════════════════════════════════════════════════════════════════════════
--}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{projectTitle}} - Research Report</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       CSS RESET & BASE STYLES
    ═══════════════════════════════════════════════════════════════ */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --primary-color: {{#if accentColor}}{{accentColor}}{{else}}#7c3aed{{/if}};
      --secondary-color: #6d28d9;
      --accent-color: #0891b2;
      --success-color: #059669;
      --warning-color: #d97706;
      --text-color: #1f2937;
      --text-light: #6b7280;
      --text-muted: #9ca3af;
      --border-color: #e5e7eb;
      --bg-light: #f9fafb;
      --bg-purple: #f5f3ff;
      --bg-cyan: #ecfeff;
      --bg-green: #ecfdf5;
      --font-serif: 'Times New Roman', 'Georgia', serif;
      --font-sans: 'Segoe UI', 'Arial', sans-serif;
    }

    /* ═══════════════════════════════════════════════════════════════
       PAGE SETUP
    ═══════════════════════════════════════════════════════════════ */
    @page {
      size: A4;
      margin: 1in;
    }

    @media print {
      body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
      .page-break { page-break-before: always; }
      .no-break { page-break-inside: avoid; }
    }

    html {
      font-size: 12pt;
    }

    body {
      font-family: var(--font-serif);
      line-height: 2;
      color: var(--text-color);
      background: #fff;
      max-width: 8.27in;
      margin: 0 auto;
      padding: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       TITLE PAGE
    ═══════════════════════════════════════════════════════════════ */
    .title-page {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 2rem;
      position: relative;
    }

    .title-page::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 8px;
      background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
    }

    .title-page::after {
      content: "";
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 8px;
      background: linear-gradient(90deg, var(--accent-color), var(--primary-color));
    }

    .institution-block {
      margin-bottom: 2rem;
    }

    .institution-logo {
      width: 90px;
      height: 90px;
      margin: 0 auto 1rem;
    }

    .institution-logo img {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }

    .university-name {
      font-family: var(--font-sans);
      font-size: 1.1rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary-color);
    }

    .institution-name {
      font-family: var(--font-sans);
      font-size: 1.3rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-top: 0.25rem;
    }

    .department-name {
      font-family: var(--font-sans);
      font-size: 1rem;
      margin-top: 0.5rem;
      color: var(--text-light);
    }

    .title-block {
      margin: 2rem 0;
      padding: 2rem;
      max-width: 85%;
    }

    .report-label {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 3px;
      color: var(--text-muted);
      margin-bottom: 1rem;
      padding: 0.5rem 1.5rem;
      border: 1px solid var(--border-color);
      display: inline-block;
      border-radius: 20px;
    }

    .project-title {
      font-size: 1.6rem;
      font-weight: 700;
      line-height: 1.4;
      color: var(--primary-color);
      margin-bottom: 0.75rem;
    }

    .project-subtitle {
      font-size: 1.1rem;
      font-style: italic;
      color: var(--text-light);
    }

    .author-block {
      margin: 2rem 0;
    }

    .submitted-label {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--text-muted);
      margin-bottom: 0.5rem;
    }

    .author-name {
      font-size: 1.15rem;
      font-weight: 600;
    }

    .author-details {
      font-size: 0.95rem;
      color: var(--text-light);
    }

    .guide-block {
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 1px solid var(--border-color);
    }

    .guide-label {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--text-muted);
      margin-bottom: 0.25rem;
    }

    .guide-name {
      font-size: 1rem;
      font-weight: 600;
    }

    .guide-designation {
      font-size: 0.9rem;
      color: var(--text-light);
    }

    .submission-block {
      margin-top: 2rem;
    }

    .academic-year {
      font-family: var(--font-sans);
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       ABSTRACT PAGE
    ═══════════════════════════════════════════════════════════════ */
    .abstract-page {
      padding: 2rem 0;
    }

    .page-title {
      font-family: var(--font-sans);
      font-size: 1.3rem;
      font-weight: 700;
      text-transform: uppercase;
      text-align: center;
      letter-spacing: 3px;
      margin-bottom: 2rem;
      color: var(--primary-color);
    }

    .page-title::after {
      content: "";
      display: block;
      width: 60px;
      height: 3px;
      background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
      margin: 0.75rem auto 0;
      border-radius: 2px;
    }

    .abstract-content {
      text-align: justify;
      text-indent: 2em;
    }

    .keywords-box {
      margin-top: 2rem;
      padding: 1rem 1.25rem;
      background: var(--bg-purple);
      border-radius: 8px;
      border-left: 4px solid var(--primary-color);
    }

    .keywords-label {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary-color);
    }

    .keywords-list {
      font-style: italic;
      color: var(--text-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       CHAPTER / SECTION STYLING
    ═══════════════════════════════════════════════════════════════ */
    .chapter {
      margin-bottom: 2rem;
    }

    .chapter-header {
      margin-bottom: 1.5rem;
      padding-bottom: 0.75rem;
      border-bottom: 2px solid var(--primary-color);
    }

    .chapter-label {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: var(--text-muted);
    }

    .chapter-title {
      font-family: var(--font-sans);
      font-size: 1.4rem;
      font-weight: 700;
      color: var(--primary-color);
      text-transform: uppercase;
      margin-top: 0.25rem;
    }

    .chapter-content {
      text-align: justify;
    }

    .chapter-content p {
      text-indent: 2em;
      margin-bottom: 1rem;
    }

    .chapter-content p.no-indent {
      text-indent: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       SECTION HEADINGS
    ═══════════════════════════════════════════════════════════════ */
    .section-heading {
      font-family: var(--font-sans);
      font-size: 1.1rem;
      font-weight: 600;
      margin: 1.5rem 0 1rem;
      color: var(--secondary-color);
    }

    .subsection-heading {
      font-family: var(--font-sans);
      font-size: 1rem;
      font-weight: 600;
      margin: 1.25rem 0 0.75rem;
      color: var(--text-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       RESEARCH QUESTION BOX
    ═══════════════════════════════════════════════════════════════ */
    .research-question-box {
      background: linear-gradient(135deg, var(--bg-cyan) 0%, #cffafe 100%);
      border: 2px solid var(--accent-color);
      border-radius: 12px;
      padding: 1.5rem;
      margin: 1.5rem 0;
      text-align: center;
      position: relative;
    }

    .research-question-box::before {
      content: "?";
      position: absolute;
      top: -18px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--accent-color);
      color: #fff;
      width: 36px;
      height: 36px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      font-weight: bold;
      font-family: var(--font-sans);
    }

    .research-question-box .rq-label {
      font-family: var(--font-sans);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--accent-color);
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .research-question-box .rq-text {
      font-size: 1.15rem;
      font-style: italic;
      line-height: 1.6;
    }

    /* ═══════════════════════════════════════════════════════════════
       HYPOTHESIS BOX
    ═══════════════════════════════════════════════════════════════ */
    .hypothesis-box {
      background: var(--bg-purple);
      border-left: 5px solid var(--primary-color);
      border-radius: 0 10px 10px 0;
      padding: 1.25rem 1.5rem;
      margin: 1.5rem 0;
    }

    .hypothesis-box .hypo-label {
      font-family: var(--font-sans);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--primary-color);
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .hypothesis-box .hypo-text {
      font-style: italic;
      line-height: 1.8;
    }

    .hypothesis-box .hypo-null,
    .hypothesis-box .hypo-alt {
      margin-top: 0.75rem;
      padding: 0.5rem 0.75rem;
      background: #fff;
      border-radius: 6px;
      font-size: 0.95rem;
    }

    .hypothesis-box .hypo-tag {
      font-family: var(--font-sans);
      font-weight: 600;
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       OBJECTIVES LIST
    ═══════════════════════════════════════════════════════════════ */
    .objectives-list {
      list-style: none;
      margin: 1rem 0;
    }

    .objectives-list li {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      padding: 0.6rem 0;
      border-bottom: 1px dashed var(--border-color);
    }

    .objectives-list li:last-child {
      border-bottom: none;
    }

    .objectives-list .obj-num {
      background: var(--primary-color);
      color: #fff;
      width: 26px;
      height: 26px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: var(--font-sans);
      font-size: 0.8rem;
      font-weight: 600;
      flex-shrink: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       LITERATURE REVIEW CARDS
    ═══════════════════════════════════════════════════════════════ */
    .lit-review-card {
      background: #fff;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      margin-bottom: 1rem;
      overflow: hidden;
    }

    .lit-review-card .source-header {
      background: var(--bg-light);
      padding: 0.75rem 1rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
      border-bottom: 1px solid var(--border-color);
    }

    .lit-review-card .source-icon {
      font-size: 1.25rem;
    }

    .lit-review-card .source-title {
      font-family: var(--font-sans);
      font-size: 0.95rem;
      font-weight: 600;
    }

    .lit-review-card .source-author {
      font-family: var(--font-sans);
      font-size: 0.8rem;
      color: var(--text-light);
    }

    .lit-review-card .source-body {
      padding: 1rem;
    }

    .lit-review-card .source-summary {
      margin-bottom: 0.75rem;
      text-indent: 0 !important;
    }

    .lit-review-card .source-relevance {
      background: var(--bg-green);
      padding: 0.5rem 0.75rem;
      border-radius: 6px;
      font-size: 0.9rem;
    }

    .lit-review-card .relevance-label {
      font-family: var(--font-sans);
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--success-color);
      font-weight: 600;
    }

    /* ═══════════════════════════════════════════════════════════════
       METHODOLOGY BOX
    ═══════════════════════════════════════════════════════════════ */
    .methodology-box {
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 1.5rem;
      margin: 1rem 0;
    }

    .method-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      margin-top: 1rem;
    }

    .method-item {
      background: #fff;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 1rem;
      text-align: center;
    }

    .method-item .method-icon {
      font-size: 1.75rem;
      margin-bottom: 0.5rem;
    }

    .method-item .method-label {
      font-family: var(--font-sans);
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-muted);
    }

    .method-item .method-value {
      font-family: var(--font-sans);
      font-size: 0.95rem;
      font-weight: 600;
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       DATA COLLECTION
    ═══════════════════════════════════════════════════════════════ */
    .data-collection-box {
      border: 1px solid var(--border-color);
      border-radius: 10px;
      overflow: hidden;
      margin: 1rem 0;
    }

    .data-collection-header {
      background: var(--primary-color);
      color: #fff;
      padding: 0.75rem 1rem;
      font-family: var(--font-sans);
      font-weight: 600;
    }

    .data-collection-body {
      padding: 1rem;
    }

    .data-collection-body ul {
      list-style: none;
      margin: 0;
    }

    .data-collection-body li {
      padding: 0.5rem 0;
      padding-left: 1.5rem;
      position: relative;
      border-bottom: 1px dashed var(--border-color);
    }

    .data-collection-body li:last-child {
      border-bottom: none;
    }

    .data-collection-body li::before {
      content: "▸";
      position: absolute;
      left: 0;
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       DATA TABLE
    ═══════════════════════════════════════════════════════════════ */
    .data-table {
      width: 100%;
      border-collapse: collapse;
      margin: 1rem 0;
      font-size: 0.95rem;
    }

    .data-table caption {
      font-family: var(--font-sans);
      font-weight: 600;
      text-align: left;
      margin-bottom: 0.5rem;
      color: var(--primary-color);
    }

    .data-table th {
      background: var(--primary-color);
      color: #fff;
      padding: 0.75rem;
      text-align: left;
      font-family: var(--font-sans);
      font-weight: 600;
    }

    .data-table td {
      padding: 0.75rem;
      border-bottom: 1px solid var(--border-color);
    }

    .data-table tr:nth-child(even) {
      background: var(--bg-light);
    }

    .table-note {
      font-size: 0.85rem;
      color: var(--text-light);
      font-style: italic;
      margin-top: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       FINDINGS
    ═══════════════════════════════════════════════════════════════ */
    .findings-list {
      list-style: none;
      margin: 1rem 0;
    }

    .finding-item {
      display: flex;
      gap: 1rem;
      padding: 1rem;
      background: var(--bg-light);
      border-radius: 10px;
      margin-bottom: 0.75rem;
      border-left: 4px solid var(--accent-color);
    }

    .finding-item .finding-num {
      background: var(--accent-color);
      color: #fff;
      width: 30px;
      height: 30px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 600;
      flex-shrink: 0;
    }

    .finding-item .finding-text {
      flex: 1;
      line-height: 1.7;
    }

    /* ═══════════════════════════════════════════════════════════════
       STATISTICAL RESULTS
    ═══════════════════════════════════════════════════════════════ */
    .stats-box {
      background: var(--bg-purple);
      border: 1px solid #ddd6fe;
      border-radius: 10px;
      padding: 1.25rem;
      margin: 1rem 0;
    }

    .stats-box .stats-title {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--primary-color);
      margin-bottom: 0.75rem;
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 0.75rem;
    }

    .stat-item {
      background: #fff;
      border-radius: 8px;
      padding: 0.75rem;
      text-align: center;
    }

    .stat-item .stat-value {
      font-family: var(--font-sans);
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--primary-color);
    }

    .stat-item .stat-label {
      font-family: var(--font-sans);
      font-size: 0.7rem;
      text-transform: uppercase;
      color: var(--text-muted);
    }

    /* ═══════════════════════════════════════════════════════════════
       LIMITATIONS BOX
    ═══════════════════════════════════════════════════════════════ */
    .limitations-box {
      background: #fffbeb;
      border: 1px solid #fde68a;
      border-left: 5px solid var(--warning-color);
      border-radius: 0 10px 10px 0;
      padding: 1.25rem;
      margin: 1rem 0;
    }

    .limitations-box .lim-title {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 600;
      text-transform: uppercase;
      color: var(--warning-color);
      margin-bottom: 0.75rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .limitations-box ul {
      list-style: none;
      margin: 0;
    }

    .limitations-box li {
      padding: 0.4rem 0;
      padding-left: 1.5rem;
      position: relative;
    }

    .limitations-box li::before {
      content: "⚠";
      position: absolute;
      left: 0;
      font-size: 0.9rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       CONCLUSION BOX
    ═══════════════════════════════════════════════════════════════ */
    .conclusion-box {
      background: linear-gradient(135deg, var(--bg-green) 0%, #d1fae5 100%);
      border: 1px solid #a7f3d0;
      border-radius: 12px;
      padding: 1.5rem;
      margin: 1rem 0;
    }

    .conclusion-box p {
      text-indent: 2em;
      line-height: 2;
    }

    /* ═══════════════════════════════════════════════════════════════
       RECOMMENDATIONS
    ═══════════════════════════════════════════════════════════════ */
    .recommendations-list {
      list-style: none;
      margin: 1rem 0;
    }

    .recommendations-list li {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      padding: 0.6rem 0;
    }

    .recommendations-list li::before {
      content: "💡";
      flex-shrink: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       FUTURE SCOPE
    ═══════════════════════════════════════════════════════════════ */
    .future-scope-box {
      background: var(--bg-cyan);
      border: 1px solid #a5f3fc;
      border-radius: 10px;
      padding: 1.25rem;
      margin: 1rem 0;
    }

    .future-scope-box .fs-title {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 600;
      text-transform: uppercase;
      color: var(--accent-color);
      margin-bottom: 0.75rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .future-scope-box ul {
      list-style: none;
      margin: 0;
    }

    .future-scope-box li {
      padding: 0.4rem 0;
      padding-left: 1.5rem;
      position: relative;
    }

    .future-scope-box li::before {
      content: "→";
      position: absolute;
      left: 0;
      color: var(--accent-color);
      font-weight: bold;
    }

    /* ═══════════════════════════════════════════════════════════════
       REFERENCES
    ═══════════════════════════════════════════════════════════════ */
    .references-section {
      margin-top: 2rem;
    }

    .reference-item {
      padding-left: 2rem;
      text-indent: -2rem;
      margin-bottom: 0.75rem;
      line-height: 1.6;
    }

    .reference-item .ref-num {
      font-weight: 600;
      color: var(--primary-color);
    }

    /* Citation Style Tag */
    .citation-tag {
      font-family: var(--font-sans);
      display: inline-block;
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      padding: 0.15rem 0.5rem;
      border-radius: 4px;
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-light);
      margin-left: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       APPENDIX
    ═══════════════════════════════════════════════════════════════ */
    .appendix-section {
      margin-top: 2rem;
    }

    .appendix-item {
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 1rem;
      margin-bottom: 1rem;
    }

    .appendix-item .appendix-label {
      font-family: var(--font-sans);
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--primary-color);
      margin-bottom: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
    ═══════════════════════════════════════════════════════════════ */
    .report-footer {
      margin-top: 2rem;
      padding-top: 1rem;
      border-top: 2px solid var(--border-color);
      display: flex;
      justify-content: space-between;
      font-family: var(--font-sans);
      font-size: 0.85rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>

  <!-- ═══════════════════════════════════════════════════════════════
       TITLE PAGE
  ═══════════════════════════════════════════════════════════════ -->
  <div class="title-page">
    <div class="institution-block">
      {{#if institutionLogo}}
      <div class="institution-logo">
        <img src="{{institutionLogo}}" alt="Logo">
      </div>
      {{/if}}
      
      {{#if university}}
      <div class="university-name">{{university}}</div>
      {{/if}}
      <div class="institution-name">{{institution}}</div>
      <div class="department-name">Department of {{department}}</div>
    </div>
    
    <div class="title-block">
      <div class="report-label">Research Project Report</div>
      <h1 class="project-title">{{projectTitle}}</h1>
      {{#if projectSubtitle}}
      <div class="project-subtitle">{{projectSubtitle}}</div>
      {{/if}}
    </div>
    
    <div class="author-block">
      <div class="submitted-label">Submitted By</div>
      {{#if teamMembers}}
        {{#each teamMembers}}
        <div class="author-name">{{this.name}}</div>
        <div class="author-details">{{this.rollNumber}}</div>
        {{/each}}
      {{else}}
      <div class="author-name">{{studentName}}</div>
      <div class="author-details">
        Roll No: {{rollNumber}}
        {{#if course}} | {{course}}{{/if}}
        {{#if semester}} | {{semester}}{{/if}}
      </div>
      {{/if}}
      
      {{#if guide}}
      <div class="guide-block">
        <div class="guide-label">Under the Supervision of</div>
        <div class="guide-name">{{guide.name}}</div>
        <div class="guide-designation">{{guide.designation}}, {{guide.department}}</div>
      </div>
      {{/if}}
    </div>
    
    <div class="submission-block">
      <div class="academic-year">{{academicYear}}</div>
    </div>
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       ABSTRACT PAGE
  ═══════════════════════════════════════════════════════════════ -->
  <div class="abstract-page page-break">
    <h2 class="page-title">Abstract</h2>
    <div class="abstract-content">
      {{abstract}}
    </div>
    
    {{#if keywords}}
    <div class="keywords-box">
      <span class="keywords-label">Keywords: </span>
      <span class="keywords-list">
        {{#each keywords}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
      </span>
    </div>
    {{/if}}
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 1: INTRODUCTION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if introduction}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-label">Chapter 1</div>
      <h2 class="chapter-title">Introduction</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{introduction}}</p>
      
      {{#if research.researchQuestion}}
      <h3 class="section-heading">1.1 Research Question</h3>
      <div class="research-question-box">
        <div class="rq-label">Central Research Question</div>
        <div class="rq-text">{{research.researchQuestion}}</div>
      </div>
      {{/if}}
      
      {{#if research.hypothesis}}
      <h3 class="section-heading">1.2 Hypothesis</h3>
      <div class="hypothesis-box">
        <div class="hypo-label">Research Hypothesis</div>
        <div class="hypo-text">{{research.hypothesis}}</div>
        
        {{#if research.nullHypothesis}}
        <div class="hypo-null">
          <span class="hypo-tag">H₀ (Null):</span> {{research.nullHypothesis}}
        </div>
        {{/if}}
        
        {{#if research.altHypothesis}}
        <div class="hypo-alt">
          <span class="hypo-tag">H₁ (Alternative):</span> {{research.altHypothesis}}
        </div>
        {{/if}}
      </div>
      {{/if}}
      
      {{#if objectives}}
      <h3 class="section-heading">1.3 Research Objectives</h3>
      <ul class="objectives-list">
        {{#each objectives}}
        <li>
          <span class="obj-num">{{math @index "+" 1}}</span>
          <span>{{this}}</span>
        </li>
        {{/each}}
      </ul>
      {{/if}}
      
      {{#if scope}}
      <h3 class="section-heading">1.4 Scope of Study</h3>
      <p>{{scope}}</p>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 2: LITERATURE REVIEW
  ═══════════════════════════════════════════════════════════════ -->
  {{#if literatureReview}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-label">Chapter 2</div>
      <h2 class="chapter-title">Literature Review</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{literatureReview}}</p>
      
      {{#if research.literatureReview}}
      <h3 class="section-heading">2.1 Review of Related Studies</h3>
      {{#each research.literatureReview}}
      <div class="lit-review-card">
        <div class="source-header">
          <span class="source-icon">📄</span>
          <div>
            <div class="source-title">{{this.source}}</div>
            {{#if this.author}}
            <div class="source-author">{{this.author}} {{#if this.year}}({{this.year}}){{/if}}</div>
            {{/if}}
          </div>
        </div>
        <div class="source-body">
          <p class="source-summary">{{this.summary}}</p>
          {{#if this.relevance}}
          <div class="source-relevance">
            <div class="relevance-label">Relevance to Current Study</div>
            <div>{{this.relevance}}</div>
          </div>
          {{/if}}
        </div>
      </div>
      {{/each}}
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 3: METHODOLOGY
  ═══════════════════════════════════════════════════════════════ -->
  {{#if methodology}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-label">Chapter 3</div>
      <h2 class="chapter-title">Research Methodology</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{methodology}}</p>
      
      {{#if research.researchDesign}}
      <div class="methodology-box">
        <div class="method-grid">
          {{#if research.researchDesign}}
          <div class="method-item">
            <div class="method-icon">📊</div>
            <div class="method-label">Research Design</div>
            <div class="method-value">{{research.researchDesign}}</div>
          </div>
          {{/if}}
          
          {{#if research.sampleSize}}
          <div class="method-item">
            <div class="method-icon">👥</div>
            <div class="method-label">Sample Size</div>
            <div class="method-value">{{research.sampleSize}}</div>
          </div>
          {{/if}}
          
          {{#if research.samplingMethod}}
          <div class="method-item">
            <div class="method-icon">🎯</div>
            <div class="method-label">Sampling Method</div>
            <div class="method-value">{{research.samplingMethod}}</div>
          </div>
          {{/if}}
        </div>
      </div>
      {{/if}}
      
      {{#if research.dataCollection}}
      <h3 class="section-heading">3.1 Data Collection Methods</h3>
      <div class="data-collection-box">
        <div class="data-collection-header">Data Collection Instruments</div>
        <div class="data-collection-body">
          <ul>
            {{#each research.dataCollection}}
            <li>{{this}}</li>
            {{/each}}
          </ul>
        </div>
      </div>
      {{/if}}
      
      {{#if research.dataAnalysisMethods}}
      <h3 class="section-heading">3.2 Data Analysis Methods</h3>
      <p class="no-indent">{{research.dataAnalysisMethods}}</p>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 4: DATA ANALYSIS & FINDINGS
  ═══════════════════════════════════════════════════════════════ -->
  {{#if results}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-label">Chapter 4</div>
      <h2 class="chapter-title">Data Analysis & Findings</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{results}}</p>
      
      {{#if dataAnalysis}}
      <h3 class="section-heading">4.1 Data Analysis</h3>
      <p>{{dataAnalysis}}</p>
      {{/if}}
      
      {{#if research.statisticalResults}}
      <div class="stats-box">
        <div class="stats-title">Statistical Results</div>
        <div class="stats-grid">
          {{#each research.statisticalResults}}
          <div class="stat-item">
            <div class="stat-value">{{this.value}}</div>
            <div class="stat-label">{{this.label}}</div>
          </div>
          {{/each}}
        </div>
      </div>
      {{/if}}
      
      {{#if findings}}
      <h3 class="section-heading">4.2 Key Findings</h3>
      <ul class="findings-list">
        {{#each findings}}
        <li class="finding-item">
          <span class="finding-num">{{math @index "+" 1}}</span>
          <span class="finding-text">{{this}}</span>
        </li>
        {{/each}}
      </ul>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 5: DISCUSSION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if discussion}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-label">Chapter 5</div>
      <h2 class="chapter-title">Discussion</h2>
    </div>
    
    <div class="chapter-content">
      <p>{{discussion}}</p>
      
      {{#if limitations}}
      <h3 class="section-heading">5.1 Limitations of the Study</h3>
      <div class="limitations-box">
        <div class="lim-title">⚠️ Study Limitations</div>
        <ul>
          {{#each limitations}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CHAPTER 6: CONCLUSION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if conclusion}}
  <div class="chapter page-break">
    <div class="chapter-header">
      <div class="chapter-label">Chapter 6</div>
      <h2 class="chapter-title">Conclusion & Recommendations</h2>
    </div>
    
    <div class="chapter-content">
      <h3 class="section-heading">6.1 Conclusion</h3>
      <div class="conclusion-box">
        <p>{{conclusion}}</p>
      </div>
      
      {{#if recommendations}}
      <h3 class="section-heading">6.2 Recommendations</h3>
      <ul class="recommendations-list">
        {{#each recommendations}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      {{/if}}
      
      {{#if futureScope}}
      <h3 class="section-heading">6.3 Scope for Future Research</h3>
      <div class="future-scope-box">
        <div class="fs-title">🔮 Future Research Directions</div>
        <ul>
          {{#each futureScope}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       REFERENCES
  ═══════════════════════════════════════════════════════════════ -->
  {{#if references}}
  <div class="references-section page-break">
    <h2 class="page-title">References {{#if citationStyle}}<span class="citation-tag">{{citationStyle}}</span>{{/if}}</h2>
    
    {{#each references}}
    <div class="reference-item">
      <span class="ref-num">[{{math @index "+" 1}}]</span>
      {{#if this.authors}}
        {{#each this.authors}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
      {{/if}}
      {{#if this.year}} ({{this.year}}).{{/if}}
      {{#if this.title}} {{this.title}}.{{/if}}
      {{#if this.source}} <em>{{this.source}}</em>.{{/if}}
      {{#if this.publisher}} {{this.publisher}}.{{/if}}
      {{#if this.url}} Available at: {{this.url}}{{/if}}
    </div>
    {{/each}}
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       APPENDIX
  ═══════════════════════════════════════════════════════════════ -->
  {{#if appendix}}
  <div class="appendix-section page-break">
    <h2 class="page-title">Appendix</h2>
    
    {{#each appendix}}
    <div class="appendix-item">
      <div class="appendix-label">{{this.label}}: {{this.title}}</div>
      {{#if this.content}}
      <p>{{this.content}}</p>
      {{/if}}
      {{#if this.imageUrl}}
      <img src="{{this.imageUrl}}" alt="{{this.title}}" style="max-width: 100%;">
      {{/if}}
    </div>
    {{/each}}
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       FOOTER
  ═══════════════════════════════════════════════════════════════ -->
  <footer class="report-footer">
    <div>{{studentName}} | {{rollNumber}}</div>
    <div>{{institution}} | {{academicYear}}</div>
  </footer>

</body>
</html>
