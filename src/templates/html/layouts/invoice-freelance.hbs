{{!-- 
================================================================================
SORIVA TEMPLATES - FREELANCE INVOICE
================================================================================
@file invoice-freelance.hbs
@version 2.0.0
@author Soriva AI (Risenex Dynamics)
@copyright 2025 Risenex Dynamics. All rights reserved.

SYNCED WITH: invoice-fields.config.ts
INTERFACE: FreelanceInvoiceData extends BaseInvoiceData

FEATURES:
- Freelancer Profile Section
- Portfolio Link
- Project Details
- Deliverables Tracking
- Revisions Information
- Usage Rights & Copyright
- Modern Creative Design
================================================================================
--}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice #{{invoiceNumber}} | {{seller.name}}</title>
    <style>
        :root {
            --primary: {{#if brandColor}}{{brandColor}}{{else}}#EC4899{{/if}};
            --primary-light: #F472B6;
            --primary-dark: #BE185D;
            --success: #059669;
            --warning: #D97706;
            --danger: #DC2626;
            --text: #111827;
            --text-secondary: #4B5563;
            --text-muted: #6B7280;
            --border: #E5E7EB;
            --bg-light: #F9FAFB;
            --gradient: linear-gradient(135deg, #EC4899 0%, #8B5CF6 100%);
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Arial, sans-serif; font-size: 10pt; color: var(--text); line-height: 1.5; }
        .hindi { font-family: 'Noto Sans Devanagari', 'Mangal', sans-serif; }
        @page { size: A4; margin: 10mm; }
        .invoice { width: 210mm; min-height: 297mm; margin: 0 auto; padding: 8mm; background: #fff; }
        
        /* Header */
        .header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 15px; }
        .freelancer-profile { display: flex; align-items: center; gap: 15px; }
        .profile-photo { width: 70px; height: 70px; border-radius: 50%; background: var(--gradient); display: flex; align-items: center; justify-content: center; color: #fff; font-size: 24pt; font-weight: 700; overflow: hidden; }
        .profile-photo img { width: 100%; height: 100%; object-fit: cover; }
        .freelancer-info { }
        .freelancer-name { font-size: 16pt; font-weight: 700; color: var(--text); }
        .freelancer-title { font-size: 11pt; color: var(--primary); font-weight: 500; }
        .freelancer-contact { font-size: 9pt; color: var(--text-secondary); margin-top: 5px; }
        .portfolio-link { display: inline-block; margin-top: 5px; padding: 4px 12px; background: var(--gradient); color: #fff; text-decoration: none; border-radius: 15px; font-size: 8pt; font-weight: 600; }
        .title-section { text-align: right; }
        .invoice-title { font-size: 28pt; font-weight: 700; background: var(--gradient); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; text-transform: uppercase; letter-spacing: 2px; }
        .invoice-title-hindi { font-size: 11pt; color: var(--text-muted); }
        .status-badge { display: inline-block; padding: 5px 15px; border-radius: 20px; font-size: 9pt; font-weight: 700; text-transform: uppercase; margin-top: 8px; }
        .status-paid { background: #D1FAE5; color: #059669; }
        .status-pending { background: #FEF3C7; color: #D97706; }
        .status-overdue { background: #FEE2E2; color: #DC2626; }
        
        /* Divider */
        .gradient-divider { height: 4px; background: var(--gradient); border-radius: 2px; margin-bottom: 15px; }
        
        <!-- Project Banner -->
        .project-banner { background: var(--bg-light); border-radius: 12px; padding: 15px 20px; margin-bottom: 15px; border-left: 5px solid var(--primary); }
        .project-label { font-size: 8pt; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; }
        .project-name { font-size: 18pt; font-weight: 700; color: var(--text); margin: 3px 0; }
        .project-desc { font-size: 10pt; color: var(--text-secondary); }
        .project-timeline { display: inline-block; margin-top: 8px; padding: 5px 12px; background: #FDF4FF; color: var(--primary-dark); border-radius: 5px; font-size: 9pt; font-weight: 500; }
        
        /* Meta Grid */
        .meta-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; margin-bottom: 15px; }
        .meta-card { background: var(--bg-light); padding: 12px; border-radius: 8px; text-align: center; }
        .meta-label { font-size: 8pt; color: var(--text-muted); text-transform: uppercase; }
        .meta-value { font-size: 12pt; font-weight: 700; color: var(--text); margin-top: 3px; }
        .meta-value.highlight { color: var(--primary); }
        
        /* Parties */
        .parties { display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 15px; }
        .party-box { padding: 15px; border-radius: 10px; background: var(--bg-light); }
        .party-box.from { border-top: 4px solid var(--primary); }
        .party-box.to { border-top: 4px solid var(--success); }
        .party-label { font-size: 9pt; font-weight: 600; color: var(--text-muted); text-transform: uppercase; margin-bottom: 8px; }
        .party-name { font-size: 12pt; font-weight: 700; color: var(--text); }
        .party-address { font-size: 9pt; color: var(--text-secondary); line-height: 1.6; margin-top: 5px; }
        .party-tax { margin-top: 8px; padding-top: 8px; border-top: 1px dashed var(--border); font-size: 9pt; }
        
        /* Deliverables */
        .deliverables-section { margin-bottom: 15px; }
        .section-title { font-size: 12pt; font-weight: 700; color: var(--text); margin-bottom: 12px; display: flex; align-items: center; gap: 8px; }
        .section-title span { color: var(--primary); }
        .deliverable-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; }
        .deliverable-card { background: #fff; border: 1px solid var(--border); border-radius: 10px; padding: 12px; transition: all 0.3s ease; }
        .deliverable-card:hover { border-color: var(--primary); box-shadow: 0 4px 12px rgba(236, 72, 153, 0.15); }
        .deliverable-card.completed { border-left: 4px solid var(--success); background: #F0FDF4; }
        .deliverable-card.approved { border-left: 4px solid var(--primary); background: #FDF4FF; }
        .deliverable-card.in_progress { border-left: 4px solid var(--warning); background: #FFFBEB; }
        .deliverable-card.pending { border-left: 4px solid var(--border); }
        .deliverable-name { font-size: 10pt; font-weight: 600; color: var(--text); }
        .deliverable-desc { font-size: 8pt; color: var(--text-muted); margin-top: 3px; }
        .deliverable-footer { display: flex; justify-content: space-between; align-items: center; margin-top: 8px; padding-top: 8px; border-top: 1px dashed var(--border); }
        .deliverable-status { font-size: 8pt; font-weight: 600; text-transform: uppercase; padding: 3px 8px; border-radius: 10px; }
        .deliverable-status.completed { background: #D1FAE5; color: #059669; }
        .deliverable-status.approved { background: #FCE7F3; color: #BE185D; }
        .deliverable-status.in_progress { background: #FEF3C7; color: #D97706; }
        .deliverable-status.pending { background: #F3F4F6; color: #6B7280; }
        .deliverable-amount { font-size: 11pt; font-weight: 700; color: var(--text); }
        
        /* Items Table */
        .items-table { width: 100%; border-collapse: collapse; margin-bottom: 15px; border-radius: 10px; overflow: hidden; }
        .items-table thead { background: var(--gradient); color: #fff; }
        .items-table th { padding: 12px 10px; text-align: left; font-size: 9pt; font-weight: 600; text-transform: uppercase; }
        .items-table th .hindi { font-size: 7pt; font-weight: 400; opacity: 0.8; display: block; }
        .items-table th.center { text-align: center; }
        .items-table th.right { text-align: right; }
        .items-table td { padding: 12px 10px; border-bottom: 1px solid var(--border); font-size: 9pt; vertical-align: top; }
        .items-table tbody tr:hover { background: #FDF4FF; }
        .items-table tbody tr:last-child td { border-bottom: none; }
        .center { text-align: center; }
        .right { text-align: right; }
        .item-desc { font-weight: 500; }
        .item-sub { font-size: 8pt; color: var(--text-muted); }
        .amount { font-weight: 700; }
        
        /* Revisions Box */
        .revisions-box { background: #FDF4FF; border: 1px solid #F5D0FE; border-radius: 10px; padding: 12px 15px; margin-bottom: 15px; display: flex; justify-content: space-between; align-items: center; }
        .revisions-info { }
        .revisions-title { font-size: 10pt; font-weight: 700; color: var(--primary-dark); }
        .revisions-detail { font-size: 9pt; color: var(--text-secondary); }
        .revisions-count { font-size: 24pt; font-weight: 700; color: var(--primary); }
        .revisions-label { font-size: 8pt; color: var(--text-muted); }
        .extra-revision { background: #fff; padding: 8px 12px; border-radius: 8px; text-align: center; }
        .extra-rate { font-size: 12pt; font-weight: 700; color: var(--text); }
        .extra-label { font-size: 8pt; color: var(--text-muted); }
        
        /* Usage Rights */
        .rights-section { background: var(--bg-light); border-radius: 10px; padding: 15px; margin-bottom: 15px; }
        .rights-title { font-size: 10pt; font-weight: 700; color: var(--text); margin-bottom: 10px; display: flex; align-items: center; gap: 8px; }
        .rights-content { font-size: 9pt; color: var(--text-secondary); line-height: 1.6; }
        .copyright-badge { display: inline-flex; align-items: center; gap: 5px; margin-top: 10px; padding: 6px 12px; border-radius: 5px; font-size: 9pt; font-weight: 600; }
        .copyright-badge.transfer { background: #D1FAE5; color: #059669; }
        .copyright-badge.license { background: #FEF3C7; color: #D97706; }
        
        /* Totals */
        .totals-wrapper { display: flex; justify-content: flex-end; margin-bottom: 15px; }
        .totals-box { width: 300px; background: var(--bg-light); border-radius: 10px; padding: 15px; }
        .total-row { display: flex; justify-content: space-between; padding: 6px 0; font-size: 10pt; }
        .total-row.grand { background: var(--gradient); color: #fff; margin: 10px -15px -15px -15px; padding: 12px 15px; border-radius: 0 0 10px 10px; font-size: 14pt; font-weight: 700; }
        .total-label { color: var(--text-secondary); }
        .total-row.grand .total-label { color: rgba(255,255,255,0.9); }
        
        /* Amount Words */
        .amount-words { background: var(--gradient); color: #fff; padding: 15px 20px; border-radius: 10px; margin-bottom: 15px; }
        .amount-words-label { font-size: 9pt; opacity: 0.8; }
        .amount-words-value { font-size: 12pt; font-weight: 600; }
        .amount-words-hindi { font-size: 10pt; opacity: 0.9; margin-top: 5px; }
        
        /* Payment Section */
        .payment-section { display: grid; grid-template-columns: 1fr 1fr; gap: 15px; margin-bottom: 15px; }
        .bank-box { background: var(--bg-light); padding: 15px; border-radius: 10px; }
        .bank-title { font-size: 10pt; font-weight: 700; color: var(--primary); margin-bottom: 10px; }
        .bank-info { font-size: 9pt; color: var(--text-secondary); line-height: 1.8; }
        .bank-info strong { color: var(--text); }
        .upi-box { background: var(--bg-light); padding: 15px; border-radius: 10px; display: flex; align-items: center; gap: 15px; }
        .upi-qr { width: 85px; height: 85px; background: #fff; border: 2px solid var(--primary-light); border-radius: 10px; display: flex; align-items: center; justify-content: center; }
        .upi-qr img { max-width: 75px; }
        .upi-title { font-size: 10pt; font-weight: 700; color: var(--primary); }
        .upi-id { font-size: 11pt; font-weight: 600; background: #fff; padding: 8px 12px; border-radius: 6px; margin-top: 8px; border: 1px solid var(--border); }
        
        /* Notes & Terms */
        .notes-box { background: var(--bg-light); padding: 15px; border-radius: 10px; margin-bottom: 15px; }
        .notes-title { font-size: 10pt; font-weight: 700; color: var(--text); margin-bottom: 5px; }
        .notes-content { font-size: 9pt; color: var(--text-secondary); }
        .terms-box { margin-bottom: 15px; }
        .terms-title { font-size: 10pt; font-weight: 700; color: var(--text); margin-bottom: 8px; }
        .terms-list { font-size: 8pt; color: var(--text-muted); padding-left: 15px; }
        .terms-list li { margin-bottom: 4px; }
        
        /* Signature */
        .signature-section { display: flex; justify-content: space-between; padding-top: 20px; border-top: 2px solid var(--border); }
        .sig-block { text-align: center; width: 180px; }
        .sig-line { border-bottom: 2px solid var(--text); height: 50px; margin-bottom: 5px; }
        .sig-name { font-size: 10pt; font-weight: 600; }
        .sig-role { font-size: 9pt; color: var(--text-muted); }
        
        /* Footer */
        .footer { margin-top: 15px; text-align: center; padding-top: 15px; border-top: 1px solid var(--border); }
        .footer-thanks { font-size: 14pt; font-weight: 700; background: var(--gradient); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
        .footer-thanks-hindi { font-size: 10pt; color: var(--text-muted); margin-top: 3px; }
        .footer-info { font-size: 8pt; color: var(--text-muted); margin-top: 10px; }
        
        /* Print */
        @media print {
            body { -webkit-background-clip: text !important; -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }
            .invoice { width: 100%; padding: 0; }
        }
    </style>
</head>
<body>
    <div class="invoice">
        <!-- HEADER -->
        <div class="header">
            <div class="freelancer-profile">
                <div class="profile-photo">
                    {{#if seller.logo}}
                    <img src="{{seller.logo}}" alt="{{seller.name}}">
                    {{else}}
                    {{firstLetter seller.name}}
                    {{/if}}
                </div>
                <div class="freelancer-info">
                    <div class="freelancer-name">{{seller.name}}</div>
                    {{#if freelancerTitle}}<div class="freelancer-title">{{freelancerTitle}}</div>{{/if}}
                    <div class="freelancer-contact">
                        {{#if seller.contact.email}}✉ {{seller.contact.email}}{{/if}}
                        {{#if seller.contact.phone}} | 📞 {{seller.contact.phone}}{{/if}}
                    </div>
                    {{#if portfolioUrl}}
                    <a href="{{portfolioUrl}}" class="portfolio-link">🔗 View Portfolio</a>
                    {{/if}}
                </div>
            </div>
            <div class="title-section">
                <div class="invoice-title">Invoice</div>
                <div class="invoice-title-hindi hindi">बीजक</div>
                <span class="status-badge status-{{paymentStatus}}">
                    {{paymentStatus}}
                </span>
            </div>
        </div>

        <!-- GRADIENT DIVIDER -->
        <div class="gradient-divider"></div>

        <!-- PROJECT BANNER -->
        {{#if projectName}}
        <div class="project-banner">
            <div class="project-label">Project</div>
            <div class="project-name">{{projectName}}</div>
            {{#if projectDescription}}<div class="project-desc">{{projectDescription}}</div>{{/if}}
            {{#if projectTimeline}}<span class="project-timeline">📅 {{projectTimeline}}</span>{{/if}}
        </div>
        {{/if}}

        <!-- META GRID -->
        <div class="meta-grid">
            <div class="meta-card">
                <div class="meta-label">Invoice No.</div>
                <div class="meta-value">{{invoiceNumber}}</div>
            </div>
            <div class="meta-card">
                <div class="meta-label">Invoice Date</div>
                <div class="meta-value">{{invoiceDate}}</div>
            </div>
            <div class="meta-card">
                <div class="meta-label">Due Date</div>
                <div class="meta-value">{{paymentTerms.dueDate}}</div>
            </div>
            <div class="meta-card">
                <div class="meta-label">Total Amount</div>
                <div class="meta-value highlight">{{currency}} {{totals.grandTotal}}</div>
            </div>
        </div>

        <!-- PARTIES -->
        <div class="parties">
            <div class="party-box from">
                <div class="party-label">From / Freelancer</div>
                <div class="party-name">{{seller.name}}</div>
                <div class="party-address">
                    {{seller.address.line1}}{{#if seller.address.line2}}, {{seller.address.line2}}{{/if}}<br>
                    {{seller.address.city}}, {{seller.address.state}} - {{seller.address.pincode}}
                </div>
                {{#if seller.taxRegistration.pan}}
                <div class="party-tax">
                    <strong>PAN:</strong> {{seller.taxRegistration.pan}}
                    {{#if seller.taxRegistration.gstin}} | <strong>GSTIN:</strong> {{seller.taxRegistration.gstin}}{{/if}}
                </div>
                {{/if}}
            </div>
            <div class="party-box to">
                <div class="party-label">Bill To / Client</div>
                <div class="party-name">{{buyer.name}}</div>
                <div class="party-address">
                    {{buyer.address.line1}}{{#if buyer.address.line2}}, {{buyer.address.line2}}{{/if}}<br>
                    {{buyer.address.city}}, {{buyer.address.state}} - {{buyer.address.pincode}}
                </div>
                {{#if buyer.taxRegistration.gstin}}
                <div class="party-tax">
                    <strong>GSTIN:</strong> {{buyer.taxRegistration.gstin}}
                </div>
                {{/if}}
            </div>
        </div>

        <!-- DELIVERABLES -->
        {{#if deliverables}}
        <div class="deliverables-section">
            <div class="section-title"><span>📦</span> Deliverables</div>
            <div class="deliverable-grid">
                {{#each deliverables}}
                <div class="deliverable-card {{status}}">
                    <div class="deliverable-name">{{name}}</div>
                    {{#if description}}<div class="deliverable-desc">{{description}}</div>{{/if}}
                    <div class="deliverable-footer">
                        <span class="deliverable-status {{status}}">
                            {{#ifEquals status 'completed'}}✓ Completed{{/ifEquals}}
                            {{#ifEquals status 'approved'}}✓ Approved{{/ifEquals}}
                            {{#ifEquals status 'in_progress'}}⏳ In Progress{{/ifEquals}}
                            {{#ifEquals status 'pending'}}○ Pending{{/ifEquals}}
                        </span>
                        <span class="deliverable-amount">₹{{amount}}</span>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{/if}}

        <!-- ITEMS TABLE -->
        <table class="items-table">
            <thead>
                <tr>
                    <th style="width: 5%;">S.No<span class="hindi">क्र.</span></th>
                    <th style="width: 45%;">Description<span class="hindi">विवरण</span></th>
                    <th class="center" style="width: 10%;">Qty</th>
                    <th class="right" style="width: 15%;">Rate<span class="hindi">दर</span></th>
                    <th class="right" style="width: 10%;">Tax</th>
                    <th class="right" style="width: 15%;">Amount<span class="hindi">राशि</span></th>
                </tr>
            </thead>
            <tbody>
                {{#each lineItems}}
                <tr>
                    <td class="center">{{slNo}}</td>
                    <td>
                        <div class="item-desc">{{description}}</div>
                        {{#if descriptionHindi}}<div class="item-sub hindi">{{descriptionHindi}}</div>{{/if}}
                        {{#if sacCode}}<div class="item-sub">SAC: {{sacCode}}</div>{{/if}}
                    </td>
                    <td class="center">{{quantity}} {{unit}}</td>
                    <td class="right">{{unitPrice}}</td>
                    <td class="right">{{totalTax}}</td>
                    <td class="right amount">{{netAmount}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <!-- REVISIONS BOX -->
        {{#if revisionsIncluded}}
        <div class="revisions-box">
            <div class="revisions-info">
                <div class="revisions-title">🔄 Revisions Included</div>
                <div class="revisions-detail">This project includes {{revisionsIncluded}} revision(s) at no extra cost.</div>
            </div>
            <div style="text-align: center;">
                <div class="revisions-count">{{revisionsIncluded}}</div>
                <div class="revisions-label">Revisions</div>
            </div>
            {{#if additionalRevisionRate}}
            <div class="extra-revision">
                <div class="extra-rate">₹{{additionalRevisionRate}}</div>
                <div class="extra-label">per extra revision</div>
            </div>
            {{/if}}
        </div>
        {{/if}}

        <!-- USAGE RIGHTS -->
        {{#if usageRights}}
        <div class="rights-section">
            <div class="rights-title">📜 Usage Rights & License</div>
            <div class="rights-content">{{usageRights}}</div>
            {{#if copyrightTransfer}}
            <span class="copyright-badge transfer">✓ Full Copyright Transfer Included</span>
            {{else}}
            <span class="copyright-badge license">ℹ Licensed Use Only</span>
            {{/if}}
        </div>
        {{/if}}

        <!-- TOTALS -->
        <div class="totals-wrapper">
            <div class="totals-box">
                <div class="total-row">
                    <span class="total-label">Subtotal</span>
                    <span>{{totals.grossAmount}}</span>
                </div>
                {{#if totals.totalDiscount}}
                <div class="total-row" style="color: var(--success);">
                    <span class="total-label">Discount</span>
                    <span>- {{totals.totalDiscount}}</span>
                </div>
                {{/if}}
                {{#if totals.totalTax}}
                <div class="total-row">
                    <span class="total-label">Tax (GST)</span>
                    <span>{{totals.totalTax}}</span>
                </div>
                {{/if}}
                {{#if totals.roundOff}}
                <div class="total-row">
                    <span class="total-label">Round Off</span>
                    <span>{{totals.roundOff}}</span>
                </div>
                {{/if}}
                <div class="total-row grand">
                    <span class="total-label">Total Due</span>
                    <span>{{currency}} {{totals.grandTotal}}</span>
                </div>
            </div>
        </div>

        <!-- AMOUNT IN WORDS -->
        <div class="amount-words">
            <div class="amount-words-label">Amount in Words:</div>
            <div class="amount-words-value">{{totals.amountInWords}}</div>
            {{#if totals.amountInWordsHindi}}
            <div class="amount-words-hindi hindi">{{totals.amountInWordsHindi}}</div>
            {{/if}}
        </div>

        <!-- PAYMENT SECTION -->
        <div class="payment-section">
            {{#if seller.bankDetails}}
            <div class="bank-box">
                <div class="bank-title">🏦 Bank Details</div>
                <div class="bank-info">
                    <strong>Account Name:</strong> {{seller.bankDetails.accountName}}<br>
                    <strong>Account No:</strong> {{seller.bankDetails.accountNumber}}<br>
                    <strong>Bank:</strong> {{seller.bankDetails.bankName}}<br>
                    {{#if seller.bankDetails.branch}}<strong>Branch:</strong> {{seller.bankDetails.branch}}<br>{{/if}}
                    <strong>IFSC:</strong> {{seller.bankDetails.ifscCode}}
                </div>
            </div>
            {{/if}}
            {{#if seller.upiDetails}}
            <div class="upi-box">
                <div class="upi-qr">
                    {{#if seller.upiDetails.qrCode}}
                    <img src="{{seller.upiDetails.qrCode}}" alt="UPI QR">
                    {{else}}
                    <span style="font-size: 8pt; color: var(--text-muted);">Scan to Pay</span>
                    {{/if}}
                </div>
                <div>
                    <div class="upi-title">📱 Pay via UPI</div>
                    <div class="upi-id">{{seller.upiDetails.upiId}}</div>
                </div>
            </div>
            {{/if}}
        </div>

        <!-- NOTES -->
        {{#if notes}}
        <div class="notes-box">
            <div class="notes-title">📝 Notes</div>
            <div class="notes-content">{{notes}}</div>
        </div>
        {{/if}}

        <!-- TERMS & CONDITIONS -->
        {{#if termsAndConditions}}
        <div class="terms-box">
            <div class="terms-title">Terms & Conditions</div>
            <ol class="terms-list">
                {{#each termsAndConditions}}
                <li>{{this}}</li>
                {{/each}}
            </ol>
        </div>
        {{/if}}

        <!-- SIGNATURE -->
        <div class="signature-section">
            <div class="sig-block">
                <div class="sig-line"></div>
                <div class="sig-name">Client Approval</div>
                <div class="sig-role">Date: ___________</div>
            </div>
            <div class="sig-block">
                <div class="sig-line">
                    {{#if signature}}<img src="{{signature}}" alt="Signature" style="max-height: 45px;">{{/if}}
                </div>
                <div class="sig-name">{{seller.name}}</div>
                <div class="sig-role">Freelancer</div>
            </div>
        </div>

        <!-- FOOTER -->
        <div class="footer">
            <div class="footer-thanks">Thank you for working with me!</div>
            <div class="footer-thanks-hindi hindi">मेरे साथ काम करने के लिए धन्यवाद!</div>
            <div class="footer-info">
                This invoice was generated digitally. | Payment due by {{paymentTerms.dueDate}}
                {{#if seller.contact.email}} | Questions? Contact: {{seller.contact.email}}{{/if}}
            </div>
        </div>
    </div>
</body>
</html>
