{{!-- 
  ═══════════════════════════════════════════════════════════════════════════════
  SORIVA - ASSIGNMENT TEMPLATE: HOMEWORK
  Style: Standard homework with questions and answers
  Best For: School homework, college assignments, practice problems
  Features: Q&A format, marks display, workings, sub-questions support
  ═══════════════════════════════════════════════════════════════════════════════
--}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - Homework Assignment</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       CSS RESET & BASE STYLES
    ═══════════════════════════════════════════════════════════════ */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --primary-color: {{#if accentColor}}{{accentColor}}{{else}}#2563eb{{/if}};
      --secondary-color: #1d4ed8;
      --success-color: #059669;
      --warning-color: #d97706;
      --danger-color: #dc2626;
      --text-color: #1f2937;
      --text-light: #6b7280;
      --text-muted: #9ca3af;
      --border-color: #e5e7eb;
      --bg-light: #f9fafb;
      --bg-blue: #eff6ff;
      --bg-green: #ecfdf5;
      --bg-amber: #fffbeb;
      --font-sans: 'Segoe UI', 'Arial', sans-serif;
      --font-mono: 'Consolas', 'Monaco', monospace;
    }

    /* ═══════════════════════════════════════════════════════════════
       PAGE SETUP
    ═══════════════════════════════════════════════════════════════ */
    @page {
      size: A4;
      margin: 0.6in;
    }

    @media print {
      body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
      .page-break { page-break-before: always; }
      .no-print { display: none; }
    }

    html {
      font-size: 11pt;
    }

    body {
      font-family: var(--font-sans);
      line-height: 1.6;
      color: var(--text-color);
      background: #fff;
      padding: 0;
      max-width: 8.5in;
      margin: 0 auto;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER SECTION
    ═══════════════════════════════════════════════════════════════ */
    .assignment-header {
      border: 2px solid var(--primary-color);
      border-radius: 12px;
      overflow: hidden;
      margin-bottom: 1.5rem;
    }

    .header-top {
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
      color: #fff;
      padding: 1rem 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .institution-info {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .institution-logo {
      width: 50px;
      height: 50px;
      background: #fff;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
    }

    .institution-name {
      font-size: 1.1rem;
      font-weight: 600;
    }

    .institution-dept {
      font-size: 0.85rem;
      opacity: 0.9;
    }

    .assignment-badge {
      background: rgba(255,255,255,0.2);
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .header-body {
      padding: 1.25rem 1.5rem;
      background: var(--bg-light);
    }

    .assignment-title {
      font-size: 1.4rem;
      font-weight: 700;
      color: var(--primary-color);
      margin-bottom: 0.25rem;
    }

    .assignment-subtitle {
      font-size: 1rem;
      color: var(--text-light);
      margin-bottom: 1rem;
    }

    .header-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
    }

    .header-item {
      display: flex;
      flex-direction: column;
    }

    .header-item .label {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-muted);
      margin-bottom: 0.15rem;
    }

    .header-item .value {
      font-size: 0.95rem;
      color: var(--text-color);
      font-weight: 500;
    }

    /* ═══════════════════════════════════════════════════════════════
       STUDENT INFO BAR
    ═══════════════════════════════════════════════════════════════ */
    .student-bar {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      padding: 1rem 1.5rem;
      background: #fff;
      border-top: 1px solid var(--border-color);
    }

    .student-field {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .student-field .field-label {
      font-size: 0.8rem;
      color: var(--text-light);
    }

    .student-field .field-value {
      font-weight: 600;
      color: var(--text-color);
    }

    .student-field .field-line {
      border-bottom: 1px solid var(--text-color);
      min-width: 120px;
      display: inline-block;
    }

    /* ═══════════════════════════════════════════════════════════════
       MARKS & DATE BOX
    ═══════════════════════════════════════════════════════════════ */
    .info-boxes {
      display: flex;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    .info-box {
      flex: 1;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      padding: 1rem;
      text-align: center;
    }

    .info-box.marks {
      background: var(--bg-blue);
      border-color: #bfdbfe;
    }

    .info-box.due-date {
      background: var(--bg-amber);
      border-color: #fde68a;
    }

    .info-box .box-label {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-light);
      margin-bottom: 0.25rem;
    }

    .info-box .box-value {
      font-size: 1.5rem;
      font-weight: 700;
    }

    .info-box.marks .box-value {
      color: var(--primary-color);
    }

    .info-box.due-date .box-value {
      color: var(--warning-color);
      font-size: 1rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       INSTRUCTIONS SECTION
    ═══════════════════════════════════════════════════════════════ */
    .instructions-section {
      background: var(--bg-light);
      border: 1px solid var(--border-color);
      border-radius: 10px;
      padding: 1rem 1.25rem;
      margin-bottom: 1.5rem;
    }

    .instructions-title {
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary-color);
      margin-bottom: 0.75rem;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 0.4rem;
    }

    .instructions-list {
      list-style: none;
      margin: 0;
    }

    .instructions-list li {
      padding: 0.3rem 0;
      padding-left: 1.5rem;
      position: relative;
      font-size: 0.9rem;
      color: var(--text-color);
    }

    .instructions-list li::before {
      content: "•";
      position: absolute;
      left: 0.5rem;
      color: var(--primary-color);
      font-weight: bold;
    }

    /* ═══════════════════════════════════════════════════════════════
       QUESTIONS SECTION
    ═══════════════════════════════════════════════════════════════ */
    .questions-section {
      margin-bottom: 2rem;
    }

    .section-title {
      font-size: 1rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--primary-color);
      margin-bottom: 1rem;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--primary-color);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       QUESTION CARD
    ═══════════════════════════════════════════════════════════════ */
    .question-card {
      border: 1px solid var(--border-color);
      border-radius: 10px;
      margin-bottom: 1.25rem;
      overflow: hidden;
    }

    .question-header {
      background: var(--bg-light);
      padding: 0.75rem 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid var(--border-color);
    }

    .question-number {
      background: var(--primary-color);
      color: #fff;
      width: 32px;
      height: 32px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 0.9rem;
    }

    .question-meta {
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .question-type {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-light);
      background: #fff;
      padding: 0.25rem 0.6rem;
      border-radius: 12px;
      border: 1px solid var(--border-color);
    }

    .question-marks {
      background: var(--bg-blue);
      color: var(--primary-color);
      font-size: 0.8rem;
      font-weight: 600;
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
    }

    .question-body {
      padding: 1rem;
    }

    .question-text {
      font-size: 1rem;
      line-height: 1.7;
      color: var(--text-color);
      margin-bottom: 0.75rem;
    }

    /* Sub-questions */
    .sub-questions {
      margin-top: 0.75rem;
      padding-left: 1.5rem;
    }

    .sub-question {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 0.5rem;
    }

    .sub-question .sub-label {
      font-weight: 600;
      color: var(--primary-color);
      min-width: 25px;
    }

    .sub-question .sub-text {
      flex: 1;
    }

    .sub-question .sub-marks {
      font-size: 0.75rem;
      color: var(--text-light);
    }

    /* ═══════════════════════════════════════════════════════════════
       ANSWER SECTION
    ═══════════════════════════════════════════════════════════════ */
    .answer-section {
      background: var(--bg-green);
      border-top: 2px solid var(--success-color);
      padding: 1rem;
      margin-top: 0.5rem;
    }

    .answer-label {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--success-color);
      font-weight: 600;
      margin-bottom: 0.5rem;
      display: flex;
      align-items: center;
      gap: 0.3rem;
    }

    .answer-text {
      font-size: 0.95rem;
      line-height: 1.7;
      color: var(--text-color);
    }

    /* Workings / Steps */
    .workings-section {
      background: #fff;
      border: 1px solid #d1fae5;
      border-radius: 8px;
      padding: 0.75rem;
      margin-top: 0.75rem;
    }

    .workings-title {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-light);
      margin-bottom: 0.5rem;
    }

    .workings-steps {
      list-style: none;
    }

    .workings-steps li {
      padding: 0.25rem 0;
      padding-left: 1.5rem;
      position: relative;
      font-family: var(--font-mono);
      font-size: 0.9rem;
    }

    .workings-steps li::before {
      content: "→";
      position: absolute;
      left: 0;
      color: var(--success-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       ANSWER SPACE (for blank worksheets)
    ═══════════════════════════════════════════════════════════════ */
    .answer-space {
      border: 1px dashed var(--border-color);
      border-radius: 8px;
      padding: 1rem;
      margin-top: 0.75rem;
      min-height: 80px;
      background: repeating-linear-gradient(
        transparent,
        transparent 27px,
        #e5e7eb 28px
      );
    }

    .answer-space.large {
      min-height: 150px;
    }

    .answer-space-label {
      font-size: 0.75rem;
      color: var(--text-muted);
      margin-bottom: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       CODE BLOCK
    ═══════════════════════════════════════════════════════════════ */
    .code-block {
      background: #1e293b;
      color: #e2e8f0;
      border-radius: 8px;
      padding: 1rem;
      margin: 0.75rem 0;
      font-family: var(--font-mono);
      font-size: 0.85rem;
      overflow-x: auto;
    }

    .code-block .code-header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 0.5rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid #334155;
    }

    .code-block .code-lang {
      color: #94a3b8;
      font-size: 0.75rem;
      text-transform: uppercase;
    }

    .code-block pre {
      margin: 0;
      white-space: pre-wrap;
    }

    .code-output {
      background: #f1f5f9;
      color: var(--text-color);
      border-radius: 6px;
      padding: 0.75rem;
      margin-top: 0.5rem;
      font-family: var(--font-mono);
      font-size: 0.85rem;
    }

    .code-output .output-label {
      font-size: 0.7rem;
      color: var(--text-light);
      margin-bottom: 0.3rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       DIAGRAM SPACE
    ═══════════════════════════════════════════════════════════════ */
    .diagram-space {
      border: 2px dashed var(--border-color);
      border-radius: 10px;
      padding: 2rem;
      margin: 0.75rem 0;
      text-align: center;
      min-height: 150px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      color: var(--text-muted);
    }

    .diagram-space img {
      max-width: 100%;
      max-height: 200px;
    }

    .diagram-space .diagram-label {
      font-size: 0.8rem;
      margin-top: 0.5rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       MCQ OPTIONS
    ═══════════════════════════════════════════════════════════════ */
    .mcq-options {
      margin-top: 0.75rem;
    }

    .mcq-option {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.5rem 0.75rem;
      margin-bottom: 0.4rem;
      border: 1px solid var(--border-color);
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.2s ease;
    }

    .mcq-option:hover {
      background: var(--bg-light);
    }

    .mcq-option.correct {
      background: var(--bg-green);
      border-color: var(--success-color);
    }

    .mcq-option .option-letter {
      background: var(--bg-light);
      width: 28px;
      height: 28px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 600;
      font-size: 0.85rem;
      color: var(--primary-color);
    }

    .mcq-option.correct .option-letter {
      background: var(--success-color);
      color: #fff;
    }

    /* ═══════════════════════════════════════════════════════════════
       TRUE/FALSE OPTIONS
    ═══════════════════════════════════════════════════════════════ */
    .tf-options {
      display: flex;
      gap: 1rem;
      margin-top: 0.75rem;
    }

    .tf-option {
      flex: 1;
      padding: 0.75rem;
      border: 2px solid var(--border-color);
      border-radius: 8px;
      text-align: center;
      font-weight: 600;
    }

    .tf-option.true {
      border-color: var(--success-color);
      color: var(--success-color);
    }

    .tf-option.false {
      border-color: var(--danger-color);
      color: var(--danger-color);
    }

    .tf-option.selected {
      background: var(--bg-green);
    }

    /* ═══════════════════════════════════════════════════════════════
       FILL IN BLANKS
    ═══════════════════════════════════════════════════════════════ */
    .blank-line {
      display: inline-block;
      border-bottom: 2px solid var(--primary-color);
      min-width: 100px;
      margin: 0 0.25rem;
      text-align: center;
      color: var(--success-color);
      font-weight: 500;
    }

    /* ═══════════════════════════════════════════════════════════════
       SUMMARY / SCORE BOX
    ═══════════════════════════════════════════════════════════════ */
    .score-summary {
      background: linear-gradient(135deg, var(--bg-blue) 0%, #dbeafe 100%);
      border: 2px solid var(--primary-color);
      border-radius: 12px;
      padding: 1.25rem;
      margin-top: 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .score-summary .summary-left {
      display: flex;
      flex-direction: column;
    }

    .score-summary .summary-label {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--text-light);
    }

    .score-summary .summary-value {
      font-size: 1.75rem;
      font-weight: 700;
      color: var(--primary-color);
    }

    .score-summary .score-breakdown {
      text-align: right;
    }

    .score-summary .score-item {
      font-size: 0.85rem;
      color: var(--text-light);
    }

    /* ═══════════════════════════════════════════════════════════════
       DECLARATION
    ═══════════════════════════════════════════════════════════════ */
    .declaration-section {
      margin-top: 2rem;
      padding: 1rem;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      background: var(--bg-light);
    }

    .declaration-text {
      font-size: 0.9rem;
      color: var(--text-color);
      margin-bottom: 1rem;
      font-style: italic;
    }

    .signature-area {
      display: flex;
      justify-content: space-between;
      margin-top: 1.5rem;
    }

    .signature-box {
      text-align: center;
    }

    .signature-line {
      border-top: 1px solid var(--text-color);
      width: 150px;
      margin-bottom: 0.25rem;
      padding-top: 0.5rem;
    }

    .signature-label {
      font-size: 0.8rem;
      color: var(--text-light);
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
    ═══════════════════════════════════════════════════════════════ */
    .assignment-footer {
      margin-top: 2rem;
      padding-top: 1rem;
      border-top: 1px solid var(--border-color);
      display: flex;
      justify-content: space-between;
      font-size: 0.8rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>

  <!-- ═══════════════════════════════════════════════════════════════
       HEADER
  ═══════════════════════════════════════════════════════════════ -->
  <header class="assignment-header">
    <div class="header-top">
      <div class="institution-info">
        {{#if institutionLogo}}
        <div class="institution-logo">
          <img src="{{institutionLogo}}" alt="Logo" style="width: 100%; height: 100%; object-fit: contain;">
        </div>
        {{else}}
        <div class="institution-logo">🏫</div>
        {{/if}}
        <div>
          <div class="institution-name">{{institution}}</div>
          {{#if department}}
          <div class="institution-dept">{{department}}</div>
          {{/if}}
        </div>
      </div>
      
      {{#if assignmentNumber}}
      <div class="assignment-badge">Assignment #{{assignmentNumber}}</div>
      {{/if}}
    </div>
    
    <div class="header-body">
      <h1 class="assignment-title">{{title}}</h1>
      {{#if subtitle}}
      <div class="assignment-subtitle">{{subtitle}}</div>
      {{/if}}
      
      <div class="header-grid">
        <div class="header-item">
          <span class="label">Subject</span>
          <span class="value">{{subject}}{{#if subjectCode}} ({{subjectCode}}){{/if}}</span>
        </div>
        {{#if chapter}}
        <div class="header-item">
          <span class="label">Chapter/Topic</span>
          <span class="value">{{chapter}}{{#if topic}} - {{topic}}{{/if}}</span>
        </div>
        {{/if}}
        {{#if instructor}}
        <div class="header-item">
          <span class="label">Instructor</span>
          <span class="value">{{instructor}}</span>
        </div>
        {{/if}}
      </div>
    </div>
    
    <div class="student-bar">
      <div class="student-field">
        <span class="field-label">Name:</span>
        {{#if studentName}}
        <span class="field-value">{{studentName}}</span>
        {{else}}
        <span class="field-line"></span>
        {{/if}}
      </div>
      
      <div class="student-field">
        <span class="field-label">Roll No:</span>
        {{#if rollNumber}}
        <span class="field-value">{{rollNumber}}</span>
        {{else}}
        <span class="field-line" style="min-width: 80px;"></span>
        {{/if}}
      </div>
      
      <div class="student-field">
        <span class="field-label">Class:</span>
        {{#if class}}
        <span class="field-value">{{class}}{{#if section}} - {{section}}{{/if}}</span>
        {{else}}
        <span class="field-line" style="min-width: 60px;"></span>
        {{/if}}
      </div>
      
      {{#if semester}}
      <div class="student-field">
        <span class="field-label">Semester:</span>
        <span class="field-value">{{semester}}</span>
      </div>
      {{/if}}
    </div>
  </header>

  <!-- ═══════════════════════════════════════════════════════════════
       INFO BOXES (Marks & Due Date)
  ═══════════════════════════════════════════════════════════════ -->
  <div class="info-boxes">
    {{#if totalMarks}}
    <div class="info-box marks">
      <div class="box-label">Total Marks</div>
      <div class="box-value">{{totalMarks}}</div>
    </div>
    {{/if}}
    
    {{#if dueDate}}
    <div class="info-box due-date">
      <div class="box-label">Due Date</div>
      <div class="box-value">{{formatDate dueDate}}</div>
    </div>
    {{/if}}
    
    {{#if obtainedMarks}}
    <div class="info-box marks" style="background: var(--bg-green); border-color: #a7f3d0;">
      <div class="box-label">Marks Obtained</div>
      <div class="box-value" style="color: var(--success-color);">{{obtainedMarks}} / {{totalMarks}}</div>
    </div>
    {{/if}}
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       INSTRUCTIONS
  ═══════════════════════════════════════════════════════════════ -->
  {{#if instructions}}
  <div class="instructions-section">
    <div class="instructions-title">📋 Instructions</div>
    <ul class="instructions-list">
      {{#each instructions}}
      <li>{{this}}</li>
      {{/each}}
    </ul>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       QUESTIONS
  ═══════════════════════════════════════════════════════════════ -->
  {{#if questions}}
  <section class="questions-section">
    <div class="section-title">📝 Questions</div>
    
    {{#each questions}}
    <div class="question-card">
      <div class="question-header">
        <div class="question-number">{{this.questionNumber}}</div>
        <div class="question-meta">
          {{#if this.questionType}}
          <span class="question-type">{{this.questionType}}</span>
          {{/if}}
          {{#if ../showMarks}}
          {{#if this.marks}}
          <span class="question-marks">{{this.marks}} marks</span>
          {{/if}}
          {{/if}}
        </div>
      </div>
      
      <div class="question-body">
        <div class="question-text">{{this.questionText}}</div>
        
        {{!-- Sub-questions --}}
        {{#if this.subQuestions}}
        <div class="sub-questions">
          {{#each this.subQuestions}}
          <div class="sub-question">
            <span class="sub-label">({{this.label}})</span>
            <span class="sub-text">{{this.text}}</span>
            {{#if ../../showMarks}}
            {{#if this.marks}}
            <span class="sub-marks">[{{this.marks}}]</span>
            {{/if}}
            {{/if}}
          </div>
          {{/each}}
        </div>
        {{/if}}
        
        {{!-- MCQ Options --}}
        {{#if this.options}}
        <div class="mcq-options">
          {{#each this.options}}
          <div class="mcq-option {{#if this.correct}}correct{{/if}}">
            <span class="option-letter">{{this.letter}}</span>
            <span class="option-text">{{this.text}}</span>
          </div>
          {{/each}}
        </div>
        {{/if}}
        
        {{!-- True/False --}}
        {{#ifEquals this.questionType "true-false"}}
        <div class="tf-options">
          <div class="tf-option true">TRUE</div>
          <div class="tf-option false">FALSE</div>
        </div>
        {{/ifEquals}}
        
        {{!-- Diagram --}}
        {{#if this.diagram}}
        <div class="diagram-space">
          <img src="{{this.diagram}}" alt="Diagram">
          <div class="diagram-label">Figure {{this.questionNumber}}</div>
        </div>
        {{/if}}
        
        {{!-- Code Question --}}
        {{#if this.code}}
        <div class="code-block">
          {{#if this.code.language}}
          <div class="code-header">
            <span class="code-lang">{{this.code.language}}</span>
          </div>
          {{/if}}
          <pre>{{this.code.snippet}}</pre>
        </div>
        {{#if this.code.output}}
        <div class="code-output">
          <div class="output-label">Output:</div>
          {{this.code.output}}
        </div>
        {{/if}}
        {{/if}}
        
        {{!-- Answer Section (if showAnswers is true) --}}
        {{#if ../../showAnswers}}
        {{#if this.answer}}
        <div class="answer-section">
          <div class="answer-label">✓ Answer</div>
          <div class="answer-text">{{this.answer}}</div>
          
          {{!-- Workings/Steps --}}
          {{#if this.workings}}
          {{#if ../../showWorkings}}
          <div class="workings-section">
            <div class="workings-title">Solution Steps:</div>
            <ul class="workings-steps">
              {{#each this.workings}}
              <li>{{this}}</li>
              {{/each}}
            </ul>
          </div>
          {{/if}}
          {{/if}}
        </div>
        {{/if}}
        {{else}}
        {{!-- Answer Space for blank worksheets --}}
        <div class="answer-space-label">Answer:</div>
        <div class="answer-space {{#ifEquals this.questionType 'long-answer'}}large{{/ifEquals}}"></div>
        {{/if}}
        
      </div>
    </div>
    {{/each}}
  </section>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       SCORE SUMMARY (if marks obtained provided)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if obtainedMarks}}
  <div class="score-summary">
    <div class="summary-left">
      <span class="summary-label">Total Score</span>
      <span class="summary-value">{{obtainedMarks}} / {{totalMarks}}</span>
    </div>
    {{#if grade}}
    <div class="score-breakdown">
      <div class="score-item">Grade: <strong>{{grade}}</strong></div>
    </div>
    {{/if}}
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       FEEDBACK (if provided)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if feedback}}
  <div class="instructions-section" style="margin-top: 1.5rem; background: var(--bg-amber); border-color: #fde68a;">
    <div class="instructions-title" style="color: var(--warning-color);">💬 Instructor Feedback</div>
    <p style="font-size: 0.95rem; line-height: 1.7;">{{feedback}}</p>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       DECLARATION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if declaration}}
  <div class="declaration-section">
    <div class="declaration-text">{{declaration}}</div>
    
    <div class="signature-area">
      <div class="signature-box">
        <div class="signature-line">{{#if signature}}{{signature}}{{/if}}</div>
        <div class="signature-label">Student's Signature</div>
      </div>
      <div class="signature-box">
        <div class="signature-line">{{#if signatureDate}}{{formatDate signatureDate}}{{/if}}</div>
        <div class="signature-label">Date</div>
      </div>
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       FOOTER
  ═══════════════════════════════════════════════════════════════ -->
  <footer class="assignment-footer">
    <div>{{studentName}} | {{class}}{{#if section}} - {{section}}{{/if}}</div>
    <div>{{institution}}</div>
  </footer>

</body>
</html>
