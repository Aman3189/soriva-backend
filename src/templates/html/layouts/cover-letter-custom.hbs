<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{fullName}} - Cover Letter</title>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - COVER LETTER TEMPLATE (COMPREHENSIVE)
       Style: Custom / Flexible
       Color: User-defined via CSS Variables
       Supports: Fresher → Executive → International
       
       CSS VARIABLES FOR CUSTOMIZATION:
       --primary-color: Main brand color
       --secondary-color: Accent color
       --text-color: Body text
       --heading-color: Headings
       --bg-color: Background
       --border-radius: Corner rounding (0 = sharp, 20px = rounded)
       ═══════════════════════════════════════════════════════════════ */
    
    :root {
      /* ═══ CUSTOMIZABLE COLORS ═══ */
      --primary-color: {{#if primaryColor}}{{primaryColor}}{{else}}#2563eb{{/if}};
      --secondary-color: {{#if secondaryColor}}{{secondaryColor}}{{else}}#3b82f6{{/if}};
      --accent-color: {{#if accentColor}}{{accentColor}}{{else}}#dbeafe{{/if}};
      --text-color: {{#if textColor}}{{textColor}}{{else}}#374151{{/if}};
      --heading-color: {{#if headingColor}}{{headingColor}}{{else}}#111827{{/if}};
      --muted-color: {{#if mutedColor}}{{mutedColor}}{{else}}#6b7280{{/if}};
      --border-color: {{#if borderColor}}{{borderColor}}{{else}}#e5e7eb{{/if}};
      --bg-light: {{#if bgLight}}{{bgLight}}{{else}}#f9fafb{{/if}};
      
      /* ═══ CUSTOMIZABLE STYLE ═══ */
      --border-radius: {{#if borderRadius}}{{borderRadius}}{{else}}8px{{/if}};
      --header-style: {{#if headerStyle}}{{headerStyle}}{{else}}gradient{{/if}};
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Nunito', sans-serif;
      background: #e5e7eb;
      color: var(--text-color);
      line-height: 1.7;
      padding: 30px;
    }

    .letter-container {
      width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: white;
      box-shadow: 0 15px 60px rgba(0,0,0,0.15);
      border-radius: var(--border-radius);
      position: relative;
      overflow: hidden;
    }

    /* Multi-page support */
    .page-break {
      page-break-before: always;
      padding-top: 40px;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER / LETTERHEAD - Custom Style
       ═══════════════════════════════════════════════════════════════ */
    
    .header {
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
      color: white;
      padding: 35px 50px;
      position: relative;
      overflow: hidden;
    }

    .header::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -20%;
      width: 60%;
      height: 200%;
      background: rgba(255,255,255,0.08);
      border-radius: 50%;
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: relative;
      z-index: 1;
    }

    .sender-info {
      flex: 1;
    }

    .sender-name {
      font-size: 30px;
      font-weight: 800;
      letter-spacing: -0.5px;
      margin-bottom: 6px;
    }

    .sender-title {
      font-size: 13px;
      font-weight: 400;
      color: rgba(255,255,255,0.85);
      margin-bottom: 18px;
    }

    .sender-contacts {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
      font-size: 12px;
    }

    .contact-item {
      display: flex;
      align-items: center;
      gap: 8px;
      color: rgba(255,255,255,0.9);
      background: rgba(255,255,255,0.15);
      padding: 5px 12px;
      border-radius: calc(var(--border-radius) * 2);
    }

    .contact-item i {
      font-size: 11px;
      color: rgba(255,255,255,0.7);
    }

    .contact-item a {
      color: rgba(255,255,255,0.9);
      text-decoration: none;
    }

    .contact-item a:hover {
      color: white;
    }

    .initials-badge {
      width: 75px;
      height: 75px;
      border-radius: 50%;
      background: rgba(255,255,255,0.2);
      border: 3px solid rgba(255,255,255,0.3);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 26px;
      font-weight: 800;
      flex-shrink: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       LETTER BODY
       ═══════════════════════════════════════════════════════════════ */
    
    .letter-body {
      padding: 45px 50px 85px;
    }

    /* Date and Recipient */
    .letter-meta {
      margin-bottom: 35px;
    }

    .letter-date {
      font-size: 13px;
      color: var(--muted-color);
      margin-bottom: 25px;
    }

    .recipient-block {
      margin-bottom: 5px;
    }

    .recipient-name {
      font-size: 15px;
      font-weight: 700;
      color: var(--primary-color);
      margin-bottom: 3px;
    }

    .recipient-title {
      font-size: 13px;
      color: var(--muted-color);
      margin-bottom: 3px;
    }

    .company-name {
      font-size: 14px;
      font-weight: 600;
      color: var(--heading-color);
      margin-bottom: 3px;
    }

    .company-address {
      font-size: 13px;
      color: var(--muted-color);
      line-height: 1.6;
    }

    /* Subject Line - Custom Style */
    .subject-line {
      margin: 30px 0;
      padding: 18px 22px;
      background: var(--accent-color);
      border-radius: var(--border-radius);
      border-left: 4px solid var(--primary-color);
    }

    .subject-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: var(--primary-color);
      font-weight: 700;
      margin-bottom: 5px;
    }

    .subject-text {
      font-size: 15px;
      font-weight: 700;
      color: var(--heading-color);
    }

    .job-reference {
      font-size: 12px;
      color: var(--muted-color);
      margin-top: 5px;
    }

    .job-meta {
      display: flex;
      gap: 15px;
      margin-top: 12px;
      font-size: 11px;
    }

    .job-meta-item {
      display: flex;
      align-items: center;
      gap: 6px;
      background: white;
      padding: 4px 10px;
      border-radius: calc(var(--border-radius) / 2);
      color: var(--muted-color);
    }

    .job-meta-item i {
      font-size: 10px;
      color: var(--primary-color);
    }

    /* Salutation */
    .salutation {
      font-size: 15px;
      font-weight: 600;
      color: var(--heading-color);
      margin-bottom: 22px;
    }

    /* Letter Content */
    .letter-content {
      font-size: 13px;
      color: var(--text-color);
    }

    .letter-content p {
      margin-bottom: 18px;
      line-height: 1.85;
    }

    .letter-content p:last-child {
      margin-bottom: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       HIGHLIGHT BOXES - Custom Style
       ═══════════════════════════════════════════════════════════════ */
    
    .highlight-box {
      margin: 28px 0;
      padding: 22px;
      background: var(--bg-light);
      border-radius: var(--border-radius);
      border: 1px solid var(--border-color);
    }

    .highlight-box-title {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: var(--primary-color);
      font-weight: 700;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .highlight-box-title i {
      font-size: 14px;
    }

    /* Skills Tags - Custom */
    .skills-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .skill-tag {
      padding: 7px 15px;
      background: var(--primary-color);
      color: white;
      border-radius: calc(var(--border-radius) * 2);
      font-size: 12px;
      font-weight: 600;
    }

    .skill-tag.secondary {
      background: white;
      color: var(--primary-color);
      border: 1px solid var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       ACHIEVEMENTS SECTION - Custom Style
       ═══════════════════════════════════════════════════════════════ */
    
    .achievements-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }

    .achievement-item {
      background: white;
      padding: 16px 18px;
      border-radius: var(--border-radius);
      border-left: 4px solid var(--primary-color);
      box-shadow: 0 2px 8px rgba(0,0,0,0.04);
    }

    .achievement-metric {
      font-size: 20px;
      font-weight: 800;
      color: var(--primary-color);
      margin-bottom: 3px;
    }

    .achievement-title {
      font-size: 12px;
      font-weight: 600;
      color: var(--heading-color);
    }

    .achievement-context {
      font-size: 11px;
      color: var(--muted-color);
      margin-top: 4px;
    }

    /* ═══════════════════════════════════════════════════════════════
       LEADERSHIP SECTION - Custom Style
       ═══════════════════════════════════════════════════════════════ */
    
    .leadership-stats {
      display: flex;
      justify-content: space-between;
      gap: 15px;
      flex-wrap: wrap;
    }

    .leadership-stat {
      flex: 1;
      min-width: 95px;
      background: white;
      padding: 18px 15px;
      border-radius: var(--border-radius);
      text-align: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.04);
    }

    .leadership-stat-value {
      font-size: 26px;
      font-weight: 800;
      color: var(--primary-color);
    }

    .leadership-stat-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--muted-color);
      margin-top: 4px;
      font-weight: 600;
    }

    /* ═══════════════════════════════════════════════════════════════
       FRESHER SECTION - Education & Projects
       ═══════════════════════════════════════════════════════════════ */
    
    .education-highlight {
      background: white;
      padding: 18px;
      border-radius: var(--border-radius);
      display: flex;
      align-items: center;
      gap: 18px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.04);
    }

    .education-icon {
      width: 55px;
      height: 55px;
      background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
      border-radius: var(--border-radius);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 22px;
      flex-shrink: 0;
    }

    .education-details {
      flex: 1;
    }

    .education-degree {
      font-size: 15px;
      font-weight: 700;
      color: var(--heading-color);
    }

    .education-college {
      font-size: 13px;
      color: var(--text-color);
      margin-top: 2px;
    }

    .education-meta {
      font-size: 12px;
      color: var(--muted-color);
      margin-top: 5px;
    }

    /* Projects List */
    .projects-list {
      margin-top: 18px;
    }

    .project-item {
      background: white;
      padding: 14px 16px;
      border-radius: var(--border-radius);
      margin-bottom: 10px;
      border-left: 3px solid var(--primary-color);
      box-shadow: 0 2px 6px rgba(0,0,0,0.03);
    }

    .project-item:last-child {
      margin-bottom: 0;
    }

    .project-name {
      font-size: 13px;
      font-weight: 700;
      color: var(--heading-color);
    }

    .project-tech {
      font-size: 11px;
      color: var(--muted-color);
      margin-top: 4px;
    }

    /* ═══════════════════════════════════════════════════════════════
       INTERNATIONAL SECTION - Visa & Relocation
       ═══════════════════════════════════════════════════════════════ */
    
    .international-box {
      background: #fffbeb;
      border-color: #fcd34d;
    }

    .international-box .highlight-box-title {
      color: #b45309;
    }

    .visa-info {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
    }

    .visa-item {
      flex: 1;
      min-width: 145px;
      background: white;
      padding: 14px 16px;
      border-radius: var(--border-radius);
      border-left: 3px solid #f59e0b;
    }

    .visa-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #b45309;
      margin-bottom: 4px;
      font-weight: 600;
    }

    .visa-value {
      font-size: 13px;
      font-weight: 600;
      color: var(--heading-color);
    }

    /* Language Proficiency */
    .languages-list {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 15px;
    }

    .language-item {
      background: white;
      padding: 6px 14px;
      border-radius: calc(var(--border-radius) * 2);
      font-size: 12px;
      border: 1px solid #fcd34d;
    }

    .language-name {
      font-weight: 600;
      color: #b45309;
    }

    .language-level {
      color: var(--muted-color);
      margin-left: 4px;
    }

    /* ═══════════════════════════════════════════════════════════════
       CERTIFICATIONS SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .certifications-list {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .cert-item {
      background: white;
      padding: 8px 14px;
      border-radius: calc(var(--border-radius) * 2);
      font-size: 12px;
      display: flex;
      align-items: center;
      gap: 8px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.03);
    }

    .cert-item i {
      color: #10b981;
      font-size: 13px;
    }

    .cert-name {
      font-weight: 600;
      color: var(--heading-color);
    }

    .cert-issuer {
      color: var(--muted-color);
      font-size: 11px;
    }

    /* ═══════════════════════════════════════════════════════════════
       AVAILABILITY SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .availability-box {
      background: #ecfdf5;
      border-color: #a7f3d0;
    }

    .availability-box .highlight-box-title {
      color: #047857;
    }

    .availability-info {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
    }

    .availability-item {
      display: flex;
      align-items: center;
      gap: 12px;
      background: white;
      padding: 12px 16px;
      border-radius: var(--border-radius);
    }

    .availability-item i {
      color: #10b981;
      font-size: 18px;
    }

    .availability-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #047857;
      font-weight: 600;
    }

    .availability-value {
      font-size: 13px;
      font-weight: 600;
      color: var(--heading-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       CLOSING SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .closing-section {
      margin-top: 40px;
    }

    .sign-off {
      font-size: 14px;
      color: var(--text-color);
      margin-bottom: 38px;
    }

    .signature-name {
      font-size: 20px;
      font-weight: 800;
      color: var(--primary-color);
      margin-bottom: 4px;
    }

    .signature-title {
      font-size: 12px;
      color: var(--muted-color);
    }

    .signature-contact {
      font-size: 11px;
      color: var(--muted-color);
      margin-top: 8px;
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
       ═══════════════════════════════════════════════════════════════ */
    
    .footer {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      padding: 16px 50px;
      background: var(--bg-light);
      border-top: 1px solid var(--border-color);
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 25px;
      font-size: 11px;
      color: var(--muted-color);
    }

    .footer-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .footer-item i {
      font-size: 11px;
      color: var(--primary-color);
    }

    .footer-item a {
      color: var(--muted-color);
      text-decoration: none;
    }

    .footer-item a:hover {
      color: var(--primary-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       PRINT STYLES
       ═══════════════════════════════════════════════════════════════ */
    
    @media print {
      body {
        background: white;
        padding: 0;
      }
      
      .letter-container {
        box-shadow: none;
        width: 100%;
        min-height: auto;
        border-radius: 0;
      }

      .page-break {
        page-break-before: always;
      }
    }

    @page {
      size: A4;
      margin: 0;
    }
  </style>
</head>
<body>
  <div class="letter-container">
    
    <!-- ═══════════════════════════════════════════════════════════════
         HEADER / LETTERHEAD - Custom Style
         ═══════════════════════════════════════════════════════════════ -->
    {{#if showLetterhead}}
    <header class="header">
      <div class="header-content">
        <div class="sender-info">
          <h1 class="sender-name">{{fullName}}</h1>
          {{#if currentRole}}
          <p class="sender-title">{{currentRole}}{{#if currentCompany}} • {{currentCompany}}{{/if}}</p>
          {{else}}
            {{#if degree}}
            <p class="sender-title">{{degree}}{{#if collegeName}} • {{collegeName}}{{/if}}</p>
            {{/if}}
          {{/if}}
          
          <div class="sender-contacts">
            {{#if email}}
            <span class="contact-item">
              <i class="fas fa-envelope"></i>
              <a href="mailto:{{email}}">{{email}}</a>
            </span>
            {{/if}}
            
            {{#if phone}}
            <span class="contact-item">
              <i class="fas fa-phone"></i>
              {{phone}}
            </span>
            {{/if}}
            
            {{#if city}}
            <span class="contact-item">
              <i class="fas fa-map-marker-alt"></i>
              {{city}}{{#if state}}, {{state}}{{/if}}{{#if country}} • {{country}}{{/if}}
            </span>
            {{/if}}
            
            {{#if linkedin}}
            <span class="contact-item">
              <i class="fab fa-linkedin"></i>
              <a href="https://{{linkedin}}" target="_blank">LinkedIn</a>
            </span>
            {{/if}}

            {{#if github}}
            <span class="contact-item">
              <i class="fab fa-github"></i>
              <a href="https://{{github}}" target="_blank">GitHub</a>
            </span>
            {{/if}}

            {{#if portfolio}}
            <span class="contact-item">
              <i class="fas fa-globe"></i>
              <a href="https://{{portfolio}}" target="_blank">Portfolio</a>
            </span>
            {{/if}}
          </div>
        </div>
        
        <div class="initials-badge">
          {{initials fullName}}
        </div>
      </div>
    </header>
    {{/if}}

    <!-- ═══════════════════════════════════════════════════════════════
         LETTER BODY
         ═══════════════════════════════════════════════════════════════ -->
    <main class="letter-body">
      
      <!-- Date and Recipient -->
      <div class="letter-meta">
        {{#if showDate}}
        <p class="letter-date">{{formatDate date}}</p>
        {{/if}}
        
        {{#if showRecipientAddress}}
        <div class="recipient-block">
          {{#if recipientName}}
          <p class="recipient-name">{{recipientName}}</p>
          {{/if}}
          {{#if recipientTitle}}
          <p class="recipient-title">{{recipientTitle}}</p>
          {{/if}}
          <p class="company-name">{{companyName}}</p>
          {{#if companyAddress}}
          <p class="company-address">
            {{companyAddress}}
            {{#if companyCity}}<br>{{companyCity}}{{#if companyState}}, {{companyState}}{{/if}}{{/if}}
            {{#if companyCountry}}<br>{{companyCountry}}{{/if}}
          </p>
          {{/if}}
        </div>
        {{/if}}
      </div>

      <!-- Subject Line -->
      <div class="subject-line">
        <p class="subject-label">Subject</p>
        <p class="subject-text">Application for {{jobTitle}} Position</p>
        {{#if jobReference}}
        <p class="job-reference">Reference: {{jobReference}}</p>
        {{/if}}
        
        {{#if jobType}}
        <div class="job-meta">
          {{#if jobType}}
          <span class="job-meta-item">
            <i class="fas fa-briefcase"></i> {{jobType}}
          </span>
          {{/if}}
          {{#if workMode}}
          <span class="job-meta-item">
            <i class="fas fa-building"></i> {{workMode}}
          </span>
          {{/if}}
          {{#if referredBy}}
          <span class="job-meta-item">
            <i class="fas fa-user-friends"></i> Referred by {{referredBy}}
          </span>
          {{/if}}
        </div>
        {{/if}}
      </div>

      <!-- Salutation -->
      <p class="salutation">{{salutation}},</p>

      <!-- Letter Content -->
      <div class="letter-content">
        <!-- Opening Paragraph -->
        <p>{{openingParagraph}}</p>
        
        <!-- Body Paragraphs -->
        {{#each bodyParagraphs}}
        <p>{{this}}</p>
        {{/each}}

        <!-- ═══════════════════════════════════════════════════════════════
             FRESHER SECTION - Education & Projects
             ═══════════════════════════════════════════════════════════════ -->
        {{#if isFresher}}
        {{#if collegeName}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-graduation-cap"></i> Education</p>
          <div class="education-highlight">
            <div class="education-icon">
              <i class="fas fa-university"></i>
            </div>
            <div class="education-details">
              <p class="education-degree">{{degree}}</p>
              <p class="education-college">{{collegeName}}</p>
              <p class="education-meta">
                {{#if graduationYear}}Class of {{graduationYear}}{{/if}}
                {{#if cgpa}} • CGPA: {{cgpa}}{{/if}}
              </p>
            </div>
          </div>

          {{#if academicProjects}}
          <div class="projects-list">
            <p style="font-size: 11px; text-transform: uppercase; letter-spacing: 1.5px; color: var(--primary-color); margin: 18px 0 12px; font-weight: 700;">Key Projects</p>
            {{#each academicProjects}}
            <div class="project-item">
              <p class="project-name">{{this.name}}{{#if this.role}} ({{this.role}}){{/if}}</p>
              {{#if this.technologies}}
              <p class="project-tech">{{join this.technologies " • "}}</p>
              {{/if}}
            </div>
            {{/each}}
          </div>
          {{/if}}
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             KEY SKILLS SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showSkillsSection}}
        {{#if keySkills}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-star"></i> Key Skills & Expertise</p>
          <div class="skills-tags">
            {{#each keySkills}}
            <span class="skill-tag">{{this}}</span>
            {{/each}}
          </div>
          
          {{#if technicalSkills}}
          <div style="margin-top: 15px;">
            <p style="font-size: 10px; text-transform: uppercase; letter-spacing: 1px; color: var(--muted-color); margin-bottom: 10px; font-weight: 600;">Technical Skills</p>
            <div class="skills-tags">
              {{#each technicalSkills}}
              <span class="skill-tag secondary">{{this}}</span>
              {{/each}}
            </div>
          </div>
          {{/if}}
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             ACHIEVEMENTS SECTION (Mid/Senior/Executive)
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showAchievements}}
        {{#if achievements}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-trophy"></i> Key Achievements</p>
          <div class="achievements-grid">
            {{#each achievements}}
            <div class="achievement-item">
              <p class="achievement-metric">{{this.metric}}</p>
              <p class="achievement-title">{{this.title}}</p>
              {{#if this.context}}
              <p class="achievement-context">{{this.context}}</p>
              {{/if}}
            </div>
            {{/each}}
          </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             LEADERSHIP SECTION (Senior/Executive)
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showMetrics}}
        {{#if teamSize}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-users-cog"></i> Leadership & Impact</p>
          <div class="leadership-stats">
            {{#if teamSize}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{teamSize}}+</p>
              <p class="leadership-stat-label">Team Size</p>
            </div>
            {{/if}}
            {{#if directReports}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{directReports}}</p>
              <p class="leadership-stat-label">Direct Reports</p>
            </div>
            {{/if}}
            {{#if budgetManaged}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{budgetManaged}}</p>
              <p class="leadership-stat-label">Budget</p>
            </div>
            {{/if}}
            {{#if revenueGenerated}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{revenueGenerated}}</p>
              <p class="leadership-stat-label">Revenue</p>
            </div>
            {{/if}}
            {{#if yearsOfExperience}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{yearsOfExperience}}+</p>
              <p class="leadership-stat-label">Years Exp</p>
            </div>
            {{/if}}
          </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             CERTIFICATIONS SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if certifications}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-certificate"></i> Certifications</p>
          <div class="certifications-list">
            {{#each certifications}}
            <div class="cert-item">
              <i class="fas fa-check-circle"></i>
              <span>
                <span class="cert-name">{{this.name}}</span>
                {{#if this.issuer}}<span class="cert-issuer"> • {{this.issuer}}</span>{{/if}}
              </span>
            </div>
            {{/each}}
          </div>
        </div>
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             INTERNATIONAL/VISA SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if requiresSponsorship}}
        <div class="highlight-box international-box">
          <p class="highlight-box-title"><i class="fas fa-globe-americas"></i> Work Authorization & Relocation</p>
          <div class="visa-info">
            {{#if workAuthorizationStatus}}
            <div class="visa-item">
              <p class="visa-label">Authorization</p>
              <p class="visa-value">{{workAuthorizationStatus}}</p>
            </div>
            {{/if}}
            {{#if currentVisaStatus}}
            <div class="visa-item">
              <p class="visa-label">Visa Status</p>
              <p class="visa-value">{{currentVisaStatus}}</p>
            </div>
            {{/if}}
            {{#if relocationTimeline}}
            <div class="visa-item">
              <p class="visa-label">Timeline</p>
              <p class="visa-value">{{relocationTimeline}}</p>
            </div>
            {{/if}}
            {{#if targetLocation}}
            <div class="visa-item">
              <p class="visa-label">Target Location</p>
              <p class="visa-value">{{targetLocation}}</p>
            </div>
            {{/if}}
          </div>

          {{#if languages}}
          <div class="languages-list">
            {{#each languages}}
            <span class="language-item">
              <span class="language-name">{{this.language}}</span>
              <span class="language-level">({{this.level}})</span>
            </span>
            {{/each}}
          </div>
          {{/if}}

          {{#if ieltsScore}}
          <p style="font-size: 12px; margin-top: 12px; color: var(--muted-color);">
            <strong>IELTS:</strong> {{ieltsScore}}
          </p>
          {{/if}}
        </div>
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             AVAILABILITY SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if availableFrom}}
        <div class="highlight-box availability-box">
          <p class="highlight-box-title"><i class="fas fa-calendar-check"></i> Availability</p>
          <div class="availability-info">
            {{#if availableFrom}}
            <div class="availability-item">
              <i class="fas fa-play-circle"></i>
              <div>
                <p class="availability-label">Available From</p>
                <p class="availability-value">{{availableFrom}}</p>
              </div>
            </div>
            {{/if}}
            {{#if noticePeriod}}
            <div class="availability-item">
              <i class="fas fa-clock"></i>
              <div>
                <p class="availability-label">Notice Period</p>
                <p class="availability-value">{{noticePeriod}}</p>
              </div>
            </div>
            {{/if}}
            {{#if interviewAvailability}}
            <div class="availability-item">
              <i class="fas fa-video"></i>
              <div>
                <p class="availability-label">Interview</p>
                <p class="availability-value">{{interviewAvailability}}</p>
              </div>
            </div>
            {{/if}}
          </div>
        </div>
        {{/if}}
        
        <!-- Closing Paragraph -->
        <p>{{closingParagraph}}</p>
      </div>

      <!-- ═══════════════════════════════════════════════════════════════
           CLOSING & SIGNATURE
           ═══════════════════════════════════════════════════════════════ -->
      {{#if showSignature}}
      <div class="closing-section">
        <p class="sign-off">{{signOff}},</p>
        <p class="signature-name">{{fullName}}</p>
        {{#if currentRole}}
        <p class="signature-title">{{currentRole}}{{#if currentCompany}} • {{currentCompany}}{{/if}}</p>
        {{else}}
          {{#if degree}}
          <p class="signature-title">{{degree}}</p>
          {{/if}}
        {{/if}}
        {{#if phone}}
        <p class="signature-contact">{{phone}} {{#if email}}• {{email}}{{/if}}</p>
        {{/if}}
      </div>
      {{/if}}

    </main>

    <!-- ═══════════════════════════════════════════════════════════════
         FOOTER
         ═══════════════════════════════════════════════════════════════ -->
    <footer class="footer">
      {{#if email}}
      <span class="footer-item">
        <i class="fas fa-envelope"></i>
        <a href="mailto:{{email}}">{{email}}</a>
      </span>
      {{/if}}
      
      {{#if phone}}
      <span class="footer-item">
        <i class="fas fa-phone"></i>
        {{phone}}
      </span>
      {{/if}}
      
      {{#if linkedin}}
      <span class="footer-item">
        <i class="fab fa-linkedin"></i>
        <a href="https://{{linkedin}}" target="_blank">{{linkedin}}</a>
      </span>
      {{/if}}

      {{#if portfolio}}
      <span class="footer-item">
        <i class="fas fa-globe"></i>
        <a href="https://{{portfolio}}" target="_blank">{{portfolio}}</a>
      </span>
      {{/if}}
    </footer>

  </div>
</body>
</html>
