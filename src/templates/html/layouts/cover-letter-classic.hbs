<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{fullName}} - Cover Letter</title>
  <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700&family=Open+Sans:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - COVER LETTER TEMPLATE (COMPREHENSIVE)
       Style: Classic / Traditional
       Color: Dark Green (#1e3a2f)
       Supports: Fresher → Executive → International
       ═══════════════════════════════════════════════════════════════ */
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Open Sans', sans-serif;
      background: #e8e8e8;
      color: #333;
      line-height: 1.7;
      padding: 30px;
    }

    .letter-container {
      width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: white;
      box-shadow: 0 15px 80px rgba(0,0,0,0.2);
      border-radius: 0;
      position: relative;
      overflow: hidden;
      border-top: 6px solid #1e3a2f;
    }

    /* Multi-page support */
    .page-break {
      page-break-before: always;
      padding-top: 40px;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER / LETTERHEAD - Classic Style
       ═══════════════════════════════════════════════════════════════ */
    
    .header {
      padding: 40px 50px 30px;
      border-bottom: 2px solid #1e3a2f;
      text-align: center;
    }

    .sender-name {
      font-family: 'Merriweather', serif;
      font-size: 32px;
      font-weight: 700;
      color: #1e3a2f;
      letter-spacing: 2px;
      margin-bottom: 8px;
    }

    .sender-title {
      font-size: 13px;
      font-weight: 400;
      color: #666;
      text-transform: uppercase;
      letter-spacing: 3px;
      margin-bottom: 20px;
    }

    .sender-contacts {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 25px;
      font-size: 12px;
      color: #555;
    }

    .contact-item {
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .contact-item i {
      font-size: 12px;
      color: #1e3a2f;
    }

    .contact-item a {
      color: #555;
      text-decoration: none;
    }

    .contact-item a:hover {
      color: #1e3a2f;
    }

    /* Decorative line */
    .header-decoration {
      width: 60px;
      height: 3px;
      background: #1e3a2f;
      margin: 20px auto 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       LETTER BODY
       ═══════════════════════════════════════════════════════════════ */
    
    .letter-body {
      padding: 40px 60px 80px;
    }

    /* Date and Recipient */
    .letter-meta {
      margin-bottom: 35px;
    }

    .letter-date {
      font-size: 13px;
      color: #666;
      margin-bottom: 30px;
      font-style: italic;
    }

    .recipient-block {
      margin-bottom: 5px;
    }

    .recipient-name {
      font-family: 'Merriweather', serif;
      font-size: 15px;
      font-weight: 700;
      color: #1e3a2f;
      margin-bottom: 3px;
    }

    .recipient-title {
      font-size: 13px;
      color: #555;
      margin-bottom: 3px;
    }

    .company-name {
      font-size: 14px;
      font-weight: 600;
      color: #333;
      margin-bottom: 3px;
    }

    .company-address {
      font-size: 13px;
      color: #666;
      line-height: 1.6;
    }

    /* Subject Line - Classic Style */
    .subject-line {
      margin: 30px 0;
      padding: 18px 25px;
      background: #f8f9f7;
      border: 1px solid #d4d9d2;
      border-left: 4px solid #1e3a2f;
    }

    .subject-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #888;
      margin-bottom: 5px;
    }

    .subject-text {
      font-family: 'Merriweather', serif;
      font-size: 14px;
      font-weight: 700;
      color: #1e3a2f;
    }

    .job-reference {
      font-size: 12px;
      color: #666;
      margin-top: 5px;
      font-style: italic;
    }

    .job-meta {
      display: flex;
      gap: 20px;
      margin-top: 10px;
      font-size: 11px;
      color: #777;
    }

    .job-meta-item {
      display: flex;
      align-items: center;
      gap: 5px;
    }

    .job-meta-item i {
      font-size: 10px;
      color: #1e3a2f;
    }

    /* Salutation */
    .salutation {
      font-family: 'Merriweather', serif;
      font-size: 15px;
      font-weight: 400;
      color: #333;
      margin-bottom: 25px;
    }

    /* Letter Content */
    .letter-content {
      font-size: 13px;
      color: #444;
      text-align: justify;
    }

    .letter-content p {
      margin-bottom: 20px;
      line-height: 1.9;
      text-indent: 30px;
    }

    .letter-content p:first-child {
      text-indent: 0;
    }

    .letter-content p:last-child {
      margin-bottom: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       HIGHLIGHT BOXES - Classic Style
       ═══════════════════════════════════════════════════════════════ */
    
    .highlight-box {
      margin: 30px 0;
      padding: 25px;
      background: #f8f9f7;
      border: 1px solid #d4d9d2;
      border-radius: 0;
    }

    .highlight-box-title {
      font-family: 'Merriweather', serif;
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #1e3a2f;
      font-weight: 700;
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 1px solid #d4d9d2;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .highlight-box-title i {
      font-size: 14px;
    }

    /* Skills Tags - Classic */
    .skills-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .skill-tag {
      padding: 8px 16px;
      background: white;
      border: 1px solid #1e3a2f;
      font-size: 12px;
      color: #1e3a2f;
      font-weight: 500;
    }

    /* ═══════════════════════════════════════════════════════════════
       ACHIEVEMENTS SECTION - Classic Style
       ═══════════════════════════════════════════════════════════════ */
    
    .achievements-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }

    .achievement-item {
      background: white;
      padding: 15px 18px;
      border: 1px solid #d4d9d2;
      border-left: 3px solid #1e3a2f;
    }

    .achievement-metric {
      font-family: 'Merriweather', serif;
      font-size: 18px;
      font-weight: 700;
      color: #1e3a2f;
      margin-bottom: 3px;
    }

    .achievement-title {
      font-size: 12px;
      font-weight: 600;
      color: #333;
    }

    .achievement-context {
      font-size: 11px;
      color: #666;
      margin-top: 3px;
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       LEADERSHIP SECTION - Classic Style
       ═══════════════════════════════════════════════════════════════ */
    
    .leadership-stats {
      display: flex;
      justify-content: space-between;
      gap: 15px;
      flex-wrap: wrap;
    }

    .leadership-stat {
      flex: 1;
      min-width: 100px;
      background: white;
      padding: 18px;
      border: 1px solid #d4d9d2;
      text-align: center;
    }

    .leadership-stat-value {
      font-family: 'Merriweather', serif;
      font-size: 24px;
      font-weight: 700;
      color: #1e3a2f;
    }

    .leadership-stat-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #666;
      margin-top: 5px;
    }

    /* ═══════════════════════════════════════════════════════════════
       FRESHER SECTION - Education & Projects
       ═══════════════════════════════════════════════════════════════ */
    
    .education-highlight {
      background: white;
      padding: 20px;
      border: 1px solid #d4d9d2;
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .education-icon {
      width: 55px;
      height: 55px;
      background: #1e3a2f;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 22px;
    }

    .education-details {
      flex: 1;
    }

    .education-degree {
      font-family: 'Merriweather', serif;
      font-size: 15px;
      font-weight: 700;
      color: #1e3a2f;
    }

    .education-college {
      font-size: 13px;
      color: #333;
      margin-top: 2px;
    }

    .education-meta {
      font-size: 12px;
      color: #666;
      margin-top: 5px;
      font-style: italic;
    }

    /* Projects List */
    .projects-list {
      margin-top: 15px;
    }

    .project-item {
      background: white;
      padding: 12px 15px;
      border: 1px solid #d4d9d2;
      border-left: 3px solid #1e3a2f;
      margin-bottom: 10px;
    }

    .project-item:last-child {
      margin-bottom: 0;
    }

    .project-name {
      font-size: 13px;
      font-weight: 600;
      color: #1e3a2f;
    }

    .project-tech {
      font-size: 11px;
      color: #666;
      margin-top: 3px;
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       INTERNATIONAL SECTION - Visa & Relocation
       ═══════════════════════════════════════════════════════════════ */
    
    .international-box {
      background: #fffbeb;
      border-color: #fcd34d;
    }

    .international-box .highlight-box-title {
      color: #92400e;
      border-bottom-color: #fcd34d;
    }

    .visa-info {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
    }

    .visa-item {
      flex: 1;
      min-width: 150px;
      background: white;
      padding: 15px;
      border: 1px solid #fcd34d;
      border-left: 3px solid #d97706;
    }

    .visa-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #92400e;
      margin-bottom: 5px;
    }

    .visa-value {
      font-size: 13px;
      font-weight: 500;
      color: #333;
    }

    /* Language Proficiency */
    .languages-list {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 15px;
    }

    .language-item {
      background: white;
      padding: 8px 15px;
      border: 1px solid #fcd34d;
      font-size: 12px;
    }

    .language-name {
      font-weight: 600;
      color: #92400e;
    }

    .language-level {
      color: #666;
      margin-left: 5px;
    }

    /* ═══════════════════════════════════════════════════════════════
       CERTIFICATIONS SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .certifications-list {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .cert-item {
      background: white;
      padding: 10px 15px;
      border: 1px solid #d4d9d2;
      font-size: 12px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .cert-item i {
      color: #1e3a2f;
      font-size: 14px;
    }

    .cert-name {
      font-weight: 600;
      color: #333;
    }

    .cert-issuer {
      color: #666;
      font-size: 11px;
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       AVAILABILITY SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .availability-box {
      background: #f0fdf4;
      border-color: #86efac;
    }

    .availability-box .highlight-box-title {
      color: #166534;
      border-bottom-color: #86efac;
    }

    .availability-info {
      display: flex;
      flex-wrap: wrap;
      gap: 25px;
    }

    .availability-item {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .availability-item i {
      color: #16a34a;
      font-size: 18px;
    }

    .availability-label {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #166534;
    }

    .availability-value {
      font-size: 13px;
      font-weight: 500;
      color: #333;
    }

    /* ═══════════════════════════════════════════════════════════════
       CLOSING SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .closing-section {
      margin-top: 40px;
    }

    .sign-off {
      font-family: 'Merriweather', serif;
      font-size: 14px;
      color: #333;
      margin-bottom: 45px;
    }

    .signature-line {
      width: 200px;
      border-bottom: 1px solid #333;
      margin-bottom: 10px;
    }

    .signature-name {
      font-family: 'Merriweather', serif;
      font-size: 18px;
      font-weight: 700;
      color: #1e3a2f;
      margin-bottom: 3px;
    }

    .signature-title {
      font-size: 12px;
      color: #666;
    }

    .signature-contact {
      font-size: 11px;
      color: #888;
      margin-top: 8px;
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
       ═══════════════════════════════════════════════════════════════ */
    
    .footer {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      padding: 15px 60px;
      background: #f8f9f7;
      border-top: 1px solid #d4d9d2;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 30px;
      font-size: 11px;
      color: #666;
    }

    .footer-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .footer-item i {
      color: #1e3a2f;
      font-size: 10px;
    }

    .footer-item a {
      color: #666;
      text-decoration: none;
    }

    .footer-item a:hover {
      color: #1e3a2f;
    }

    /* ═══════════════════════════════════════════════════════════════
       PRINT STYLES
       ═══════════════════════════════════════════════════════════════ */
    
    @media print {
      body {
        background: white;
        padding: 0;
      }
      
      .letter-container {
        box-shadow: none;
        width: 100%;
        min-height: auto;
      }

      .page-break {
        page-break-before: always;
      }
    }

    @page {
      size: A4;
      margin: 0;
    }
  </style>
</head>
<body>
  <div class="letter-container">
    
    <!-- ═══════════════════════════════════════════════════════════════
         HEADER / LETTERHEAD - Classic Style
         ═══════════════════════════════════════════════════════════════ -->
    {{#if showLetterhead}}
    <header class="header">
      <h1 class="sender-name">{{fullName}}</h1>
      {{#if currentRole}}
      <p class="sender-title">{{currentRole}}{{#if currentCompany}} • {{currentCompany}}{{/if}}</p>
      {{else}}
        {{#if degree}}
        <p class="sender-title">{{degree}}{{#if collegeName}} • {{collegeName}}{{/if}}</p>
        {{/if}}
      {{/if}}
      
      <div class="sender-contacts">
        {{#if email}}
        <span class="contact-item">
          <i class="fas fa-envelope"></i>
          <a href="mailto:{{email}}">{{email}}</a>
        </span>
        {{/if}}
        
        {{#if phone}}
        <span class="contact-item">
          <i class="fas fa-phone"></i>
          {{phone}}
        </span>
        {{/if}}
        
        {{#if city}}
        <span class="contact-item">
          <i class="fas fa-map-marker-alt"></i>
          {{city}}{{#if state}}, {{state}}{{/if}}{{#if country}} • {{country}}{{/if}}
        </span>
        {{/if}}
        
        {{#if linkedin}}
        <span class="contact-item">
          <i class="fab fa-linkedin"></i>
          <a href="https://{{linkedin}}" target="_blank">LinkedIn</a>
        </span>
        {{/if}}

        {{#if github}}
        <span class="contact-item">
          <i class="fab fa-github"></i>
          <a href="https://{{github}}" target="_blank">GitHub</a>
        </span>
        {{/if}}
      </div>

      <div class="header-decoration"></div>
    </header>
    {{/if}}

    <!-- ═══════════════════════════════════════════════════════════════
         LETTER BODY
         ═══════════════════════════════════════════════════════════════ -->
    <main class="letter-body">
      
      <!-- Date and Recipient -->
      <div class="letter-meta">
        {{#if showDate}}
        <p class="letter-date">{{formatDate date}}</p>
        {{/if}}
        
        {{#if showRecipientAddress}}
        <div class="recipient-block">
          {{#if recipientName}}
          <p class="recipient-name">{{recipientName}}</p>
          {{/if}}
          {{#if recipientTitle}}
          <p class="recipient-title">{{recipientTitle}}</p>
          {{/if}}
          <p class="company-name">{{companyName}}</p>
          {{#if companyAddress}}
          <p class="company-address">
            {{companyAddress}}
            {{#if companyCity}}<br>{{companyCity}}{{#if companyState}}, {{companyState}}{{/if}}{{/if}}
            {{#if companyCountry}}<br>{{companyCountry}}{{/if}}
          </p>
          {{/if}}
        </div>
        {{/if}}
      </div>

      <!-- Subject Line -->
      <div class="subject-line">
        <p class="subject-label">Subject</p>
        <p class="subject-text">Application for {{jobTitle}} Position</p>
        {{#if jobReference}}
        <p class="job-reference">Reference: {{jobReference}}</p>
        {{/if}}
        
        {{#if jobType}}
        <div class="job-meta">
          {{#if jobType}}
          <span class="job-meta-item">
            <i class="fas fa-briefcase"></i> {{jobType}}
          </span>
          {{/if}}
          {{#if workMode}}
          <span class="job-meta-item">
            <i class="fas fa-building"></i> {{workMode}}
          </span>
          {{/if}}
          {{#if referredBy}}
          <span class="job-meta-item">
            <i class="fas fa-user-friends"></i> Referred by {{referredBy}}
          </span>
          {{/if}}
        </div>
        {{/if}}
      </div>

      <!-- Salutation -->
      <p class="salutation">{{salutation}},</p>

      <!-- Letter Content -->
      <div class="letter-content">
        <!-- Opening Paragraph -->
        <p>{{openingParagraph}}</p>
        
        <!-- Body Paragraphs -->
        {{#each bodyParagraphs}}
        <p>{{this}}</p>
        {{/each}}

        <!-- ═══════════════════════════════════════════════════════════════
             FRESHER SECTION - Education & Projects
             ═══════════════════════════════════════════════════════════════ -->
        {{#if isFresher}}
        {{#if collegeName}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-graduation-cap"></i> Education</p>
          <div class="education-highlight">
            <div class="education-icon">
              <i class="fas fa-university"></i>
            </div>
            <div class="education-details">
              <p class="education-degree">{{degree}}</p>
              <p class="education-college">{{collegeName}}</p>
              <p class="education-meta">
                {{#if graduationYear}}Class of {{graduationYear}}{{/if}}
                {{#if cgpa}} • CGPA: {{cgpa}}{{/if}}
              </p>
            </div>
          </div>

          {{#if academicProjects}}
          <div class="projects-list">
            <p style="font-family: 'Merriweather', serif; font-size: 11px; text-transform: uppercase; letter-spacing: 2px; color: #1e3a2f; margin: 18px 0 12px; font-weight: 700;">Key Projects</p>
            {{#each academicProjects}}
            <div class="project-item">
              <p class="project-name">{{this.name}}{{#if this.role}} ({{this.role}}){{/if}}</p>
              {{#if this.technologies}}
              <p class="project-tech">{{join this.technologies ", "}}</p>
              {{/if}}
            </div>
            {{/each}}
          </div>
          {{/if}}
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             KEY SKILLS SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showSkillsSection}}
        {{#if keySkills}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-star"></i> Key Skills & Expertise</p>
          <div class="skills-tags">
            {{#each keySkills}}
            <span class="skill-tag">{{this}}</span>
            {{/each}}
          </div>
          
          {{#if technicalSkills}}
          <div style="margin-top: 18px;">
            <p style="font-size: 11px; color: #666; margin-bottom: 10px; text-transform: uppercase; letter-spacing: 1px;">Technical Skills:</p>
            <div class="skills-tags">
              {{#each technicalSkills}}
              <span class="skill-tag" style="background: #f8f9f7; border-style: dashed;">{{this}}</span>
              {{/each}}
            </div>
          </div>
          {{/if}}
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             ACHIEVEMENTS SECTION (Mid/Senior/Executive)
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showAchievements}}
        {{#if achievements}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-trophy"></i> Key Achievements</p>
          <div class="achievements-grid">
            {{#each achievements}}
            <div class="achievement-item">
              <p class="achievement-metric">{{this.metric}}</p>
              <p class="achievement-title">{{this.title}}</p>
              {{#if this.context}}
              <p class="achievement-context">{{this.context}}</p>
              {{/if}}
            </div>
            {{/each}}
          </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             LEADERSHIP SECTION (Senior/Executive)
             ═══════════════════════════════════════════════════════════════ -->
        {{#if showMetrics}}
        {{#if teamSize}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-users-cog"></i> Leadership & Impact</p>
          <div class="leadership-stats">
            {{#if teamSize}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{teamSize}}+</p>
              <p class="leadership-stat-label">Team Members</p>
            </div>
            {{/if}}
            {{#if directReports}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{directReports}}</p>
              <p class="leadership-stat-label">Direct Reports</p>
            </div>
            {{/if}}
            {{#if budgetManaged}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{budgetManaged}}</p>
              <p class="leadership-stat-label">Budget Managed</p>
            </div>
            {{/if}}
            {{#if revenueGenerated}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{revenueGenerated}}</p>
              <p class="leadership-stat-label">Revenue Impact</p>
            </div>
            {{/if}}
            {{#if yearsOfExperience}}
            <div class="leadership-stat">
              <p class="leadership-stat-value">{{yearsOfExperience}}+</p>
              <p class="leadership-stat-label">Years Experience</p>
            </div>
            {{/if}}
          </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             CERTIFICATIONS SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if certifications}}
        <div class="highlight-box">
          <p class="highlight-box-title"><i class="fas fa-certificate"></i> Certifications</p>
          <div class="certifications-list">
            {{#each certifications}}
            <div class="cert-item">
              <i class="fas fa-check-circle"></i>
              <span>
                <span class="cert-name">{{this.name}}</span>
                {{#if this.issuer}}<span class="cert-issuer"> • {{this.issuer}}</span>{{/if}}
              </span>
            </div>
            {{/each}}
          </div>
        </div>
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             INTERNATIONAL/VISA SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if requiresSponsorship}}
        <div class="highlight-box international-box">
          <p class="highlight-box-title"><i class="fas fa-globe-americas"></i> Work Authorization & Relocation</p>
          <div class="visa-info">
            {{#if workAuthorizationStatus}}
            <div class="visa-item">
              <p class="visa-label">Work Authorization</p>
              <p class="visa-value">{{workAuthorizationStatus}}</p>
            </div>
            {{/if}}
            {{#if currentVisaStatus}}
            <div class="visa-item">
              <p class="visa-label">Current Visa Status</p>
              <p class="visa-value">{{currentVisaStatus}}</p>
            </div>
            {{/if}}
            {{#if relocationTimeline}}
            <div class="visa-item">
              <p class="visa-label">Relocation Timeline</p>
              <p class="visa-value">{{relocationTimeline}}</p>
            </div>
            {{/if}}
            {{#if targetLocation}}
            <div class="visa-item">
              <p class="visa-label">Target Location</p>
              <p class="visa-value">{{targetLocation}}</p>
            </div>
            {{/if}}
          </div>

          {{#if languages}}
          <div class="languages-list">
            {{#each languages}}
            <span class="language-item">
              <span class="language-name">{{this.language}}</span>
              <span class="language-level">({{this.level}})</span>
            </span>
            {{/each}}
          </div>
          {{/if}}

          {{#if ieltsScore}}
          <p style="font-size: 12px; margin-top: 15px; color: #666;">
            <i class="fas fa-language" style="color: #92400e; margin-right: 6px;"></i>
            <strong>IELTS:</strong> {{ieltsScore}}
          </p>
          {{/if}}
        </div>
        {{/if}}

        <!-- ═══════════════════════════════════════════════════════════════
             AVAILABILITY SECTION
             ═══════════════════════════════════════════════════════════════ -->
        {{#if availableFrom}}
        <div class="highlight-box availability-box">
          <p class="highlight-box-title"><i class="fas fa-calendar-check"></i> Availability</p>
          <div class="availability-info">
            {{#if availableFrom}}
            <div class="availability-item">
              <i class="fas fa-play-circle"></i>
              <div>
                <p class="availability-label">Available From</p>
                <p class="availability-value">{{availableFrom}}</p>
              </div>
            </div>
            {{/if}}
            {{#if noticePeriod}}
            <div class="availability-item">
              <i class="fas fa-clock"></i>
              <div>
                <p class="availability-label">Notice Period</p>
                <p class="availability-value">{{noticePeriod}}</p>
              </div>
            </div>
            {{/if}}
            {{#if interviewAvailability}}
            <div class="availability-item">
              <i class="fas fa-video"></i>
              <div>
                <p class="availability-label">Interview Availability</p>
                <p class="availability-value">{{interviewAvailability}}</p>
              </div>
            </div>
            {{/if}}
          </div>
        </div>
        {{/if}}
        
        <!-- Closing Paragraph -->
        <p>{{closingParagraph}}</p>
      </div>

      <!-- ═══════════════════════════════════════════════════════════════
           CLOSING & SIGNATURE
           ═══════════════════════════════════════════════════════════════ -->
      {{#if showSignature}}
      <div class="closing-section">
        <p class="sign-off">{{signOff}},</p>
        <div class="signature-line"></div>
        <p class="signature-name">{{fullName}}</p>
        {{#if currentRole}}
        <p class="signature-title">{{currentRole}}{{#if currentCompany}} • {{currentCompany}}{{/if}}</p>
        {{else}}
          {{#if degree}}
          <p class="signature-title">{{degree}}</p>
          {{/if}}
        {{/if}}
        {{#if phone}}
        <p class="signature-contact">{{phone}} {{#if email}}• {{email}}{{/if}}</p>
        {{/if}}
      </div>
      {{/if}}

    </main>

    <!-- ═══════════════════════════════════════════════════════════════
         FOOTER
         ═══════════════════════════════════════════════════════════════ -->
    <footer class="footer">
      {{#if email}}
      <span class="footer-item">
        <i class="fas fa-envelope"></i>
        <a href="mailto:{{email}}">{{email}}</a>
      </span>
      {{/if}}
      
      {{#if phone}}
      <span class="footer-item">
        <i class="fas fa-phone"></i>
        {{phone}}
      </span>
      {{/if}}
      
      {{#if linkedin}}
      <span class="footer-item">
        <i class="fab fa-linkedin"></i>
        <a href="https://{{linkedin}}" target="_blank">{{linkedin}}</a>
      </span>
      {{/if}}

      {{#if portfolio}}
      <span class="footer-item">
        <i class="fas fa-globe"></i>
        <a href="https://{{portfolio}}" target="_blank">{{portfolio}}</a>
      </span>
      {{/if}}
    </footer>

  </div>
</body>
</html>
