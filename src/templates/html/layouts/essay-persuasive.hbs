{{!-- 
  ═══════════════════════════════════════════════════════════════════════════════
  SORIVA - ESSAY TEMPLATE: PERSUASIVE
  Style: Bold, impactful, call-to-action focused
  Best For: Opinion pieces, persuasive writing, advocacy essays
  Features: Strong thesis banner, emotional appeals, powerful CTAs, impact stats
  ═══════════════════════════════════════════════════════════════════════════════
--}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - Persuasive Essay</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       CSS RESET & BASE STYLES
    ═══════════════════════════════════════════════════════════════ */
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --primary-color: {{#if accentColor}}{{accentColor}}{{else}}#dc2626{{/if}};
      --secondary-color: #b91c1c;
      --accent-color: #f59e0b;
      --success-color: #059669;
      --text-color: #1f2937;
      --text-light: #6b7280;
      --text-muted: #9ca3af;
      --border-color: #e5e7eb;
      --bg-warm: #fef2f2;
      --bg-amber: #fffbeb;
      --bg-gray: #f9fafb;
      --font-display: 'Georgia', serif;
      --font-sans: 'Segoe UI', 'Arial', sans-serif;
    }

    /* ═══════════════════════════════════════════════════════════════
       PAGE SETUP
    ═══════════════════════════════════════════════════════════════ */
    @page {
      size: A4;
      margin: 0.75in;
    }

    @media print {
      body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
      .page-break { page-break-before: always; }
    }

    html {
      font-size: 11pt;
    }

    body {
      font-family: var(--font-sans);
      line-height: 1.75;
      color: var(--text-color);
      background: #fff;
      padding: 0;
      max-width: 8.5in;
      margin: 0 auto;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER - BOLD & IMPACTFUL
    ═══════════════════════════════════════════════════════════════ */
    .essay-header {
      background: linear-gradient(135deg, var(--primary-color) 0%, #991b1b 100%);
      color: #fff;
      padding: 2rem;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .essay-header::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.05'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
      opacity: 0.5;
    }

    .essay-header h1 {
      font-family: var(--font-display);
      font-size: 1.9rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
      line-height: 1.25;
      position: relative;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    }

    .essay-header .subtitle {
      font-size: 1.05rem;
      opacity: 0.95;
      font-style: italic;
      position: relative;
    }

    .essay-header .meta-line {
      margin-top: 1.25rem;
      padding-top: 1rem;
      border-top: 1px solid rgba(255,255,255,0.25);
      font-size: 0.85rem;
      opacity: 0.9;
      position: relative;
    }

    /* ═══════════════════════════════════════════════════════════════
       THESIS BANNER - THE MAIN CLAIM
    ═══════════════════════════════════════════════════════════════ -->
    .thesis-banner {
      background: var(--bg-warm);
      border: 2px solid var(--primary-color);
      border-radius: 12px;
      padding: 1.5rem;
      margin: 1.5rem 0;
      text-align: center;
      position: relative;
    }

    .thesis-banner::before {
      content: "💪";
      position: absolute;
      top: -15px;
      left: 50%;
      transform: translateX(-50%);
      background: #fff;
      padding: 0 0.75rem;
      font-size: 1.5rem;
    }

    .thesis-banner .label {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: var(--primary-color);
      margin-bottom: 0.5rem;
      font-weight: 700;
    }

    .thesis-banner .thesis-text {
      font-family: var(--font-display);
      font-size: 1.2rem;
      color: var(--secondary-color);
      font-weight: 600;
      line-height: 1.5;
      font-style: italic;
    }

    /* ═══════════════════════════════════════════════════════════════
       HOOK / OPENING IMPACT
    ═══════════════════════════════════════════════════════════════ -->
    .hook-section {
      background: linear-gradient(90deg, var(--primary-color) 4px, var(--bg-gray) 4px);
      padding: 1.25rem 1.5rem;
      margin: 1.5rem 0;
      border-radius: 0 12px 12px 0;
    }

    .hook-section .hook-text {
      font-size: 1.1rem;
      font-style: italic;
      color: var(--text-color);
      line-height: 1.7;
    }

    .hook-section .hook-type {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary-color);
      margin-top: 0.5rem;
      font-weight: 600;
    }

    /* ═══════════════════════════════════════════════════════════════
       WHY IT MATTERS - IMPACT STATS
    ═══════════════════════════════════════════════════════════════ -->
    .impact-section {
      margin: 1.5rem 0;
    }

    .section-heading {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 1rem;
      font-weight: 700;
      color: var(--text-color);
      margin-bottom: 1rem;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .section-heading .icon {
      font-size: 1.2rem;
    }

    .section-heading::after {
      content: "";
      flex: 1;
      height: 2px;
      background: linear-gradient(90deg, var(--primary-color), transparent);
      margin-left: 0.5rem;
    }

    .impact-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
    }

    .impact-card {
      background: linear-gradient(135deg, var(--bg-warm) 0%, #fff 100%);
      border: 1px solid #fecaca;
      border-radius: 12px;
      padding: 1.25rem;
      text-align: center;
    }

    .impact-card .impact-number {
      font-size: 2rem;
      font-weight: 800;
      color: var(--primary-color);
      line-height: 1;
    }

    .impact-card .impact-label {
      font-size: 0.8rem;
      color: var(--text-light);
      margin-top: 0.4rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       ARGUMENT SECTIONS
    ═══════════════════════════════════════════════════════════════ -->
    .argument-section {
      margin: 1.5rem 0;
    }

    .argument-card {
      background: #fff;
      border: 1px solid var(--border-color);
      border-left: 5px solid var(--primary-color);
      border-radius: 0 12px 12px 0;
      padding: 1.25rem;
      margin-bottom: 1rem;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .argument-card:hover {
      transform: translateX(5px);
      box-shadow: -5px 5px 15px rgba(220, 38, 38, 0.1);
    }

    .argument-card .argument-header {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 0.75rem;
    }

    .argument-card .argument-num {
      background: var(--primary-color);
      color: #fff;
      width: 30px;
      height: 30px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.85rem;
      font-weight: 700;
    }

    .argument-card .argument-title {
      font-size: 1.05rem;
      font-weight: 600;
      color: var(--text-color);
    }

    .argument-card .argument-body {
      font-size: 0.95rem;
      line-height: 1.7;
      color: var(--text-color);
    }

    .argument-card .evidence-tag {
      display: inline-flex;
      align-items: center;
      gap: 0.3rem;
      background: var(--bg-amber);
      border: 1px solid #fde68a;
      border-radius: 6px;
      padding: 0.5rem 0.75rem;
      margin-top: 0.75rem;
      font-size: 0.85rem;
    }

    .argument-card .evidence-tag .tag-icon {
      font-size: 1rem;
    }

    /* ═══════════════════════════════════════════════════════════════
       EMOTIONAL APPEAL BOX
    ═══════════════════════════════════════════════════════════════ -->
    .emotional-appeal {
      background: linear-gradient(135deg, #fdf4ff 0%, #fce7f3 100%);
      border: 1px solid #f5d0fe;
      border-radius: 12px;
      padding: 1.25rem;
      margin: 1.5rem 0;
      text-align: center;
    }

    .emotional-appeal .appeal-icon {
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .emotional-appeal .appeal-title {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: #a21caf;
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .emotional-appeal .appeal-text {
      font-family: var(--font-display);
      font-size: 1.1rem;
      font-style: italic;
      color: #86198f;
      line-height: 1.6;
    }

    /* ═══════════════════════════════════════════════════════════════
       COUNTER ARGUMENT & REBUTTAL
    ═══════════════════════════════════════════════════════════════ -->
    .counter-rebuttal-section {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin: 1.5rem 0;
    }

    .counter-box {
      background: var(--bg-gray);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 1.25rem;
    }

    .counter-box .box-header {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      margin-bottom: 0.75rem;
    }

    .counter-box .box-icon {
      font-size: 1.25rem;
    }

    .counter-box .box-title {
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-weight: 600;
    }

    .counter-box.counter .box-title {
      color: var(--text-light);
    }

    .counter-box.rebuttal {
      background: linear-gradient(135deg, #ecfdf5 0%, #d1fae5 100%);
      border-color: #a7f3d0;
    }

    .counter-box.rebuttal .box-title {
      color: var(--success-color);
    }

    .counter-box .box-content {
      font-size: 0.95rem;
      line-height: 1.6;
    }

    /* ═══════════════════════════════════════════════════════════════
       RHETORICAL APPEALS (Logos, Pathos, Ethos)
    ═══════════════════════════════════════════════════════════════ -->
    .appeals-section {
      margin: 1.5rem 0;
    }

    .appeals-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
    }

    .appeal-card {
      border-radius: 12px;
      padding: 1rem;
      text-align: center;
    }

    .appeal-card.logos {
      background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
      border: 1px solid #bfdbfe;
    }

    .appeal-card.pathos {
      background: linear-gradient(135deg, #fef2f2 0%, #fecaca 100%);
      border: 1px solid #fca5a5;
    }

    .appeal-card.ethos {
      background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%);
      border: 1px solid #bbf7d0;
    }

    .appeal-card .appeal-name {
      font-size: 0.7rem;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      font-weight: 700;
      margin-bottom: 0.3rem;
    }

    .appeal-card.logos .appeal-name { color: #1d4ed8; }
    .appeal-card.pathos .appeal-name { color: #dc2626; }
    .appeal-card.ethos .appeal-name { color: #16a34a; }

    .appeal-card .appeal-desc {
      font-size: 0.75rem;
      color: var(--text-light);
      margin-bottom: 0.5rem;
    }

    .appeal-card .appeal-list {
      list-style: none;
      font-size: 0.85rem;
      text-align: left;
    }

    .appeal-card .appeal-list li {
      padding: 0.2rem 0;
      padding-left: 1rem;
      position: relative;
    }

    .appeal-card .appeal-list li::before {
      content: "•";
      position: absolute;
      left: 0;
      font-weight: bold;
    }

    .appeal-card.logos .appeal-list li::before { color: #1d4ed8; }
    .appeal-card.pathos .appeal-list li::before { color: #dc2626; }
    .appeal-card.ethos .appeal-list li::before { color: #16a34a; }

    /* ═══════════════════════════════════════════════════════════════
       CONCLUSION
    ═══════════════════════════════════════════════════════════════ -->
    .conclusion-section {
      margin-top: 2rem;
      padding-top: 1.5rem;
      border-top: 3px solid var(--primary-color);
    }

    .conclusion-content {
      font-size: 1rem;
      line-height: 1.8;
    }

    .conclusion-content .restated-thesis {
      font-weight: 600;
      color: var(--secondary-color);
    }

    .summary-points {
      background: var(--bg-gray);
      border-radius: 10px;
      padding: 1rem 1.25rem;
      margin: 1rem 0;
    }

    .summary-points .summary-title {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary-color);
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .summary-points ul {
      list-style: none;
      margin: 0;
    }

    .summary-points li {
      padding: 0.25rem 0;
      padding-left: 1.5rem;
      position: relative;
      font-size: 0.95rem;
    }

    .summary-points li::before {
      content: "✓";
      position: absolute;
      left: 0;
      color: var(--success-color);
      font-weight: bold;
    }

    /* ═══════════════════════════════════════════════════════════════
       CALL TO ACTION - THE BIG FINISH
    ═══════════════════════════════════════════════════════════════ -->
    .cta-section {
      background: linear-gradient(135deg, var(--primary-color) 0%, #991b1b 100%);
      color: #fff;
      padding: 1.5rem 2rem;
      border-radius: 12px;
      margin-top: 1.5rem;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .cta-section::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23ffffff' fill-opacity='0.1' fill-rule='evenodd'%3E%3Cpath d='M0 40L40 0H20L0 20M40 40V20L20 40'/%3E%3C/g%3E%3C/svg%3E");
    }

    .cta-section .cta-icon {
      font-size: 2.5rem;
      margin-bottom: 0.75rem;
      position: relative;
    }

    .cta-section .cta-title {
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      opacity: 0.9;
      margin-bottom: 0.5rem;
      position: relative;
    }

    .cta-section .cta-text {
      font-family: var(--font-display);
      font-size: 1.25rem;
      font-weight: 600;
      line-height: 1.5;
      position: relative;
    }

    .cta-section .cta-subtext {
      font-size: 0.95rem;
      margin-top: 0.75rem;
      opacity: 0.9;
      font-style: italic;
      position: relative;
    }

    /* ═══════════════════════════════════════════════════════════════
       FINAL THOUGHT
    ═══════════════════════════════════════════════════════════════ -->
    .final-thought {
      text-align: center;
      margin-top: 1.5rem;
      padding: 1rem;
      font-family: var(--font-display);
      font-size: 1.1rem;
      font-style: italic;
      color: var(--text-light);
      border-top: 1px dashed var(--border-color);
      border-bottom: 1px dashed var(--border-color);
    }

    /* ═══════════════════════════════════════════════════════════════
       REFERENCES
    ═══════════════════════════════════════════════════════════════ -->
    .references-section {
      margin-top: 2rem;
      padding-top: 1rem;
      border-top: 2px solid var(--border-color);
    }

    .references-section h2 {
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: var(--primary-color);
      margin-bottom: 0.75rem;
    }

    .reference-item {
      font-size: 0.85rem;
      padding-left: 1.5rem;
      text-indent: -1.5rem;
      margin-bottom: 0.4rem;
      color: var(--text-light);
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
    ═══════════════════════════════════════════════════════════════ -->
    .essay-footer {
      margin-top: 2rem;
      padding-top: 1rem;
      border-top: 1px solid var(--border-color);
      display: flex;
      justify-content: space-between;
      font-size: 0.8rem;
      color: var(--text-muted);
    }
  </style>
</head>
<body>

  <!-- ═══════════════════════════════════════════════════════════════
       HEADER
  ═══════════════════════════════════════════════════════════════ -->
  <header class="essay-header">
    <h1>{{title}}</h1>
    
    {{#if subtitle}}
    <div class="subtitle">{{subtitle}}</div>
    {{/if}}
    
    <div class="meta-line">
      {{author}}
      {{#if class}} | {{class}}{{/if}}
      {{#if institution}} | {{institution}}{{/if}}
      {{#if date}} | {{formatDate date}}{{/if}}
    </div>
  </header>

  <!-- ═══════════════════════════════════════════════════════════════
       THESIS BANNER
  ═══════════════════════════════════════════════════════════════ -->
  <div class="thesis-banner">
    <div class="label">My Position / Central Argument</div>
    <div class="thesis-text">{{thesis}}</div>
  </div>

  <!-- ═══════════════════════════════════════════════════════════════
       HOOK / OPENING
  ═══════════════════════════════════════════════════════════════ -->
  {{#if introduction.hook}}
  <div class="hook-section">
    <div class="hook-text">{{introduction.hook}}</div>
    {{#if introduction.hookType}}
    <div class="hook-type">— {{introduction.hookType}}</div>
    {{/if}}
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       BACKGROUND / CONTEXT
  ═══════════════════════════════════════════════════════════════ -->
  {{#if introduction.background}}
  <div style="margin: 1rem 0; text-align: justify;">
    <p>{{introduction.background}}</p>
    {{#if introduction.context}}
    <p style="margin-top: 0.75rem;">{{introduction.context}}</p>
    {{/if}}
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       WHY IT MATTERS - IMPACT STATS (if provided)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if keywords}}
  <section class="impact-section">
    <div class="section-heading">
      <span class="icon">📊</span> Why This Matters
    </div>
    <div class="impact-grid">
      {{#each keywords}}
      <div class="impact-card">
        <div class="impact-label">{{this}}</div>
      </div>
      {{/each}}
    </div>
  </section>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       SUPPORTING ARGUMENTS
  ═══════════════════════════════════════════════════════════════ -->
  <section class="argument-section">
    <div class="section-heading">
      <span class="icon">💪</span> Why You Should Agree
    </div>

    {{#if argumentative.supportingArguments}}
      {{#each argumentative.supportingArguments}}
      <div class="argument-card">
        <div class="argument-header">
          <span class="argument-num">{{math @index "+" 1}}</span>
          <span class="argument-title">Reason {{math @index "+" 1}}</span>
        </div>
        <div class="argument-body">
          {{this.argument}}
        </div>
        {{#each this.evidence}}
        <div class="evidence-tag">
          <span class="tag-icon">📌</span>
          {{this.content}}
          {{#if this.source}}<em style="margin-left: 0.5rem; color: var(--text-light);">— {{this.source}}</em>{{/if}}
        </div>
        {{/each}}
      </div>
      {{/each}}
    {{else}}
      {{!-- Fallback to bodyParagraphs --}}
      {{#each bodyParagraphs}}
      <div class="argument-card">
        <div class="argument-header">
          <span class="argument-num">{{math @index "+" 1}}</span>
          <span class="argument-title">{{#if this.topic}}{{this.topic}}{{else}}Reason {{math @index "+" 1}}{{/if}}</span>
        </div>
        <div class="argument-body">
          {{#if this.topicSentence}}{{this.topicSentence}} {{/if}}
          {{#each this.supportingPoints}}{{this}} {{/each}}
          {{#if this.analysis}}{{this.analysis}}{{/if}}
        </div>
        {{#each this.evidence}}
        <div class="evidence-tag">
          <span class="tag-icon">📌</span>
          {{this.content}}
        </div>
        {{/each}}
      </div>
      {{/each}}
    {{/if}}
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       EMOTIONAL APPEAL (Pathos)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if emotionalAppeals}}
  <div class="emotional-appeal">
    <div class="appeal-icon">❤️</div>
    <div class="appeal-title">Think About This...</div>
    <div class="appeal-text">
      {{#each emotionalAppeals}}{{this}} {{/each}}
    </div>
  </div>
  {{else if argumentative.emotionalAppeals}}
  <div class="emotional-appeal">
    <div class="appeal-icon">❤️</div>
    <div class="appeal-title">Think About This...</div>
    <div class="appeal-text">
      {{#each argumentative.emotionalAppeals}}{{this}} {{/each}}
    </div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       COUNTER ARGUMENT & REBUTTAL
  ═══════════════════════════════════════════════════════════════ -->
  {{#if counterArgument}}
  <div class="counter-rebuttal-section">
    <div class="counter-box counter">
      <div class="box-header">
        <span class="box-icon">🤔</span>
        <span class="box-title">Some May Say...</span>
      </div>
      <div class="box-content">{{counterArgument}}</div>
    </div>
    
    {{#if rebuttal}}
    <div class="counter-box rebuttal">
      <div class="box-header">
        <span class="box-icon">💡</span>
        <span class="box-title">But Consider This...</span>
      </div>
      <div class="box-content">{{rebuttal}}</div>
    </div>
    {{/if}}
  </div>
  {{else if argumentative.counterArguments}}
  {{#each argumentative.counterArguments}}
  <div class="counter-rebuttal-section">
    <div class="counter-box counter">
      <div class="box-header">
        <span class="box-icon">🤔</span>
        <span class="box-title">Some May Say...</span>
      </div>
      <div class="box-content">{{this.point}}</div>
    </div>
    
    <div class="counter-box rebuttal">
      <div class="box-header">
        <span class="box-icon">💡</span>
        <span class="box-title">But Consider This...</span>
      </div>
      <div class="box-content">{{this.rebuttal}}</div>
    </div>
  </div>
  {{/each}}
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       RHETORICAL APPEALS (if detailed data provided)
  ═══════════════════════════════════════════════════════════════ -->
  {{#if argumentative.logicalAppeals}}
  <section class="appeals-section">
    <div class="section-heading">
      <span class="icon">🎯</span> My Persuasive Strategy
    </div>
    
    <div class="appeals-grid">
      {{#if argumentative.logicalAppeals}}
      <div class="appeal-card logos">
        <div class="appeal-name">Logos</div>
        <div class="appeal-desc">Logic & Facts</div>
        <ul class="appeal-list">
          {{#each argumentative.logicalAppeals}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
      
      {{#if argumentative.emotionalAppeals}}
      <div class="appeal-card pathos">
        <div class="appeal-name">Pathos</div>
        <div class="appeal-desc">Emotion</div>
        <ul class="appeal-list">
          {{#each argumentative.emotionalAppeals}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
      
      {{#if argumentative.credibilityAppeals}}
      <div class="appeal-card ethos">
        <div class="appeal-name">Ethos</div>
        <div class="appeal-desc">Credibility</div>
        <ul class="appeal-list">
          {{#each argumentative.credibilityAppeals}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
    </div>
  </section>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       CONCLUSION
  ═══════════════════════════════════════════════════════════════ -->
  <section class="conclusion-section">
    <div class="section-heading">
      <span class="icon">🏁</span> In Conclusion
    </div>
    
    <div class="conclusion-content">
      {{#if conclusion.restatedThesis}}
      <p><span class="restated-thesis">{{conclusion.restatedThesis}}</span></p>
      {{/if}}
      
      {{#if conclusion.synthesis}}
      <p style="margin-top: 0.75rem;">{{conclusion.synthesis}}</p>
      {{/if}}
    </div>
    
    {{#if conclusion.summary}}
    <div class="summary-points">
      <div class="summary-title">Key Takeaways</div>
      <ul>
        {{#each conclusion.summary}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
    </div>
    {{/if}}
    
    {{#if conclusion.implications}}
    <p style="margin-top: 1rem;"><strong>The Implications:</strong> {{conclusion.implications}}</p>
    {{/if}}
  </section>

  <!-- ═══════════════════════════════════════════════════════════════
       CALL TO ACTION
  ═══════════════════════════════════════════════════════════════ -->
  {{#if conclusion.callToAction}}
  <div class="cta-section">
    <div class="cta-icon">📣</div>
    <div class="cta-title">Take Action Now</div>
    <div class="cta-text">{{conclusion.callToAction}}</div>
    {{#if conclusion.futureDirections}}
    <div class="cta-subtext">{{conclusion.futureDirections}}</div>
    {{/if}}
  </div>
  {{else if callToAction}}
  <div class="cta-section">
    <div class="cta-icon">📣</div>
    <div class="cta-title">Take Action Now</div>
    <div class="cta-text">{{callToAction}}</div>
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       FINAL THOUGHT
  ═══════════════════════════════════════════════════════════════ -->
  {{#if conclusion.finalThought}}
  <div class="final-thought">
    "{{conclusion.finalThought}}"
  </div>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       REFERENCES
  ═══════════════════════════════════════════════════════════════ -->
  {{#if citations}}
  <section class="references-section">
    <h2>References</h2>
    {{#each citations}}
    <div class="reference-item">
      {{#if this.authors}}{{#each this.authors}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}{{/if}}
      {{#if this.year}} ({{this.year}}).{{/if}}
      {{#if this.title}} {{this.title}}.{{/if}}
      {{#if this.publisher}} {{this.publisher}}.{{/if}}
      {{#if this.url}} {{this.url}}{{/if}}
    </div>
    {{/each}}
  </section>
  {{else if bibliography}}
  <section class="references-section">
    <h2>References</h2>
    {{#each bibliography}}
    <div class="reference-item">{{this}}</div>
    {{/each}}
  </section>
  {{/if}}

  <!-- ═══════════════════════════════════════════════════════════════
       FOOTER
  ═══════════════════════════════════════════════════════════════ -->
  <footer class="essay-footer">
    <div>{{author}}{{#if institution}} | {{institution}}{{/if}}</div>
    {{#if wordCount}}
    <div>Word Count: {{wordCount}}</div>
    {{/if}}
  </footer>

</body>
</html>
