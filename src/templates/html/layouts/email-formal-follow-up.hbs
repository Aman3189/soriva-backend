<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{emailSubject}}</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - EMAIL FORMAL: FOLLOW-UP
       Post Meeting, Interview, Proposal Follow-up Emails
       Includes: followUpType, originalDate, meetingContext,
                 keyPointsDiscussed, actionItems, nextSteps
    ═══════════════════════════════════════════════════════════════ */
    
    /* CSS Custom Properties */
    :root {
      --accent-primary: {{#if accentColor}}{{accentColor}}{{else}}#059669{{/if}};
      --accent-dark: {{#if accentColor}}{{accentColor}}{{else}}#047857{{/if}};
      --accent-light: #ecfdf5;
      --accent-border: #a7f3d0;
      --info-blue: #0284c7;
      --info-light: #f0f9ff;
      --info-border: #bae6fd;
      --warning-amber: #d97706;
      --warning-light: #fffbeb;
      --warning-border: #fde68a;
      --purple-primary: #7c3aed;
      --purple-light: #f5f3ff;
      --purple-border: #ddd6fe;
      --text-primary: #1a1a1a;
      --text-secondary: #475569;
      --text-muted: #64748b;
      --text-light: #94a3b8;
      --bg-white: #ffffff;
      --bg-light: #f8f9fa;
      --bg-page: #f1f5f9;
      --border-light: #e2e8f0;
    }

    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      -webkit-print-color-adjust: exact !important;
      print-color-adjust: exact !important;
    }

    body {
      font-family: 'Georgia', 'Times New Roman', serif;
      font-size: 11pt;
      line-height: 1.7;
      color: var(--text-primary);
      background: var(--bg-page);
      padding: 20px;
    }

    /* Email Container */
    .email-container {
      max-width: 750px;
      margin: 0 auto;
      background: var(--bg-white);
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1);
      border: 1px solid var(--border-light);
    }

    /* ─────────────────────────────────────────────────────────────
       Header Section
    ───────────────────────────────────────────────────────────── */
    .followup-header {
      background: linear-gradient(135deg, var(--accent-primary) 0%, var(--accent-dark) 100%);
      padding: 26px 36px;
      border-bottom: 4px solid #34d399;
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .followup-badge {
      display: flex;
      align-items: center;
      gap: 10px;
      background: rgba(255, 255, 255, 0.15);
      padding: 10px 18px;
      border-radius: 6px;
      border: 1px solid rgba(255, 255, 255, 0.25);
    }

    .followup-icon {
      width: 22px;
      height: 22px;
      fill: none;
      stroke: #ffffff;
      stroke-width: 2;
    }

    .followup-title {
      font-family: 'Arial', sans-serif;
      font-size: 12px;
      font-weight: 700;
      color: #ffffff;
      letter-spacing: 1.5px;
      text-transform: uppercase;
    }

    .header-date {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      color: rgba(255, 255, 255, 0.85);
    }

    /* ─────────────────────────────────────────────────────────────
       Follow-up Type & Original Date Banner
    ───────────────────────────────────────────────────────────── */
    .followup-banner {
      background: var(--accent-light);
      padding: 16px 36px;
      border-bottom: 1px solid var(--accent-border);
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 12px;
    }

    .followup-type-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 600;
      padding: 8px 16px;
      border-radius: 20px;
      background: var(--bg-white);
      color: var(--accent-primary);
      border: 1px solid var(--accent-border);
    }

    .type-icon {
      width: 16px;
      height: 16px;
    }

    .original-date-info {
      display: flex;
      align-items: center;
      gap: 8px;
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      color: var(--text-secondary);
      background: var(--bg-white);
      padding: 8px 14px;
      border-radius: 6px;
      border: 1px solid var(--border-light);
    }

    .date-icon {
      width: 14px;
      height: 14px;
      stroke: var(--accent-primary);
    }

    .date-label {
      color: var(--text-muted);
    }

    .date-value {
      font-weight: 600;
      color: var(--accent-dark);
    }

    /* ─────────────────────────────────────────────────────────────
       Meta Information Section
    ───────────────────────────────────────────────────────────── */
    .email-meta {
      background: var(--bg-light);
      padding: 20px 36px;
      border-bottom: 1px solid var(--border-light);
    }

    .meta-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }

    .meta-item {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .meta-item.full-width {
      grid-column: 1 / -1;
    }

    .meta-label {
      font-family: 'Arial', sans-serif;
      font-size: 9px;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .meta-value {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      color: var(--text-primary);
    }

    .meta-value a {
      color: var(--accent-primary);
      text-decoration: none;
    }

    .meta-value a:hover {
      text-decoration: underline;
    }

    .meta-value.subject {
      font-weight: 700;
      color: var(--accent-primary);
      font-size: 12pt;
    }

    /* ─────────────────────────────────────────────────────────────
       Email Body Section
    ───────────────────────────────────────────────────────────── */
    .email-body {
      padding: 36px;
    }

    .salutation {
      font-size: 11pt;
      color: var(--text-primary);
      margin-bottom: 24px;
    }

    .email-content {
      text-align: justify;
      margin-bottom: 28px;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Meeting Context Box
    ───────────────────────────────────────────────────────────── */
    .context-box {
      background: var(--accent-light);
      border: 1px solid var(--accent-border);
      border-left: 4px solid var(--accent-primary);
      border-radius: 0 8px 8px 0;
      padding: 24px;
      margin: 24px 0;
    }

    .box-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 14px;
    }

    .box-icon {
      width: 20px;
      height: 20px;
      fill: var(--accent-primary);
    }

    .box-icon-stroke {
      width: 20px;
      height: 20px;
      fill: none;
      stroke: var(--accent-primary);
      stroke-width: 2;
    }

    .box-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: var(--accent-dark);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .box-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Key Points Discussed Box
    ───────────────────────────────────────────────────────────── */
    .keypoints-box {
      background: var(--info-light);
      border: 1px solid var(--info-border);
      border-left: 4px solid var(--info-blue);
      border-radius: 0 8px 8px 0;
      padding: 24px;
      margin: 24px 0;
    }

    .keypoints-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px;
    }

    .keypoints-icon {
      width: 20px;
      height: 20px;
      fill: var(--info-blue);
    }

    .keypoints-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: #075985;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .keypoints-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.8;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Action Items Box
    ───────────────────────────────────────────────────────────── */
    .action-box {
      background: var(--warning-light);
      border: 1px solid var(--warning-border);
      border-left: 4px solid var(--warning-amber);
      border-radius: 0 8px 8px 0;
      padding: 24px;
      margin: 24px 0;
    }

    .action-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px;
    }

    .action-icon {
      width: 20px;
      height: 20px;
      fill: var(--warning-amber);
    }

    .action-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: #92400e;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .action-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.8;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Next Steps Box
    ───────────────────────────────────────────────────────────── */
    .nextsteps-box {
      background: var(--purple-light);
      border: 1px solid var(--purple-border);
      border-left: 4px solid var(--purple-primary);
      border-radius: 0 8px 8px 0;
      padding: 24px;
      margin: 24px 0;
    }

    .nextsteps-header {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px;
    }

    .nextsteps-icon {
      width: 20px;
      height: 20px;
      fill: var(--purple-primary);
    }

    .nextsteps-title {
      font-family: 'Arial', sans-serif;
      font-size: 11px;
      font-weight: 700;
      color: #5b21b6;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .nextsteps-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.8;
      white-space: pre-line;
    }

    /* ─────────────────────────────────────────────────────────────
       Closing & Signature
    ───────────────────────────────────────────────────────────── */
    .email-closing {
      margin-top: 36px;
      padding-top: 24px;
      border-top: 1px solid var(--border-light);
    }

    .closing-phrase {
      color: var(--text-primary);
      margin-bottom: 28px;
      font-style: italic;
    }

    .signature-block {
      border-left: 4px solid var(--accent-primary);
      padding-left: 18px;
    }

    .sender-name {
      font-size: 13pt;
      font-weight: 700;
      color: var(--accent-primary);
      margin-bottom: 4px;
      font-family: 'Arial', sans-serif;
    }

    .sender-designation {
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 3px;
    }

    .sender-company {
      font-size: 10pt;
      color: var(--text-muted);
      font-style: italic;
    }

    /* Extended Signature */
    .extended-signature {
      margin-top: 16px;
      padding-top: 14px;
      border-top: 1px dashed var(--border-light);
    }

    .contact-info {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
    }

    .contact-item {
      display: flex;
      align-items: center;
      margin-bottom: 5px;
      color: var(--text-muted);
    }

    .contact-item:last-child {
      margin-bottom: 0;
    }

    .contact-icon {
      width: 14px;
      height: 14px;
      margin-right: 10px;
      stroke: var(--text-light);
    }

    .contact-link {
      color: var(--accent-primary);
      text-decoration: none;
    }

    .contact-link:hover {
      text-decoration: underline;
    }

    /* ─────────────────────────────────────────────────────────────
       Footer
    ───────────────────────────────────────────────────────────── */
    .email-footer {
      background: var(--bg-light);
      padding: 16px 36px;
      border-top: 1px solid var(--border-light);
      text-align: center;
    }

    .footer-note {
      font-family: 'Arial', sans-serif;
      font-size: 8pt;
      color: var(--text-light);
      font-style: italic;
    }

    /* ─────────────────────────────────────────────────────────────
       Print Styles
    ───────────────────────────────────────────────────────────── */
    @media print {
      body {
        background: #ffffff;
        padding: 0;
      }

      .email-container {
        box-shadow: none;
        border-radius: 0;
        border: none;
        max-width: 100%;
      }

      .followup-header,
      .context-box,
      .keypoints-box,
      .action-box,
      .nextsteps-box {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }

      @page {
        margin: 0.5in;
        size: A4;
      }
    }

    /* ─────────────────────────────────────────────────────────────
       Responsive Styles
    ───────────────────────────────────────────────────────────── */
    @media screen and (max-width: 640px) {
      body {
        padding: 10px;
      }

      .followup-header,
      .followup-banner,
      .email-meta,
      .email-body,
      .email-footer {
        padding-left: 20px;
        padding-right: 20px;
      }

      .header-content {
        flex-direction: column;
        gap: 12px;
        align-items: flex-start;
      }

      .followup-banner {
        flex-direction: column;
        align-items: flex-start;
      }

      .meta-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- ═══════════════════════════════════════════════════════════
         HEADER SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="followup-header">
      <div class="header-content">
        <div class="followup-badge">
          <svg class="followup-icon" viewBox="0 0 24 24">
            <polyline points="23 4 23 10 17 10"/>
            <path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/>
          </svg>
          <span class="followup-title">Follow-up Email</span>
        </div>
        <div class="header-date">Date: {{emailDate}}</div>
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         FOLLOW-UP TYPE & ORIGINAL DATE BANNER
    ════════════════════════════════════════════════════════════ -->
    <div class="followup-banner">
      {{#if followUp.followUpType}}
      <div class="followup-type-badge">
        <svg class="type-icon" viewBox="0 0 24 24" fill="currentColor">
          {{#ifEquals followUp.followUpType "interview"}}
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
          {{/ifEquals}}
          {{#ifEquals followUp.followUpType "meeting"}}
          <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>
          {{/ifEquals}}
          {{#ifEquals followUp.followUpType "proposal"}}
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
          {{/ifEquals}}
          {{#ifEquals followUp.followUpType "application"}}
          <rect x="2" y="7" width="20" height="14" rx="2" ry="2"/><path d="M16 21V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/>
          {{/ifEquals}}
          {{#ifEquals followUp.followUpType "networking"}}
          <circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/>
          {{/ifEquals}}
          {{#ifEquals followUp.followUpType "sales"}}
          <line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
          {{/ifEquals}}
          {{#ifEquals followUp.followUpType "project"}}
          <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/>
          {{/ifEquals}}
        </svg>
        {{#ifEquals followUp.followUpType "interview"}}Job Interview Follow-up{{/ifEquals}}
        {{#ifEquals followUp.followUpType "meeting"}}Business Meeting Follow-up{{/ifEquals}}
        {{#ifEquals followUp.followUpType "proposal"}}Proposal/Quotation Follow-up{{/ifEquals}}
        {{#ifEquals followUp.followUpType "application"}}Application Status Follow-up{{/ifEquals}}
        {{#ifEquals followUp.followUpType "networking"}}Networking Follow-up{{/ifEquals}}
        {{#ifEquals followUp.followUpType "sales"}}Sales Follow-up{{/ifEquals}}
        {{#ifEquals followUp.followUpType "project"}}Project Discussion Follow-up{{/ifEquals}}
      </div>
      {{/if}}

      {{#if followUp.originalDate}}
      <div class="original-date-info">
        <svg class="date-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
          <line x1="16" y1="2" x2="16" y2="6"/>
          <line x1="8" y1="2" x2="8" y2="6"/>
          <line x1="3" y1="10" x2="21" y2="10"/>
        </svg>
        <span class="date-label">Original Date:</span>
        <span class="date-value">{{followUp.originalDate}}</span>
      </div>
      {{/if}}
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         META INFORMATION SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="email-meta">
      <div class="meta-grid">
        <!-- From -->
        <div class="meta-item">
          <span class="meta-label">From</span>
          <span class="meta-value">
            {{senderName}}{{#if senderDesignation}}, {{senderDesignation}}{{/if}}
            {{#if senderCompany}}<br>{{senderCompany}}{{/if}}
            {{#if senderEmail}}<br><a href="mailto:{{senderEmail}}">{{senderEmail}}</a>{{/if}}
          </span>
        </div>

        <!-- To -->
        <div class="meta-item">
          <span class="meta-label">To</span>
          <span class="meta-value">
            {{recipientName}}{{#if recipientDesignation}}, {{recipientDesignation}}{{/if}}
            {{#if recipientCompany}}<br>{{recipientCompany}}{{/if}}
            {{#if recipientEmail}}<br><a href="mailto:{{recipientEmail}}">{{recipientEmail}}</a>{{/if}}
          </span>
        </div>

        <!-- Subject -->
        <div class="meta-item full-width">
          <span class="meta-label">Subject</span>
          <span class="meta-value subject">{{emailSubject}}</span>
        </div>
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         EMAIL BODY SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="email-body">
      <!-- Salutation -->
      <div class="salutation">
        {{salutation}} {{recipientName}},
      </div>

      <!-- Email Content -->
      <div class="email-content">
        {{{emailBody}}}
      </div>

      <!-- Meeting Context Box -->
      {{#if followUp.meetingContext}}
      <div class="context-box">
        <div class="box-header">
          <svg class="box-icon-stroke" viewBox="0 0 24 24">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/>
            <line x1="16" y1="2" x2="16" y2="6"/>
            <line x1="8" y1="2" x2="8" y2="6"/>
            <line x1="3" y1="10" x2="21" y2="10"/>
          </svg>
          <span class="box-title">Meeting / Interview Context</span>
        </div>
        <div class="box-content">
          {{{followUp.meetingContext}}}
        </div>
      </div>
      {{/if}}

      <!-- Key Points Discussed Box -->
      {{#if followUp.keyPointsDiscussed}}
      <div class="keypoints-box">
        <div class="keypoints-header">
          <svg class="keypoints-icon" viewBox="0 0 24 24">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
            <line x1="16" y1="13" x2="8" y2="13"/>
            <line x1="16" y1="17" x2="8" y2="17"/>
            <polyline points="10 9 9 9 8 9"/>
          </svg>
          <span class="keypoints-title">Key Points Discussed</span>
        </div>
        <div class="keypoints-content">
          {{{followUp.keyPointsDiscussed}}}
        </div>
      </div>
      {{/if}}

      <!-- Action Items Box -->
      {{#if followUp.actionItems}}
      <div class="action-box">
        <div class="action-header">
          <svg class="action-icon" viewBox="0 0 24 24">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span class="action-title">Pending Action Items</span>
        </div>
        <div class="action-content">
          {{{followUp.actionItems}}}
        </div>
      </div>
      {{/if}}

      <!-- Next Steps Box -->
      {{#if followUp.nextSteps}}
      <div class="nextsteps-box">
        <div class="nextsteps-header">
          <svg class="nextsteps-icon" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
          <span class="nextsteps-title">Proposed Next Steps</span>
        </div>
        <div class="nextsteps-content">
          {{{followUp.nextSteps}}}
        </div>
      </div>
      {{/if}}

      <!-- ─────────────────────────────────────────────────────────
           Closing & Signature
      ────────────────────────────────────────────────────────── -->
      <div class="email-closing">
        <div class="closing-phrase">{{closing}},</div>
        
        <div class="signature-block">
          <div class="sender-name">{{senderName}}</div>
          
          {{#if senderDesignation}}
          <div class="sender-designation">{{senderDesignation}}</div>
          {{/if}}
          
          {{#if senderCompany}}
          <div class="sender-company">{{senderCompany}}</div>
          {{/if}}

          {{#if includeSignature}}
          <div class="extended-signature">
            <div class="contact-info">
              {{#if senderEmail}}
              <div class="contact-item">
                <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                  <polyline points="22,6 12,13 2,6"/>
                </svg>
                <a href="mailto:{{senderEmail}}" class="contact-link">{{senderEmail}}</a>
              </div>
              {{/if}}
              
              {{#if senderPhone}}
              <div class="contact-item">
                <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                </svg>
                <span>{{senderPhone}}</span>
              </div>
              {{/if}}
            </div>
          </div>
          {{/if}}
        </div>
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         FOOTER
    ════════════════════════════════════════════════════════════ -->
    <div class="email-footer">
      <p class="footer-note">This follow-up email references a previous interaction and seeks continued engagement.</p>
    </div>
  </div>
</body>
</html>
