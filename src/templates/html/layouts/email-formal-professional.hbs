<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{emailSubject}}</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - EMAIL FORMAL: PROFESSIONAL
       General Professional & Office Communication Template
       Dynamic accent color support via CSS custom properties
    ═══════════════════════════════════════════════════════════════ */
    
    /* CSS Custom Properties for Dynamic Theming */
    :root {
      --accent-primary: {{#if accentColor}}{{accentColor}}{{else}}#1e3a5f{{/if}};
      --accent-secondary: {{#if accentColor}}{{accentColor}}{{else}}#2c5282{{/if}};
      --accent-gold: #c9a227;
      --text-primary: #1a1a1a;
      --text-secondary: #475569;
      --text-muted: #64748b;
      --text-light: #94a3b8;
      --bg-white: #ffffff;
      --bg-light: #f8f9fa;
      --bg-page: #f5f5f5;
      --border-light: #e2e8f0;
    }

    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      -webkit-print-color-adjust: exact !important;
      print-color-adjust: exact !important;
    }

    body {
      font-family: 'Georgia', 'Times New Roman', serif;
      font-size: 11pt;
      line-height: 1.7;
      color: var(--text-primary);
      background: var(--bg-page);
      padding: 20px;
    }

    /* Email Container */
    .email-container {
      max-width: 720px;
      margin: 0 auto;
      background: var(--bg-white);
      border-radius: 6px;
      overflow: hidden;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    }

    /* ─────────────────────────────────────────────────────────────
       Header Section
    ───────────────────────────────────────────────────────────── */
    .email-header {
      background: linear-gradient(135deg, var(--accent-primary) 0%, var(--accent-secondary) 100%);
      padding: 24px 32px;
      border-bottom: 4px solid var(--accent-gold);
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .email-type-badge {
      display: inline-block;
      background: rgba(255, 255, 255, 0.15);
      color: #ffffff;
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 1.5px;
      text-transform: uppercase;
      padding: 8px 16px;
      border-radius: 4px;
      border: 1px solid rgba(255, 255, 255, 0.25);
    }

    .email-date-header {
      color: rgba(255, 255, 255, 0.85);
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      letter-spacing: 0.5px;
    }

    /* ─────────────────────────────────────────────────────────────
       Meta Information Section
    ───────────────────────────────────────────────────────────── */
    .email-meta {
      background: var(--bg-light);
      padding: 20px 32px;
      border-bottom: 1px solid var(--border-light);
    }

    .meta-grid {
      display: grid;
      gap: 10px;
    }

    .meta-row {
      display: flex;
      align-items: flex-start;
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
    }

    .meta-label {
      width: 70px;
      min-width: 70px;
      color: var(--text-muted);
      font-weight: 600;
      text-transform: uppercase;
      font-size: 9px;
      letter-spacing: 0.5px;
      padding-top: 3px;
    }

    .meta-value {
      flex: 1;
      color: var(--text-primary);
      word-break: break-word;
    }

    .meta-value a {
      color: var(--accent-primary);
      text-decoration: none;
    }

    .meta-value a:hover {
      text-decoration: underline;
    }

    .meta-value.subject {
      font-weight: 600;
      color: var(--accent-primary);
      font-size: 11pt;
    }

    .meta-email {
      color: var(--text-muted);
      font-size: 9pt;
    }

    /* ─────────────────────────────────────────────────────────────
       Email Body Section
    ───────────────────────────────────────────────────────────── */
    .email-body {
      padding: 36px 32px;
    }

    /* Salutation */
    .salutation {
      font-size: 11pt;
      color: var(--text-primary);
      margin-bottom: 24px;
    }

    /* Email Content */
    .email-content {
      text-align: justify;
      margin-bottom: 28px;
      white-space: pre-line;
    }

    .email-content p {
      margin-bottom: 16px;
      text-indent: 0;
    }

    .email-content p:last-child {
      margin-bottom: 0;
    }

    /* ─────────────────────────────────────────────────────────────
       Closing & Signature Section
    ───────────────────────────────────────────────────────────── */
    .email-closing {
      margin-top: 32px;
    }

    .closing-phrase {
      color: var(--text-primary);
      margin-bottom: 28px;
      font-style: italic;
    }

    /* Signature Block */
    .signature-block {
      border-left: 4px solid var(--accent-gold);
      padding-left: 18px;
      margin-top: 12px;
    }

    .sender-name {
      font-size: 13pt;
      font-weight: 700;
      color: var(--accent-primary);
      margin-bottom: 4px;
      font-family: 'Arial', sans-serif;
    }

    .sender-designation {
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 3px;
    }

    .sender-company {
      font-size: 10pt;
      color: var(--text-muted);
      font-style: italic;
    }

    /* Extended Signature (when includeSignature is true) */
    .extended-signature {
      margin-top: 18px;
      padding-top: 16px;
      border-top: 1px dashed var(--border-light);
    }

    .contact-info {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: var(--text-muted);
    }

    .contact-item {
      display: flex;
      align-items: center;
      margin-bottom: 6px;
    }

    .contact-item:last-child {
      margin-bottom: 0;
    }

    .contact-icon {
      width: 14px;
      height: 14px;
      margin-right: 10px;
      stroke: var(--text-light);
      flex-shrink: 0;
    }

    .contact-link {
      color: var(--accent-primary);
      text-decoration: none;
    }

    .contact-link:hover {
      text-decoration: underline;
    }

    .contact-text {
      color: var(--text-secondary);
    }

    /* ─────────────────────────────────────────────────────────────
       Footer Section
    ───────────────────────────────────────────────────────────── */
    .email-footer {
      background: var(--bg-light);
      padding: 16px 32px;
      border-top: 1px solid var(--border-light);
      text-align: center;
    }

    .footer-note {
      font-family: 'Arial', sans-serif;
      font-size: 8pt;
      color: var(--text-light);
      font-style: italic;
    }

    /* ─────────────────────────────────────────────────────────────
       Print Styles
    ───────────────────────────────────────────────────────────── */
    @media print {
      body {
        background: #ffffff;
        padding: 0;
        font-size: 11pt;
      }

      .email-container {
        box-shadow: none;
        border-radius: 0;
        max-width: 100%;
      }

      .email-header {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }

      .signature-block {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }

      .email-body {
        padding: 24px;
      }

      .email-meta,
      .email-footer {
        padding-left: 24px;
        padding-right: 24px;
      }

      @page {
        margin: 0.6in;
        size: A4;
      }
    }

    /* ─────────────────────────────────────────────────────────────
       Responsive Styles
    ───────────────────────────────────────────────────────────── */
    @media screen and (max-width: 640px) {
      body {
        padding: 12px;
      }

      .email-header,
      .email-meta,
      .email-body,
      .email-footer {
        padding-left: 20px;
        padding-right: 20px;
      }

      .header-content {
        flex-direction: column;
        gap: 10px;
        align-items: flex-start;
      }

      .meta-row {
        flex-direction: column;
        gap: 2px;
      }

      .meta-label {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="email-container">
    <!-- ═══════════════════════════════════════════════════════════
         HEADER SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="email-header">
      <div class="header-content">
        <span class="email-type-badge">Professional Email</span>
        {{#if emailDate}}
        <span class="email-date-header">{{emailDate}}</span>
        {{/if}}
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         META INFORMATION SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="email-meta">
      <div class="meta-grid">
        <!-- From -->
        <div class="meta-row">
          <span class="meta-label">From:</span>
          <span class="meta-value">
            {{senderName}}{{#if senderDesignation}}, {{senderDesignation}}{{/if}}{{#if senderCompany}} — {{senderCompany}}{{/if}}
            {{#if senderEmail}}
            <br><span class="meta-email">&lt;<a href="mailto:{{senderEmail}}">{{senderEmail}}</a>&gt;</span>
            {{/if}}
          </span>
        </div>

        <!-- To -->
        <div class="meta-row">
          <span class="meta-label">To:</span>
          <span class="meta-value">
            {{recipientName}}{{#if recipientDesignation}}, {{recipientDesignation}}{{/if}}{{#if recipientCompany}} — {{recipientCompany}}{{/if}}
            {{#if recipientEmail}}
            <br><span class="meta-email">&lt;<a href="mailto:{{recipientEmail}}">{{recipientEmail}}</a>&gt;</span>
            {{/if}}
          </span>
        </div>

        <!-- Date (shown in meta if not in header) -->
        <div class="meta-row">
          <span class="meta-label">Date:</span>
          <span class="meta-value">{{emailDate}}</span>
        </div>

        <!-- Subject -->
        <div class="meta-row">
          <span class="meta-label">Subject:</span>
          <span class="meta-value subject">{{emailSubject}}</span>
        </div>
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         EMAIL BODY SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="email-body">
      <!-- Salutation -->
      <div class="salutation">
        {{salutation}} {{recipientName}},
      </div>

      <!-- Email Content -->
      <div class="email-content">
        {{{emailBody}}}
      </div>

      <!-- ─────────────────────────────────────────────────────────
           Closing & Signature
      ────────────────────────────────────────────────────────── -->
      <div class="email-closing">
        <div class="closing-phrase">{{closing}},</div>
        
        <!-- Signature Block -->
        <div class="signature-block">
          <div class="sender-name">{{senderName}}</div>
          
          {{#if senderDesignation}}
          <div class="sender-designation">{{senderDesignation}}</div>
          {{/if}}
          
          {{#if senderCompany}}
          <div class="sender-company">{{senderCompany}}</div>
          {{/if}}

          {{#if includeSignature}}
          <!-- Extended Contact Information -->
          <div class="extended-signature">
            <div class="contact-info">
              {{#if senderEmail}}
              <div class="contact-item">
                <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                  <polyline points="22,6 12,13 2,6"/>
                </svg>
                <a href="mailto:{{senderEmail}}" class="contact-link">{{senderEmail}}</a>
              </div>
              {{/if}}
              
              {{#if senderPhone}}
              <div class="contact-item">
                <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                </svg>
                <span class="contact-text">{{senderPhone}}</span>
              </div>
              {{/if}}
            </div>
          </div>
          {{/if}}
        </div>
      </div>
    </div>

    <!-- ═══════════════════════════════════════════════════════════
         FOOTER SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="email-footer">
      <p class="footer-note">This email was composed for professional communication purposes.</p>
    </div>
  </div>
</body>
</html>
