<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{subject}}</title>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - LETTER FORMAL: PROFESSIONAL
       General Business & Professional Correspondence
       Includes: Business inquiry, Proposal, Complaint, Request,
                 Acknowledgment, Apology, Confirmation, Invitation,
                 Announcement, Appreciation, Introduction, Follow-up,
                 Termination, Warning, General
    ═══════════════════════════════════════════════════════════════ */
    
    /* CSS Custom Properties */
    :root {
      --accent-primary: {{#if accentColor}}{{accentColor}}{{else}}#1e3a5f{{/if}};
      --accent-dark: {{#if accentColor}}{{accentColor}}{{else}}#152a45{{/if}};
      --accent-light: #f0f4f8;
      --accent-border: #d1dce8;
      --text-primary: #1a1a1a;
      --text-secondary: #475569;
      --text-muted: #64748b;
      --text-light: #94a3b8;
      --bg-white: #ffffff;
      --bg-light: #f8f9fa;
      --bg-page: #f1f5f9;
      --border-light: #e2e8f0;
      --border-medium: #cbd5e1;
      --warning-red: #dc2626;
      --success-green: #059669;
      --info-blue: #0284c7;
    }

    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      -webkit-print-color-adjust: exact !important;
      print-color-adjust: exact !important;
    }

    body {
      font-family: 'Georgia', 'Times New Roman', serif;
      font-size: 11pt;
      line-height: 1.7;
      color: var(--text-primary);
      background: var(--bg-page);
      padding: 20px;
    }

    /* Letter Container - A4 Style */
    .letter-container {
      max-width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: var(--bg-white);
      border-radius: 4px;
      overflow: hidden;
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1);
      border: 1px solid var(--border-light);
      padding: 0;
    }

    /* ─────────────────────────────────────────────────────────────
       Letterhead Section
    ───────────────────────────────────────────────────────────── */
    {{#if letterhead}}
    .letterhead {
      background: linear-gradient(135deg, var(--accent-primary) 0%, var(--accent-dark) 100%);
      padding: 28px 40px;
      border-bottom: 4px solid var(--accent-border);
    }

    .letterhead-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .company-info {
      display: flex;
      align-items: center;
      gap: 18px;
    }

    .company-logo {
      width: 60px;
      height: 60px;
      background: var(--bg-white);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 8px;
    }

    .company-logo img {
      max-width: 100%;
      max-height: 100%;
      object-fit: contain;
    }

    .company-logo-placeholder {
      width: 44px;
      height: 44px;
      fill: var(--accent-primary);
    }

    .company-details {
      color: #ffffff;
    }

    .company-name {
      font-family: 'Arial', sans-serif;
      font-size: 18pt;
      font-weight: 700;
      letter-spacing: 0.5px;
      margin-bottom: 4px;
    }

    .company-tagline {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      opacity: 0.85;
      letter-spacing: 0.5px;
    }

    .letterhead-contact {
      text-align: right;
      font-family: 'Arial', sans-serif;
      font-size: 8.5pt;
      color: rgba(255, 255, 255, 0.9);
      line-height: 1.6;
    }

    .letterhead-contact a {
      color: rgba(255, 255, 255, 0.9);
      text-decoration: none;
    }
    {{/if}}

    /* ─────────────────────────────────────────────────────────────
       Letter Body Section
    ───────────────────────────────────────────────────────────── */
    .letter-body {
      padding: 40px;
    }

    /* Date & Reference Section */
    .letter-header {
      margin-bottom: 30px;
    }

    .letter-date {
      font-family: 'Arial', sans-serif;
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 8px;
      {{#ifEquals letterFormat "modified-block"}}
      text-align: right;
      {{/ifEquals}}
    }

    .letter-reference {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: var(--text-muted);
      {{#ifEquals letterFormat "modified-block"}}
      text-align: right;
      {{/ifEquals}}
    }

    .ref-label {
      font-weight: 600;
    }

    /* Letter Type Badge */
    .letter-type-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-family: 'Arial', sans-serif;
      font-size: 9px;
      font-weight: 600;
      padding: 6px 14px;
      border-radius: 4px;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 20px;
      background: var(--accent-light);
      color: var(--accent-primary);
      border: 1px solid var(--accent-border);
    }

    .letter-type-badge.complaint,
    .letter-type-badge.termination,
    .letter-type-badge.warning {
      background: #fef2f2;
      color: var(--warning-red);
      border-color: #fecaca;
    }

    .letter-type-badge.appreciation,
    .letter-type-badge.acknowledgment,
    .letter-type-badge.confirmation {
      background: #ecfdf5;
      color: var(--success-green);
      border-color: #a7f3d0;
    }

    .letter-type-badge.invitation,
    .letter-type-badge.announcement {
      background: #f0f9ff;
      color: var(--info-blue);
      border-color: #bae6fd;
    }

    .badge-icon {
      width: 14px;
      height: 14px;
      fill: currentColor;
    }

    /* Recipient Address Block */
    .recipient-block {
      margin-bottom: 28px;
      line-height: 1.6;
    }

    .recipient-name {
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 2px;
    }

    .recipient-designation {
      color: var(--text-secondary);
      font-size: 10.5pt;
    }

    .recipient-company {
      color: var(--text-secondary);
      font-size: 10.5pt;
      font-weight: 500;
    }

    .recipient-address {
      color: var(--text-muted);
      font-size: 10pt;
      margin-top: 6px;
    }

    /* Subject Line */
    .subject-line {
      margin-bottom: 24px;
      padding: 12px 16px;
      background: var(--accent-light);
      border-left: 4px solid var(--accent-primary);
      border-radius: 0 4px 4px 0;
    }

    .subject-label {
      font-family: 'Arial', sans-serif;
      font-size: 8pt;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 4px;
    }

    .subject-text {
      font-weight: 600;
      color: var(--accent-primary);
      font-size: 11pt;
    }

    /* In Response To */
    .in-response-to {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: var(--text-muted);
      margin-bottom: 20px;
      font-style: italic;
    }

    /* Salutation */
    .salutation {
      font-size: 11pt;
      color: var(--text-primary);
      margin-bottom: 20px;
    }

    /* Letter Content */
    .letter-content {
      text-align: justify;
      margin-bottom: 24px;
      white-space: pre-line;
      {{#ifEquals letterFormat "semi-block"}}
      text-indent: 2em;
      {{/ifEquals}}
    }

    .letter-content p {
      margin-bottom: 16px;
      {{#ifEquals letterFormat "semi-block"}}
      text-indent: 2em;
      {{/ifEquals}}
    }

    /* Key Points Box */
    {{#if professional.keyPoints}}
    .key-points-box {
      background: var(--bg-light);
      border: 1px solid var(--border-light);
      border-radius: 6px;
      padding: 20px 24px;
      margin: 24px 0;
    }

    .key-points-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 12px;
    }

    .key-points-icon {
      width: 18px;
      height: 18px;
      fill: var(--accent-primary);
    }

    .key-points-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: var(--accent-primary);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .key-points-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.7;
      white-space: pre-line;
    }
    {{/if}}

    /* Action Required Box */
    {{#if professional.actionRequired}}
    .action-box {
      background: #fffbeb;
      border: 1px solid #fde68a;
      border-left: 4px solid #d97706;
      border-radius: 0 6px 6px 0;
      padding: 18px 22px;
      margin: 24px 0;
    }

    .action-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 10px;
    }

    .action-icon {
      width: 18px;
      height: 18px;
      fill: #d97706;
    }

    .action-title {
      font-family: 'Arial', sans-serif;
      font-size: 10px;
      font-weight: 700;
      color: #92400e;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .action-content {
      font-size: 10.5pt;
      color: var(--text-primary);
      line-height: 1.6;
    }

    .action-deadline {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: #b45309;
      font-weight: 600;
      margin-top: 10px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .deadline-icon {
      width: 14px;
      height: 14px;
      stroke: #b45309;
    }
    {{/if}}

    /* Attachments Section */
    {{#if professional.attachments}}
    .attachments-box {
      background: var(--bg-light);
      border: 1px solid var(--border-light);
      border-radius: 6px;
      padding: 16px 20px;
      margin: 24px 0;
    }

    .attachments-header {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-bottom: 10px;
    }

    .attachments-icon {
      width: 16px;
      height: 16px;
      stroke: var(--text-muted);
    }

    .attachments-title {
      font-family: 'Arial', sans-serif;
      font-size: 9px;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .attachments-list {
      font-size: 10pt;
      color: var(--text-secondary);
      white-space: pre-line;
      line-height: 1.7;
    }
    {{/if}}

    /* ─────────────────────────────────────────────────────────────
       Closing & Signature Section
    ───────────────────────────────────────────────────────────── */
    .closing-section {
      margin-top: 36px;
      {{#ifEquals letterFormat "modified-block"}}
      margin-left: 50%;
      {{/ifEquals}}
    }

    .closing-phrase {
      font-style: italic;
      margin-bottom: 40px;
      color: var(--text-primary);
    }

    .signature-block {
      margin-top: 10px;
    }

    .signature-image {
      max-height: 60px;
      margin-bottom: 8px;
    }

    .sender-name {
      font-size: 12pt;
      font-weight: 700;
      color: var(--accent-primary);
      margin-bottom: 4px;
      font-family: 'Arial', sans-serif;
    }

    .sender-designation {
      font-size: 10pt;
      color: var(--text-secondary);
      margin-bottom: 2px;
    }

    .sender-department {
      font-size: 9.5pt;
      color: var(--text-muted);
      margin-bottom: 2px;
    }

    .sender-company {
      font-size: 10pt;
      color: var(--text-secondary);
      font-weight: 500;
    }

    /* Extended Contact in Signature */
    {{#if includeSignature}}
    .signature-contact {
      margin-top: 14px;
      padding-top: 12px;
      border-top: 1px dashed var(--border-light);
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
    }

    .contact-item {
      display: flex;
      align-items: center;
      margin-bottom: 4px;
      color: var(--text-muted);
    }

    .contact-item:last-child {
      margin-bottom: 0;
    }

    .contact-icon {
      width: 12px;
      height: 12px;
      margin-right: 8px;
      stroke: var(--text-light);
    }

    .contact-link {
      color: var(--accent-primary);
      text-decoration: none;
    }

    .contact-link:hover {
      text-decoration: underline;
    }
    {{/if}}

    /* ─────────────────────────────────────────────────────────────
       Footer Section (No Letterhead Alternative)
    ───────────────────────────────────────────────────────────── */
    {{#unless letterhead}}
    .letter-footer {
      margin-top: 40px;
      padding-top: 20px;
      border-top: 1px solid var(--border-light);
    }

    .sender-address-block {
      font-family: 'Arial', sans-serif;
      font-size: 9pt;
      color: var(--text-muted);
      line-height: 1.6;
    }
    {{/unless}}

    /* ─────────────────────────────────────────────────────────────
       Print Styles
    ───────────────────────────────────────────────────────────── */
    @media print {
      body {
        background: #ffffff;
        padding: 0;
      }

      .letter-container {
        box-shadow: none;
        border-radius: 0;
        border: none;
        max-width: 100%;
        min-height: auto;
      }

      .letterhead,
      .letter-type-badge,
      .subject-line,
      .key-points-box,
      .action-box,
      .attachments-box {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }

      @page {
        margin: 15mm;
        size: A4;
      }
    }

    /* ─────────────────────────────────────────────────────────────
       Responsive Styles
    ───────────────────────────────────────────────────────────── */
    @media screen and (max-width: 640px) {
      body {
        padding: 10px;
      }

      .letter-body {
        padding: 24px 20px;
      }

      {{#if letterhead}}
      .letterhead {
        padding: 20px;
      }

      .letterhead-content {
        flex-direction: column;
        gap: 16px;
        text-align: center;
      }

      .company-info {
        flex-direction: column;
      }

      .letterhead-contact {
        text-align: center;
      }
      {{/if}}

      .closing-section {
        margin-left: 0 !important;
      }
    }
  </style>
</head>
<body>
  <div class="letter-container">
    <!-- ═══════════════════════════════════════════════════════════
         LETTERHEAD SECTION (Conditional)
    ════════════════════════════════════════════════════════════ -->
    {{#if letterhead}}
    <div class="letterhead">
      <div class="letterhead-content">
        <div class="company-info">
          <div class="company-logo">
            {{#if companyLogo}}
            <img src="{{companyLogo}}" alt="Company Logo">
            {{else}}
            <svg class="company-logo-placeholder" viewBox="0 0 24 24">
              <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
            </svg>
            {{/if}}
          </div>
          <div class="company-details">
            <div class="company-name">{{senderCompany}}</div>
            {{#if senderDepartment}}
            <div class="company-tagline">{{senderDepartment}}</div>
            {{/if}}
          </div>
        </div>
        <div class="letterhead-contact">
          {{#if senderAddress}}{{senderAddress}}<br>{{/if}}
          {{#if senderCity}}{{senderCity}}{{#if senderState}}, {{senderState}}{{/if}} {{senderPincode}}<br>{{/if}}
          {{#if senderPhone}}{{senderPhone}}<br>{{/if}}
          {{#if senderEmail}}<a href="mailto:{{senderEmail}}">{{senderEmail}}</a><br>{{/if}}
          {{#if senderWebsite}}<a href="{{senderWebsite}}">{{senderWebsite}}</a>{{/if}}
        </div>
      </div>
    </div>
    {{/if}}

    <!-- ═══════════════════════════════════════════════════════════
         LETTER BODY SECTION
    ════════════════════════════════════════════════════════════ -->
    <div class="letter-body">
      <!-- Date & Reference -->
      <div class="letter-header">
        <div class="letter-date">{{letterDate}}</div>
        {{#if referenceNumber}}
        <div class="letter-reference">
          <span class="ref-label">Ref:</span> {{referenceNumber}}
        </div>
        {{/if}}
      </div>

      <!-- Letter Type Badge -->
      {{#if professional.letterType}}
      <div class="letter-type-badge {{professional.letterType}}">
        <svg class="badge-icon" viewBox="0 0 24 24">
          {{#ifEquals professional.letterType "business-inquiry"}}
          <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3" fill="none" stroke="currentColor" stroke-width="2"/><line x1="12" y1="17" x2="12.01" y2="17" stroke="currentColor" stroke-width="2"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "business-proposal"}}
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "complaint"}}
          <circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "request"}}
          <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "acknowledgment"}}
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "apology"}}
          <circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2" transform="rotate(180 12 14)"/><line x1="9" y1="9" x2="9.01" y2="9"/><line x1="15" y1="9" x2="15.01" y2="9"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "confirmation"}}
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/><path d="M9 16l2 2 4-4"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "invitation"}}
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "announcement"}}
          <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/><line x1="9" y1="9" x2="15" y2="9"/><line x1="9" y1="13" x2="13" y2="13"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "appreciation"}}
          <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "introduction"}}
          <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "follow-up"}}
          <polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "termination"}}
          <circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "warning"}}
          <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>
          {{/ifEquals}}
          {{#ifEquals professional.letterType "general"}}
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/>
          {{/ifEquals}}
        </svg>
        {{#ifEquals professional.letterType "business-inquiry"}}Business Inquiry{{/ifEquals}}
        {{#ifEquals professional.letterType "business-proposal"}}Business Proposal{{/ifEquals}}
        {{#ifEquals professional.letterType "complaint"}}Complaint{{/ifEquals}}
        {{#ifEquals professional.letterType "request"}}Request{{/ifEquals}}
        {{#ifEquals professional.letterType "acknowledgment"}}Acknowledgment{{/ifEquals}}
        {{#ifEquals professional.letterType "apology"}}Apology{{/ifEquals}}
        {{#ifEquals professional.letterType "confirmation"}}Confirmation{{/ifEquals}}
        {{#ifEquals professional.letterType "invitation"}}Invitation{{/ifEquals}}
        {{#ifEquals professional.letterType "announcement"}}Announcement{{/ifEquals}}
        {{#ifEquals professional.letterType "appreciation"}}Appreciation{{/ifEquals}}
        {{#ifEquals professional.letterType "introduction"}}Introduction{{/ifEquals}}
        {{#ifEquals professional.letterType "follow-up"}}Follow-up{{/ifEquals}}
        {{#ifEquals professional.letterType "termination"}}Termination{{/ifEquals}}
        {{#ifEquals professional.letterType "warning"}}Warning{{/ifEquals}}
        {{#ifEquals professional.letterType "general"}}General Letter{{/ifEquals}}
      </div>
      {{/if}}

      <!-- Recipient Address Block -->
      <div class="recipient-block">
        <div class="recipient-name">{{recipientName}}</div>
        {{#if recipientDesignation}}
        <div class="recipient-designation">{{recipientDesignation}}</div>
        {{/if}}
        {{#if recipientCompany}}
        <div class="recipient-company">{{recipientCompany}}</div>
        {{/if}}
        {{#if recipientDepartment}}
        <div class="recipient-designation">{{recipientDepartment}}</div>
        {{/if}}
        <div class="recipient-address">
          {{#if recipientAddress}}{{recipientAddress}}<br>{{/if}}
          {{#if recipientCity}}{{recipientCity}}{{#if recipientState}}, {{recipientState}}{{/if}} {{recipientPincode}}{{/if}}
          {{#if recipientCountry}}<br>{{recipientCountry}}{{/if}}
        </div>
      </div>

      <!-- Subject Line -->
      {{#if subject}}
      <div class="subject-line">
        <div class="subject-label">Subject</div>
        <div class="subject-text">{{subject}}</div>
      </div>
      {{/if}}

      <!-- In Response To -->
      {{#if professional.inResponseTo}}
      <div class="in-response-to">
        In response to: {{professional.inResponseTo}}
      </div>
      {{/if}}

      <!-- Salutation -->
      <div class="salutation">
        {{salutation}} {{recipientName}},
      </div>

      <!-- Letter Content -->
      <div class="letter-content">
        {{{letterBody}}}
      </div>

      <!-- Key Points Box -->
      {{#if professional.keyPoints}}
      <div class="key-points-box">
        <div class="key-points-header">
          <svg class="key-points-icon" viewBox="0 0 24 24">
            <line x1="8" y1="6" x2="21" y2="6"/>
            <line x1="8" y1="12" x2="21" y2="12"/>
            <line x1="8" y1="18" x2="21" y2="18"/>
            <line x1="3" y1="6" x2="3.01" y2="6"/>
            <line x1="3" y1="12" x2="3.01" y2="12"/>
            <line x1="3" y1="18" x2="3.01" y2="18"/>
          </svg>
          <span class="key-points-title">Key Points</span>
        </div>
        <div class="key-points-content">
          {{{professional.keyPoints}}}
        </div>
      </div>
      {{/if}}

      <!-- Action Required Box -->
      {{#if professional.actionRequired}}
      <div class="action-box">
        <div class="action-header">
          <svg class="action-icon" viewBox="0 0 24 24">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
            <polyline points="22 4 12 14.01 9 11.01"/>
          </svg>
          <span class="action-title">Action Required</span>
        </div>
        <div class="action-content">
          {{{professional.actionRequired}}}
        </div>
        {{#if professional.deadline}}
        <div class="action-deadline">
          <svg class="deadline-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <polyline points="12 6 12 12 16 14"/>
          </svg>
          Deadline: {{professional.deadline}}
        </div>
        {{/if}}
      </div>
      {{/if}}

      <!-- Attachments Section -->
      {{#if professional.attachments}}
      <div class="attachments-box">
        <div class="attachments-header">
          <svg class="attachments-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48"/>
          </svg>
          <span class="attachments-title">Enclosures / Attachments</span>
        </div>
        <div class="attachments-list">
          {{{professional.attachments}}}
        </div>
      </div>
      {{/if}}

      <!-- ─────────────────────────────────────────────────────────
           Closing & Signature Section
      ────────────────────────────────────────────────────────── -->
      <div class="closing-section">
        <div class="closing-phrase">{{closing}},</div>
        
        <div class="signature-block">
          {{#if signatureImage}}
          <img src="{{signatureImage}}" alt="Signature" class="signature-image">
          {{/if}}
          
          <div class="sender-name">{{senderName}}</div>
          
          {{#if senderDesignation}}
          <div class="sender-designation">{{senderDesignation}}</div>
          {{/if}}
          
          {{#if senderDepartment}}
          <div class="sender-department">{{senderDepartment}}</div>
          {{/if}}
          
          {{#if senderCompany}}
          <div class="sender-company">{{senderCompany}}</div>
          {{/if}}

          {{#if includeSignature}}
          <div class="signature-contact">
            {{#if senderEmail}}
            <div class="contact-item">
              <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <polyline points="22,6 12,13 2,6"/>
              </svg>
              <a href="mailto:{{senderEmail}}" class="contact-link">{{senderEmail}}</a>
            </div>
            {{/if}}
            
            {{#if senderPhone}}
            <div class="contact-item">
              <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
              </svg>
              <span>{{senderPhone}}</span>
            </div>
            {{/if}}

            {{#if senderWebsite}}
            <div class="contact-item">
              <svg class="contact-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <line x1="2" y1="12" x2="22" y2="12"/>
                <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
              </svg>
              <a href="{{senderWebsite}}" class="contact-link">{{senderWebsite}}</a>
            </div>
            {{/if}}
          </div>
          {{/if}}
        </div>
      </div>

      <!-- Footer for Non-Letterhead Letters -->
      {{#unless letterhead}}
      <div class="letter-footer">
        <div class="sender-address-block">
          {{#if senderAddress}}{{senderAddress}}<br>{{/if}}
          {{#if senderCity}}{{senderCity}}{{#if senderState}}, {{senderState}}{{/if}} {{senderPincode}}<br>{{/if}}
          {{#if senderCountry}}{{senderCountry}}{{/if}}
        </div>
      </div>
      {{/unless}}
    </div>
  </div>
</body>
</html>
