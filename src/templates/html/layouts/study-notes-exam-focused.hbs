<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}} - Exam Notes</title>
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script src="https://polyfill.io/v3/polyfill.min.js?features=es6"></script>
  <script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
  <style>
    /* ═══════════════════════════════════════════════════════════════
       SORIVA - STUDY NOTES TEMPLATE
       Style: Exam-Focused
       Purpose: Board Exams, JEE, NEET, UPSC, SSC, Banking
       Features: PYQs, Marks, Weightage, Important Tags, Quick Revision
       "Exam Ready - Score Maximum!"
       ═══════════════════════════════════════════════════════════════ */
    
    :root {
      /* Exam Theme - Professional Blue */
      --primary: #1e3a5f;
      --primary-light: #2d5a87;
      --secondary: #0ea5e9;
      --secondary-light: #7dd3fc;
      
      /* Importance Colors */
      --critical: #dc2626;
      --critical-light: #fecaca;
      --critical-bg: #fef2f2;
      --important: #ea580c;
      --important-light: #fed7aa;
      --important-bg: #fff7ed;
      --moderate: #0891b2;
      --moderate-light: #a5f3fc;
      --moderate-bg: #ecfeff;
      
      /* Marks/Score Colors */
      --marks-high: #16a34a;
      --marks-medium: #ca8a04;
      --marks-low: #64748b;
      
      /* PYQ Colors */
      --pyq: #7c3aed;
      --pyq-light: #ddd6fe;
      --pyq-bg: #f5f3ff;
      
      /* Base Colors */
      --bg: #ffffff;
      --bg-subtle: #f8fafc;
      --bg-muted: #f1f5f9;
      --text: #0f172a;
      --text-secondary: #475569;
      --text-muted: #94a3b8;
      --border: #e2e8f0;
      --border-dark: #cbd5e1;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'IBM Plex Sans', -apple-system, sans-serif;
      background: linear-gradient(135deg, #e0e7ff 0%, #e0f2fe 100%);
      color: var(--text);
      line-height: 1.7;
      padding: 30px;
      font-size: 14px;
    }

    /* ═══════════════════════════════════════════════════════════════
       MAIN CONTAINER
       ═══════════════════════════════════════════════════════════════ */
    
    .notes-container {
      width: 210mm;
      min-height: 297mm;
      margin: 0 auto;
      background: var(--bg);
      box-shadow: 0 10px 40px rgba(30, 58, 95, 0.15);
      position: relative;
      overflow: hidden;
    }

    .page-break {
      page-break-before: always;
      padding-top: 30px;
    }

    /* ═══════════════════════════════════════════════════════════════
       HEADER - Exam Style
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showHeader}}
    .header {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
      color: white;
      padding: 25px 40px;
      position: relative;
    }

    .header::before {
      content: '';
      position: absolute;
      top: 0;
      right: 0;
      width: 200px;
      height: 100%;
      background: linear-gradient(135deg, transparent 0%, rgba(255,255,255,0.05) 100%);
    }

    .header-top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 15px;
      position: relative;
      z-index: 1;
    }

    .exam-info {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }

    .exam-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 14px;
      background: var(--secondary);
      border-radius: 4px;
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .exam-badge i {
      font-size: 12px;
    }

    .subject-badge {
      padding: 6px 14px;
      background: rgba(255,255,255,0.15);
      border-radius: 4px;
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .weightage-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 8px 16px;
      background: var(--marks-high);
      border-radius: 4px;
      font-size: 13px;
      font-weight: 700;
    }

    .weightage-badge i {
      font-size: 14px;
    }

    .header-main {
      position: relative;
      z-index: 1;
    }

    .notes-title {
      font-size: 26px;
      font-weight: 700;
      margin-bottom: 8px;
      line-height: 1.3;
    }

    .notes-subtitle {
      font-size: 14px;
      opacity: 0.9;
      margin-bottom: 15px;
    }

    .header-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      font-size: 12px;
      opacity: 0.9;
    }

    .meta-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .meta-item i {
      font-size: 11px;
      opacity: 0.8;
    }

    /* Difficulty Indicator */
    .difficulty-meter {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 8px 15px;
      background: rgba(0,0,0,0.2);
      border-radius: 4px;
      margin-top: 15px;
    }

    .difficulty-label {
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .difficulty-bars {
      display: flex;
      gap: 3px;
    }

    .difficulty-bar {
      width: 20px;
      height: 6px;
      background: rgba(255,255,255,0.3);
      border-radius: 2px;
    }

    .difficulty-bar.active { background: #fbbf24; }
    .difficulty-bar.active.hard { background: #ef4444; }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       EXAM STATS BAR
       ═══════════════════════════════════════════════════════════════ */
    
    .exam-stats-bar {
      display: flex;
      background: var(--bg-muted);
      border-bottom: 1px solid var(--border);
    }

    .exam-stat {
      flex: 1;
      padding: 15px 20px;
      text-align: center;
      border-right: 1px solid var(--border);
    }

    .exam-stat:last-child {
      border-right: none;
    }

    .exam-stat-value {
      font-size: 20px;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 2px;
    }

    .exam-stat-label {
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-muted);
    }

    /* ═══════════════════════════════════════════════════════════════
       MAIN CONTENT
       ═══════════════════════════════════════════════════════════════ */
    
    .notes-body {
      padding: 30px 40px;
    }

    /* ═══════════════════════════════════════════════════════════════
       IMPORTANCE LEGEND
       ═══════════════════════════════════════════════════════════════ */
    
    .importance-legend {
      display: flex;
      gap: 20px;
      padding: 12px 20px;
      background: var(--bg-subtle);
      border: 1px solid var(--border);
      border-radius: 6px;
      margin-bottom: 25px;
      font-size: 11px;
    }

    .legend-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .legend-dot {
      width: 10px;
      height: 10px;
      border-radius: 2px;
    }

    .legend-dot.critical { background: var(--critical); }
    .legend-dot.important { background: var(--important); }
    .legend-dot.moderate { background: var(--moderate); }
    .legend-dot.pyq { background: var(--pyq); }

    .legend-label {
      color: var(--text-secondary);
      font-weight: 500;
    }

    /* ═══════════════════════════════════════════════════════════════
       SECTION STYLES
       ═══════════════════════════════════════════════════════════════ */
    
    .section {
      margin-bottom: 30px;
    }

    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
      padding-bottom: 10px;
      border-bottom: 2px solid var(--primary);
    }

    .section-title-group {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .section-icon {
      width: 32px;
      height: 32px;
      background: var(--primary);
      color: white;
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
    }

    .section-title {
      font-size: 16px;
      font-weight: 700;
      color: var(--primary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .section-title-hi {
      font-size: 12px;
      color: var(--text-muted);
      font-weight: 400;
      text-transform: none;
      margin-left: 8px;
    }

    .section-marks {
      display: flex;
      align-items: center;
      gap: 6px;
      padding: 5px 12px;
      background: var(--marks-high);
      color: white;
      border-radius: 4px;
      font-size: 11px;
      font-weight: 700;
    }

    .section-marks i {
      font-size: 12px;
    }

    /* ═══════════════════════════════════════════════════════════════
       SUMMARY BOX - Exam Style
       ═══════════════════════════════════════════════════════════════ */
    
    .summary-box {
      background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
      color: white;
      padding: 20px 25px;
      border-radius: 8px;
      margin-bottom: 25px;
      position: relative;
      overflow: hidden;
    }

    .summary-box::before {
      content: 'SUMMARY';
      position: absolute;
      top: 10px;
      right: 15px;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 1px;
      opacity: 0.5;
    }

    .summary-box p {
      font-size: 14px;
      line-height: 1.8;
    }

    /* ═══════════════════════════════════════════════════════════════
       KEY POINTS - With Importance Markers
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showKeyPoints}}
    .key-points-list {
      list-style: none;
    }

    .key-point-item {
      display: flex;
      gap: 15px;
      padding: 15px;
      margin-bottom: 10px;
      border-radius: 6px;
      border-left: 4px solid var(--border);
      background: var(--bg-subtle);
      transition: all 0.2s;
    }

    .key-point-item:hover {
      background: var(--bg-muted);
    }

    .key-point-item.critical {
      border-left-color: var(--critical);
      background: var(--critical-bg);
    }

    .key-point-item.important {
      border-left-color: var(--important);
      background: var(--important-bg);
    }

    .key-point-item.moderate {
      border-left-color: var(--moderate);
      background: var(--moderate-bg);
    }

    .key-point-marker {
      width: 28px;
      height: 28px;
      border-radius: 4px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      font-weight: 700;
      color: white;
      flex-shrink: 0;
      background: var(--text-muted);
    }

    .key-point-item.critical .key-point-marker { background: var(--critical); }
    .key-point-item.important .key-point-marker { background: var(--important); }
    .key-point-item.moderate .key-point-marker { background: var(--moderate); }

    .key-point-content {
      flex: 1;
    }

    .key-point-text {
      font-size: 14px;
      color: var(--text);
      font-weight: 500;
      margin-bottom: 5px;
    }

    .key-point-explanation {
      font-size: 13px;
      color: var(--text-secondary);
    }

    .key-point-tags {
      display: flex;
      gap: 8px;
      margin-top: 8px;
      flex-wrap: wrap;
    }

    .exam-tag {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      padding: 3px 10px;
      background: var(--pyq-light);
      color: var(--pyq);
      border-radius: 3px;
      font-size: 10px;
      font-weight: 600;
    }

    .marks-tag {
      padding: 3px 10px;
      background: rgba(22, 163, 74, 0.1);
      color: var(--marks-high);
      border-radius: 3px;
      font-size: 10px;
      font-weight: 600;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       DEFINITIONS - Exam Style
       ═══════════════════════════════════════════════════════════════ */
    
    .definition-card {
      padding: 18px;
      margin-bottom: 12px;
      border: 1px solid var(--border);
      border-radius: 6px;
      position: relative;
    }

    .definition-card.frequently-asked {
      border-color: var(--pyq);
      background: var(--pyq-bg);
    }

    .definition-card.frequently-asked::before {
      content: 'FREQUENTLY ASKED';
      position: absolute;
      top: -8px;
      right: 15px;
      padding: 2px 10px;
      background: var(--pyq);
      color: white;
      font-size: 9px;
      font-weight: 700;
      letter-spacing: 0.5px;
      border-radius: 3px;
    }

    .definition-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 10px;
    }

    .definition-term {
      font-size: 16px;
      font-weight: 700;
      color: var(--primary);
    }

    .definition-term-hi {
      font-size: 13px;
      color: var(--text-muted);
      font-weight: 400;
      margin-left: 8px;
    }

    .definition-marks {
      padding: 3px 10px;
      background: var(--primary);
      color: white;
      border-radius: 3px;
      font-size: 10px;
      font-weight: 700;
    }

    .definition-meaning {
      font-size: 14px;
      color: var(--text-secondary);
      line-height: 1.7;
      margin-bottom: 10px;
    }

    .definition-example {
      font-size: 13px;
      color: var(--text-muted);
      padding: 10px 15px;
      background: var(--bg-subtle);
      border-radius: 4px;
      border-left: 3px solid var(--secondary);
    }

    /* ═══════════════════════════════════════════════════════════════
       FORMULAS - Exam Priority
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showFormulas}}
    .formula-card {
      border: 1px solid var(--border);
      border-radius: 8px;
      margin-bottom: 15px;
      overflow: hidden;
    }

    .formula-card.must-remember {
      border-color: var(--critical);
    }

    .formula-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 18px;
      background: var(--bg-muted);
      border-bottom: 1px solid var(--border);
    }

    .formula-card.must-remember .formula-header {
      background: var(--critical-bg);
    }

    .formula-name {
      font-size: 14px;
      font-weight: 700;
      color: var(--text);
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .formula-priority {
      padding: 4px 12px;
      border-radius: 3px;
      font-size: 10px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .formula-priority.must { background: var(--critical); color: white; }
    .formula-priority.important { background: var(--important); color: white; }
    .formula-priority.good { background: var(--moderate); color: white; }

    .formula-body {
      padding: 20px;
    }

    .formula-display {
      font-family: 'JetBrains Mono', monospace;
      font-size: 22px;
      font-weight: 600;
      text-align: center;
      padding: 25px;
      background: linear-gradient(135deg, var(--bg-subtle) 0%, var(--bg-muted) 100%);
      border-radius: 6px;
      color: var(--primary);
      margin-bottom: 15px;
      border: 2px dashed var(--border);
    }

    .formula-card.must-remember .formula-display {
      border-color: var(--critical-light);
      background: linear-gradient(135deg, #fff5f5 0%, #fef2f2 100%);
    }

    .formula-description {
      font-size: 13px;
      color: var(--text-secondary);
      text-align: center;
      margin-bottom: 15px;
    }

    .formula-variables {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      justify-content: center;
      padding: 15px;
      background: var(--bg-subtle);
      border-radius: 6px;
    }

    .variable-chip {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 12px;
      background: white;
      border: 1px solid var(--border);
      border-radius: 4px;
      font-size: 12px;
    }

    .variable-symbol {
      font-family: 'JetBrains Mono', monospace;
      font-weight: 600;
      color: var(--primary);
    }

    .variable-name {
      color: var(--text-secondary);
    }

    .variable-unit {
      color: var(--text-muted);
      font-size: 11px;
    }

    .formula-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 18px;
      background: var(--bg-subtle);
      border-top: 1px solid var(--border);
      font-size: 11px;
    }

    .formula-pyq {
      display: flex;
      align-items: center;
      gap: 6px;
      color: var(--pyq);
      font-weight: 600;
    }

    .formula-conditions {
      color: var(--text-muted);
      font-style: italic;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       THEOREMS & LAWS
       ═══════════════════════════════════════════════════════════════ */
    
    .theorem-card {
      border: 1px solid var(--border);
      border-radius: 8px;
      margin-bottom: 15px;
      overflow: hidden;
    }

    .theorem-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 20px;
      background: var(--primary);
      color: white;
    }

    .theorem-name {
      font-size: 15px;
      font-weight: 700;
    }

    .theorem-marks-badge {
      padding: 4px 12px;
      background: rgba(255,255,255,0.2);
      border-radius: 3px;
      font-size: 11px;
      font-weight: 600;
    }

    .theorem-body {
      padding: 20px;
    }

    .theorem-statement {
      font-size: 14px;
      color: var(--text);
      line-height: 1.8;
      padding: 15px 20px;
      background: var(--bg-subtle);
      border-left: 4px solid var(--primary);
      border-radius: 0 6px 6px 0;
      margin-bottom: 15px;
    }

    .theorem-section {
      margin-bottom: 15px;
    }

    .theorem-section-title {
      font-size: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-muted);
      margin-bottom: 8px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .theorem-section-content {
      font-size: 13px;
      color: var(--text-secondary);
      line-height: 1.7;
    }

    .theorem-applications-list {
      list-style: none;
    }

    .theorem-application-item {
      padding: 6px 0 6px 20px;
      position: relative;
      font-size: 13px;
      color: var(--text-secondary);
    }

    .theorem-application-item::before {
      content: '→';
      position: absolute;
      left: 0;
      color: var(--primary);
      font-weight: 600;
    }

    /* ═══════════════════════════════════════════════════════════════
       CHEMICAL REACTIONS
       ═══════════════════════════════════════════════════════════════ */
    
    .reaction-card {
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 15px;
      position: relative;
    }

    .reaction-card.important-reaction {
      border-color: var(--important);
      background: var(--important-bg);
    }

    .reaction-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
    }

    .reaction-name {
      font-size: 14px;
      font-weight: 700;
      color: var(--primary);
    }

    .reaction-tags {
      display: flex;
      gap: 8px;
    }

    .reaction-type-badge {
      padding: 4px 10px;
      background: var(--secondary);
      color: white;
      border-radius: 3px;
      font-size: 10px;
      font-weight: 600;
    }

    .reaction-equation {
      font-family: 'JetBrains Mono', monospace;
      font-size: 18px;
      text-align: center;
      padding: 20px;
      background: white;
      border: 1px solid var(--border);
      border-radius: 6px;
      color: var(--text);
      margin-bottom: 12px;
    }

    .reaction-details {
      display: flex;
      gap: 20px;
      font-size: 12px;
      color: var(--text-muted);
      justify-content: center;
    }

    .reaction-detail-item {
      display: flex;
      align-items: center;
      gap: 5px;
    }

    .reaction-detail-item i {
      color: var(--secondary);
    }

    /* ═══════════════════════════════════════════════════════════════
       BIOLOGICAL PROCESSES
       ═══════════════════════════════════════════════════════════════ */
    
    .bio-process-card {
      border: 1px solid var(--border);
      border-radius: 8px;
      margin-bottom: 15px;
      overflow: hidden;
    }

    .bio-process-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 20px;
      background: linear-gradient(135deg, #059669 0%, #10b981 100%);
      color: white;
    }

    .bio-process-name {
      font-size: 15px;
      font-weight: 700;
    }

    .bio-process-body {
      padding: 20px;
    }

    .bio-steps-list {
      list-style: none;
      counter-reset: biostep;
    }

    .bio-step-item {
      counter-increment: biostep;
      display: flex;
      gap: 15px;
      padding: 12px 0;
      border-bottom: 1px dashed var(--border);
    }

    .bio-step-item:last-child {
      border-bottom: none;
    }

    .bio-step-num {
      width: 28px;
      height: 28px;
      background: #10b981;
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      font-weight: 700;
      flex-shrink: 0;
    }

    .bio-step-num::before {
      content: counter(biostep);
    }

    .bio-step-text {
      font-size: 14px;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    .bio-io-container {
      display: flex;
      gap: 15px;
      margin-top: 15px;
      padding-top: 15px;
      border-top: 1px solid var(--border);
    }

    .bio-io-box {
      flex: 1;
      padding: 15px;
      border-radius: 6px;
    }

    .bio-io-box.inputs {
      background: #dbeafe;
      border: 1px solid #93c5fd;
    }

    .bio-io-box.outputs {
      background: #dcfce7;
      border: 1px solid #86efac;
    }

    .bio-io-label {
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 6px;
    }

    .bio-io-box.inputs .bio-io-label { color: #1d4ed8; }
    .bio-io-box.outputs .bio-io-label { color: #15803d; }

    .bio-io-items {
      font-size: 13px;
      color: var(--text-secondary);
    }

    /* ═══════════════════════════════════════════════════════════════
       TIMELINE
       ═══════════════════════════════════════════════════════════════ */
    
    .timeline {
      position: relative;
      padding-left: 35px;
    }

    .timeline::before {
      content: '';
      position: absolute;
      left: 12px;
      top: 0;
      bottom: 0;
      width: 3px;
      background: var(--primary);
    }

    .timeline-item {
      position: relative;
      margin-bottom: 20px;
      padding: 15px 20px;
      background: var(--bg-subtle);
      border-radius: 6px;
      border: 1px solid var(--border);
    }

    .timeline-item::before {
      content: '';
      position: absolute;
      left: -29px;
      top: 20px;
      width: 12px;
      height: 12px;
      background: var(--secondary);
      border: 3px solid white;
      border-radius: 50%;
      box-shadow: 0 0 0 2px var(--primary);
    }

    .timeline-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 8px;
    }

    .timeline-year {
      font-family: 'JetBrains Mono', monospace;
      font-size: 14px;
      font-weight: 700;
      color: var(--primary);
      padding: 3px 12px;
      background: var(--secondary-light);
      border-radius: 3px;
    }

    .timeline-pyq {
      font-size: 10px;
      color: var(--pyq);
      font-weight: 600;
    }

    .timeline-event {
      font-size: 15px;
      font-weight: 600;
      color: var(--text);
      margin-bottom: 5px;
    }

    .timeline-description {
      font-size: 13px;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    .timeline-significance {
      margin-top: 10px;
      padding: 10px;
      background: rgba(124, 58, 237, 0.1);
      border-radius: 4px;
      font-size: 12px;
      color: var(--pyq);
    }

    .timeline-significance strong {
      font-weight: 600;
    }

    /* ═══════════════════════════════════════════════════════════════
       HISTORICAL FIGURES - One-liners Style
       ═══════════════════════════════════════════════════════════════ */
    
    .personality-card {
      display: flex;
      gap: 15px;
      padding: 15px;
      border: 1px solid var(--border);
      border-radius: 6px;
      margin-bottom: 12px;
    }

    .personality-avatar {
      width: 50px;
      height: 50px;
      background: var(--primary);
      color: white;
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 18px;
      font-weight: 700;
      flex-shrink: 0;
    }

    .personality-info {
      flex: 1;
    }

    .personality-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 5px;
    }

    .personality-name {
      font-size: 15px;
      font-weight: 700;
      color: var(--text);
    }

    .personality-years {
      font-size: 11px;
      color: var(--text-muted);
      padding: 2px 8px;
      background: var(--bg-muted);
      border-radius: 3px;
    }

    .personality-title {
      font-size: 12px;
      color: var(--secondary);
      font-weight: 500;
      margin-bottom: 8px;
    }

    .personality-one-liners {
      list-style: none;
    }

    .one-liner-item {
      font-size: 13px;
      color: var(--text-secondary);
      padding: 4px 0 4px 15px;
      position: relative;
    }

    .one-liner-item::before {
      content: '•';
      position: absolute;
      left: 0;
      color: var(--primary);
      font-weight: bold;
    }

    /* ═══════════════════════════════════════════════════════════════
       MNEMONICS - Memory Box
       ═══════════════════════════════════════════════════════════════ */
    
    .mnemonic-box {
      background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
      border: 2px solid #f59e0b;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 15px;
      text-align: center;
    }

    .mnemonic-header {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      margin-bottom: 12px;
    }

    .mnemonic-icon {
      font-size: 24px;
    }

    .mnemonic-label {
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: #92400e;
    }

    .mnemonic-topic {
      font-size: 13px;
      color: #b45309;
      margin-bottom: 10px;
    }

    .mnemonic-text {
      font-size: 24px;
      font-weight: 800;
      color: #78350f;
      letter-spacing: 3px;
      padding: 15px 20px;
      background: white;
      border-radius: 6px;
      margin-bottom: 12px;
      border: 1px dashed #f59e0b;
    }

    .mnemonic-explanation {
      font-size: 13px;
      color: #92400e;
    }

    .mnemonic-items {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 8px;
      margin-top: 12px;
    }

    .mnemonic-item {
      padding: 5px 12px;
      background: white;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 600;
      color: #92400e;
      border: 1px solid #fbbf24;
    }

    /* ═══════════════════════════════════════════════════════════════
       EXAMPLES - With Marks
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showExamples}}
    .example-card {
      border: 1px solid var(--border);
      border-radius: 8px;
      margin-bottom: 15px;
      overflow: hidden;
    }

    .example-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 12px 20px;
      background: var(--secondary);
      color: white;
    }

    .example-title {
      font-size: 14px;
      font-weight: 700;
    }

    .example-meta {
      display: flex;
      gap: 10px;
    }

    .example-difficulty {
      padding: 3px 10px;
      background: rgba(255,255,255,0.2);
      border-radius: 3px;
      font-size: 10px;
      font-weight: 600;
    }

    .example-marks {
      padding: 3px 10px;
      background: var(--marks-high);
      border-radius: 3px;
      font-size: 10px;
      font-weight: 700;
    }

    .example-body {
      padding: 20px;
    }

    .example-question {
      font-size: 14px;
      color: var(--text);
      padding: 15px;
      background: var(--bg-subtle);
      border-radius: 6px;
      margin-bottom: 20px;
      line-height: 1.7;
      border-left: 4px solid var(--secondary);
    }

    .example-solution-header {
      font-size: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-muted);
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .example-steps {
      list-style: none;
      counter-reset: step;
    }

    .example-step {
      counter-increment: step;
      display: flex;
      gap: 12px;
      padding: 10px 0;
      border-bottom: 1px dashed var(--border);
      font-size: 13px;
      color: var(--text-secondary);
    }

    .example-step:last-child {
      border-bottom: none;
    }

    .example-step::before {
      content: 'Step ' counter(step);
      font-size: 10px;
      font-weight: 700;
      color: var(--secondary);
      background: var(--bg-muted);
      padding: 3px 8px;
      border-radius: 3px;
      flex-shrink: 0;
    }

    .example-answer {
      margin-top: 15px;
      padding: 15px 20px;
      background: linear-gradient(135deg, #059669 0%, #10b981 100%);
      color: white;
      border-radius: 6px;
      font-size: 15px;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .example-answer i {
      font-size: 18px;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       TABLES
       ═══════════════════════════════════════════════════════════════ */
    
    .data-table-container {
      margin-bottom: 25px;
      overflow-x: auto;
    }

    .data-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 13px;
    }

    .data-table caption {
      font-size: 14px;
      font-weight: 700;
      color: var(--text);
      margin-bottom: 10px;
      text-align: left;
    }

    .data-table th {
      background: var(--primary);
      color: white;
      padding: 12px 15px;
      text-align: left;
      font-weight: 600;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.3px;
    }

    .data-table td {
      padding: 12px 15px;
      border-bottom: 1px solid var(--border);
      color: var(--text-secondary);
    }

    .data-table tr:nth-child(even) {
      background: var(--bg-subtle);
    }

    .data-table tr:hover {
      background: var(--bg-muted);
    }

    /* ═══════════════════════════════════════════════════════════════
       PRACTICE QUESTIONS - PYQ Style
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showPracticeQuestions}}
    .pyq-section {
      background: var(--pyq-bg);
      border: 2px solid var(--pyq);
      border-radius: 8px;
      padding: 25px;
      margin-bottom: 25px;
    }

    .pyq-section-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 2px dashed var(--pyq-light);
    }

    .pyq-section-title {
      font-size: 16px;
      font-weight: 700;
      color: var(--pyq);
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .pyq-section-title i {
      font-size: 20px;
    }

    .pyq-stats {
      display: flex;
      gap: 15px;
      font-size: 11px;
    }

    .pyq-stat {
      padding: 5px 12px;
      background: white;
      border-radius: 4px;
      color: var(--pyq);
      font-weight: 600;
    }

    .practice-question {
      background: white;
      border: 1px solid var(--pyq-light);
      border-radius: 6px;
      padding: 20px;
      margin-bottom: 15px;
    }

    .question-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }

    .question-num {
      width: 35px;
      height: 35px;
      background: var(--pyq);
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 14px;
      font-weight: 700;
    }

    .question-meta {
      display: flex;
      gap: 10px;
      align-items: center;
    }

    .question-type {
      padding: 4px 12px;
      background: var(--bg-muted);
      border-radius: 3px;
      font-size: 10px;
      font-weight: 600;
      color: var(--text-secondary);
      text-transform: uppercase;
    }

    .question-marks-badge {
      padding: 4px 12px;
      background: var(--marks-high);
      color: white;
      border-radius: 3px;
      font-size: 11px;
      font-weight: 700;
    }

    .question-pyq-badge {
      display: flex;
      align-items: center;
      gap: 5px;
      padding: 4px 12px;
      background: var(--pyq);
      color: white;
      border-radius: 3px;
      font-size: 10px;
      font-weight: 600;
    }

    .question-text {
      font-size: 14px;
      color: var(--text);
      line-height: 1.8;
      margin-bottom: 15px;
    }

    .mcq-options {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
    }

    .mcq-option {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 12px 15px;
      background: var(--bg-subtle);
      border: 1px solid var(--border);
      border-radius: 6px;
      font-size: 13px;
      color: var(--text-secondary);
      cursor: pointer;
      transition: all 0.2s;
    }

    .mcq-option:hover {
      border-color: var(--pyq);
      background: var(--pyq-bg);
    }

    .option-letter {
      width: 28px;
      height: 28px;
      background: white;
      border: 2px solid var(--border);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 12px;
      font-weight: 700;
      color: var(--text-muted);
      flex-shrink: 0;
    }

    .mcq-option:hover .option-letter {
      border-color: var(--pyq);
      color: var(--pyq);
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       QUICK REVISION - Exam Box
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showRevisionSection}}
    .revision-box {
      background: linear-gradient(135deg, var(--critical) 0%, #ef4444 100%);
      color: white;
      border-radius: 8px;
      padding: 25px;
      margin-bottom: 25px;
    }

    .revision-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
    }

    .revision-title {
      font-size: 18px;
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .revision-title i {
      font-size: 22px;
    }

    .revision-count {
      padding: 5px 15px;
      background: rgba(255,255,255,0.2);
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
    }

    .revision-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
    }

    .revision-item {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      padding: 12px;
      background: rgba(255,255,255,0.1);
      border-radius: 6px;
    }

    .revision-bullet {
      width: 8px;
      height: 8px;
      background: white;
      border-radius: 2px;
      flex-shrink: 0;
      margin-top: 5px;
    }

    .revision-content {
      flex: 1;
    }

    .revision-text {
      font-size: 13px;
      line-height: 1.5;
    }

    .revision-category {
      display: inline-block;
      padding: 2px 8px;
      background: rgba(255,255,255,0.2);
      border-radius: 3px;
      font-size: 10px;
      margin-top: 6px;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       ONE-LINERS SECTION
       ═══════════════════════════════════════════════════════════════ */
    
    .one-liners-section {
      background: var(--bg-subtle);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 25px;
    }

    .one-liners-title {
      font-size: 14px;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .one-liner-row {
      display: flex;
      padding: 10px 0;
      border-bottom: 1px dashed var(--border);
      font-size: 13px;
    }

    .one-liner-row:last-child {
      border-bottom: none;
    }

    .one-liner-q {
      width: 40%;
      color: var(--text);
      font-weight: 500;
      padding-right: 15px;
    }

    .one-liner-a {
      width: 60%;
      color: var(--text-secondary);
      padding-left: 15px;
      border-left: 2px solid var(--secondary);
    }

    /* ═══════════════════════════════════════════════════════════════
       TIPS & MISTAKES
       ═══════════════════════════════════════════════════════════════ */
    
    .tips-mistakes-row {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin-bottom: 25px;
    }

    .tips-box {
      background: #f0fdf4;
      border: 1px solid #86efac;
      border-radius: 8px;
      padding: 20px;
    }

    .tips-title {
      font-size: 14px;
      font-weight: 700;
      color: #15803d;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .tips-title i {
      font-size: 16px;
    }

    .tip-item {
      font-size: 13px;
      color: #166534;
      padding: 8px 0 8px 20px;
      position: relative;
      line-height: 1.5;
    }

    .tip-item::before {
      content: '✓';
      position: absolute;
      left: 0;
      color: #22c55e;
      font-weight: bold;
    }

    .mistakes-box {
      background: #fef2f2;
      border: 1px solid #fecaca;
      border-radius: 8px;
      padding: 20px;
    }

    .mistakes-title {
      font-size: 14px;
      font-weight: 700;
      color: #991b1b;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .mistakes-title i {
      font-size: 16px;
    }

    .mistake-item {
      font-size: 13px;
      color: #b91c1c;
      padding: 8px 0 8px 20px;
      position: relative;
      line-height: 1.5;
    }

    .mistake-item::before {
      content: '✗';
      position: absolute;
      left: 0;
      color: #ef4444;
      font-weight: bold;
    }

    /* ═══════════════════════════════════════════════════════════════
       REFERENCES
       ═══════════════════════════════════════════════════════════════ */
    
    .references-section {
      padding: 15px 20px;
      background: var(--bg-subtle);
      border-radius: 6px;
      margin-bottom: 20px;
    }

    .references-title {
      font-size: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      color: var(--text-muted);
      margin-bottom: 10px;
    }

    .reference-item {
      font-size: 12px;
      color: var(--text-secondary);
      padding: 5px 0;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .reference-item i {
      color: var(--secondary);
      width: 14px;
    }

    .reference-item a {
      color: var(--secondary);
      text-decoration: none;
    }

    .reference-item a:hover {
      text-decoration: underline;
    }

    /* ═══════════════════════════════════════════════════════════════
       FOOTER
       ═══════════════════════════════════════════════════════════════ */
    
    {{#if showFooter}}
    .footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 40px;
      background: var(--primary);
      color: white;
      font-size: 11px;
    }

    .footer-left {
      display: flex;
      gap: 20px;
    }

    .footer-item {
      display: flex;
      align-items: center;
      gap: 6px;
      opacity: 0.9;
    }

    .footer-item i {
      font-size: 10px;
    }

    .footer-right {
      font-weight: 600;
    }

    .footer-motivation {
      text-align: center;
      padding: 10px;
      background: var(--secondary);
      font-size: 12px;
      font-weight: 600;
    }
    {{/if}}

    /* ═══════════════════════════════════════════════════════════════
       PRINT STYLES
       ═══════════════════════════════════════════════════════════════ */
    
    @media print {
      body {
        background: white;
        padding: 0;
      }
      
      .notes-container {
        box-shadow: none;
        width: 100%;
        min-height: auto;
      }

      .page-break {
        page-break-before: always;
      }
    }

    @page {
      size: A4;
      margin: 0;
    }

    /* ═══════════════════════════════════════════════════════════════
       RESPONSIVE
       ═══════════════════════════════════════════════════════════════ */
    
    @media screen and (max-width: 900px) {
      body {
        padding: 15px;
      }

      .notes-container {
        width: 100%;
        min-height: auto;
      }

      .header,
      .notes-body {
        padding: 20px;
      }

      .exam-stats-bar {
        flex-wrap: wrap;
      }

      .exam-stat {
        flex: 1 1 50%;
      }

      .mcq-options,
      .revision-grid,
      .tips-mistakes-row {
        grid-template-columns: 1fr;
      }

      .one-liner-row {
        flex-direction: column;
      }

      .one-liner-q,
      .one-liner-a {
        width: 100%;
        padding: 0;
        border: none;
      }

      .one-liner-a {
        margin-top: 5px;
        padding-left: 15px;
        border-left: 2px solid var(--secondary);
      }
    }
  </style>
</head>
<body>
  <div class="notes-container">
    
    <!-- ═══════════════════════════════════════════════════════════════
         HEADER
         ═══════════════════════════════════════════════════════════════ -->
    {{#if showHeader}}
    <header class="header">
      <div class="header-top">
        <div class="exam-info">
          {{#if targetExam}}
          <span class="exam-badge"><i class="fas fa-bullseye"></i> {{targetExam}}</span>
          {{/if}}
          {{#if subject}}
          <span class="subject-badge">{{subject}}</span>
          {{/if}}
        </div>
        {{#if weightage}}
        <div class="weightage-badge"><i class="fas fa-percent"></i> {{weightage}} Weightage</div>
        {{/if}}
      </div>

      <div class="header-main">
        <h1 class="notes-title">{{title}}</h1>
        {{#if subtitle}}
        <p class="notes-subtitle">{{subtitle}}</p>
        {{/if}}

        <div class="header-meta">
          {{#if chapter}}
          <span class="meta-item"><i class="fas fa-bookmark"></i> {{chapter}}</span>
          {{/if}}
          {{#if class}}
          <span class="meta-item"><i class="fas fa-graduation-cap"></i> {{class}}</span>
          {{/if}}
          {{#if board}}
          <span class="meta-item"><i class="fas fa-university"></i> {{board}}</span>
          {{/if}}
          {{#if estimatedTime}}
          <span class="meta-item"><i class="fas fa-clock"></i> {{estimatedTime}}</span>
          {{/if}}
        </div>

        {{#if difficulty}}
        <div class="difficulty-meter">
          <span class="difficulty-label">Difficulty:</span>
          <div class="difficulty-bars">
            <span class="difficulty-bar active"></span>
            <span class="difficulty-bar {{#if (or (eq difficulty 'medium') (eq difficulty 'hard'))}}active{{/if}}"></span>
            <span class="difficulty-bar {{#if (eq difficulty 'hard')}}active hard{{/if}}"></span>
          </div>
          <span style="font-size: 11px; text-transform: uppercase; letter-spacing: 0.5px;">{{difficulty}}</span>
        </div>
        {{/if}}
      </div>
    </header>
    {{/if}}

    <!-- Exam Stats Bar -->
    {{#if examStats}}
    <div class="exam-stats-bar">
      {{#if examStats.totalMarks}}
      <div class="exam-stat">
        <div class="exam-stat-value">{{examStats.totalMarks}}</div>
        <div class="exam-stat-label">Total Marks</div>
      </div>
      {{/if}}
      {{#if examStats.questionsCount}}
      <div class="exam-stat">
        <div class="exam-stat-value">{{examStats.questionsCount}}</div>
        <div class="exam-stat-label">Questions</div>
      </div>
      {{/if}}
      {{#if examStats.pyqCount}}
      <div class="exam-stat">
        <div class="exam-stat-value">{{examStats.pyqCount}}</div>
        <div class="exam-stat-label">PYQs</div>
      </div>
      {{/if}}
      {{#if examStats.formulasCount}}
      <div class="exam-stat">
        <div class="exam-stat-value">{{examStats.formulasCount}}</div>
        <div class="exam-stat-label">Formulas</div>
      </div>
      {{/if}}
    </div>
    {{/if}}

    <!-- ═══════════════════════════════════════════════════════════════
         MAIN CONTENT
         ═══════════════════════════════════════════════════════════════ -->
    <main class="notes-body">

      <!-- Importance Legend -->
      <div class="importance-legend">
        <div class="legend-item">
          <span class="legend-dot critical"></span>
          <span class="legend-label">Must Know</span>
        </div>
        <div class="legend-item">
          <span class="legend-dot important"></span>
          <span class="legend-label">Important</span>
        </div>
        <div class="legend-item">
          <span class="legend-dot moderate"></span>
          <span class="legend-label">Good to Know</span>
        </div>
        <div class="legend-item">
          <span class="legend-dot pyq"></span>
          <span class="legend-label">Asked in Exam</span>
        </div>
      </div>

      <!-- Summary -->
      {{#if summary}}
      <div class="summary-box">
        <p>{{summary}}</p>
      </div>
      {{/if}}

      <!-- Key Points -->
      {{#if showKeyPoints}}
      {{#if keyPoints}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon"><i class="fas fa-star"></i></div>
            <h2 class="section-title">Key Points <span class="section-title-hi">मुख्य बिंदु</span></h2>
          </div>
        </div>
        <ul class="key-points-list">
          {{#each keyPoints}}
          <li class="key-point-item {{#if this.importance}}{{lowercase this.importance}}{{/if}}">
            <div class="key-point-marker">
              {{#if (eq this.importance "critical")}}!{{/if}}
              {{#if (eq this.importance "important")}}★{{/if}}
              {{#unless this.importance}}•{{/unless}}
            </div>
            <div class="key-point-content">
              <p class="key-point-text">{{this.point}}</p>
              {{#if this.explanation}}
              <p class="key-point-explanation">{{this.explanation}}</p>
              {{/if}}
              <div class="key-point-tags">
                {{#if this.examRelevance}}
                <span class="exam-tag"><i class="fas fa-bullseye"></i> {{this.examRelevance}}</span>
                {{/if}}
                {{#if this.marks}}
                <span class="marks-tag">{{this.marks}} marks</span>
                {{/if}}
              </div>
            </div>
          </li>
          {{/each}}
        </ul>
      </section>
      {{/if}}
      {{/if}}

      <!-- Definitions -->
      {{#if definitions}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon"><i class="fas fa-book"></i></div>
            <h2 class="section-title">Definitions <span class="section-title-hi">परिभाषाएं</span></h2>
          </div>
        </div>
        {{#each definitions}}
        <div class="definition-card {{#if this.frequentlyAsked}}frequently-asked{{/if}}">
          <div class="definition-header">
            <h3 class="definition-term">
              {{this.term}}
              {{#if this.hindiMeaning}}<span class="definition-term-hi">({{this.hindiMeaning}})</span>{{/if}}
            </h3>
            {{#if this.marks}}
            <span class="definition-marks">{{this.marks}} marks</span>
            {{/if}}
          </div>
          <p class="definition-meaning">{{this.meaning}}</p>
          {{#if this.example}}
          <p class="definition-example"><strong>Example:</strong> {{this.example}}</p>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Formulas -->
      {{#if showFormulas}}
      {{#if formulas}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon"><i class="fas fa-superscript"></i></div>
            <h2 class="section-title">Formulas <span class="section-title-hi">सूत्र</span></h2>
          </div>
        </div>
        {{#each formulas}}
        <div class="formula-card {{#if (eq this.importance 'critical')}}must-remember{{/if}}">
          <div class="formula-header">
            <h3 class="formula-name">
              {{#if (eq this.importance 'critical')}}<i class="fas fa-exclamation-circle" style="color: var(--critical);"></i>{{/if}}
              {{this.name}}
            </h3>
            {{#if this.importance}}
            <span class="formula-priority {{#if (eq this.importance 'critical')}}must{{else if (eq this.importance 'important')}}important{{else}}good{{/if}}">
              {{this.importance}}
            </span>
            {{/if}}
          </div>
          <div class="formula-body">
            <div class="formula-display">{{this.formula}}</div>
            {{#if this.description}}
            <p class="formula-description">{{this.description}}</p>
            {{/if}}
            {{#if this.variables}}
            <div class="formula-variables">
              {{#each this.variables}}
              <span class="variable-chip">
                <span class="variable-symbol">{{this.symbol}}</span>
                <span class="variable-name">= {{this.name}}</span>
                {{#if this.unit}}<span class="variable-unit">({{this.unit}})</span>{{/if}}
              </span>
              {{/each}}
            </div>
            {{/if}}
          </div>
          {{#if this.pyqYears}}
          <div class="formula-footer">
            <span class="formula-pyq"><i class="fas fa-history"></i> Asked: {{this.pyqYears}}</span>
            {{#if this.conditions}}
            <span class="formula-conditions">{{this.conditions}}</span>
            {{/if}}
          </div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}
      {{/if}}

      <!-- Theorems & Laws -->
      {{#if theorems}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon"><i class="fas fa-scroll"></i></div>
            <h2 class="section-title">Theorems & Laws</h2>
          </div>
        </div>
        {{#each theorems}}
        <div class="theorem-card">
          <div class="theorem-header">
            <h3 class="theorem-name">{{this.name}}</h3>
            {{#if this.marks}}
            <span class="theorem-marks-badge">{{this.marks}} marks</span>
            {{/if}}
          </div>
          <div class="theorem-body">
            <div class="theorem-statement">{{this.statement}}</div>
            {{#if this.proof}}
            <div class="theorem-section">
              <p class="theorem-section-title"><i class="fas fa-pen"></i> Proof</p>
              <p class="theorem-section-content">{{this.proof}}</p>
            </div>
            {{/if}}
            {{#if this.applications}}
            <div class="theorem-section">
              <p class="theorem-section-title"><i class="fas fa-cogs"></i> Applications</p>
              <ul class="theorem-applications-list">
                {{#each this.applications}}
                <li class="theorem-application-item">{{this}}</li>
                {{/each}}
              </ul>
            </div>
            {{/if}}
          </div>
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Laws -->
      {{#if laws}}
      {{#each laws}}
      <div class="theorem-card">
        <div class="theorem-header">
          <h3 class="theorem-name">{{this.name}}</h3>
        </div>
        <div class="theorem-body">
          <div class="theorem-statement">{{this.statement}}</div>
          {{#if this.applications}}
          <div class="theorem-section">
            <p class="theorem-section-title"><i class="fas fa-cogs"></i> Applications</p>
            <ul class="theorem-applications-list">
              {{#each this.applications}}
              <li class="theorem-application-item">{{this}}</li>
              {{/each}}
            </ul>
          </div>
          {{/if}}
        </div>
      </div>
      {{/each}}
      {{/if}}

      <!-- Chemical Reactions -->
      {{#if chemicalReactions}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon" style="background: #0891b2;"><i class="fas fa-atom"></i></div>
            <h2 class="section-title">Chemical Reactions</h2>
          </div>
        </div>
        {{#each chemicalReactions}}
        <div class="reaction-card {{#if this.important}}important-reaction{{/if}}">
          <div class="reaction-header">
            <h3 class="reaction-name">{{this.name}}</h3>
            <div class="reaction-tags">
              {{#if this.reactionType}}
              <span class="reaction-type-badge">{{this.reactionType}}</span>
              {{/if}}
            </div>
          </div>
          <div class="reaction-equation">{{this.equation}}</div>
          <div class="reaction-details">
            {{#if this.conditions}}
            <span class="reaction-detail-item"><i class="fas fa-thermometer-half"></i> {{this.conditions}}</span>
            {{/if}}
          </div>
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Biological Processes -->
      {{#if biologicalProcesses}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon" style="background: #059669;"><i class="fas fa-dna"></i></div>
            <h2 class="section-title">Biological Processes</h2>
          </div>
        </div>
        {{#each biologicalProcesses}}
        <div class="bio-process-card">
          <div class="bio-process-header">
            <h3 class="bio-process-name">{{this.name}}</h3>
          </div>
          <div class="bio-process-body">
            {{#if this.steps}}
            <ol class="bio-steps-list">
              {{#each this.steps}}
              <li class="bio-step-item">
                <span class="bio-step-num"></span>
                <span class="bio-step-text">{{this}}</span>
              </li>
              {{/each}}
            </ol>
            {{/if}}
            {{#if this.inputs}}
            <div class="bio-io-container">
              <div class="bio-io-box inputs">
                <p class="bio-io-label">Inputs</p>
                <p class="bio-io-items">{{join this.inputs ", "}}</p>
              </div>
              {{#if this.outputs}}
              <div class="bio-io-box outputs">
                <p class="bio-io-label">Outputs</p>
                <p class="bio-io-items">{{join this.outputs ", "}}</p>
              </div>
              {{/if}}
            </div>
            {{/if}}
          </div>
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Timeline -->
      {{#if timeline}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon" style="background: #7c3aed;"><i class="fas fa-history"></i></div>
            <h2 class="section-title">Timeline</h2>
          </div>
        </div>
        <div class="timeline">
          {{#each timeline}}
          <div class="timeline-item">
            <div class="timeline-header">
              <span class="timeline-year">{{this.year}}</span>
              {{#if this.askedInExam}}
              <span class="timeline-pyq"><i class="fas fa-check-circle"></i> Asked in {{this.askedInExam}}</span>
              {{/if}}
            </div>
            <h4 class="timeline-event">{{this.event}}</h4>
            {{#if this.description}}
            <p class="timeline-description">{{this.description}}</p>
            {{/if}}
            {{#if this.significance}}
            <div class="timeline-significance"><strong>Significance:</strong> {{this.significance}}</div>
            {{/if}}
          </div>
          {{/each}}
        </div>
      </section>
      {{/if}}

      <!-- Historical Figures -->
      {{#if historicalFigures}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon"><i class="fas fa-user-tie"></i></div>
            <h2 class="section-title">Important Personalities</h2>
          </div>
        </div>
        {{#each historicalFigures}}
        <div class="personality-card">
          <div class="personality-avatar">{{initials this.name}}</div>
          <div class="personality-info">
            <div class="personality-header">
              <h3 class="personality-name">{{this.name}}</h3>
              {{#if this.lifespan}}<span class="personality-years">{{this.lifespan}}</span>{{/if}}
            </div>
            {{#if this.title}}<p class="personality-title">{{this.title}}</p>{{/if}}
            {{#if this.contributions}}
            <ul class="personality-one-liners">
              {{#each this.contributions}}
              <li class="one-liner-item">{{this}}</li>
              {{/each}}
            </ul>
            {{/if}}
          </div>
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Mnemonics -->
      {{#if mnemonics}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon" style="background: #f59e0b;"><i class="fas fa-brain"></i></div>
            <h2 class="section-title">Memory Tricks</h2>
          </div>
        </div>
        {{#each mnemonics}}
        <div class="mnemonic-box">
          <div class="mnemonic-header">
            <span class="mnemonic-icon">🧠</span>
            <span class="mnemonic-label">Remember This!</span>
          </div>
          <p class="mnemonic-topic">{{this.topic}}</p>
          <div class="mnemonic-text">{{this.mnemonic}}</div>
          <p class="mnemonic-explanation">{{this.explanation}}</p>
          {{#if this.items}}
          <div class="mnemonic-items">
            {{#each this.items}}
            <span class="mnemonic-item">{{this}}</span>
            {{/each}}
          </div>
          {{/if}}
        </div>
        {{/each}}
      </section>
      {{/if}}

      <!-- Solved Examples -->
      {{#if showExamples}}
      {{#if examples}}
      <section class="section">
        <div class="section-header">
          <div class="section-title-group">
            <div class="section-icon"><i class="fas fa-calculator"></i></div>
            <h2 class="section-title">Solved Examples</h2>
          </div>
        </div>
        {{#each examples}}
        <div class="example-card">
          <div class="example-header">
            <span class="example-title">{{#if this.title}}{{this.title}}{{else}}Example {{math @index "+" 1}}{{/if}}</span>
            <div class="example-meta">
              {{#if this.difficulty}}
              <span class="example-difficulty">{{this.difficulty}}</span>
              {{/if}}
              {{#if this.marks}}
              <span class="example-marks">{{this.marks}} marks</span>
              {{/if}}
            </div>
          </div>
          <div class="example-body">
            <div class="example-question"><strong>Q:</strong> {{this.question}}</div>
            <p class="example-solution-header"><i class="fas fa-lightbulb"></i> Solution</p>
            {{#if this.steps}}
            <ol class="example-steps">
              {{#each this.steps}}
              <li class="example-step">{{this}}</li>
              {{/each}}
            </ol>
            {{else}}
            <p style="font-size: 13px; color: var(--text-secondary);">{{this.solution}}</p>
            {{/if}}
            {{#if this.answer}}
            <div class="example-answer"><i class="fas fa-check-circle"></i> Answer: {{this.answer}}</div>
            {{/if}}
          </div>
        </div>
        {{/each}}
      </section>
      {{/if}}
      {{/if}}

      <!-- Tables -->
      {{#if tables}}
      {{#each tables}}
      <div class="data-table-container">
        <table class="data-table">
          {{#if this.title}}<caption>{{this.title}}</caption>{{/if}}
          <thead>
            <tr>
              {{#each this.headers}}
              <th>{{this}}</th>
              {{/each}}
            </tr>
          </thead>
          <tbody>
            {{#each this.rows}}
            <tr>
              {{#each this}}
              <td>{{this}}</td>
              {{/each}}
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
      {{/each}}
      {{/if}}

      <!-- One-Liners -->
      {{#if oneLiners}}
      <div class="one-liners-section">
        <h3 class="one-liners-title"><i class="fas fa-bolt"></i> Quick One-Liners for Revision</h3>
        {{#each oneLiners}}
        <div class="one-liner-row">
          <div class="one-liner-q">{{this.question}}</div>
          <div class="one-liner-a">{{this.answer}}</div>
        </div>
        {{/each}}
      </div>
      {{/if}}

      <!-- Practice Questions (PYQs) -->
      {{#if showPracticeQuestions}}
      {{#if practiceQuestions}}
      <div class="pyq-section">
        <div class="pyq-section-header">
          <h2 class="pyq-section-title"><i class="fas fa-file-alt"></i> Practice & Previous Year Questions</h2>
          <div class="pyq-stats">
            <span class="pyq-stat">{{practiceQuestions.length}} Questions</span>
          </div>
        </div>
        {{#each practiceQuestions}}
        <div class="practice-question">
          <div class="question-header">
            <span class="question-num">{{math @index "+" 1}}</span>
            <div class="question-meta">
              {{#if this.type}}<span class="question-type">{{this.type}}</span>{{/if}}
              {{#if this.marks}}<span class="question-marks-badge">{{this.marks}} marks</span>{{/if}}
              {{#if this.year}}<span class="question-pyq-badge"><i class="fas fa-calendar"></i> {{this.examName}} {{this.year}}</span>{{/if}}
            </div>
          </div>
          <p class="question-text">{{this.question}}</p>
          {{#if this.options}}
          <div class="mcq-options">
            {{#each this.options}}
            <div class="mcq-option">
              <span class="option-letter">{{optionLetter @index}}</span>
              <span>{{this}}</span>
            </div>
            {{/each}}
          </div>
          {{/if}}
        </div>
        {{/each}}
      </div>
      {{/if}}
      {{/if}}

      <!-- Quick Revision -->
      {{#if showRevisionSection}}
      {{#if quickRevision}}
      <div class="revision-box">
        <div class="revision-header">
          <h2 class="revision-title"><i class="fas fa-bolt"></i> Quick Revision</h2>
          <span class="revision-count">{{quickRevision.length}} points</span>
        </div>
        <div class="revision-grid">
          {{#each quickRevision}}
          <div class="revision-item">
            <span class="revision-bullet"></span>
            <div class="revision-content">
              <span class="revision-text">{{this.point}}</span>
              {{#if this.category}}<span class="revision-category">{{this.category}}</span>{{/if}}
            </div>
          </div>
          {{/each}}
        </div>
      </div>
      {{/if}}
      {{/if}}

      <!-- Tips & Mistakes -->
      {{#if examTips}}
      {{#if commonMistakes}}
      <div class="tips-mistakes-row">
        <div class="tips-box">
          <h3 class="tips-title"><i class="fas fa-lightbulb"></i> Exam Tips</h3>
          {{#each examTips}}
          <p class="tip-item">{{this}}</p>
          {{/each}}
        </div>
        <div class="mistakes-box">
          <h3 class="mistakes-title"><i class="fas fa-exclamation-triangle"></i> Common Mistakes</h3>
          {{#each commonMistakes}}
          <p class="mistake-item">{{this}}</p>
          {{/each}}
        </div>
      </div>
      {{else}}
      <div class="tips-box" style="margin-bottom: 25px;">
        <h3 class="tips-title"><i class="fas fa-lightbulb"></i> Exam Tips</h3>
        {{#each examTips}}
        <p class="tip-item">{{this}}</p>
        {{/each}}
      </div>
      {{/if}}
      {{else}}
      {{#if commonMistakes}}
      <div class="mistakes-box" style="margin-bottom: 25px;">
        <h3 class="mistakes-title"><i class="fas fa-exclamation-triangle"></i> Common Mistakes</h3>
        {{#each commonMistakes}}
        <p class="mistake-item">{{this}}</p>
        {{/each}}
      </div>
      {{/if}}
      {{/if}}

      <!-- References -->
      {{#if references}}
      <div class="references-section">
        <h3 class="references-title">References</h3>
        {{#each references}}
        <div class="reference-item">
          {{#if (eq this.type "textbook")}}<i class="fas fa-book"></i>{{/if}}
          {{#if (eq this.type "ncert")}}<i class="fas fa-book-reader"></i>{{/if}}
          {{#if (eq this.type "video")}}<i class="fab fa-youtube"></i>{{/if}}
          {{#unless this.type}}<i class="fas fa-link"></i>{{/unless}}
          {{#if this.url}}
          <a href="{{this.url}}" target="_blank">{{this.title}}</a>
          {{else}}
          <span>{{this.title}}{{#if this.author}} — {{this.author}}{{/if}}</span>
          {{/if}}
        </div>
        {{/each}}
      </div>
      {{/if}}

      {{#if ncertReference}}
      <div class="reference-item" style="background: var(--bg-muted); padding: 12px 15px; border-radius: 6px; border-left: 4px solid var(--secondary);">
        <i class="fas fa-book-reader" style="color: var(--secondary);"></i>
        <span><strong>NCERT Reference:</strong> {{ncertReference}}</span>
      </div>
      {{/if}}

    </main>

    <!-- ═══════════════════════════════════════════════════════════════
         FOOTER
         ═══════════════════════════════════════════════════════════════ -->
    {{#if showFooter}}
    <footer class="footer">
      <div class="footer-left">
        {{#if author}}
        <span class="footer-item"><i class="fas fa-user"></i> {{author}}</span>
        {{/if}}
        {{#if lastUpdated}}
        <span class="footer-item"><i class="fas fa-calendar"></i> {{formatDate lastUpdated}}</span>
        {{/if}}
      </div>
      <div class="footer-right">
        {{#if showPageNumbers}}Page 1{{/if}}
      </div>
    </footer>
    <div class="footer-motivation">🎯 Stay Focused • Practice Daily • Score Maximum! 💯</div>
    {{/if}}

  </div>
</body>
</html>
